# 인수인계 — AI PB 어시스턴트 (`ai-pb-agent`)

> 최종 갱신 2026-08-06(F1 조정 선택지). **다음 세션은 이 문서부터 읽으세요.**
> ⚠️ **그 뒤 커밋 일부가 아직 안 들어왔다** — 노트 검토 흐름 재편(초안 단계 제거 · 준법 `제거`
> 판정 신설, 2026-08-03) · 기본 테마 다크 복귀 · 로컬 접속 비밀번호 면제. §0-1의 상태 전이와
> 테마 서술은 **옛 흐름**이니 코드(`db.py` 머리주석 · `layout.tsx`)를 정본으로 볼 것.
> 읽는 순서: §0(지금 상태) → §0-1(되돌리면 안 되는 결정) → §2(함정) → 나머지는 필요할 때.
> ⚠️ **절 번호를 바꾸지 말 것** — 코드가 `§0-1`·`§1`·`§1-1`·`§1-2`·`§2`·`§7`로 이 문서를 가리킨다.
> ⚠️ **이 문서는 결정과 함정만 담는다** — "무엇을 언제 확인했다"는 이력은 커밋 로그에 있다.
> 관련 문서: 런타임 지침 `CLAUDE.md` · 측정 `docs/EVAL.md` · 가치 `docs/VALUE.md`.
> **git 추적 대상 — 자주 커밋할 것.**

---

## 0. 현재 상태

계획서 필수(W1~W6) + 선택(F1)이 전부 라이브 검증으로 닫혔다. **다음 할 일은 §7에서 고른다.**

**데이터(2026-07-30)**: `notes`=6(초안 3·검토 1·심의 1·발행 1) · **`briefs`=0** · `audit_log`=456 ·
`pb_sessions`=12(pending 4) · `pb_customers`=50.
⚠️ **브리프가 비어 있다** — 발표·데모 전에 아래 표대로 다시 만들 것.

**비어 있을 때 다시 채우기**

| 무엇 | 어떻게 | 비용 |
|---|---|---|
| 고객 문의(큐) | `scripts/seed_pb_sessions.py`(§6) | 0 |
| 노트 | F3 라이브 실행(§6) | ~100초·크레딧 |
| 브리프 | 브리핑 카드 버튼 또는 `POST /api/briefs/run` | 40~50초·크레딧 |

- 빈 상태에서도 대시보드 API 8종은 정상이다. `citation_rate`·`publish_rate`는 `0`이 아니라
  **`null`**로 온다(분모 0을 0%로 쓰면 거짓말이다). `/api/briefs/latest` 404도 "아직 없다"는 상태다.
- **상태가 한 곳에 몰리면 화면이 절반만 돈다** — 초안·검토중·심의중·발행완료가 각각 최소 1건은
  있어야 큐와 통과율이 의미를 갖는다. 하드 블록을 보려면 목표주가가 실린 노트가 하나 필요하다.
- ⚠️ **노트와 브리프 종목이 겹쳐야** `PrepMemo`에 브리핑·노트가 같이 붙는다.

**착수 전 필수**: `git pull` 후 `docker compose build backend && up -d --force-recreate backend`
— `redis` 의존성 때문에 안 하면 F1이 `ModuleNotFoundError`로 죽는다.

---

## 0-1. 제품 확정 사항 — 되돌리지 말 것

### 사용자 = PB 한 명

애널리스트가 아니라 **상담 전 사실을 확인하는 PB**이고, 공유 감독 콘솔이 아니라 **개인 화면**이다.
**AI는 PB를 대신하지 않는다** — 고객에게 나가는 말은 PB가 직접 쓴다. 상담 모달은 "AI 답변
승인·전송"이 아니라 **"고객 문의 대응 준비"**다. ⚠️ **회신을 대신 쓰는 화면으로 되돌리지 말 것.**

### 데이터 스코핑은 화면이 아니라 서버

- **`main.PB_NAME`**(기본 `"PB"`)으로 `/api/customers`·`/api/sessions`·`summary.customers_total`·
  `pb_watchlist`를 좁힌다. 프론트 `MY_PB`와 **같은 값이어야 한다.**
- **남의 고객 상세는 404**(403이 아니다 — 존재를 알려주면 목록을 거른 의미가 없다). 세션
  승인·반려도 막았다: 읽기만 좁히면 id를 아는 것만으로 남의 건을 처리할 수 있다.
- 값이 하나뿐이어도 **서버 스코핑은 유지한다** — 필터가 서버에 있어야 정보장벽이다.
  고객 50명은 전원 `'PB'`. 3인 배정 복원은 `restore_pb_assignment.sql`.

### 역할 2종 — 갈리는 지점은 발행 하나

`초안 → [사실 확인] PB → [준법 심의 요청] PB → [발행] 준법`
**거절 2종**: `검토중 —[보류]PB→ 보류됨`(종결) · `심의중 —[반려]준법→ 검토중`(되돌림)

- **거절이 없으면 "사람이 확인한다"가 아니라 "사람이 눌러 준다"가 된다** — 게이트 차단은 기계의
  거절이라 사람의 판단을 대신하지 못한다. 사유는 고정값(`main.REJECT_REASONS`/`DISCARD_REASONS`).
- ⚠️ **되돌리는 전이는 담당자 칸을 덮어쓰지 않는다**(`db.advance_status(record_actor=False)`) —
  덮으면 확인한 PB가 사라진다. 되돌린 사람은 감사로그가 남긴다.
- ⚠️ 종결 상태는 `db.NOTE_TERMINAL`에 모아 **큐·대기 집계에서 뺀다**. 단 `publish_rate` 분모에서는
  안 뺀다 — 발행까지 못 간 것도 통과율이 말할 사실이다.
- 토글은 **역할 전환이 아니라 화면 전환**(`내 화면`/`준법 화면 (데모)`). **준법 화면엔 브리핑도 F1도 없다.**
- ⚠️ **못 하는 조작은 버튼을 아예 안 그린다.** 회색 버튼은 "지금은 안 되는 것"으로 읽히는데 그건
  남의 조작이다. 남는 건 사유 한 줄(`.m-deny`). 배분은 **셀프 클레임**.
- **되돌릴 수 없는 조작은 두 번 누른다**(무장 → 실행): 노트 보류·반려(사유 셀렉트) · 브리핑 삭제.
  ⚠️ 되돌릴 수 있는 조작은 즉시 실행이 맞다(문장 확인 `.acksel` · 브리핑 재생성).
- 이름은 사람 이름이 아니라 **역할명**(`PB`/`준법`). 정규화는 `types.ts::actorLabel()` 한 곳.
  ⚠️ **앱은 감사를 소급 수정하지 않는다**(append-only). 지운 사실도 **이벤트를 더해서** 남긴다.

### 화면 구조

- **탭 축 = "읽기 / 하기"**(PB `상담 준비`/`작성·검토`, 준법 `심의`/`감시`). 탭 줄은 그 역할의 탭이
  **2개 이상일 때만** 낸다.
- **F1 입구는 둘**(우하단 FAB=아무 종목, 고객 카드 인라인=보유 종목). FAB은 모달이 열리면, 그리고
  **인라인 채팅이 화면에 서 있을 때도** 숨는다 — 떠 있는 것은 무엇이든 덮으므로 자리를 옮겨 풀
  문제가 아니고, 둘이 같은 일을 한다. ⚠️ 관찰 대상은 카드가 아니라 **채팅 칸**(`.cust-chat`)이다
  (카드로 잡으면 그 탭에서 FAB이 거의 늘 사라진다).
- **긴 실행이 도는 뷰·모달은 `hidden`으로만 감춘다 — 언마운트 금지.** 언마운트되면 cleanup이 SSE를
  닫아 **크레딧만 쓰고 결과가 안 나온다.** 해당하는 곳 둘: **F3 생성 뷰**(1~2분) · **전역 F1 모달**
  (닫아도 대화·스트림 유지). 실행 표시는 `onRunningChange` → F3는 탭 라벨 `●`, F1은 FAB `●`.
  - 오버레이가 **동시에 둘** 있을 수 있어 `#overlay`가 아니라 **`.overlay` 클래스**다(같은 id 둘 금지).
  - ⚠️ 전역 F1의 대화를 끊는 건 닫기가 아니라 **`↻ 새 대화`**다 — 말풍선과 함께 **세션 id도 버린다**
    (안 버리면 다음 질문이 앞 대화의 종목을 이어받는다). 입력창은 비우지 않는다.
  - ⚠️ **마운트 시점에만 읽는 prop을 두지 말 것** — 두 번째 열기부터 조용히 무시된다. 입력창을
    채우는 길은 `prefill`(n 카운터) 하나다.
  - 회귀는 `frontend/f1_modal_check.mjs`가 잡는다(§6). 언마운트로 되돌리면 빈 대화가 정상처럼
    보여서 **이 검사 없이는 조용히 깨진다.**
- **모달은 Esc로 닫힌다.** 규칙은 `page.tsx` 한 곳(`setModal(null)` — 배경 클릭·`×`와 같은 일).
  ⚠️ 무장 상태를 Esc로 되돌리는 단계는 두지 않는다(두 번 눌러야 닫히는 모달이 된다).
- **`AI가 오늘 한 일`은 접이식 한 줄**, 값은 `db.today_activity`(프론트에서 세면 최근 N건 제한에
  걸린다). **0인 항목은 적지 않는다.**
  - **자리는 준법의 감시 탭 하나다**(2026-08-06). 이 줄은 "오늘 손댈 것"이 아니라 **지켜본
    결과**라, 감시 탭의 다른 카드(AI 신뢰도·알림·감사로그)와 성격이 같고 원본도 같은 감사로그다.
  - ⚠️ 예전에는 감시 탭이 없는 역할(PB)의 첫 탭 맨 위에도 놓았는데 걷어냈다 — PB의 첫 화면
    맨 윗자리는 **상담 준비**(브리핑·고객)의 것이고, 지켜본 결과가 그 앞에 설 이유가 없다.
    **PB 화면에 되살리지 말 것.**
- **브리핑 머리말의 조작은 급이 셋이다**: 브리프가 없으면 빈 상태의 1차 CTA(`생성`, 옐로) · 있으면
  조용한 `↻ 다시 생성`(`.btn`) · 그 뒤에 더 작은 `삭제`(`.btn-quiet`).
  - ⚠️ 빈 상태 라벨은 `브리핑`이 아니라 **`생성`**이다(2026-07-30) — 버튼은 **무엇을**이 아니라
    **무엇을 하는지**를 말하고(카드 제목이 이미 `브리핑`이다), 이러면 `↻ 다시 생성`과 짝이 맞는다.
  - ⚠️ **재생성에 확인 단계도 옐로도 두지 말 것** — 크레딧을 쓰지만 다시 누르면 되는 조작이다.
    라우트가 블로킹 POST라 **버튼 라벨이 진행을 말한다**(`생성 중…`).
  - ⚠️ **삭제는 반대로 두 번 눌러야 실행된다**(무장은 라벨이 아니라 적색과 옆에 서는 `취소`가
    말한다). 색은 `--critical`이고 가르는 건 **형태**다(막대가 아니라 글자) — **새 색 금지.**
  - ⚠️ **지우는 범위는 날짜 단위다**(`db.delete_briefs_on`). 같은 날 재실행분이 회차로 쌓이는데
    화면은 `latest_brief()` 하나만 그려서, **보이는 한 행만 지우면 직전 회차가 올라와 날짜 표기까지
    같은 화면이 다시 선다.** 화면은 id만 보내고 날짜로 넓히는 판단은 서버가 한다.
- **전체 고지는 맨 아래 한 줄**(`.disclaimer`, 13px, `--ink-2`). `--muted`(3.4:1)를 안 쓴 건
  **읽히지 않는 고지는 고지가 아니어서**다. ⚠️ **프론트 전용** — `compliance.py::NOTICES`(백엔드가
  본문에 강제 삽입)와 별개다. `NOTICES`를 고치면 `CLAUDE.md` 표도 같이 고친다.
  - ⚠️ **브리핑 카드는 자기 고지를 달지 않는다**(결정). 백엔드 고지는 `content_md`에 남아 게이트가
    검사하고 화면에서는 이 줄이 같은 말을 한다. `Brief` 타입에 `content_md`가 없는 건 누락이 아니라
    그 결과다 — **"브리핑에 고지가 없다"고 다시 열지 말 것.**
- **카드 맨 위는 요약 불릿 3~4줄이다**(`brief.digest` → `.digest`, 2026-08-06).
  **지수 줄보다 위**이고, 여기서 "오늘 무슨 일이 있었나"가 통째로 끝난다 — 그 아래(지수·종목
  카드)는 근거다. 이게 붙기 전 카드는 정보가 모자란 게 아니라 **시작점이 없어서** 나열로 읽혔다.
  - 순서가 곧 읽는 순서다: **어제와 달라진 것 → 유의사항**(있을 때만) **→ 종목 한 줄씩**(맨 아래).
  - ⚠️ **걷어낸 불릿 셋**(2026-08-06). 되살리기 전에 이유부터 읽을 것:
    - `먼저 볼 것`·`조용합니다` — 종목 줄이 생기면서 **아래 줄과 같은 말**을 하고 있었다.
      규칙(`brief.pick_lead`·`quiet_note`)은 남겼다(`f1.portfolio_summary`와 같은 처방).
    - `시장 대비`(`코스피 ▲3.76% · 보유 3종목 중 2종목이 지수를 밑돌았습니다`) — **어느 지수와
      견줄지를 종목의 시장으로 고르지 않고 첫 지수(코스피)를 무조건 썼다.** 코스닥 종목이
      브리프에 들어오면 조용히 틀린 비교가 된다. 되살리려면 `krx_quote`의 `market`(`mrktCtg`)이
      **실제로 무엇으로 오는지부터 확인**해야 한다(이 저장소에 기록이 없다 — 읽기만 하고 쓴 적이
      없는 필드다). 지수 등락 자체는 바로 아래 지수 줄이 이미 보여 준다. 함수는 지웠다:
      결함이 남은 채 배선만 끊어 두면 다음 사람이 그대로 되살린다.
  - **⑤ 종목 줄만 "없으면 안 낸다" 규칙에서 빠진다** — 브리프에 실린 종목은 **전부** 한 줄씩
    갖는다. 한 종목이 조용하다고 빼면 목록에서 빠진 것인지 조용한 것인지 모른다.
    문법은 **있는 것만 적기**다 — 0건인 종류는 아예 안 쓴다("뉴스 0건"은 자리만 먹는다).
    `주요 공시`라는 말이 있는 종목에만 나오므로 `(주요 공시 없음)` 같은 꼬리를 달지 않는다.
    - **문장은 두 갈래다**(2026-08-06): **LLM이 쓴 한 문장**이 통과하면 그것, 아니면 **규칙 문장**.
      규칙 문장은 건수가 아니라 **이름으로** 말한다(`주요 공시 「X」 외 1건 · 뉴스 「제목」 외 2건`).
      제목은 `_TITLE_CAP`에서 자른다(원문은 바로 아래 카드에 있다).
      - ⚠️ **인용은 공시·뉴스 모두 낫표(「」)다.** 큰따옴표로 감싸면 제목 안의 따옴표와 겹쳐
        `"로이터 "삼성전자…""`가 된다(실제로 그렇게 나왔다). 종류는 앞의 `공시`·`뉴스`가 가른다.
      - ⚠️ 주요 공시가 **아닌** 공시는 이름을 인용하지 않는다 — 대개 임원 보고가 같은 이름으로
        반복될 뿐이고 이름이 무게를 나르지도 않는다.
    - **⑤만 LLM이 문장을 쓴다**(`brief.SUMMARY_SYSTEM_PROMPT` · `main._stock_summaries`).
      F2에서 유일한 예외이고, 감싸는 게 다섯이다:
      ① 입력은 **공시명·뉴스 제목뿐**(`brief.summary_input` — 기사 본문도 시세도 보유 수도 안 나간다).
      ② **전언 형식 강제**("…보도가 이어졌습니다") — 회사·주가 평가를 막으면 투자권유·단정 표현
         금지가 문장 형식으로 지켜지고, **제목의 낚시가 사실 판단으로 승격되지 않는다.**
      ③ 나온 문장을 **순수 함수가 다시 검사**한다(`brief.parse_summaries`): 형식·길이·금지 표현·
         **입력에 없는 숫자**. 마지막이 "새 수치 금지"를 실제로 강제하는 지점이다.
      ④ 화면에 **`AI 요약` 배지**(`.digest-ai`) — 규칙 문장과 갈리는 자리가 여기뿐이다.
      ⑤ 버려지면 **규칙 문장으로 떨어진다.** 조용히 비지 않는다 — LLM 실패는 "오늘 조용했다"와
         다르고, 그 둘이 화면에서 같아 보이면 안 된다.
      - ⚠️ **이 불릿은 게이트를 타지 않는다**(본문이 아니라 `lead_json`이다). 그래서 검증이
        게이트가 아니라 `parse_summaries`에 있다 — 그 함수를 느슨하게 만들면 검사하는 곳이 사라진다.
      - ⚠️ 실패해도 **브리프를 죽이지 않는다**(`_stock_summaries`는 예외를 삼키고 `{}`를 준다).
        지수 조회와 같은 규약이다. `scripts/seed_brief.py`는 크레딧 없이 도는 시드라 요약이
        아예 붙지 않는다 — 규칙 문장으로만 나온다.
    - ⚠️ 공시 건수는 **카드에 실린 전부**(접힌 임원 보고 포함)다 — 카드가 5건이라 적는데 불릿이
      0건이라 적으면 어느 쪽이 맞는지 화면에서 알 수 없다.
    - ⚠️ **"밤사이 공시·뉴스가 없다"를 ④에 다시 넣지 말 것** — 종목 줄이 종목마다 그 말을 하므로
      같은 사실이 두 줄이 된다.
  - ⚠️ **문장을 전부 코드가 만든다**(LLM 미개입). 없는 사실이 안 섞이는 대신 **말할 수 있는 것도
    여기 적힌 것뿐**이다 — 규칙에 없는 통찰은 나오지 않는다. 그게 계약이다.
  - ⚠️ **불릿당 한 문장.** 두 문장을 허용하면 규칙이 문장을 이어 붙이기 시작하고 브리핑이
    리포트가 된다. 길어질 것 같으면 문장을 늘리지 말고 **불릿을 따로 세운다**(유의사항이 종류마다
    따로 서는 이유). 테스트로 고정돼 있다(`test_digest_every_bullet_is_one_sentence`).
  - ⚠️ **없으면 그 불릿을 안 낸다.** 0건을 나열하면 아무 일 없는 날도 일한 것처럼 보인다
    (`AI가 오늘 한 일`과 같은 규칙). 예외는 `delta` 하나 — 거기서는 "없다"가 답이다.
  - ⚠️ **화면에서 문장을 조립하지 말 것.** 백엔드가 완성한 `{kind, text, href, link_text}`를
    그대로 찍는다. 조립하면 저장된 브리프와 화면이 다른 말로 같은 날을 설명하게 된다.
  - ⚠️ **밑줄은 `link_text` 구간에만 건다**(`page.tsx`의 `DigestText`). 불릿 전체를 앵커로
    감싸면 `먼저 볼 것:`과 `(21명 보유)`까지 링크가 되는데 그 둘은 공시 원문이 아니다 —
    누르면 어디로 가는지가 밑줄 범위와 맞아야 한다. `link_text`를 `text`에서 못 찾으면
    **링크 없이 문장 전체를 찍는다**(문장이 사라지는 쪽으로 틀리면 안 된다).
  - 리드만 **마침표가 없다** — 문장이 아니라 `먼저 볼 것: {볼 것} (곁 정보)` 꼴의 라벨+항목이다.
    곁 정보(보유 수·어제도 있던 건)는 **괄호 하나에 `·`로 모은다**: 괄호가 둘 연달아 서면
    어느 쪽이 무엇인지 안 읽힌다.
  - **① 리드**(`brief.pick_lead`) — ① `major` 공시(어제 없던 것 우선) → ② 없으면 창에서
    **가장 컸던**(rank 1) 움직임 → ③ 없으면 `quiet_note`. 불릿에 **담당 고객 N명 보유**가 붙는다
    (`main.holdings_index` — 종목 선정과 같은 집계를 쓴다). 나가는 건 집계까지다(가드레일 1).
    - ⚠️ **요약하지 않는다. 고른다.** 리드가 가리키는 것은 *이미 출처가 붙은 줄*이라 링크가 그
      줄의 것 그대로 따라온다(불릿 전체가 그 앵커다).
    - ⚠️ **본문(`content_md`·`sentences`)에 넣지 말 것.** 새 사실이 아니라 기존 줄을 가리키는
      것이라, 본문에 또 적으면 같은 사실이 두 번 세어져 **출처 부착률의 분모가 흔들린다**.
      저장 자리는 `briefs.lead_json`(market_json과 같은 급)이다.
    - ⚠️ rank 2·3은 리드로 고르지 않는다 — 리드는 "가장"이어야 뜻이 서고, 2위를 올리면 왜 그게
      맨 위인지 화면이 설명하지 못한다(카드 꼬리표가 이미 다 말하고 있다).
  - **유의사항** — "없다"와 **"못 가져왔다"**를 가르는 자리(지수 미연결·시세 없음·조회 0건).
    ⚠️ 적색을 쓰지 않는다 — 게이트 차단(`--critical`)과 같은 무게로 읽히면 안 된다. 이건 막힌
    것이 아니라 덜 채워진 것이다. 게이트 미통과는 카드 아래에 이미 적색으로 뜨므로 여기 넣지 않는다.
  - **② 평소 대비 꼬리표**(`market.rank_recent_move` → `brief.recent_move_text`) — `20거래일 중
    가장 큰 하락`. ⚠️ **화면에서는 걷어냈다**(2026-08-06) — 시세 줄은 값 하나를 읽는 자리인데
    판정 문구가 가격과 기준일 사이에 끼면서 좁은 카드에서 두 줄로 접혔다. 판정·문구는 브리프
    기록과 `_quote_line`에 남는다. 되살린다면 **시세 줄이 아닌 다른 줄**이어야 한다. **추가 조회가 0회다**: `krx_quote`도 `fetch_index`도 이미 한 요청으로 수십
    일치를 받아 놓고 마지막 한 줄만 쓰고 버리고 있었다(그래서 `krx_server.LOOKBACK_DAYS`가
    14 → 28이다 — 직전 영업일 찾기가 아니라 **비교 창**이 이유다. 좁히면 `MIN_WINDOW` 아래로
    떨어져 꼬리표가 조용히 사라진다).
    - ⚠️ **같은 방향끼리만 견준다.** 절대값으로 줄 세우면 "가장 큰 하락"이 실은 상승장 한복판의
      작은 하락일 수 있다.
    - ⚠️ **창이 짧거나 보합이면 아무 말도 하지 않는다**(None). "5거래일 중 가장 큰 하락"은 없는
      무게를 실어 주는 말이다. 상위 `NOTABLE_RANK`(3) 밖은 **아무 말도 안 한다** — 등수를
      그대로 적으면 "4위"가 크다는 뜻으로 읽히고, `평소 수준`이라고 적던 것도 걷어냈다
      (2026-08-06: 카드 셋에 매일 붙어 정작 **다를 때의 문구가 그 사이에 묻혔다**).
      ⚠️ 그래서 "평범했다"와 "판단 못 했다"가 화면에서 같아진다(둘 다 무표시) — PB가 할 일이
      어느 쪽이든 같아서 받아들인 대가이고, 판정 원본은 `quote.recent`에 그대로 남는다.
    - **기준일은 시세 줄 오른쪽 끝**이다(`.bcard .bquote .bcode`의 `margin-left: auto` ·
      `.mkt-asof`와 같은 처방). ⚠️ `.bquote`에 `gap`을 주지 말 것 — `246,000원`과 `▲2.50%`까지
      벌어지는데 그 둘은 한 값이다.
    - ⚠️ **문구는 백엔드가 완성해서 보낸다**(`quote.recent_text`). 화면이 `{of, direction, rank}`로
      문장을 조립하면 카드와 브리프 본문이 같은 사실을 다른 말로 적게 된다.
  - **② 어제 대비 새로 생긴 것**(`brief.seen_keys`·`mark_new`) — 어제도 있던 줄은 **톤만 낮춘다**
    (`.bline.seen`). 지우지도 접지도 표를 달지도 않는다: delta는 "새것에 표시를 붙이는 것"이
    아니라 **새것만 또렷한 것**이라 글자가 안 늘어난다. 비교는 공짜다(어제 브리프가 이미 DB에 있다).
    - ⚠️ **비교 기준은 `db.brief_before(오늘)`이지 `latest_brief()`가 아니다.** 브리프는 재실행마다
      한 행씩 쌓여서, 직전 행을 기준으로 잡으면 **오늘 두 번째 실행부터 전부 "어제도 있던 것"**이
      되어 강조가 통째로 꺼진다. id가 아니라 **날짜로** 자른다.
    - ⚠️ **어제가 없으면 필드 자체를 안 붙인다**(전부 `is_new=True`가 아니다). "견줘 봤더니 전부
      새것"과 "견줄 것이 없었다"는 다르다. 화면도 `is_new === false`로 **좁혀** 판정한다 —
      falsy로 보면 첫 브리프가 통째로 흐려진다(`page.tsx`의 `seen()` 하나가 규칙).
    - ⚠️ 뉴스는 링크가 아니라 **제목 키**로 맞춘다(`_title_key`, `pick_news`의 중복 판정과 같은 키)
      — 같은 기사가 어제와 다른 URL로 실리면 어제 본 것이 오늘 새것으로 뜬다.
    - **리드도 `is_new`를 우선한다** — 어제 맨 위였던 공시를 오늘 또 올리면 브리핑이 아니다.
      다만 새것이 하나도 없으면 **옛 주요 공시라도 올린다**(비우면 "오늘은 조용하다"로 잘못 읽힌다).
  - **조용한 줄**(`brief.quiet_note`, 리드 자리) — 주요 공시가 하나도 없을 때만. 브리핑의 절반은 **없다는
    사실**을 말해 주는 것이고, 그게 없으면 읽고 넘어갔다는 확신이 안 생긴다.
    - ⚠️ 리드와 **동시에 뜨지 않는다**(백엔드가 배타적으로 만든다) — 같이 서면 "먼저 볼 것이 있다"와
      "볼 게 없다"가 나란히 놓인다. 개수는 **화면에 실제로 실린 것**을 센다(수집 원본이 아니다).
  - **색이 가르는 것은 요약과 근거다**(2026-08-06): 바탕이 깔린 상자는 `.digest` 하나뿐이고,
    지수 줄(`.mkt`)과 종목 카드(`.bcard`)는 **같은 면**이다 — 둘 다 데이터다. `.mkt`에 바탕을
    다시 깔지 말 것.
  - ⚠️ **새 색을 만들지 않는다.** 눈이 불릿부터 가야 하지만 그 일은 색이 아니라 **자리**가 한다
    (카드 맨 위, 위에 아무것도 없다). 리드만 명도로 한 단계 올린다(`--ink`). 옐로를 쓰면 브리핑이
    페이지 전체의 1차 CTA를 가져간다. 꼬리표도 색을 안 쓴다 — 방향은 이미 `▲▼`와 `.delta`가 말했다.
  - 2026-08-06 이전 브리프는 `lead_json`이 `{}`라 불릿이 아예 안 그려진다(다시 생성하면 붙는다).
- **임원·주요주주 보고는 종목 카드 안에서 접힌 한 줄이다**(2026-08-06 —
  `임원·주요주주 보고 N건 ›`, 뉴스 **아래**). 대형주는 이게 매일 수십 건이라(삼성전자 5일 = 81건)
  뒤로 미는 것만으로는 조용한 날 카드를 그것이 다 채웠다. 자리를 갈라 백엔드가 몫을 따로 세고
  (`brief.INSIDER_LIMIT`), 화면은 건수만 적는다.
  - ⚠️ **접히는 건 임원 개인의 소액 매매 신고 하나뿐이다.** 5%룰(`주식등의대량보유상황보고서`)은
    이름에 "보유상황보고"가 들어가 한 묶음이었는데(2026-08-06 실화면에서 접혀 있었다) 무게가
    정반대다 — 누가 지분을 5% 이상 사거나 팔았다는 신고이고 자주 나오지도 않는다. `_MATERIAL`로
    올렸다. **이 줄을 `지분공시`라고 부르지 말 것**: 5%룰도 지분공시인데 그건 위에 선다.
  - `풍문또는보도에대한해명`도 명시적으로 `_MATERIAL`이다 — 대개 M&A·실적 루머에 대한 회사의
    답이라 PB가 상담에서 먼저 질문받는 종류다(그전에는 `기타`로 떨어져 있었다).
    **`확정`·`미확정`·`부인` 셋 다 같은 자리다** — 등급은 *볼지 말지*를 정하고 괄호는 *뭐라고
    말할지*를 정하는 다른 축이다. 특히 `부인`을 내리면 PB에게 **루머만 남고 회사의 부인은
    안 남는다.**
    - ⚠️ **맨 `조회공시요구`로 넓히지 말 것.** 조회공시에는 **시황변동**(주가 급변)이 있고 그
      답변은 대개 "공시할 중요한 정보 없음" 한 줄이다 — 넓히면 맨 위를 그게 가져간다. 풍문 건은
      이름의 `풍문`으로 이미 잡히므로 넓은 키워드는 **원치 않는 것만 더 잡는다**(`조회공시요구(풍문`
      까지만 둔 이유). DART 실제 보고서명은 **확인 못 했다** — 표기 변형 대비로만 남긴 키워드다.
  - ⚠️ **등급(`importance`)을 글자로 내지 말 것.** 태그는 그대로 `공시` 하나이고 색·굵기도 같다 —
    등급이 하는 일은 무엇을 접을지 정하는 것뿐이다. `주요`/`정기`/`지분` 배지를 줄마다 붙이면
    줄이려고 접은 글자가 도로 늘어난다. 같은 이유로 머리말에 `중요만/전체` 토글도 두지 않았다
    (접힘 줄이 카드 안에서 이미 그 일을 한다).
  - ⚠️ **프론트에서 보고서명으로 등급을 다시 판정하지 말 것** — 정본은 `brief.IMPORTANCE` 하나다.
    옛 브리프에는 필드가 없어 전부 펴진다(`other` 취급) — 누락이 아니라 폴백이다.
  - ⚠️ **지우지 않는다.** 접힘은 화면의 일이고 `content_md`·`sentences`(게이트 대상 기록)에는
    임원 보고도 그대로 남는다. `PrepLines`(준비 줄 2개)에서만 제외된다 — 거기 올라오면 정작
    상담에서 말할 공시가 밀린다.
  - 접힘 상태는 **종목별**이다(`ownOpen`) — 카드 접기(`briefOpen`)가 셋을 함께 움직이는 것과 다르다.
    한 종목을 파고드는 조작이고, 최대 5줄이라 3열 격자의 높이를 흔들지 않는다.
- 타일 설명줄은 **바로 위 숫자를 쪼갠 것**(처리 대기 10 = 노트 6 + 문의 4). 고객 표의 `#`는 id가
  아니라 **표에 보이는 자리**(`visibleCustomers`).
- **행 hover·포인터 커서는 `.tbl-scroll`(고객 목록)에만.** 전역 `tbody tr`이면 클릭 핸들러가 없는
  보유 종목·재무 수치 표까지 눌리는 것처럼 보인다.

### 발행된 노트 카드 — 「작성·검토」 탭, 생성 카드와 큐 **사이**

- 순서는 **만든다 → 다 된 것을 읽는다 → 처리할 것을 본다**(2026-08-06). 발행분을 큐 위에 두는
  이유: **상담에 실제로 쓸 수 있는 등급은 발행분뿐**이고 큐에 있는 건 아직 쓸 수 없는 것들이다.
- 이름이 `발행된 노트`인 건 **PDF로 열리는 유일한 등급이 발행분**이어서다(`notePdfUrl`이 다른
  상태면 서버가 409). ⚠️ `완성된 노트`·`노트 보관함`으로 바꾸면 **이름과 PDF의 연결이 끊긴다** —
  왜 여기 것만 PDF가 되는지를 화면이 설명하지 못한다.
- **`처리 대기`(큐)와 대상이 다르다**: 이쪽은 **읽을 것**(`/api/notes`), 저쪽은 **처리할 일**
  (`/api/dashboard/queue` — 발행분을 뺀다). 같은 목록의 두 표현이 아니다(§2 · `/api/notes` 주석).
  - 그래서 큐 카드 이름을 `종목 노트` → **`처리 대기`**로 되돌렸다(2026-08-06). 위에 `발행된
    노트`가 서면서 `종목 노트`가 둘을 다 가리키는 것처럼 읽혔고, **코드의 나머지는 줄곧
    `처리 대기`를 쓰고 있었다**(생성·보류·처리 완료 토스트, `PILL` 주석의 `처리 대기 → 처리 완료`,
    `db.py`·`main.py` 주석). 카드 하나만 갈려 있던 것이다.
  - ⚠️ 이 카드는 **준법 화면과 공유한다**(`queueCard` 하나를 두 화면이 쓴다) — 이름을 또 바꾸면
    양쪽이 같이 바뀐다.
- ⚠️ **다시 정렬하지 말 것** — `/api/notes`가 이미 최신순(`ORDER BY id DESC`)이다.
- ⚠️ 상태 배지를 줄마다 달지 않는다 — 한 상태만 담는 카드라 모든 행이 제목을 반복한다
  (큐에서 종류 배지를 뺀 것과 같은 이유).
- 본문 상세를 못 받은 건은 **PDF 버튼이 눌리지 않는다**(사유는 title). 누르면 모달이 빈 채로
  뜨는 쪽보다, 안 눌리고 이유를 말하는 쪽이 낫다.

### 노트 생성 카드(F3)

- **화면에 `a1`·`a2`·`a4`·`a5`를 적지 않는다.** 읽는 사람은 PB이지 개발자가 아니다. 단계 이름은
  `법인 확인`·`재무 핵심수치`·`뉴스`·`노트 초안 작성`, F1 라우팅 배지는 `공시`·`재무`·`뉴스`.
  ⚠️ **코드 안의 키는 그대로** — SSE `progress` 이벤트의 `agent` 값이다.
- **단계 배지는 전부 `.chip.chat`(파랑)** — 같은 실행의 연속된 단계다. 색이 종류를 나르는 건
  **큐 행 쪽뿐**이다.
- **뉴스는 받아 온 것을 전부 그린다**(`.gen-scroll`, `max-height` 168px + 스크롤). 전엔 `slice(0,5)`
  인데 배지는 `news.length`를 적어 **"10건"이라 써 놓고 5건만 보였다** — 숫자와 목록이 갈리면 어느
  쪽이 사실인지 알 수 없다.
- **원문 링크에는 `↗`**(`.sbadge-ext` + `title` + `rel="noopener noreferrer"`) — `sources.tsx`와
  같은 어휘다. ⚠️ `↗`는 `aria-hidden`이라 `title`이 없으면 스크린리더에 새 탭 단서가 없다.
- **재무 카드의 `2025 · 별도`에서 `별도`를 지우지 말 것.** DART `fs_div`다(`CFS`=연결/`OFS`=별도).
  연결은 자회사 포함, 별도는 모회사 단독이라 같은 회사도 값이 크게 다르다. a5도 노트 문장에
  "별도 기준"이라 적으므로 카드에서만 빼면 **카드와 노트가 다른 말을 한다.**

### 발행분 종목 노트 PDF (2026-08-06)

발행된 노트만 `GET /api/notes/{id}/pdf`로 받는다. 버튼은 노트 모달의 조작 줄(고지 아래)에
`status === 'published'`일 때만 서고 **PB·준법 양쪽에 낸다** — 통과시키는 사람은 준법이지만
상담에 들고 가는 사람은 PB다.

- **조립은 코드가 한다(LLM 미개입)** — `brief.assemble`과 같은 규칙이다. 다시 쓰게 하면 각주가
  문장과 어긋나고(가드레일 3) 이미 통과한 게이트를 다시 받아야 한다.
- ⚠️ **`제거` 판정이 산출물에서 실제로 빠지는 자리가 여기다.** 그전까지는 게이트 판정용
  사본(`main._effective_md`)에서만 빠졌다. **저장은 그대로 둔다**(`content_md`는 AI가 쓴 원문) —
  뺀 문장은 **2면 이력표**에 판정·담당과 함께 남는다.
- **문서는 2면**: 1면 최종본(본문 · 번호 각주 · 출처 목록 · 필수 고지) · 2면 이력(단계별 담당·시각 ·
  준법 확인 사유 · 뺀 문장). 고지 문구는 `compliance.NOTICES["F3"]`에서 그대로 가져온다.
- ⚠️ **각주 번호는 본문 등장 순서**라 화면 모달과 다를 수 있다(모달은 `reviewTier`로 재정렬한다).
  문서는 서술 순서가 정본이고, 화면은 처리 순서가 정본이다.
- ⚠️ **뺀 문장이 유일하게 인용하던 출처는 목록에서도 빠진다** — 본문 어디에도 없는 각주가 남으면
  목록이 문서를 설명하지 못한다.
- ⚠️ **한글 폰트가 없으면 통째로 빈 칸이 되고 예외는 안 난다.** 이미지의 `fonts-nanum`이 없으면
  라우트가 **거부**한다(503). `⚠`(U+26A0)는 나눔고딕에 없어 렌더 단계에서 **빼고**, 경고의 무게는
  테두리 상자가 진다(`_renderable` — 빈 칸으로 남기면 고지 앞이 벌어져 오탈자로 읽힌다).
- **게이트를 한 번 더 본다.** 재파싱으로 확인이 무효가 되면(§1-2) 지금 기준으로는 통과 못 하는
  본문일 수 있다 — 그때는 **막히는 쪽**으로 판정하고 409다.
- ⚠️ **fetch가 아니라 링크로 연다** — 파일명을 담은 헤더를 CORS로 다시 열 필요가 없고, 링크
  이동은 예비요청(OPTIONS)을 안 타 §2의 CORS 함정에서 비켜 있다.

**여는 자리 — 상담 준비의 노트 줄은 발행분만, 누르면 PDF를 띄운다**(2026-08-06).

예전엔 종목별 **최신 1건**이라 새 초안 하나가 발행분을 덮었다(실측: 기아 #23이 #32에 가려
열리지 않았다). 규칙은 `page.tsx::pickNotes`(순수)로 옮겼고 **발행분만** 담는다.

- **줄에 알약도 날짜도 안 붙인다** — `노트  기아 종목 노트 열기` 한 줄이다. 고를 게 하나뿐이면
  그 옆의 설명은 고르는 데 쓰이지 않는다.
- **누르면 검토 화면이 아니라 최종본 PDF가 뜬다**(`{kind:'notepdf', id}` → `.pdfmodal` 안의
  `<iframe>`). 상담 직전에 필요한 건 문장별 판정 도구가 아니라 **읽을 문서**다.
  ⚠️ 화면용 HTML을 따로 만들지 말 것 — 백엔드가 만든 PDF를 그대로 싣는다(두 벌이면 각주
  번호부터 갈린다). 저장·인쇄는 브라우저 뷰어 툴바가 이미 갖고 있고, 뷰어가 없는 브라우저용
  탈출구로 머리말에 `새 탭에서 열기`(`.btn-quiet`)만 둔다.
  ⚠️ `inline=1`이 붙어야 **뜨고**, 없으면 첨부라 **내려받는다**(라우트 하나, 쓰임 둘).
- **미발행 노트는 이 줄에 안 온다.** 감추는 게 아니라 자리가 다르다 — 검토·심의는 `작성·검토`
  탭의 큐가 맡는다("읽을 것"과 "처리할 일"은 다른 목록이다, §2).
- ⚠️ **모달은 종목이 아니라 노트 id로 연다**(`{kind:'note'|'notepdf', id}`). 종목으로 열면 한
  종목에 노트가 둘일 때 **큐에서 고른 건과 열리는 건이 갈린다**(큐 행은 #32인데 #23이 열렸다).
- 빈 상태 문구도 같이 고쳤다: `오늘 브리핑·발행된 노트에 이 종목은 없습니다` — 초안만 있는
  종목에서 "노트가 없다"고 하면 거짓이 된다.

### 상담 준비 메모 — AI가 낸 것 중 PB가 담은 것 (2026-08-06)

**문제: 채팅에서 PB가 하는 일이 "질문 고르기" 하나뿐이었다.** 노트 화면에는 문장마다
`승인`/`제거`가 있는데 채팅에는 판정할 자리가 없고, 답변이 **조정 선택지까지** 내면서도
고르는 동작은 화면에 없었다(고르는 건 PB라고 문서에만 적혀 있었다). 대화는 휘발돼 상담
준비물로도 안 남는다 — "AI한테 일만 던지고 나는 아무것도 안 한다"는 그 셋의 합이다.

**채택한 구조**: 답변 문장·선택지 옆의 `담기` → 카드 안 `상담 준비 메모` → `PDF로 받기`.
AI는 재료를 내고 **고르는 노동이 PB의 것**이며, 산출물도 PB의 것이다.

- **항목은 셋이고 문서에서 갈려 읽힌다**: `sentence`(AI 문장 · 출처 배지째 간다) ·
  `option`(코드가 뽑은 선택지 · 근거와 `바꾸지 않는 것`까지) · `memo`(PB가 쓴 줄).
  - **담은 줄은 고칠 수 있고, 고친 줄은 `memo`가 된다**(2026-08-06). AI 문장을 고쳐 놓고
    `sentence`로 남기면 문서의 `AI 분석` 구역에 사람이 쓴 말이 앉고, 붙어 있던 각주가
    **AI가 쓰지 않은 문장을 뒷받침하는 것처럼** 인쇄된다(가드레일 3·4). 손을 댄 순간 저자가
    바뀐 것이라 종류도 같이 바꾼다 — 화면 배지가 `AI`→`PB`로 뒤집혀 그 자리에서 말한다.
    ⚠️ **선택지(`option`)에는 고치기를 내지 않는다** — 이름만 바꿔도 근거·`바꾸지 않는 것`
       줄이 그대로 남아 문서가 앞뒤로 다른 말을 한다. 마음에 안 들면 빼는 것이 맞다.
  - **문서는 `AI 분석`·`PB 메모` 두 구역으로 갈린다**(2026-08-06). 제목은 **누가 쓴 줄인지가
    바뀔 때** 서고, 항목은 전부 불릿이며 **PB 메모도 AI 문장과 같은 크기**다(그전에는 줄마다
    `[PB 메모]` 라벨을 달고 8pt 회색이라, 사람이 직접 쓴 유일한 줄이 각주보다 작았다).
    순서는 `prep_order`가 **AI가 낸 것 먼저, PB가 쓴 줄 나중**으로 묶고, **묶음 안에서는
    담은 순서를 지킨다**(무엇을 먼저 꺼낼지가 이미 PB의 판단이다).
    ⚠️ 화면 상자는 **담은 순서 그대로** 둔다 — 거기는 방금 담은 것을 확인하는 자리라
       마지막에 담은 줄이 마지막에 서야 한다. 문서와 순서가 다른 것은 의도다.
    ⚠️ `build_prep`은 **맨 앞에서 한 번** 순서를 바꾼다 — 문장 목록·각주 번호가 다 이
       순서를 쓴다. 렌더 직전에 바꾸면 각주가 1,3,2로 붙는다(번호는 첫 등장 순).
    ⚠️ 제목이 서는 근거는 위치가 아니라 **저자가 바뀌었다는 사실**이다(조건을 "앞에 한 번씩
       박기"로 바꾸지 말 것 — 순서 규칙이 바뀌면 제목이 저자와 어긋난다). 규정 문제다(가드레일 4).
- **담는 동안에는 저장하지 않는다.** 화면이 들고 있다가 PDF를 받을 때만 서버로 간다 —
  ⚠️ `sessionStorage`도 쓰지 않는다. 새로고침하면 담은 것이 사라지고, 그 사실을 상자가
  말한다("저장되지 않습니다").
- **만든 메모는 남는다**(2026-08-06 개정 — `prep_notes` 테이블). 처음에는 인쇄만 하고 아무것도
  남기지 않았는데, 그러면 **어제 만든 메모를 다시 열 방법이 없었다**(감사로그에는 "몇 건을
  인쇄했다"만 있지 무엇을 담았는지가 없다). 지금은 게이트를 통과해 PDF가 만들어진 뒤
  **재료(`items_json`)와 그때의 고객 사실(`customer_json`)**을 한 행으로 남긴다.
  - **PDF 바이트가 아니라 재료를 담는다** — 다시 열 때 같은 함수로 다시 그리므로 문서 규칙을
    고치면 옛 메모도 따라오고, **게이트도 다시 받게 할 수 있다**(오늘 막혀야 할 것이 오늘
    열려서는 안 된다 — 여는 라우트가 `check_note`를 다시 돌린다).
  - **고객 사실은 스냅샷이다.** 지금 값으로 그리면 어제 날짜 문서에 오늘 잔고가 실린다.
    시각도 `created_at`을 되먹여 **그때 인쇄한 것과 같은 PDF**가 나온다.
    ⚠️ **계좌번호는 담지 않는다**(PDF도 안 쓴다) — 이 표가 계좌번호의 두 번째 사본이 될
       이유가 없다. 원본은 `pb_customers`다.
  - **저장 실패가 인쇄를 막지 않는다** — 손에 든 PDF는 이미 게이트를 통과한 것이고, 목록에서
    못 여는 것보다 지금 상담 자료를 못 받는 쪽이 크다(실패는 로그 + `prep_id: null`).
  - 화면은 **고객 카드 바로 아래 `상담 준비 메모` 카드**다. `발행된 노트`와 같은 형식(제목 ·
    시각 · PDF)이되 **고객으로 한 겹 접는다** — 한 고객에게 날짜가 다른 여러 건이 쌓인다.
    **기본은 접힘**이다 — 한동안 지금 고른 고객의 묶음을 펼쳐 뒀는데, 메모를 새로 만들면 그
    고객이 곧 고른 고객이라 **새 줄이 펼쳐진 채로 끼어들어** 카드 높이가 그때마다 뛰었다.
    ⚠️ 노트와 달리 **상태가 없다**(승인 흐름을 타지 않는 PB 본인용 문서다) — 상태 배지를
       달지 말 것. ⚠️ 준법 화면에는 안 낸다(고객 카드가 없는 화면이다).
    ⚠️ `<details>`의 `onToggle`에서 **`e.currentTarget`을 setState 갱신 함수 안에서 읽지 말 것** —
       그 함수는 렌더 중에 도는데 그때 이벤트는 이미 비워져 있어 화면이 통째로 죽는다(실측).
  - **줄마다 삭제(쓰레기통)가 있고 `DELETE /api/prep-notes/{id}`를 부른다.** 되돌릴 수 없으므로
    (재료가 사라지면 다시 그릴 수 없다) **두 번 누른다**: 첫 누름은 그 줄만 무장시키고
    (`삭제`·`취소`가 뜬다), 무장은 **한 번에 한 줄**이다 — 브리핑 삭제와 같은 규칙·같은 색
    (`.btn-quiet.danger`). ⚠️ 지우는 범위는 **보이는 한 줄뿐**이다(브리핑이 그 날짜 전부를
    지우는 것과 다르다 — 여기서는 목록의 줄과 문서가 1:1이다).
    ⚠️ **감사로그는 남는다**(append-only): 만든 기록·연 기록·지운 기록이 그대로 있고, 사라지는
       건 다시 열 수 있는 재료뿐이다. 평소 아이콘 색은 `--muted`이고 **빨강은 손을 얹었을 때만**
       든다 — 늘 빨갛게 두면 목록이 경고문처럼 읽힌다.
  - 검사: `frontend/prep_list_check.mjs`(가로채기 없이 실백엔드로 돈다 — 메모가 한 건은 있어야 한다).
- ⚠️ **PB가 쓴 줄도 게이트를 받는다** — 사람이 썼다고 규정을 비켜 가지 않는다(금지 표현·MNPI·
  지연시세). 다만 `interpretation`이라 미인용 집계에는 안 들어간다(사람 메모에 각주를
  요구하는 건 규칙이 아니다). `test_pb_memo_is_checked_like_everything_else`가 고정한다.
- ⚠️ **고객 정보는 클라이언트가 보내지 않는다.** 이름·나이·잔고는 서버가 DB에서 읽는다 —
  화면이 보낸 문구를 인쇄하면 문서가 화면과 다른 말을 할 수 있다. 담당 아니면 **404**.
- **문서 머리에 고객의 현재 상태가 같이 실린다**(2026-08-06):
  - **위험 플래그**는 자산배분 줄 **바로 아래 굵은 한 줄**이고 앞에 `⚑`가 선다(화면 배지와
    같은 어휘). ⚠️ **판정을 다시 하지 않는다** — 저장된 `flag_reasons`를 인용만 한다.
    ⚠️ 없으면 줄 자체를 안 낸다("플래그 없음"을 적지 않는다).
    ⚠️ 색이 아니라 **굵기**로 가른다 — 이 종이는 흑백으로 인쇄돼 상담 자리에 놓인다.
    ⚠️ **`⚑`는 글자가 아니라 그림이다**(`notepdf._flag_mark`) — U+2691이 나눔고딕에 없어
       글자로 쓰면 `_renderable`이 조용히 지운다(`⚠`가 고지에서 사라졌던 것과 같은 함정).
       기호 하나 때문에 폰트를 더 얹지 않는다는 결정은 그대로고, 대신 선 두 개로 그린다.
       `test_flag_mark_is_drawn_because_the_font_has_no_flag`가 이 전제를 고정한다.
  - **미처리 고객 문의**는 보유 표와 `AI 분석` **사이**에 선다(고객 카드가 질문 칩 위에 두는
    것과 같은 순서 — 무엇을 확인할지 고르기 전에 고객이 이미 무엇을 물었는지가 먼저다).
    원문과 **접수 시각이 한 줄**이고 시각은 오른쪽 끝이다(`_plain_table` — 문단으로는 못 한다).
    여러 건일 때 시각이 세로로 맞아떨어져 언제 것인지 훑힌다. ⚠️ **주제(topic)는 안 싣는다** —
    원문 옆에 요약 제목이 서면 원문 대신 그걸 읽는다(고객 카드에서 주제를 뺀 것과 같은 이유).
    ⚠️ **회신문을 쓰는 자리가 아니다**(가드레일 4) — 원문을 옮길 뿐이다.
    ⚠️ 원문은 신뢰하지 않는 데이터이고, **MNPI·PII·인젝션은 라우트가 채팅과 같은 문지기
       (`compliance.input_guard`)로 거른다.** 걸린 건은 원문 없이 `[확인 필요]` 한 줄로만
       실리고(`prep_note_ask_withheld` 감사) **메모 전체를 막지는 않는다** — PB가 손쓸 수 없는
       사유(고객이 쓴 문장)로 상담 자료가 통째로 사라지면 안 된다.
  - 둘 다 **스냅샷에 담긴다** — 문의는 나중에 처리되고 플래그는 값이 바뀌지만, 다시 여는
    문서는 그때 인쇄한 것이어야 한다.
- **문서는 고객 단위다**(2026-08-06 결정): 이름은 싣고 **계좌번호·생년월일은 안 싣는다**.
  ⚠️ **파일명에도 이름을 안 쓴다**(`상담메모_고객19_0806.pdf`) — 목록·메일 제목처럼 본문을
  열지 않고 보이는 자리다. 페이지 꼬리말에도 이름을 반복하지 않는다(1면에만).
- **고지가 둘이고 역할이 다르다**: `PREP_PURPOSE`(문서 성격 — "고객에게 제공하지 않습니다",
  맨 앞) · `NOTICES["F1"]`(규정 문구 — 맨 뒤). ⚠️ 성격 문구를 `NOTICES`에 넣지 말 것 —
  거긴 규정이 요구하는 문구의 자리이고, 고치면 `CLAUDE.md` 표까지 따라온다.
- **선택지 후보는 SSE로 내려오지만 화면에는 카드를 그리지 않는다**(2026-08-06 추가 → 같은 날
  제거). 카드가 **답변을 닫는 고지문 아래**에 머리말 없이 서서, 답변의 일부인지 별개인지가
  화면에서 갈리지 않았다 — 말풍선은 고지문으로 닫히는 것이 맞다.
  ⚠️ 후보 계산(`f1.rebalance_options`)과 프롬프트 경로는 그대로다 — 답변 산문이 선택지와
     근거를 말한다. 없어진 건 **카드로 담는 경로**뿐이고, `PrepItem`의 `option` 종류와
     PDF의 `선택지:` 항목은 백엔드에 테스트째로 남아 있다(되살릴 때 그대로 쓴다).
  ⚠️ 되살린다면 **고지문 위, 머리말과 함께**. `options` SSE 리스너와 `Turn.options`도 같이.
- 담긴 목록은 **PB가 담은 순서 그대로** 인쇄한다 — 무엇을 먼저 꺼낼지가 이미 그 사람의 판단이다.
- **`담기`는 출처 배지와 모양이 갈려야 한다**(2026-08-06). 둘은 같은 크기로 나란히 서는데
  하는 일이 정반대다 — 배지(`보유`·`해석`)는 문장이 무엇에 근거하는지 말하는 **라벨**(읽는 것 ·
  상태 없음)이고 `담기`는 PB가 누르는 **조작**이다. 처음엔 둘 다 `무채색 + 얇은 실선 + 10~11px`
  이라 갈리는 게 모서리 반경뿐이었고, 폭 30px에서 그건 안 읽혔다. 지금 종류를 나르는 건 셋이다:
  **모난 사각**(배지는 알약) · **아이콘**(＋/✓) · **눌리면 변한다**(배지는 절대 안 변한다).
  ⚠️ 색으로 가르려 하지 말 것 — 색 예산은 배지끼리 이미 다 썼다(§0-1). 옐로는 예외인데,
     그건 새 색이 아니라 이 화면에서 **"PB가 누르는 것"의 색**을 조작 쪽에 마저 쓴 것이다
     (배지에는 옐로를 쓰지 않는다 — 노트 한 장이 통째로 강조 색이 된다).
  - **`담기`는 토글이다** — `✓ 담김`으로 상태를 말하므로 다시 누르면 빠진다. 담긴 여부의
    주인은 **상담 메모 쪽**이고(`page.tsx::pickedKeys`, 본문으로 맞춘다) `F1Chat`은 받아
    그리기만 한다. ⚠️ 채팅이 따로 들고 있으면 메모 상자에서 ×로 뺀 뒤에도 버튼만 `담김`으로
    남아 화면이 거짓말을 한다. PB가 손으로 쓴 줄(`memo`)만 토글에서 빼 둔다.
  - 문장 사이 간격(`.chat-answer`)은 **문장 안 요소 간격보다 뚜렷이 넓게.** 7px이던 때는
    오른쪽 끝으로 밀린 앞 문장 배지와 다음 문장 맨 앞의 `담기`가 붙어 **한 쌍처럼** 읽혔다.
  - **한 문장 = `[조작 거터][문장 본문]` 두 칸.** 셋이 한 줄의 형제(flex-wrap)였을 때는
    **문장 길이가 배치를 정했다** — 짧으면 한 줄, 길면 배지가 자기 줄로 떨어지고 본문은
    첫 줄만 버튼 옆에서 시작해 둘째 줄부터 왼쪽 끝으로 돌아왔다(왼쪽 정렬선이 문장마다 무너짐).
    조작은 **글줄 밖**, 배지는 **본문 안 인라인**(마지막 낱말 뒤에만 붙는다 — 각주가 하는 일).
    ⚠️ 거터에 글자를 넣지 말 것(아이콘만) — 폭은 본문에서 나온다. `담기`는 `aria-label`과
       tooltip이 나른다. 선택지 카드는 거터가 없어 거기서만 `＋ 담기`로 글자를 쓴다.

### 출처 표시 = 노트는 번호 각주 · 대화는 인라인 배지

규칙은 `sources.tsx` **한 곳**이다. 공시·뉴스는 **원문 링크**(새 탭). 시세·보유는 사람이 볼
페이지가 없어 링크가 아니다 — "눌리게 생겼는데 아무 일도 안 일어나는" 걸 피한다.

- ⚠️ **표시는 전부 문장 흐름(`.stext`) 안에 둔다**(해석·UNSOURCED·확인함). `.sbadges`를 형제 칸으로
  두면 폭을 다 쓰는 순간 통째로 아랫줄로 밀려 문장마다 빈 줄이 생긴다.
- ⚠️ `.fnrefs`에 **`line-height: 0` 금지** — `<sup>` 높이가 0이 돼 안의 버튼을 못 누른다.
- 번호는 **화면에 보이는 순서**로(모달이 `reviewTier`로 재정렬한다). 같은 출처는 번호를 공유한다.
- **F1 대화는 인라인 배지**(답변이 2~4문장이라 하단 목록이 답변보다 길어진다). ⚠️ 날짜가 같아도
  **다른 기사끼리는 합치지 말 것** — 한쪽 링크가 사라져 가드레일 3 위반이다.
- ⚠️ 각주 목록의 공시는 문서명이 아니라 **접수번호**로 나간다 — 저장된 출처에 `report_nm`이 없다.

### 모달 = 고정 머리말 + 스크롤 본문

`.modal`은 세로 flex 기둥. `.m-head`만 **고정**, `.m-body`가 **스크롤**한다.

- ⚠️ `.m-body`에 **`min-height: 0` 필수**(없으면 상자가 `max-height`를 넘겨 자라 스크롤이 안 생긴다),
  **`flex-grow`는 금지**(짧은 노트에서 본문이 0으로 접힌다). sticky도 해 봤지만 상자 안쪽
  `padding`을 문장이 스쳐 지나갔다. **되돌리지 말 것.**
- **필수 고지(`.wm`)는 본문 맨 아래**(출처 목록 아래·조작 줄 위) — 결정 직전에 반드시 지나친다.
  ⚠️ 조작 줄보다 아래로 내리지 말 것. ⚠️ 고지와 차단 안내(`.ackbar`)를 **같은 틴트 상자 둘로 쌓지
  말 것** — 급이 다르다. 고지는 면 없는 한 줄, 차단은 왼쪽 적색 막대.
- 머리말에 조작이 둘 이상이면 `.m-acts`로 묶는다 — `.m-close`의 `margin-left: auto`가 둘로 갈려
  사이가 벌어지기 때문이다.

### 색·서체 — 다크 토큰 기본 + 강조색 하나

근거·대비비는 `dashboard.css` 머리주석에 있다. ⚠️ 시안 `docs/design/pb-admin-dashboard.html`은
개편 전에서 멈춰 있다 — 값을 고칠 때 시안을 따라가지 말 것.

- **강조색은 옐로 `#fcd535` 하나** — 1차 CTA·선택 상태 전용. ⚠️ **두 번째 브랜드 색 금지.**
  보조 색조는 `#3b82f6`·`#2dbdb6` 둘뿐이다. ⚠️ **새 색을 만들지 말 것** — 무채색이나 형태로 푼다.
- **처음 열면 라이트, 토큰 기본은 다크**(둘이 다르다). `:root`가 다크이고 `layout.tsx`가
  `<html data-theme="light">`를 **서버에서 박는다**. ⚠️ 기본값을 바꾸려면 CSS가 아니라 그 속성과
  `page.tsx::serverTheme()`을 **같이** 고친다. ⚠️ 부트스트랩은 `layout.tsx`의 **body 첫 자식 동기
  스크립트**다 — React 상태로 옮기면 다크 사용자에게 흰 화면이 번쩍인다(`suppressHydrationWarning`도
  그 짝이라 내리지 말 것).
- **등락은 국내 관례**(상승=적/하락=청)이고 **상태색과 분리한다** — 뒤집으면 게이트 차단과 발행
  성공이 같이 뒤집힌다. ⚠️ `--mkt-up`과 `--critical`이 같은 값(`#ff5c72`)인 건 의도다. 가르는 건
  색이 아니라 **형태** — `▲`로 되돌리지 말 것.
- **`⚑`는 어디서나 맨 글리프다**(라벨·테두리 없음). 사는 자리 셋 모두 같은 모양이다: 고객 표 칸
  (`.flag.icon` — 폭 고정 20px 변종일 뿐이다) · 고객 상세 이름 옆 · `AI가 보는 정보`의 가명 옆.
  ⚠️ **`aria-label`·`title`을 빼지 마라** — 라벨이 없으니 그 둘이 유일한 설명이다.
  ▲가 아닌 이유: 수익률 옆에서 상승 화살표로 읽힌다. ⚑는 방향이 없다.
- **서체 = Inter(글) + IBM Plex Sans(숫자)**. ⚠️ 둘 다 **한글 글리프가 없어** 한글은 폴백으로
  떨어진다(의도한 것). `--font-num`은 **읽고 비교하는 수**에만.
- **도넛 = 청록 4단계**(`charts.tsx` 단일 출처). ⚠️ 다크·라이트에서 **농도 순서가 뒤집힌다.**

### 고객 포트폴리오 카드 = 찾기 · 읽기 · 묻기 3열

`260px | minmax(0,1fr) | minmax(0,1fr)` — 리스트 / 상세 / 채팅.

- **채팅 = `포트폴리오 질문`**. 답하는 게 셋이다: **종목**(에이전트 조회) · **구성**(집중도·배분·
  성향 대비 — **코드가 계산**) · **조정 선택지**(후보는 코드가 뽑고 근거는 보유·시세·뉴스에서 온다).
  칩은 두 줄이고(종목 · 분석), 분석 줄 안에 조회형 셋과 제안형 둘이 같이 선다
  (`types.PORTFOLIO_CHIPS`).
  - ⚠️ **종목 칩과 분석 칩을 한 `.cchat-chips`에 담지 말 것** — 한 상자면 종목이 많은 고객에서
    `집중도`가 종목 사이에 끼어 줄바꿈된다. `<div>` 둘로 나눠 **종목 한 줄 · 분석 한 줄**
    (보유 0이면 종목 줄 자체를 안 낸다).
  - ⚠️ **주어는 포트폴리오까지만 넓혔지 사람으로 옮기지 않았다.** "이 고객에게 묻기"로 읽히면 답할
    수 없는 질문과 해서는 안 되는 질문(회신문 대필, 가드레일 4)을 부른다. **되돌리지 말 것** —
    입력 가드가 한글 이름을 못 잡아 방어선이 아직 UI다(§7).
  - **가드레일 1의 명시적 예외**: 수치는 `f1.portfolio_facts`(순수)가 계산하고 LLM은 문장만 ·
    이름·계좌·나이는 프롬프트에도 답변에도 없다 · 위험 판정은 저장된 `flag_reasons` 인용뿐.
    출처는 `[^hold]` → 배지 **`보유`**(한 낱말 — "내부 계좌데이터"는 고지가 이미 말한다),
    `as_of`는 **None**(스냅샷 시각 컬럼이 없어 날짜를 안 지어낸다).
  - **조정 선택지까지 쓴다**(2026-08-04 개정 — 옛 규칙은 "쏠림 관찰까지만"이었다). 읽는 사람이
    PB이고 최종 판단도 PB가 하므로 관찰에서 멈추는 건 도구를 덜 쓰는 것이었다. 지금은 **선택지** ·
    각 선택지의 **근거** · 그 선택지가 **바꾸지 않는 것**(`keeps`)까지 쓴다.
    - ⚠️ **그래도 셋은 금지다**: ① 특정 안을 권하거나 순위 매기기 ② 목표 비중·조정 금액 같은
      **새 수치**(입력에 없는 값이다) ③ 미보유 종목 매수 권유 — 언급할 때는 **그 문장에서
      미보유임을 밝힌다**. 규칙 전문은 `f1.ANSWER_SYSTEM_PROMPT`에 있다.
    - ⚠️ **머리말·개수 세기를 쓰지 않는다** — "두 갈래를 적는다" 같은 문장은 근거가 없어 각주를
      못 붙이고 그대로 게이트에 걸린다. `keeps` 문장에는 **각주를 붙인다**(실측: "자산군 70%는
      그대로 남는다"의 70%가 계좌데이터라 각주 없이 나가 게이트에 걸렸다).
  - **선택지 후보도 코드가 뽑는다**(`f1.rebalance_options`, 순수·결정론적). LLM에게 "선택지를
    생각해봐"라고 하면 데이터에 없는 근거가 섞이고 거기에도 각주를 붙이려 든다 — 후보 4종
    (단일 종목 집중 · 업종 쏠림 · 자산군 배분 · 상위 종목 편중)과 근거 줄, 각주 태그까지 규칙이
    만들고 LLM은 문장으로 옮기기만 한다(`f1._advice_block`). "수치는 코드가 계산한다"의 연장이다.
    - ⚠️ **후보 문턱값(`_OPT_*`)을 "위험 플래그"라 부르지 말 것.** 플래그는 시드가 계산해 저장한
      판정(단일종목 65% 등)이고, 문턱은 **무엇을 이야깃거리로 올릴지** 고르는 훨씬 낮은 별개 기준
      이다(단일 30%·2위의 2배 · 업종 40% · 상위3 70%). 같은 말로 부르면 PB가 "규칙에 걸렸다"고
      오해한다 — 프롬프트에서도 그 말을 막아 뒀다.
    - ⚠️ **문턱을 넘는 게 없으면 후보는 빈 리스트다.** 채우려고 만들지 않는다 — 후보가 없다는
      것 자체가 "지금 구성에서 규칙이 짚을 게 없다"는 답이다.
    - ⚠️ 업종 분류는 **`f1.SECTORS`(이 저장소가 정한 표)**라 근거 줄의 `src`가 `none`이고
      **각주를 붙이지 않는다**(화면에서 `해석`으로 뜬다). 붙이면 계좌데이터가 그렇게 말한 것처럼
      읽힌다. 사전에 없는 종목은 버리지 않고 `분류 없음`으로 남긴다.
    - ⚠️ **후보·등락은 비식별화된 payload만 보고 계산한다**(`f1._equity_pct`) — 원본 facts를 쓰면
      허용 목록 밖의 값이 후보 문장을 타고 프롬프트로 샌다. `egress_guard`는 payload만 검사하지
      **후보 블록은 안 본다.**
  - **포트폴리오 라우트는 둘이고 가르는 건 비용이다**(2026-08-04). 조회형(`portfolio`)은 이미
    계산된 내부 데이터만 써서 **에이전트를 안 돌린다**(크레딧 0·즉답). 제안형
    (`portfolio_advice`)은 50종목 시세 배치 + 후보 종목 뉴스 위임이라 **수십 초**가 든다.
    - ⚠️ **제안형을 조회형보다 먼저 검사한다**(`f1.route`) — "리밸런싱"·"비중 줄일까"에는 조회형
      키워드도 같이 들어 있어 뒤에 두면 제안형이 영영 안 걸린다. `_INTENTS`(시세·실적·공시·뉴스)
      보다도 먼저다("편승할 만한 종목"에는 종목이 없어 종목 라우트로 가면 되묻기로 떨어진다).
    - **분석 칩의 질문문은 계약이다**(라벨은 자유, q는 계약): 조회형 셋은 `f1._PORTFOLIO_KEYWORDS`,
      제안형 둘(`조정 선택지`·`최근 흐름`)은 `f1._ADVICE_KEYWORDS`를 포함해야 한다.
      ⚠️ q에서 `리밸런싱`·`편승`을 빼면 조회형으로 떨어져 **선택지가 안 나온다.**
    - **뉴스 조회는 3종목까지**(`main.ADVICE_NEWS_LIMIT`) — 위임 한 건이 15~20초라 상한이 없으면
      답까지 몇 분이다. 자른 사실은 진행 SSE에 남긴다(조용히 줄이면 "뉴스가 없는 종목"과
      구분되지 않는다). ⚠️ **시세·뉴스가 실패해도 후보 자체는 낸다** — 안 그러면 PB가 "제안할 게
      없다"와 "조회가 실패했다"를 구분할 수 없다.
    - 미보유 종목은 **따로 묶어** 넘기고 상한 3이다(`f1.momentum_view`) — 섞으면 답변이 미보유
      종목을 보유인 것처럼 말한다. ⚠️ **보유 종목의 순위는 보유 종목 안에서만 매긴다**(실측:
      5종목 보유인데 "보유 50종목 중 14위"로 나갔다) — 그래서 `momentum_ranking`을 두 번 부르고
      모집단 크기 `of`를 함께 돌려준다.
  - 스코핑은 서버다: `/api/chat/stream?customer_id=`가 담당이 아니면 **404**.
- **칩은 입력창을 채울 뿐 보내지 않는다**(실행은 크레딧). 프리필은 effect가 아니라 **렌더 중 조정**.
- **대화는 고객별로 남는다**(2026-08-06). 고객을 바꾸면 컴포넌트는 갈아 끼워지지만(`key`) 대화는
  `chatKeep`의 `customer-{고객id}` 자리에 보관돼, 그 고객으로 돌아오면 말풍선·입력창이
  되살아난다(상담 준비 메모와 같은 이유 — 고객을 오가며 훑는 것이 이 화면의 기본 동작이다).
  - ⚠️ 그전에는 **새로 시작하는 것이 결정**이었다. 이유는 `last_entity`가 **앞 고객의 종목을
    이어받는 것**이었는데, 보관 키가 고객별이라 **세션 id도 고객별**로 저장돼 그 위험은 그대로
    막혀 있다 — 돌아온 대화는 자기 세션을, 새 고객은 언제나 빈 세션을 쓴다.
    **키를 고객 없이 뭉뚱그리지 말 것**(그 순간 종목이 번진다).
  - 비우는 길은 채팅 칸 제목 줄의 **`↻ 새 대화`**다(고객을 바꾸는 것이 곧 비우기였으므로, 그
    자리를 대신 만든 것이다). 보관본을 **빈 대화 + 쓰던 질문**으로 덮고 `key`의 nonce를 올려
    새 마운트로 갈아 끼운다 — 말풍선과 함께 **세션 id도 버려야** 다음 질문이 방금 지운 종목을
    이어받지 않고, **입력창은 남아야** 한다(모달의 `새 대화`와 같은 규칙: 쓰던 질문은 사람의
    것이다). 답변이 도는 동안은 눌리지 않는다(끊으면 받는 중인 답을 크레딧만 쓰고 버린다).
  - ⚠️ 새로고침하면 사라진다(서버·`sessionStorage` 어디에도 담지 않는다 — F1의 규칙 그대로).
- **보유 표의 종목 이름 = 그 종목의 최종본 PDF를 여는 버튼**(2026-08-06). 그전에는 눌러서 준비 줄
  (공시·뉴스·노트)을 펼쳤는데, 상담 직전에 실제로 여는 건 결국 노트였고 펼침은 그 앞 한 단계였다.
  - ⚠️ **발행분이 없는 종목은 버튼을 안 그린다**(`.hrow-plain`) — 회색 버튼이나 빈 뷰어를 여는
    대신 누를 것이 없는 상태로 둔다. 누를 수 있다는 표시는 **점선 밑줄**이다(`.linklike`와 같은
    어휘). ⚠️ 색으로 가르지 말 것 — 이 표의 색은 등락·상태의 몫이다.
  - ⚠️ **꺾쇠 칸(`.hcaret`)도 같이 걷어냈다** — 펼칠 것이 없으면 그 기호는 거짓말이다.
  - ⚠️ **이 표에서 공시·뉴스 줄이 빠졌다.** 그건 브리핑 카드와 **고객 문의 모달**(`PrepMemo`)에
    그대로 있다 — `PrepLines`의 자리는 이제 문의 모달 하나다.
- **`PrepMemo`에 다시 넣지 말 것 셋**: "이 종목 묻기" 버튼(칩과 중복) · 시세 줄 · `상담 준비` 라벨.
- **고객 상세에 요약 줄을 되살리지 말 것.** 세 조각 중 둘은 같은 패널에 이미 있고(도넛 범례·`⚑`),
  남은 하나(주식 내 비중)는 **표에 빠진 열**이라 `.holdings .pct-eq`로 옮겼다(머리글 필요).
  ⚠️ **화면에서 비중을 다시 나누지 말 것** — 분모와 반올림은 `portfolio_facts`가 단일 출처다.
  **위험 플래그 사유는 플래그가 있을 때만 낸다** — 없다는 건 이름 옆 ⚑의 부재가 이미 말한다.
- ⚠️ **`flex` 축약형 금지** — `flex: 1`은 basis `0%`인데 높이 미정 컨테이너에서 `content`로 해석돼
  표가 스크롤을 잃는다. `flex: 1 1 0px`도 소용없다(**Next 16 CSS 최적화가 `0px`를 떨어뜨린다**).
  `flex-grow: 1` + `height: 0`을 쓴다.
- **반응형**: ≤1080px에서 채팅이 아래로, 로그는 `flex-grow:0`+`max-height:40vh`. ≤720px는 세로로 쌓인다.

### 비식별화 경계 — 고객 데이터가 외부 모델로 나가기 전

현실 배치에서는 **망분리된 내부 GPU**가 원본에서 식별정보를 빼고 금액을 비율로 바꾼 뒤 외부
모델에 넘긴다. 이 레포엔 그 GPU가 없어 **규칙(순수 코드)이 그 자리를 맡는다.**

- ⚠️ **정직하게: LLM에게 비식별화를 시키는 것 자체는 보안이 아니다**(그 모델이 이미 원본을
  봤다). 얻는 건 기밀성이 아니라 **구조** — 경계가 코드에 있고, 지나는 것이 검사받는다.
- **경계는 `main.chat_stream` 한 곳**: `portfolio_facts`(원본) → `redact_portfolio` →
  `egress_guard` → 프롬프트. ⚠️ **`portfolio_facts`를 고쳐 여기 맞추지 말 것** — 화면 보유 표의
  비중이 쓰는 단일 출처다. 경계는 그 **뒤**에 선다.
- **화면은 경계 안이라 실금액을 그대로 본다.** 변환본은 오직 프롬프트용이다.
- 변환은 **규칙이 한다**(결정론적·크레딧 0·테스트 가능). ⚠️ dict 변환을 LLM에 맡기지 말 것 —
  비율 자릿수가 회차마다 달라진다. 자유 텍스트 변환기는 아직 없다(`mode:'llm'` 자리만 비워 뒀다).
- **삭제가 아니라 일반화**가 기본 수단이다(지우면 답변이 얕아진다): 이름 → 가명 `고객 #1` ·
  나이 → 나이대 `30대` · 잔고 → 구간 `10억~50억` · 종목별 금액 → 비중 · 계좌번호 → 제외.
  ⚠️ **가명은 익명이 아니다**(원본 DB와 대조하면 특정된다) — 그래도 두는 건 대화의 **주어**가
  필요해서다. ⚠️ **`customer_id`·`age`는 `portfolio_facts`에 담지 말 것**(가드레일 1) —
  `redact_portfolio`가 인자로 받아 그 자리에서 바꾼다.
  - ⚠️ **구간 폭을 좁히지 말 것.** 비중이 같이 나가 `구간 × 비중`으로 금액이 역산된다 —
    **폭이 곧 방어선**이다(잔고 5구간 · 나이 10년). `test_bands_are_wide_enough_…`가 고정한다.
- **`egress_guard`가 실제 방어선**: 허용 키 화이트리스트 · 담당 고객명 대조 · 계좌 형식 ·
  payload 안의 100만 이상 정수. 걸리면 차단이고 에이전트는 안 돈다(크레딧 0).
  - ⚠️ **자리가 둘인 건 의도다**: ① 라우팅 **전**에 질문 문장만 ② 프롬프트 완성 후 전체.
    ①이 없으면 종목 질문은 a1·a2·a4가 먼저 돌아 이름 하나 때문에 크레딧을 쓰고 막힌다.
  - ⚠️ **큰 정수 규칙을 프롬프트 전체에 걸지 말 것** — KRX 종가·DART 재무수치가 공개데이터로
    들어 있어 종목 질문이 통째로 막힌다(`test_guard_does_not_block_public_market_and_dart_numbers`).
- **표시(`redaction.tsx`) = 고객 상세의 "가려진 쌍둥이"**. `.detail` 클래스를 그대로 빌려 같은
  형식으로 그리고 값만 변환본으로 바꾼다 — 나란히 놓으면 제자리에서 갈린다.
  - 라벨은 **`AI가 보는 정보`** 하나. ⚠️ `비식별화`로 되돌리지 말 것(법 용어로는 **가명처리**이고
    `비식별화`는 현행법 용어가 아니다). `가명처리`로도 안 간 건 **읽는 사람이 PB**여서다.
    **코드 어휘는 `redact`·`비식별화` 그대로 둔다**(a1·a2를 화면에 안 적는 것과 같은 규칙).
  - ⚠️ **접이식은 알약이 아니다** — 테두리·면 없이 **꺾쇠 + 굵은 한 줄**. **칩은 알약, 접이식은 꺾쇠.**
  - ⚠️ **펼친 상자 안은 표 하나뿐이다**(산문으로 되풀이하면 상자가 답변보다 길어진다).
    ⚠️ **평가금액 열을 지우지 말 것**(`가림`으로 남긴다) — 없으면 왼쪽 상세와 줄이 어긋난다.
    ⚠️ 왼쪽 상세 마크업을 고치면 **여기도 같이 볼 것.**
- **프론트가 맡는 몫은 둘뿐이다.** ⚠️ **프론트에서 변환하지 말 것** — 브라우저는 이미
  `/api/customers`로 원본을 받아 그린다(`₩14.3억`을 그리려면 그래야 한다). 거기서 가려도 외부
  호출은 백엔드에서 나가 보장이 안 생기고, 같은 규칙이 두 곳으로 갈린다.
  - **보내기 전 경고**(`F1Chat::outboundWarning`) — ⚠️ **차단이 아니다.** 보내기 버튼도 살려 두고
    상자도 안 두른다(권위는 백엔드, 오탐이면 그냥 보낼 수 있어야 한다).
  - **미리보기**(`GET /api/customers/{id}/egress-preview`) — 채팅과 **같은 백엔드 함수**(크레딧 0).
    자리는 **고객 카드에만**. ⚠️ 프론트에서 비율을 다시 계산하지 말 것.
    - ⚠️ **한 화면에 상자는 하나다**(2026-08-06). 미리보기가 선 화면에서는 답변 말풍선 안의
      같은 상자를 **안 낸다**(`F1Chat`: `!preview && t.redaction`) — 라벨이 같아서 둘이면
      어느 쪽을 읽는지가 흐려진다. 말풍선 쪽이 남는 자리는 고객 문의 모달(미리보기 없음)뿐.
    - ⚠️ **크게 보기(`⤢`)로 넘어가면 접힌 상태로 돌아간다**(`viewMode` → `collapseOn`).
      같은 DOM에 클래스만 바꾸는 구조라 열어 둔 `<details>`가 그대로 따라 올라온다 —
      크게 보려던 것은 대화지 이 표가 아니다.

### 고객 문의 모달 = 확인할 사실 + 그 자리에서 묻기

큐에서 연 문의는 **질문이 이미 특정된 유일한 화면**이다.

- **문의가 가리키는 종목은 `topic`·질문 원문에서 찾는다**(`ReviewModal::askedHolding`, 순수).
  기준은 **보유 종목명**이라 계좌 이야기(리밸런싱·연금·ISA)는 자연히 안 걸리고, 겹치면 긴
  이름이 이긴다. ⚠️ **전 보유 종목을 늘어놓지 말 것** — 이미 특정된 질문이 목록에 묻힌다.
- **사실은 `PrepMemo` 한 곳에서 온다.** 카드는 전 종목·금액 없음, 모달은 `only`(한 종목) +
  `amounts`(평가금액·주식 내 비중) — 모달엔 보유 표가 없어 "얼마나 들고 있나"를 말할 자리가
  여기뿐이다. **에이전트는 안 돈다**(크레딧 0).
- **대화는 문의별로 보관된다**(`page.tsx`의 `ChatKeep` Map). 이 모달은 닫으면 **언마운트된다** —
  전역 F1처럼 감춰 둘 수 없다(큐의 한 건에 딸려 있어 감추면 어느 문의의 대화인지가 사라진다).
  - 키는 `session-<문의 id>`다. ⚠️ **키를 고객 id로 바꾸지 말 것** — 같은 고객의 다른 문의끼리
    대화가 번져 앞 건의 종목을 이어받는다.
  - 닫는 순간 SSE는 끊긴다. 그래서 보관된 마지막 턴이 `running`이면 **끊겼다고 적어 둔다**(안 적으면
    다시 열었을 때 끝나지 않는 `조회 중…`이 선다). 새로 고치면 사라진다(전역 F1과 같은 기준).
- **인라인 F1은 고객 카드의 것과 같은 컴포넌트·같은 칩**(`types.PORTFOLIO_CHIPS`가 단일 출처).
  ⚠️ **주어는 여전히 포트폴리오다** — 문의 모달이라 "이 고객에게 뭐라고 답하지"로 더 쉽게
  미끄러진다. 제목·칩을 카드와 글자까지 같게 두는 이유가 그것이다(가드레일 4).
- **담당 PB가 아니면 사실도 채팅도 안 그린다**(정보장벽). 서버도 `customer_id` 스코핑으로 404다.
- 대화 로그는 **`42vh`**로 묶는다. ⚠️ 고정 px로 묶지 말 것 — 240px에서는 모달에 세로 여백이
  남는데도 네 문장 답변이 안쪽 스크롤에 걸렸다(스크롤이 둘이면 바깥 것부터 밀어야 한다).
- 라벨 아래 구분선은 **목록이 든 상자에만**(`고객 정보`·`문의 종목`). `고객 질문`은 문장 하나다.

### 명칭·표기

- **"팩트 노트" → "종목 노트"**. ⚠️ **아직 세 번째 이름이 있다** — 백엔드 큐 제목과 노트 모달이
  `실적·공시 노트`다. `CLAUDE.md` 기능 표는 아직 `F3 종목 팩트 노트`.
- **날짜는 `api.ts` 한 곳으로. 함수가 둘이고 섞으면 안 된다.**
  - `fmtDate` = **시각 없는 날짜**(공시 `20260722`·뉴스 RFC 2822). 형식이 달라 화면마다 자르다
    **`뉴스 Wed, 22 Ju`가 사용자에게 나갔다.** 못 읽는 값은 원문을 돌려준다.
  - `fmtDateTime` = **시각 있는 사건**. ⚠️ 여기에 `fmtDate`를 쓰면 ISO 타임스탬프가 날짜 분기에
    걸려 **UTC 원문을 그대로 자른다** — KST 00~09시 사건이 하루 전으로 찍힌다.
  - **하루 경계는 KST 고정**(`bizDay`) — 브리핑이 내세우는 게 하필 "밤사이 뉴스"다.
- **화면 문구에 `—`를 접속사로 쓰지 않는다.** 한 문장을 두 동강 내는 건 사람이 안 쓰는 말투라
  "AI가 썼다"가 먼저 읽힌다. 문장은 **마침표로 끊고**, 라벨과 목록 사이는 **콜론**, 툴팁의 곁말은
  **괄호**로. "정상 동작했습니다" 같은 기계 말투도 쓰지 않는다.
  - ⚠️ **예외 둘은 `—`가 맞다.** ① 값 없음 표시(`'—'`) — 빈칸은 "0%"로도 읽힌다.
    ② **규제 문구**(`types.ts::WATERMARK`는 `compliance.py`와 글자까지 1:1이다).

### 런타임 프롬프트도 같이 정렬한다

**`CLAUDE.md`는 문서가 아니라 실제 프롬프트다** — 모든 `query()`가 `cwd`=레포 루트로 돌고
`setting_sources`를 안 좁혀 파일이 그대로 실린다. 화면·문서만 PB로 바꿨을 때 에이전트는 여전히
자기를 "금융 리서치 코파일럿"으로 알고 있었다. 같이 볼 곳: `f1.py::ANSWER_SYSTEM_PROMPT` ·
`main.py::BRIEF_SYSTEM_PROMPT` · `.claude/agents/a5.md`.

---

## 1. a5는 위임이 아니라 backend 2차 `query()` — `Agent` 도구 비동기 함정

*(코드 여러 곳이 "HANDOFF §1"로 가리킨다. 상세 배경은 `CLAUDE.md` "검증됨" 절.)*

`Agent` 도구는 **비동기**다 — 결과가 `"Async agent launched successfully"`로 즉시 돌아오고
서브에이전트는 백그라운드에서 돈다. **O가 턴을 끝내면 스트림이 닫히고 도구를 안 부르는 a5가
잘린다.**(a1·a2·a4는 도구 결과가 스트림에 빨리 들어와 살아남는다.)

**채택한 구조**: O의 1차 쿼리가 끝나면 backend가 a5를 **메인 에이전트로 하는 2차 `query()`**로
직접 돌린다(`main.py::event_gen`). a5가 메인이라 `text_delta`가 `parent_tool_use_id=None`으로 흘러
토큰 스트리밍이 산다. 지침은 `a5.md` **본문만** 읽어 `system_prompt`로 쓰고, 입력은 O의 산문이
아니라 **도구 결과를 그대로 직렬화**(`_a5_input`, LLM 미개입).

- `background: false`도 위임을 동기화하지만 **StreamEvent가 안 나와 토큰 스트리밍이 죽는다** → 안 씀.
- ❌ **반복 금지**: 프롬프트에 "a5 결과를 기다려/인용해"를 넣었더니 O가 **a2 수치로 문장을 지어내
  a5가 쓴 것처럼 인용**했다(가드레일 3 위반). 지금은 O 프롬프트에서 노트 단계를 아예 뺐다.

---

## 1-1. 출처 부착률·게이트 정의 — 지표 함정

*(코드가 "HANDOFF §1-1"로 가리킨다.)*

**핵심 교훈: 지표를 믿기 전에 지표부터 검증하라.** 낮은 수치를 보면 산출물을 탓하기 전에 측정기를
의심할 것 — 측정 정의 버그 4건을 잡았고 전부 a5 오류가 아니었다.

**확정된 정의**(`citations.citation_stats()` **단일 출처** — 게이트·대시보드가 같이 쓴다):
- 문장마다 `kind`: `heading`·`boilerplate`·`claim`(사실 주장)·`interpretation`(해석·전망).
- **부착률 분모 = `claim`만.** 해석 문장은 규칙상 각주를 안 붙이므로 뺀다. 안 빼면 a5의 해석
  문장 수 변덕이 품질로 오독된다.
- **분류는 보수적으로** — 애매하면 `claim`. 헐거우면 미인용 문장이 분모에서 빠져 부착률이
  후해지는데, **컴플라이언스 지표는 그 방향으로 틀리면 안 된다.**
- **게이트(`unsourced_count`)는 F3에서 해석 문장도 센다** — 발행 전 사람이 판단하도록 올리는 게
  설계다. 지표와 게이트 정의가 다른 건 의도된 것. (F1은 발행 단계가 없어 `claim`만 센다.)

⚠️ **분류기를 바꾸면** 기존 노트의 `sentences_json`은 저장 시점에 굳어 있으므로
`scripts/reparse_notes.py`로 재파싱해 대시보드와 맞춘다(멱등·크레딧 0).

**⚠️ `_BOILERPLATE_RE`는 두 번 빗나갔다 — 어형을 좇지 말 것.** a5가 스스로 붙이는 고지문구가
회차마다 달라진다(접두 기호 · 끼어드는 수식어). 지금은 **뜻의 서명** 세 갈래로 잡는다: ① 노트가
자기를 가리키는 말 ② AI 초안 서명(수식어 허용) ③ 순수 면책 표현(`투자권유 … 아님`·`내부 참고용`).
- 넓히는 방향이라 위 "보수적으로"와 반대다 → **고지에만 쓰이는 표현으로만** 넓혔고,
  `test_real_note_sentences_are_not_boilerplate`로 본문이 새지 않는 걸 고정했다.
- **근본 해법은 따로 있다(미착수)**: `compliance.WATERMARK`를 백엔드가 이미 본문에 강제 삽입하므로
  a5의 자기 고지는 **중복**이다. ⚠️ `a5.md`에는 그것을 **쓰라는 지시가 없다** — 모델이 스스로
  붙이는 것이라, 지울 게 아니라 **금지**를 적어야 한다. 고치면 결과는 라이브 실행으로만 보인다.
- 모달은 `kind !== 'boilerplate'`로 이미 거른다(`ReviewModal.tsx`) — **분류만 고치면 목록에서도 빠진다.**

---

**지연시세 규칙은 문장 단위다 (2026-08-06 수정).** 예전에는 **문서 단위 공존**이었다 —
「시세 낱말이 어딘가 있다」+「가격·퍼센트가 어딘가 있다」. 그래서 시세를 한 줄도 인용하지 않은
노트가 **자기 실적 수치 때문에** 막혔다(실측: 노트 #33의 "주가가 최근 큰 폭 하락했으나…"가
다른 문단의 "매출액 300조8,709억원"과 만나 걸렸다).

- 지금은 `compliance.quotes_market_data(sentence)` 하나가 판정한다: ① 출처가 `[^krx]`(시세
  그 자체)이거나 ② **시세 낱말과 수치가 같은 문장** 안에 있을 때. ②는 뉴스에서 옮긴
  "주가 7만원 돌파"를 계속 잡는다 — 옛 주석이 남긴 판단 그대로다.
- ⚠️ **뺀 문장은 인덱스로 받는다**(`check_note(..., removed_indices=)`). 본문에 원문이 남아
  있는지로 추론하면 본문과 문장 목록이 어긋나는 순간 규칙이 **조용히 꺼진다** — 컴플라이언스
  규칙이 틀려도 되는 방향은 그 반대다. ⚠️ 확인(ack)은 여전히 이 규칙을 못 푼다(§1-2).
- ⚠️ **문장 목록이 비면 문서 단위로 되돌아간다** — 파싱 실패로 규칙이 사라지느니 과검출한다.
- 🔸 남은 흠: `목표주가`가 `주가`를 부분문자열로 품어 "목표주가 49만원" 문장이 시세 인용으로도
  잡힌다. 그 문장은 `FORBIDDEN_PHRASES`로 어차피 막히므로 실무 영향은 없다.

## 1-2. 미인용 문장 확인(ack) — 게이트를 사람이 여는 유일한 경로

*(§1-1을 먼저 읽을 것.)*

**해결한 문제: 발행이 구조적으로 불가능했다.** 게이트는 해석 문장까지 미인용으로 세는데 막던 문장은
각주를 붙일 수 **없는** 것들이었다(해석·전망 · 고지 · 데이터 설명) — §1-1의 보수적 분류가 **발행을
잠그는 쪽으로 작동**한 것이지 a5 잘못이 아니다.

**채택한 구조**: 준법이 미인용 문장마다 **사유를 골라 확인**하면 그 문장만 집계에서 빠진다.
사유는 고정값(`main.ACK_REASONS`) — 감사 대상이라 자유 입력이 아니다. 확인·해제 전부 감사로그에 남는다.

- **면제되는 건 미인용 규칙 하나뿐.** 투자권유·광고성 표현, MNPI, 지연시세 고지 누락은 **못 푼다**
  (`test_ack_cannot_waive_forbidden_phrase`로 고정). 목표주가가 실린 노트는 확인을 다 해도 막힌다.
- **심의 단계에서만** 확인할 수 있다 — 초안에서 미리 풀면 검토가 형식이 된다. 해제는 언제나 가능.
- ⚠️ 확인 대상은 "해석으로 분류된 문장"이 아니라 **미인용 문장 전부**다.
- **재파싱하면 확인이 무효가 된다.** 확인은 문장 **인덱스**로 저장되므로, 저장할 때 원문 앞 60자를
  같이 남기고(`notes.acks_json`) 대조한다. 어긋나면 **막히는 쪽**으로 판정한다.
  - **판정은 `compliance.live_acks` 한 곳이다.** 게이트(`check_note`의 `acked_indices`)·노트 상세
    응답(`_note_to_dict`)·확인 응답(`POST …/ack`)이 **같은 함수**에서 나온다. 한때 대조가 발행
    경로에만 있어서, 무효 인덱스가 다른 미인용 문장을 가리키면 **"이제 발행할 수 있습니다"라고 말한
    뒤 409로 막혔다** — `.ackbar`가 막으려고 만든 상황이 바로 그것이다.
  - ⚠️ **프론트에서 60자 대조를 다시 구현하지 말 것** — 컴플라이언스 판정이 두 곳으로 갈린다.
    화면은 받은 목록을 그대로 믿으면 되고, 그게 맞는 이유는 경계에서 이미 걸렀기 때문이다.
  - **저장은 건드리지 않는다.** `acks_json`에는 무효분도 남고 이력은 감사로그에 있다. 재파싱을
    되돌리면 다시 유효해진다. `test_live_acks_…`·`test_gate_and_screen_count_the_same_acks`가 고정한다.

---

## 1-3. 금지 표현 예외(waiver) — 사유를 손으로 적는 유일한 자리

*(§1-2와 나란히 읽을 것. 둘 다 "사람이 게이트를 여는 경로"인데 **여는 규칙이 다르다**.)*

**해결한 문제: 규칙이 제시와 인용을 구분하지 못한다.** `FORBIDDEN_PHRASES`는 부분문자열
검사라, 제3자(증권사) 목표주가를 **각주와 함께 인용한** 문장도 똑같이 막았다(실측: 노트 #33의
"골드만삭스가 …목표주가를 49만원으로 제시했으며"). 확인(ack)은 미인용 전용이라 준법에게
남는 수단이 **문장 삭제**뿐이었다 — "이건 사실 보도이고 투자권유가 아니다"라는 판단을 남길
자리가 없었다.

**채택한 구조**: 준법이 **사유를 직접 적어** 그 문장의 금지 표현 위반만 통과시킨다.
`POST /api/notes/{id}/waive` · 저장은 `notes.waivers_json` · 감사 `waiver_added`/`waiver_removed`.

- ⚠️ **여는 건 금지 표현 규칙 하나뿐이다.** 미인용은 확인이, 지연시세는 문장 수정·제거가
  푼다. **MNPI는 어떤 예외로도 안 열린다**(판단이 아니라 정보장벽이다).
- ⚠️ **예외는 문장 단위다.** 같은 표현이 다른 문장에 또 있으면 그건 그대로 막힌다 — 한 문장을
  통과시킨 판단이 본문 전체로 번지면 예외가 아니라 규칙 해제가 된다.
- ⚠️ **사유가 자유 입력인 유일한 곳이다**(반려·보류·확인은 전부 고정값). 통과의 근거가 건마다
  달라서인데, 그 대신 **사유별 집계를 포기했다.** 빈 사유는 400이고 상한은 `WAIVER_MAX_LEN`(200자).
- **심의 단계·준법에게만** 열린다(확인과 같은 조건). 되돌리기는 언제나 가능하고, 되돌리는 쪽은
  검사하지 않는다 — 조건이 달라진 뒤 남은 예외를 되돌릴 길이 없으면 안 된다.
- **어느 문장이 막고 있는지는 백엔드가 찾아 준다**(`blocking_phrases` ·
  `compliance.forbidden_carriers`). ⚠️ **금지 표현 목록을 프론트로 복사하지 말 것** —
  컴플라이언스 어휘가 두 곳으로 갈린다(§1-2의 "60자 대조를 다시 구현하지 말 것"과 같은 규칙).
- 유효성 대조는 `live_acks` 한 곳을 그대로 쓴다 — 재파싱으로 인덱스가 밀리면 무효이고
  **막히는 쪽**으로 판정된다.
- **게이트를 부르는 자리도 하나로 모았다**(`main._live_violations`). 발행·PDF·확인 응답이
  각자 인자를 조립하면 한 곳만 빠뜨려도 화면과 발행이 갈린다.
- 화면: 문장 흐름 안 배지(`투자권유 표현 · 목표주가` → `예외 승인 · 목표주가`) + 아래 입력 줄
  (`.waive`). 예외가 걸리면 입력창 대신 **사유를 그대로 보여준다**. 안내 줄(`.ackbar`)도 같이
  고쳤다 — 예전에는 미인용만 세어 다 확인하면 "이제 발행할 수 있습니다"라고 적었는데, 금지
  표현이 남아 있어도 그렇게 말했다(노트 #33 실측).

## 2. 반드시 알아둘 함정·운영 수칙

- 🚩 **`git commit`·`git push`는 실행 전에 반드시 묻는다 — 로컬 커밋도 예외 없다.** 메시지는
  **메모 말투**(`주제 — 변경 나열`), `~했다` 아님.
- **자주 커밋할 것** — 이전 세션의 대시보드 구현이 **미커밋 상태로 통째 유실**된 적이 있다.
  위 규칙과 충돌이 아니다: **"자주 커밋하자고 자주 제안하라"**는 뜻이다.
- 🚩 **크레딧이 없으면 코드 버그처럼 보인다.** `AssistantMessage(error='billing_error', …)` →
  SDK가 `Exception: Claude Code returned an error result: success`로 올린다. 진단 한 줄:
  ```
  docker compose exec -T backend python -c "
  import anyio; from claude_agent_sdk import query, ClaudeAgentOptions
  async def m():
      async for msg in query(prompt='1+1은?', options=ClaudeAgentOptions(max_turns=1)): print(msg)
  anyio.run(m)"
  ```
  크레딧 없이도 되는 것: 자체 점검 11종 · 프론트 타입·린트 · **재생 SSE 화면 검증**(§6).
- **`pb_customers`·`pb_sessions`는 앱에 생성 경로가 없다.** 제품에서 문의는 **외부에서 유입되는
  것**이라(고객 앱·콜센터) 생성 화면이 없는 건 설계상 맞다 — 없던 건 시드 경로였다(§6).
  - ⚠️ **고객을 지우고 새로 만들지 말 것** — id가 바뀌면 감사로그의 `session_id` 참조와 옛 덤프가
    같이 어긋난다. 고객 데이터는 **UPDATE로 열만 바꾼다**(§3).
  - ⚠️ **UPDATE로 덮어쓸 때도 복원 경로를 먼저 만들어라.**
- ⚠️ **되돌릴 수 없는 조작을 검증할 때는 버릴 데이터를 만들어 그것에 대고 눌러라.** 브리프는 가짜
  날짜로 더미 행을 넣어 확인했다 — 실제 브리프는 다시 만들려면 크레딧·40~50초가 든다. 손대기 전에
  `pg_dump --table=… --data-only`로 백업해 두면 되돌릴 수 있다.
- **MCP 서버에서 `backend`를 import하려면 sys.path 부트스트랩이 필요하다**. **서버가 안 뜨면 그
  도구가 통째로 조용히 사라진다** — 실제로 krx가 죽어 브리프 3종목 **시세가 전부 빈** 채 게이트를
  통과했다. 손대면 `backend/.venv/bin/python backend/mcp_servers/<x>_server.py </dev/null`로 확인.
- **"오늘"의 정본은 `backend/bizdate.py` — KST 고정.** 컨테이너·Postgres가 UTC라 `date.today()`·
  `ts::date`를 그대로 쓰면 하루가 **KST 09:00에 끊긴다**. 새 날짜는 `biz_today()`, SQL은 `biz_date_sql()`.
  ⚠️ `bizdate`는 **stdlib만 쓴다**(krx MCP가 부트스트랩으로 끌어간다 — 무거운 의존성 = 서버 사망).
  ⚠️ 같은 상수가 **세 곳**(`bizdate.py` 정본 · `dart_server.py` 복사 · 프론트 `api.ts`).
- **"처리할 일의 목록"과 "읽을 것의 목록"은 다르다** — 큐는 발행분을 빼고 `/api/notes`는 포함한다.
  노트를 **읽으려면** 후자를 써라. 전자를 쓰면 **PB가 상담에 써도 되는 유일한 등급**이 사라진다.
- **도구 결과가 토큰 한도를 넘으면 `is_error=False`인 채 에러 문자열이 온다** → `_tool_failed()`로
  걸러 `completed` 오집계를 막는다. 자체 MCP는 반환량 상한을 둘 것.
- **원본과 대조할 때는 파이프라인과 같은 조건으로 조회할 것** — `days=2` 결과를 7일 창과 비교해
  "4건 유실"이라 오진한 적이 있다(실제 유실 없음).
- **SSE 이벤트명을 `error`로 쓰지 말 것**(EventSource 연결 오류 핸들러와 겹친다) → `run_error`.
  예외가 나도 `run_error`+`done`을 반드시 보낸다.
- 🚩 **새 HTTP 메서드로 라우트를 추가하면 `main.py`의 `CORSMiddleware allow_methods`도 같이 늘려라.**
  안 늘리면 예비요청(OPTIONS)이 400 `Disallowed CORS method`로 끊기고, 화면에는 원인과 무관한
  **`Load failed`**(WebKit) / `Failed to fetch`(Chromium)만 뜬다. **서버 로그에 그 라우트가 아예
  안 찍혀** 라우트 버그처럼 보이지도 않는다. 진단:
  `curl -i -X OPTIONS <url> -H "Origin: http://localhost:3000" -H "Access-Control-Request-Method: DELETE"`.
- 🚩 **데모 배포에서 긴 요청은 30초에 끊긴다 — 성공한 실행이 화면에서 실패로 보인다.**
  Next의 rewrites 프록시 기본 타임아웃이 **30초**다(`proxy-request.js`의 `proxyTimeout || 30000`).
  브리핑 40~50초·F3 ~100초가 전부 걸린다. 실제로 **백엔드는 200으로 완주해 DB에 저장했는데**
  화면은 500 `브리핑 생성에 실패했습니다`를 띄웠다 — **크레딧이 나간 뒤**라 이 방향의 오류가 더 나쁘다.
  프론트 로그의 `Failed to proxy … socket hang up(ECONNRESET)`만이 단서다.
  해결은 `next.config.ts`의 `experimental.proxyTimeout`(현재 300초).
  - ⚠️ **이 키는 공식 문서에 없다**(config 스키마에만 있다). Next를 올릴 때 살아 있는지 확인할 것 —
    조용히 무시되면 증상이 그대로 돌아온다.
  - ⚠️ **터널을 범인으로 의심하지 말 것**: 실측으로 Cloudflare 퀵 터널은 **최소 120초까지 통과**한다
    (30·60·100·120초 지연 응답 전부 200). 끊은 쪽은 늘 Next였다.
  - **로컬 개발은 rewrites를 안 타므로 이 함정이 안 보인다** — 데모 배포에서만 드러난다.
- ⚠️ **playwright `route.fulfill()`로 응답을 가로채면 CORS·프리플라이트를 못 본다** — 요청이 네트워크에
  나가지 않으므로 통과한 것처럼 보인다(위 버그를 화면 검사 8종이 전부 통과한 채로 놓쳤다).
  화면 상태만 볼 때는 가로채도 되지만, **새 메서드·새 헤더를 쓰는 라우트는 한 번은 진짜로 보내라.**
- **재생 SSE로 화면을 검증할 땐 코드의 실제 분기를 보고 시나리오를 짤 것** — "파싱 실패=노트 없음"
  으로 짰다가 허구를 검증할 뻔했다. `scenario_check.mjs` 참고.
- ⚠️ **`map` 콜백의 반환식 앞에 `{/* */}`를 넣지 말 것** — `=> ( … )`는 표현식 하나만 담아 JSX가
  통째로 깨진다. 주석은 `map` 바깥으로.
- **`scripts/seed_brief.py`는 F2 파이프라인이 아니다**(화면 검증용 시드) — **F2 결과로 소개하면 안 된다.**
- **DART 전자공시는 PDF가 아니라 HTML 뷰어** — "페이지 번호"가 없다. `viewer_url`+`rcept_dt`가 최대 해상도.
- **실화면은 목업으로 폴백하지 않는다** — 백엔드가 없으면 연결 실패를 그대로 말한다.
- **레포에 prettier 설정이 없어** 에디터가 파일을 재포맷하고 diff가 부푼다.
- **`docs/ARCHITECTURE.md`가 F1을 "확장 예정"으로 적어둔 채다** — F1은 구현됐다(§4).

---

## 3. 백엔드 라우트 · 목업 데이터

**라우트**: `/api/dashboard/{summary|queue|agents|audit}` · `/api/customers[/{id}]` ·
`GET /api/customers/{id}/egress-preview`(비식별화 미리보기 · LLM 미개입) ·
**`POST /api/customers/{id}/prep-note/pdf`**(상담 준비 메모 PDF · **만든 것을 `prep_notes`에
남긴다** · `prep_note_exported` 감사) · `GET /api/prep-notes`(목록 · 담당 고객만 · 본문 없음) ·
**`GET /api/prep-notes/{id}/pdf`**(남겨 둔 메모를 그때 그대로 다시 그린다 · 게이트 재통과 ·
`prep_note_opened` 감사) · `DELETE /api/prep-notes/{id}`(**한 건만** · 되돌릴 수 없음 ·
`prep_note_deleted` 감사) ·
`/api/sessions` · `/api/sessions/{id}/{approve|reject}` · `GET /api/notes`(발행분 포함 색인) ·
`POST /api/notes/{id}/ack`(§1-2) · **`POST /api/notes/{id}/waive`**(금지 표현 예외 · 사유 자유
입력 · §1-3) · `POST /api/notes/{id}/{review|deliberate|publish}` ·
`POST /api/notes/{id}/{reject|discard}`(거절 2종) · **`GET /api/notes/{id}/pdf`**(발행분 최종본 ·
LLM 미개입 · `note_exported` 감사) · SSE `research/stream`·`chat/stream` ·
`POST /api/briefs/run` · `GET /api/briefs/latest` · `DELETE /api/briefs/{id}`(그 브리프의
**날짜에 속한 행 전부** · 되돌릴 수 없음 · `brief_deleted` 감사).

- `/api/dashboard/audit`는 `limit`(기본 30, 최대 200) 또는 **`note_id`**를 받는다. 노트를 지정하면
  **limit을 안 쓰고 전건**을 준다 — 감사로그의 87%가 도구 호출이라 최근 N건 창이 그것으로 다 찬다.
- ⚠️ **고객·상담 계열은 `main.PB_NAME`으로 서버에서 좁혀진다.** **노트 계열은 안 좁힌다.**
- 🔸 `/api/sessions`는 **프론트가 부르지 않는다**(쓰는 곳이 없어 뺐다). 문의 상세가 필요해지면
  **쓰는 자리와 같이** 들여올 것. 세션 `active` 상태는 만들지도 읽지도 않는 죽은 값이다.

**목업 고객 50명**: **위험 플래그 규칙 3종**(①안정형인데 주식>40% ②단일종목≥65% ③수익률≤−5%)
+사유 저장. 수치는 전부 실집계, 비교 데이터가 없는 델타는 감춘다.
**AI 신뢰도 카드**: 출처 부착률(목표 90% 미터)·발행 통과율·게이트 차단(7일).

**보유 유니버스 12종목** — 전 종목 **DART 법인 존재 + KRX 시세 조회**를 확인했다.

| 종목 | 코드 | 보유 | 종목 | 코드 | 보유 |
|---|---|---:|---|---|---:|
| 삼성전자 | 005930 | 23 | 두산에너빌리티 | 034020 | 11 |
| SK하이닉스 | 000660 | 21 | HD현대중공업 | 329180 | 11 |
| 한화에어로스페이스 | 012450 | 18 | 현대차 | 005380 | 12 |
| 삼성바이오로직스 | 207940 | 14 | 기아 | 000270 | 10 |
| LG에너지솔루션 | 373220 | 13 | NAVER | 035420 | 9 |
| KB금융 | 105560 | 13 | HD현대일렉트릭 | 267260 | 8 |

- **상위 3개가 곧 브리핑 유니버스다**(`pb_watchlist`, `BRIEF_MAX_STOCKS=3`) — 삼성전자·SK하이닉스·
  한화에어로스페이스. 4위(14명)와 벌어져 동수 tie-break에 안 걸린다. ⚠️ `main.FALLBACK_WATCHLIST`도
  **같은 3종목으로 맞춰 뒀다** — 다르면 시드가 빈 순간 "빈 DB"인지 "다른 고객군"인지 구분이 안 된다.
- 재배정은 `scripts/reseed_holdings.py`(난수 없음 · **UPDATE SQL만 출력**). **`holdings`·
  `flag_reasons`만 바꾼다** — `alloc`·`return_pct`를 안 건드리고 **금액 배열을 재사용**하므로
  집중도 %까지 같다(바뀌는 건 종목명뿐).
- ⚠️ **유니버스를 바꾸면 `f1.ALIASES`도 같이 넓힌다** — 없으면 보유 종목 칩(코드로 감)은 멀쩡한데
  **손으로 친 종목명만** 되묻기로 빠져 원인을 찾기 어렵다. 짧은 별칭은 넣지 않는다(부분매칭 사고).
- ⚠️ `079550`의 DART 법인명은 **LIG디펜스앤에어로스페이스**다(옛 LIG넥스원).

---

## 4. F1 대화형 종목 Q&A (규칙 라우팅 + Redis 멀티턴)

입구 둘 다 같은 `/api/chat/stream`·`F1Chat`(인라인은 `compact`+`prefill`).
**라우팅·데이터취합·게이트는 코드가, LLM은 답변 문장만.**

- **입력 가드가 F1의 핵심**(`compliance.input_guard`) — 자유 텍스트라 F3·F2에 없던 공격면.
  MNPI·인젝션·PII를 **에이전트 前에** 잡으면 도구가 아예 안 돈다(크레딧 0).
- **규칙 라우팅**(`f1.route`, 순수) — 6자리 코드/별칭 + 의도 키워드 → 에이전트. LLM에 안 맡긴 이유:
  **데이터 소스 선택은 컴플라이언스 경계라 감사 가능한 규칙이어야 한다.**
- **포트폴리오 라우트 2종**(`has_portfolio=True`일 때만) — 둘 다 **종목 추출보다 먼저** 판정하고,
  종목이 없어도 되묻지 않는다(대상이 이미 정해져 있다).
  - **조회형 `portfolio`** — 집중·배분·성향 키워드(`_PORTFOLIO_KEYWORDS`). **에이전트를 안 돌린다**
    (근거가 이미 계산된 내부 데이터라 조회할 도구가 없다. 크레딧 0).
  - **제안형 `portfolio_advice`**(2026-08-04) — 조정·발굴 키워드(`_ADVICE_KEYWORDS`). 후보 계산
    (`rebalance_options`) → 50종목 시세 배치(`market.fetch_change_batch`, 실측 2.5초) → 후보 종목
    뉴스 위임(상한 3)까지 돌아 **수십 초**가 든다. ⚠️ **조회형보다 먼저 검사한다** — 조회형
    키워드가 같이 들어 있는 질문이 많다. 상세 규칙은 §0-1(고객 포트폴리오 카드).
- **게이트 F1 분기**: `CHAT_NOTICE`에 "지연시세"를 넣어 QUOTE 게이트를 자기충족. 발행 단계가
  없어 해석 문장은 위반으로 안 세고 **근거 없는 사실 주장만** 잡는다.
- **멀티턴(Redis)** — 종목이 없으면 **직전 종목을 이어받는다**(의도는 매 턴 새로 판단).
  `session_store.py`는 **라우팅 맥락만** 저장(턴 상한 8·TTL 1h) — 답변 본문·도구 결과는 안 담는다
  (턴 넘나들며 날조 방지). 만료·미지의 세션 id는 **빈 맥락**으로 떨어진다(에러가 아니다).
- **대화는 브라우저 state다** — 서버에 남는 건 위 라우팅 맥락뿐이라, 말풍선은 `F1Chat`이 들고 있다.
  전역 F1은 모달을 닫아도 유지되고 문의 모달은 `ChatKeep`이 문의별로 보관한다(§0-1). **새로 고치면
  사라진다.** ⚠️ 답변을 `sessionStorage`에 담지 말 것 — 고객 이야기가 브라우저 저장소에 쌓인다
  (입력창 `autoComplete`를 끈 것과 같은 이유).
- ⚠️ **추측은 하나까지다.** 종목을 이어받았는데 의도 키워드도 없으면 **되묻는다** — 예전엔 기본값
  까지 태워서 `"오늘 점심 뭐 먹지?"`가 직전 종목의 a2 조회를 실제로 돌렸다.
  되묻기는 두 종류(`f1.clarify_text`). ⚠️ **마크다운 금지**(`.chat-clarify`가 그대로 렌더한다).
- ⚠️ **`_PORTFOLIO_KEYWORDS`에 없는 말은 답할 데이터가 있어도 못 묻는다.** 반대로 넓히면 종목
  질문을 뺏는다 — 맨 `수익`은 안 넣었다(`"카카오 수익성"`은 a2). **종목별 수익률은 데이터에 없다.**

---

## 5. 아키텍처 한눈에

```
[F3] 종목코드 → O → a1 ‖ (a2 재무 · a4 뉴스) → 도구결과 직렬화 → 2차 query로 a5(§1)
     → citations → 워터마크 강제 → 게이트 → notes 저장 → SSE
     재무·뉴스 둘 다 없으면 a5 안 돌리고 사유만 보낸다
[F2] briefs/run → pb_watchlist → O → a1 ‖ a4 + krx, 지수는 market.py가 직접
     → brief.assemble(LLM 미개입) → 고지 강제 → 게이트 → 저장. 새 에이전트 0개
     리드·조용한 줄은 규칙이 **고른다**(pick_lead·quiet_note) — 본문 밖 lead_json에 산다
     어제 대비 새것은 brief_before(오늘)와 대조 — 같은 날 회차를 기준 삼으면 강조가 꺼진다
     평소 대비 꼬리표는 krx_quote가 이미 받아 온 수십 일치로 — 추가 조회 0회
     공시 중요도(0주요 1정기 2기타 3임원보고): 임원 보고는 빼지 않고 자리를 따로 준다
     뉴스도 같은 처방: 시황 나열·중복 제목은 뒤로 민다(brief.pick_news)
[F1] chat/stream → portfolio_facts(순수) → redact(비식별화 경계) → input_guard·egress_guard(질문문)
     → f1.route → ①종목: 에이전트 ②조회형: 계산된 값 그대로(크레딧 0)
                  ③제안형: rebalance_options + 시세 배치 + 뉴스(≤3) — 비식별화된 payload만 본다
     → egress_guard(프롬프트 전체) → 2차 query → citations → 고지 → 게이트 → session_store
     후보·근거·각주 태그까지 코드가 만들고 LLM은 문장으로 옮기기만 한다
[사람] 사실 확인(PB) → 심의 요청(PB) → [미인용 확인(준법)] → 발행(준법, 게이트 재검사)
```

- **큐는 역할에 따라 자리가 다르다**(`queueCard`를 한 번 정의하고 옮긴다) — 준법에겐 노트 탭이
  없어 큐를 옮기면 심의할 노트를 찾을 방법이 없어진다.
- **알림은 감사로그의 부분집합이지만 목적이 다르다**: 알림=골라낸 신호, 감사로그=완전한 원장.

**파일 대응**: a5 작문 지침=`a5.md` / 출처매칭·문장범주·부착률=`citations.py`(단일 출처) /
**발행분 PDF=`notepdf.py`**(코드 조립·LLM 미개입) /
F1=`f1.py`(라우팅·포트폴리오 사실·**조정 선택지 후보**)·`session_store.py` / 브리프=`brief.py` /
지수·**등락 배치**=`market.py` /
고지·게이트·입력가드·**반출가드**·**확인 유효성**=`compliance.py` / **비식별화 경계=`redact.py`** /
상태·로그=`db.py` / **날짜 경계=`bizdate.py`** / 배선·API=`main.py` /
MCP=`mcp_servers/{dart,news,krx}_server.py` /
화면=`dashboard/{page,ResearchCard,ReviewModal,F1Chat,PrepMemo,charts,api,types}.tsx` /
**출처 표시=`sources.tsx`** / **비식별화 표시=`redaction.tsx`** /
색·서체=`dashboard.css` 머리주석 + `docs/design/DESIGN-binance.md` / 테마=`app/layout.tsx` /
**시드=`scripts/{reseed_holdings,seed_pb_sessions}.py`**(둘 다 SQL만 출력하고 DB에 직접 안 쓴다 —
되돌릴 수 없는 조작은 사람이 실행한다).

---

## 6. 환경/실행 메모

- 앱: `docker compose up`(backend 8000 / frontend 3000 / postgres 5432 / redis 6379).
  **requirements 변경 시 `docker compose build backend && up -d --force-recreate backend`**.
- 대시보드: http://localhost:3000/dashboard
- **자체 점검(크레딧·네트워크 불필요, 12종)**:
  `for t in compliance brief krx streaming tool_result a5_input brief_attribution citations run_outcome f1 redact notepdf; do docker compose exec -T backend python -m backend.test_$t; done`
- **프론트 검증**: `cd frontend && npx tsc --noEmit && npx eslint src --max-warnings=0`.
  ⚠️ **CSS는 이 둘이 검사하지 않는다.** Next.js 16: 코드 전 `frontend/node_modules/next/dist/docs/` 확인.
- **화면 재생 검증**(크레딧 0, `frontend/`에서): `node scenario_check.mjs`(F3 3시나리오) ·
  `node f1_modal_check.mjs`(전역 F1 모달: 대화 유지·×·새 대화·`●`).
- **라이브 실행(비용)**: F3 `curl -sN ".../api/research/stream?stock_code=005930" > /tmp/f3.sse`(~100초) /
  F2 `curl -s -X POST .../api/briefs/run -d '{}'`(~40초) / F1 `curl -sN ".../api/chat/stream?q=…"`.
  → `grep "^event:" *.sse | sort | uniq -c`로 이벤트별 건수부터 본다.
- **고객 문의 시드**: 한 줄이고 시각 보정이 필요 없다(`now() - interval` 기준).
  ```
  docker compose exec -T backend python /repo/backend/scripts/seed_pb_sessions.py \
    | docker exec -i ai-pb-agent-postgres-1 psql -U app -d app
  ```
  `--pending N`(기본 4) · `--done N`(기본 8) · `--append`(TRUNCATE 생략).
  ⚠️ 기본 출력이 **`TRUNCATE pb_sessions`로 시작한다.**
- **노트 재파싱**(분류기 변경 후, §1-1): `docker compose exec -T backend python -m backend.scripts.reparse_notes`
- **`pb_customers` 복원**: `TRUNCATE pb_customers;` 후 `restore_pb_customers.sql`을 흘려 넣는다 —
  INSERT만 들어 있어 **비우지 않고 다시 넣으면 PK 충돌**이다. ⚠️ `RESTART IDENTITY`는 붙이지 말 것.
- ⚠️ **`restore_pb_sessions.sql`·`shift_pb_sessions_time.sql`은 옛 경로다** — 절대시각이 박혀
  shift를 한 번 더 돌려야 했고 `id`를 박아 PK 충돌도 났다. **새로 쓸 이유가 없다.**
- 테스트 데이터 정리: `TRUNCATE notes, audit_log, briefs RESTART IDENTITY;`(`pb_*`는 지우지 말 것 — §2)
- API 키 4개(`.env`): `ANTHROPIC_API_KEY`·`DART_API_KEY`·`NAVER_CLIENT_ID/SECRET`·`KRX_API_KEY`.
  ⚠️ KRX는 **서비스별로 활용신청이 따로**다(종목시세·지수시세가 다른 신청, 둘 다 승인됨).
- **데모 배포(발표용 공개 URL)** — 절차·함정은 **`docs/DEMO-DEPLOY.md`**가 정본이다.
  ```
  docker compose -f docker-compose.yml -f docker-compose.demo.yml up -d --force-recreate frontend
  caffeinate -s cloudflared tunnel --url http://localhost:3000   # 창을 닫으면 URL이 죽는다
  ```
  앱은 노트북에서 그대로 돌고 터널만 앞에 붙는다 — **인터넷이 끊겨도 `localhost`로 데모를 잇는다**
  (클라우드에 올렸다면 그 경우 폴백이 없다). 되돌리기는 `docker compose up -d --force-recreate frontend`.
  - **바뀌는 건 프론트뿐**이다: API 호출을 상대경로로(→ `next.config.ts` rewrites가 백엔드로 넘김) ·
    비밀번호 한 겹(`src/proxy.ts`) · dev가 아니라 프로덕션 빌드.
    얻는 것은 공개 URL이 하나로 주는 것 · **CORS를 안 건드려도 되는 것**(same-origin) ·
    백엔드가 인터넷에 안 뜨는 것이다.
  - ⚠️ **터널은 3000에만 뚫는다.** 8000을 노출하면 비밀번호가 우회된다.
  - ⚠️ **개발 모드로 되돌리기 전에 터널부터 닫아라** — dev에는 비밀번호가 없어 그대로 공개된다.
  - ⚠️ Next 16에서 `middleware.ts`가 **`proxy.ts`로 이름이 바뀌었다.** 옛 이름으로 두면 조용히
    실행되지 않는다(= 인증 없이 뜬다). 빌드 로그의 `ƒ Proxy (Middleware)`로 확인한다.
  - `DEMO_PASSWORD`가 없으면 **프론트 컨테이너가 아예 안 뜬다**(무인증 공개 사고 방지, 의도).
  - 퀵 터널 주소는 **켤 때마다 바뀐다.** 고정 주소는 자기 도메인이 필요하다(Cloudflare는 무료
    서브도메인을 주지 않는다) — 발표 한 번이면 당일 켠 터널을 끝까지 유지하는 것으로 족하다.
- **발표 슬라이드** — 정본은 **`docs/presentation/slides-final.md`**(Marp). `./build.sh`가
  `slides-final.html`을 만든다(`pdf` 인자로 PDF도. 자체 완결이라 네트워크가 죽어도 열린다).
  조작: `←→` 넘기기 · `P` 발표자 노트 · `O` 전체 보기.
  - **문구·디자인 규칙은 `.md` 머리주석에 규칙째 적혀 있다** — 고치기 전에 그걸 먼저 읽을 것
    (명사 종결 · 대롱거리는 종결 금지 · 반말 수사의문 금지 · 자기 유보는 노트로, 출처는 화면에).
  - 🚩 **지금 `slides-final.html`에는 `.md`에 없는 손편집이 있다**(2026-07-30 — 표지 문구·제목
    강조·3장 카드 설명·5~7장 불렛). **`./build.sh`를 돌리는 순간 전부 사라진다.**
    빌드가 필요하면 **HTML의 현재 내용을 `.md`로 먼저 옮겨라.** 두 파일은 어긋나 있다.
  - ⚠️ 마크다운 함정: 닫는 `**` **앞이 구두점이고 뒤가 한글**이면 강조가 안 닫혀 별표가 화면에
    그대로 찍힌다(`**53%**가`). 그 자리는 `<strong>`으로 쓴다.
  - `slides-sample-1·2.html`은 손으로 쓴 **별개 덱**이라 빌드와 무관하다(형태 언어의 출처).
    구성·시간 배분·컷 라인은 `slides-7m-structure.md`.
- git: main → **personal**(hmgthb/ai-pb-agent). origin(kwangtekNa/intern)에는 푸시 안 함.
  **커밋·푸시 둘 다 사용자 확인 후에만(§2 첫 항목).**

---

## 7. 열린 항목 · 리스크

**제품 가치 순**
- **진입 단위가 종목인데 PB의 단위는 "오늘 만날 고객"이다** — 가장 좋은 화면(고객 포트폴리오)에
  가려면 페이지 최하단 표에서 고객을 직접 찾아야 한다.
- ⚠️ **월요일 브리프는 공시가 거의 빈다** — 파이프라인이 `dart_search days=2` 고정
  (`BRIEF_SYSTEM_PROMPT`)인데 월요일엔 그 창이 토·일·월이다. 거래일 기준으로 잡으면 되는데
  `bizdate`에 영업일 계산이 없다.

**규제·경계에 대해 정직하게 말해야 할 것**
- ❗ **"고객에게 말 걸지 않기"는 프롬프트 지시일 뿐 게이트 규칙이 아니다.** `compliance.py`는
  호칭·회신문 형식을 위반으로 세지 않는다(라이브 검사에서 발생은 0건이었다).
- **고객 이름 대조에 남은 구멍은 2글자 이름뿐이다** — 일반 낱말과 겹쳐 오탐이 커서 대조하지
  않는다(목업 50명은 전원 3글자). `PII_PATTERNS`는 숫자 형식만 본다.
- ❗ **자유 텍스트 비식별화기가 아직 없다.** 경계(`redact.py`)에 `mode: 'llm'` 자리는 뚫어 놨지만
  구현은 규칙(구조화 dict)뿐이다. 고객 문의 원문·상담 메모처럼 **정규식으로 안 되는 텍스트**가
  그 변환기의 일감이고, 실제 배치에서 망분리 GPU가 맡을 부분도 이쪽이다.
- ❗ **역할 강제는 여전히 화면에만 있다.** 백엔드는 `actor`를 감사로그용 문자열로 받을 뿐 "발행은
  준법만"을 강제하지 않는다. **스코핑은 서버로 내려갔지만 권한은 아직 아니다.**
- **감사로그 200건 캡은 노트에 한해 풀렸다**(§3). `note_id`가 없는 사건은 여전히 최근 200건까지만
  보인다. **원본 DB는 append-only 전건 보존.**
- ❓ **정량 가치 밴드는 추정 모델** — `docs/VALUE.md` 민감도표로 답하고 **단일 숫자로 단정하지 말 것.**
- **문제 정의 쪽 근거가 하나 생겼다(2026-07-30)**: 삼성증권 SNI PB 100명 설문(2024-03) —
  업무시간 배분이 포트폴리오 분석 28% · 오프라인 미팅 18% · 시장 동향 파악 15% · 내부 회의 15% ·
  전화/온라인 상담 14% · 맞춤 자료 작성 10%(합 100%). 만나기 **전** 셋을 더한 **53%**를 발표
  2장이 인용한다.
  - ⚠️ **이건 업무시간 *비중*이라 "상담 1건에 20분"을 받쳐 주지 않는다.** 게다가 SNI PB는
    초부유층 전담이라 **주 4건**이고, 가치 산식의 "하루 3건"과 세그먼트가 다르다.
  - ⚠️ **`docs/VALUE.md`에는 아직 안 들어갔다** — §1-1은 여전히 "20분 → 8분은 미검증 가정"만
    적고 있다. 넣을 때 위 두 한계를 같이 적을 것.
  - 해외 비교가 필요하면 Kitces Research(2019, 어드바이저 1,000명+): 상담 준비 주 5.3시간 ·
    상담 6~9건/주 → 건당 35~50분. **국내 실측은 여전히 없다**(파일럿에서 잰다).

**PDF를 붙이며 드러난 것 (2026-08-06)**
- 🔸 **준법이 나중에 다시 뽑는 길은 아직 없다.** 발행분을 여는 경로는 PB 쪽(상담 준비, 위
  `pickNotes`)과 **발행 직후 열려 있는 모달**뿐이다. 준법 화면에는 발행분 목록이 없고, 감시 탭의
  노트별 감사 조회는 전건 색인을 들고 있으면서도 문서 링크가 없다 — 붙인다면 거기가 한 줄이다.
- ✅ **금지 표현에 사람이 판단을 남길 자리가 생겼다**(2026-08-06, §1-3). 준법이 사유를 적어
  그 문장만 통과시킨다. 남은 물음은 정책 쪽이다: `CLAUDE.md` 게이트 명세의 "**규정상 필수 고지
  동반**"은 여전히 구현되지 않았고(고지를 붙여도 통과하지 않는다), 지금은 사람의 판단이 그
  자리를 대신한다. 자유 입력이라 **사유별 집계가 안 된다**는 것도 감수한 값이다.
- 🔸 **안내 줄은 금지 표현까지만 안다**(2026-08-06 부분 수정). 금지 표현이 남으면 그렇게
  말하도록 고쳤지만(§1-3), **지연시세 누락은 여전히 안내 줄에 안 뜬다** — 그 경우 "이제
  발행할 수 있습니다"라고 적고 발행에서 409를 받는다. 화면이 위반을 스스로 세는 대신
  응답의 `violations`를 그대로 그리면 이 종류가 통째로 사라진다.
- 🔸 **`f1_modal_check.mjs`가 지금 실패한다** — 내 변경과 무관하다(변경 없는 상태에서도 같다).
  첫 단계가 `button.fab`을 누르는데, 기본 탭이 고객 카드라 **인라인 채팅이 서 있어 FAB이 숨는다**
  (그 규칙 자체는 의도한 것, §0-1). 검사가 탭을 먼저 옮기도록 고쳐야 한다 — **이 검사가 죽어 있는
  동안은 전역 F1 모달의 언마운트 회귀를 아무도 안 잡는다.**

**남은 검증**
- ❓ **3시나리오 예외 상태는 재생 SSE 기준** — 라이브로 돌려본 건 아니다.
- ❓ **비식별화 경계에서 LLM 답변까지는 못 봤다.** 경계 자체는 LLM 앞이라 실데이터로 확인했다.
  **못 본 것: 구간·비율만 받은 모델이 금액을 추정해 적지 않는지**(프롬프트로 금지만 해 뒀다).

**작은 것**
- 🔸 **LLM이 쓰는 문장의 말투는 아직 안 건드렸다.** 화면 정적 문구에서는 `—` 접속사를 걷어냈지만
  노트 본문과 F1 답변은 `a5.md`·`f1.py::ANSWER_SYSTEM_PROMPT`가 지배한다. 둘 다 문체 규칙이 없다.
  고치면 **라이브 실행으로만** 결과를 볼 수 있다. §1-1의 boilerplate 재발 방지와도 겹친다.
- 🔸 `/api/notes` 전건 상세 조회.
