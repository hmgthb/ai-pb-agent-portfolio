# 인수인계 — AI PB 어시스턴트 (`ai-pb-agent`)

> 최종 갱신: 2026-07-23(저녁, PB 1인용 전환 반영) · **다음 세션은 이 문서부터 읽으세요.**
> 화면·역할·데이터 스코핑은 **§0-1이 최신**이다 — §0-0보다 §0-1을 먼저 신뢰할 것.
> 주차 계획은 `주차별_실행계획.md`(**원본 유지 — 진행 기록은 여기에만**), 런타임 지침은 `CLAUDE.md`,
> 대시보드 설계 이력은 `~/.claude/plans/inherited-beaming-canyon.md`, 측정은 `docs/EVAL.md`, 가치는 `docs/VALUE.md`.
> ⚠️ 이 문서는 이제 **git 추적 대상**이다(이전엔 `.gitignore`에 있어 한 번도 커밋 안 됐다). **자주 커밋할 것.**

---

## 0-0. 대상 사용자 = PB (2026-07-22 확정) — 되돌리지 말 것

계획서(§3-1)는 주 사용자를 **리서치센터 애널리스트**로 썼지만, 이 프로젝트의 제품은
**PB가 고객 상담 전에 참고하는 사실 정보 도구**로 확정됐다. **AI는 PB를 대신하지 않는다** —
사실을 모아줄 뿐이고, 고객에게 나가는 말은 PB가 직접 쓴다.

이 확정에 따라 바꾼 것(§0의 "다음 할 일"보다 먼저 읽을 것):

- **화면 정체성** — 브랜드 "AI PB 어시스턴트 / 상담 전 사실 확인", 상단 고지에 "PB 상담 준비용"
  명시, **기본 역할 = PB**(이전엔 관리자), 기능 레일을 PB 언어로(F1 종목 즉답 · F2 상담 전 브리핑 ·
  F3 종목 팩트 노트 · F4 피어·섹터 비교 · F5 규정 확인 — 코드는 뒤에 작게).
  ↪︎ **기능 레일은 §0-1에서 삭제됐다**(F1만 눌렸고 F4·F5는 없는 기능 광고). 고지 위치·문구,
  F3 명칭("종목 노트")도 §0-1에서 바뀌었다.
- **F2 유니버스** — 데모 3종목 하드코딩 → **고객 보유 상위 N종목**(`main.pb_watchlist`: 보유 고객 수
  우선, 동수면 금액). 브리프 카드에 "보유 고객 N명" 배지 = 선정 근거가 화면에 보인다.
  ↪︎ 집계 범위는 §0-1에서 **담당 고객 기준**이 됐고(고객 50명이 전부 한 PB의 것), 배지는
  "고객 N명 보유" 한 줄이다.
- **시장 현황(지수)** — `backend/market.py` + MCP `krx_index` 추가. ⚠️ **현재 키는 지수시세정보에
  미신청이라 403**이다 → 브리프는 `market_json`에 사유를 남기고 화면이 "미연결"이라고 말한다.
  공공데이터포털에서 **"금융위원회_지수시세정보" 활용신청**하면 그대로 켜진다(코드 수정 불필요).
  응답 필드명(idxNm/clpr/vs/fltRt/basDt)은 명세 기준 작성 — **실호출 미검증**(확인 필요).
- **상담 준비 메모** — 고객 상세에 그 고객 **보유 종목 기준**으로 브리핑·팩트 노트에서 모은 사실을
  재배열(`PrepMemo`, page.tsx). 에이전트 재실행 없음 · 없으면 없다고 말함 · "이 종목 묻기"로 F1 프리필.
- **PB 대신 말하지 않기** — 상담 모달을 "AI 답변 초안 승인·전송"에서 **"고객 문의 대응 준비"**로
  바꿨다(회신 전 확인할 사실 + "확인 완료(회신은 PB가 직접)"). 상태 라벨도 전송완료→확인완료.
- **가치 모델** — `docs/VALUE.md` §1-1에 PB 기준 재해석 추가(**PB 1인 ≈ 0.1 FTE**, 인원은 지어내지
  않음). 기존 애널리스트 밴드(~2/~5 FTE)는 폐기가 아니라 **2차 시나리오**로 유지.
- **첫 화면 순서**(정렬 후 재평가에서 나온 것 — 화면 문구만 바꾸면 첫 900px는 그대로 관리
  대시보드였다) — `AI가 오늘 한 일` → `상담 전 브리핑` → 타일 → 노트 생성 → 처리 대기 → 고객
  포트폴리오. 14일 추이 차트는 **감시 탭(옛 "AI 평가")으로 이동**(거기 단일 계열 "노트 생성"과 겹쳐 2계열로 교체).
  ↪︎ **§0-1에서 타일·노트 생성을 브리핑 위로 올렸다**(사용자 요청). 이 항목의 원래 우려
  ("첫 900px가 관리 대시보드처럼 보인다")는 여전히 유효한 반대 논거이니, 되돌릴 때 참고할 것.
- **"AI가 오늘 한 일" 줄** — 브리핑 N종목 수집 · 도구 호출 N건(에이전트 N종) · 종목 즉답 N건 ·
  팩트 노트 N건 · 마지막 실행 시각. 전부 **훅이 남긴 감사로그 오늘치**(`db.today_activity`,
  summary의 `today`). 프론트에서 audit 목록을 세면 최근 N건 제한에 걸려 조용히 적게 세인다.
  **0인 항목은 적지 않는다** — 0건을 나열하면 아무것도 안 한 날도 일한 것처럼 보인다.
- **런타임 프롬프트까지 정렬**(2026-07-23 `ffabe22`) — 화면·문서는 PB로 바꿨는데 **에이전트는
  여전히 자기를 "금융 리서치 코파일럿"으로 알고 있었다**. `CLAUDE.md`는 문서가 아니라 **실제
  프롬프트다** — 백엔드의 모든 `query()`가 `cwd`=레포 루트로 돌고 `setting_sources`를 안 좁혀서
  파일이 그대로 실린다. 고친 것: 역할(사용자=PB, 산출물은 PB가 읽을 재료) · 가드레일 1(고객
  보유데이터는 **유니버스 선정에만**, 산출물에 식별정보 금지 — 코드는 이미 그랬는데 규칙이
  어디에도 없었다) · 가드레일 4(**회신문 대필 금지**) · 서브에이전트 표(없는 A3·A6를 위임
  대상으로 알려주고 있었고 a5를 위임하라 적어 §1과 모순이었다) · 고지문구 기능명.
  코드 쪽 프롬프트도 같이: `f1.py::ANSWER_SYSTEM_PROMPT` · `main.py::BRIEF_SYSTEM_PROMPT` ·
  `a5.md`("독자는 PB다"). F2 명칭은 코드 주석까지 "상담 전 브리핑"으로 통일.
  ⚠️ `CLAUDE.md`의 **고지문구 열은 `compliance.py::NOTICES`와 1:1** — 기능 이름만 바꾸는 건
  코드에 영향이 없지만 **문구를 고치면 코드도 같이 고쳐야 한다**.
  ❗ **"고객 호칭 금지"는 아직 프롬프트 지시일 뿐 게이트 규칙이 아니다**(§7).
- **"담당 고객" 축을 데이터 경로까지 내림**(2026-07-23, 화면 감사에서 나온 3건) — 라벨은 PB인데
  코드가 안 따라간 자리들이었다. 셋 다 라이브로 확인했다.
  - ~~**브리핑 유니버스는 전사 기준이다.**~~ **§0-1에서 폐기**(고객 50명이 전부 한 PB의 것이 돼
    "전사"라는 축 자체가 없어졌다). 당시 전사 집계를 택한 이유는 "배치라 로그인 사용자가 없고
    PB별로 돌리면 크레딧이 PB 수만큼 는다"였는데, **1인용에서는 그 이유가 성립하지 않는다.**
  - **발행된 노트가 상담 준비 메모에서 사라지던 것.** 큐(`/api/dashboard/queue`)는 발행분을 빼는데
    프론트가 그 큐로 노트 맵을 만들고 있었다 — 사람이 검토·심의·발행을 끝낸 노트, 즉 **PB가 상담에
    실제로 써도 되는 유일한 등급이 그 순간 화면에서 없어졌다.** `GET /api/notes`(발행분 포함)를 새로
    두고 거기서 만든다. **처리할 일의 목록과 읽을 것의 목록은 같지 않다.**
    같이 고친 잠복 버그: 한 종목에 노트가 2건 이상이면 덮어쓰기 때문에 **옛 노트가 이겼다**(→ 최신 1건).
  - **PB가 자기가 만든 노트를 못 봤다.** 노트에 생성자가 없어서 담당자(검토자·심의자)로만 걸렀는데
    PB는 검토자가 될 수 없다 — 만들 수는 있고 찾을 수는 없었다. `notes.created_by` 추가(멱등 ALTER),
    `/api/research/stream?actor=`로 화면의 목 로그인 사용자가 실려 감사로그에도 남는다.
    ↪︎ **§0-1에서 큐의 `created_by` 필터를 아예 없앴다** — PB가 1명이니 이 대시보드의 노트는
    전부 그 사람 것이고, 그래야 `created_by=NULL`인 옛 노트도 큐에서 사라지지 않는다.
    컬럼과 기록은 그대로 남아 있다(누가 만들었는지의 기록이므로).

**라이브 확인 완료(2026-07-22)**: 새 유니버스로 F2 실행 → 브리프 #7 = SK하이닉스(보유 5명)·
셀트리온(8명)·KB금융(7명), 시세·공시·뉴스 정상, 게이트 0건. ⚠️ 첫 실행은 **시세가 전부 비었다** —
원인은 krx MCP 서버가 안 뜬 것(§2의 sys.path 함정). 고친 뒤 재실행한 결과가 위 값이다.

**라이브 확인 완료(2026-07-23, 프롬프트 정렬 후 4종)**: F1 입력가드(차단) · F1 즉답(출처·고지
정상, 위반 0) · F2 브리프 #8(51초, 위반 0) · F3 노트 #12 SK하이닉스(72초, 워터마크·각주 정상).
산출물 직접 검사로 **DB 고객명 50건 중 본문 등장 0건 · 고객 호칭 문장 0건** 확인.
노트 #12의 미인용 1건은 노트 #7과 같은 종류(§1-1의 알려진 집계 한계)라 회귀가 아니다.

**열린 결정 — 고객 상세의 "AI 진단"**: `pb_customers.diagnosis`에 박힌 **시드 예시 문구**인데
라벨이 "AI 진단"이라 AI 산출물처럼 읽힌다(50명에 5종류가 돌아간다). 셋 중 하나를 골라야 한다:
① 라벨을 "예시 문구 · 시드"로 바꿔 사실대로(10분, 대신 그 줄은 정보가 없어져 삭제가 나을 수도) /
② **보유 종목·성향·위험 플래그 실값에서 규칙으로 재작성**(1시간, LLM 없이 근거 있는 한 줄 — 권장) /
③ 삭제. **지금 상태로 데모하면 실집계 숫자들 사이에 목업 문장이 섞여 있다.**

---

## 0-1. PB **1인용** 대시보드 (2026-07-23 확정) — §0-0보다 이 절이 최신이다

§0-0이 "사용자는 PB다"를 정했다면, 이 절은 **"PB는 한 명이다"**를 정한다. 이 대시보드는
여러 PB가 공유하는 감독 콘솔이 아니라 **한 사람의 개인 화면**이다. 세 커밋으로 들어갔다:
`d463d1f`(데이터 경로) → `2139f05`(화면 정리) → `4c0853e`(통합·명칭).

발단은 사용자의 지적이었다 — 브리핑 배지가 `보유 고객 21명 · 내 담당 5명`이라 **"이 화면을
여러 PB가 같이 쓴다"**는 뜻이 됐다. 실제로 그랬고, 그게 선정까지 틀리게 하고 있었다.

### 데이터 — 스코핑을 화면이 아니라 **서버**로

- **`main.PB_NAME`**(compose 환경변수, 기본 `"PB"`)으로 `/api/customers`·`/api/sessions`·
  `summary.customers_total`·`pb_watchlist`를 전부 좁힌다. `db.list_customers(pb=)`·
  `db.list_sessions(pb=)`에 인자를 더했다(None이면 전사 — 지금 쓰는 곳은 없다).
- **남의 고객 상세는 404다**(403이 아니다 — 존재를 알려주면 목록을 거른 의미가 없다).
  세션 승인·반려도 같이 막았다: 읽기만 좁히면 id를 아는 것만으로 남의 건을 처리할 수 있다.
- ✅ 이것으로 **§2에 "프로토타입 한계"로 적혀 있던 정보장벽 구멍이 닫혔다** — 역할이
  클라이언트 필터일 때는 준법 화면에서도 `/api/customers`가 50명 전체를 브라우저로 보냈다.
- **고객 50명 전부를 한 PB로 통합**했다(`UPDATE pb_customers SET pb = 'PB'`). 시드는 원래
  3인 배정(박17·이17·최16)이었다.
  ⚠️ `pb_customers`는 **시드 소스가 유실돼 `.pyc`만 남아 있다**(§2). 덮어쓰기 전에 원래
  배정을 **`backend/scripts/restore_pb_assignment.sql`**로 남겼다 — 되돌리려면 그 파일을 실행한다.
- 🎁 **부수 효과: 브리프 재실행이 필요 없어졌다.** 통합으로 유니버스가 다시
  `SK하이닉스(21)·셀트리온(19)·KB금융(18)`이 되면서 **저장된 브리프 #8과 정확히 일치**한다.
  (17명 기준일 때는 셀트리온·KB금융·POSCO여서 화면과 어긋나 있었다.)

### 이름 — 사람 이름을 박아두지 않는다

- `PB_NAME` 기본값이 `박PB` → **`PB`(역할명)**. 프론트 `MY_PB`도 같다(둘은 **같은 값이어야
  한다** — 어긋나면 화면은 "내 고객"이라 적는데 서버는 다른 사람의 고객을 보낸다).
  상단 브랜드에서도 이름을 뺐다. **화면 전체에 `박PB`/`이PB`/`최PB` 0건.**
- 값이 하나뿐이어도 **서버 스코핑은 유지한다** — 필터가 서버에 있어야 정보장벽이고,
  데이터에 다른 담당자가 섞여도 조용히 새어 나가지 않는다.
- ~~⚠️ **과거 기록의 `박PB`·`관리자`는 그대로 뒀다**~~ → **2026-07-27에 역할명으로 정규화했다**
  (아래 참조). 당시엔 "누가 했는지의 기록이라 소급 수정 안 함"으로 뒀는데, 다시 보니 이건
  **여럿인 줄 알고 만들던 시절 한 명에게 붙인 이름표**일 뿐 실제로 다른 사람의 기록이 아니었다.
  1 PB·1 준법 전제라 `박PB`↔`PB`, `정준법`↔`준법`은 같은 사람을 다른 이름으로 부른 것뿐이다.

### 사람 이름 정규화 (2026-07-27) — DB·화면 양쪽

`박PB`·`정준법`·`관리자`·`김애널`(지난 3인·관리자·애널리스트 시절의 라벨)을 **역할명 `PB`/`준법`으로
통일**했다. 검토·심의·생성은 PB, 발행은 준법 — 컬럼·이벤트별로 나오는 값이 그 역할에 딱 맞았다
(관리자·김애널 기록이 마침 검토·심의뿐이라 §0-1대로 전부 PB로 떨어졌다).

- **화면**: `types.ts::actorLabel()` 한 곳에서 정규화(감사로그·노트 모달의 확인·심의·발행 줄·
  확인 배지·모달 감사 목록). 라이브 actor도 `ACTOR.comp` `정준법`→`준법`으로 바꿔 새 기록부터
  역할명으로 남는다(PB는 이미 `PB`였다).
- **DB**: `audit_log.actor`(28행)와 `notes`의 `reviewer/deliberator/publisher/created_by`·
  `acks_json` 안의 actor를 UPDATE했다. `_valid_acks`는 index+텍스트 앞60자만 대조하고 actor는
  안 보므로 발행 게이트에 영향 없다(#10 발행 상태 안전).
- **복원 경로**: `backend/scripts/restore_audit_actors.sql`(역매핑이 유일하지 않아 id별 원본
  스냅샷 — HANDOFF §2 "덮어쓸 때 복원 경로 먼저"). 되돌리려면 그 파일 실행.
- ⚠️ **이건 프로토타입 fixture 정리이지 런타임 동작 변경이 아니다** — 앱은 여전히 감사를 소급
  수정하지 않는다(발행 후 append-only 그대로). W7 Q&A에서 "감사 무결성"을 물으면 이 구분을 말할 것.

### 역할 — 3종 → **2종**, 준법은 "다른 사람의 화면"

- **관리자 역할 삭제.** 여러 사람이 공유하는 콘솔이라는 전제에서만 의미가 있었다.
- 관리자가 하던 **검토·심의를 PB에게 넘겼다.** 만드는 사람과 통과시키는 사람이 갈리는
  지점은 **발행 하나**로 좁혔다: `초안 → [사실 확인 시작] PB → [확인 완료 → 준법 심의 요청] PB
  → [발행] 준법`. PB 화면에서 심의중 노트는 버튼이 비활성이고 🔒 사유를 말한다.
- 토글 라벨이 `내 화면` / `준법 화면 (데모)`다 — **역할 전환이 아니라 화면 전환**이다.
  준법은 이 대시보드의 사용자가 아니라 승인 단계를 맡는 다른 사람이고, 데모에서 그 단계를
  보여줘야 해서 미리보기로 남겼다. 그래서 준법 화면에는 **상담 전 브리핑도 F1 버튼도 없다**
  (상담 준비는 PB의 일이고, 에이전트를 돌리는 쪽과 통과시키는 쪽은 갈라야 한다).
- 큐의 `created_by` 필터 제거(위 §0-0 각주 참조).

### 화면 — 뺀 것과 옮긴 것

- **기능 레일(F1~F5 카드 5장) 삭제.** 5장 중 눌러서 뭔가 일어나는 건 F1 하나였는데 F2·F3가
  같은 파란 테두리를 두르고도 클릭에 반응하지 않아 고장으로 읽혔다(실제 기능은 아래 각자의
  카드에 있다). F4·F5는 만들지 않은 기능의 자리표시였다 — **로드맵은 발표 자료가 할 일이지
  PB가 매일 보는 화면이 할 일이 아니다.**
- **F1 입구 = 우하단 고정 버튼(`.fab`).** 화면에서 F1만 성격이 다르다 — 나머지는 상담 **전**
  준비인데 F1은 상담 **중** 쓴다. 그래서 스크롤과 무관하게 고정이고, 모달로 열려 보고 있던
  화면을 잃지 않는다. 모달이 열리면 버튼은 숨는다.
- **탭 줄은 그 역할에게 탭이 2개 이상일 때만** 낸다(`tabs.length > 1`). 선택지가 하나인 탭은
  고르는 장치가 아니라 제목이다. 한때 PB에게 탭이 하나뿐이라 줄을 통째로 감췄는데,
  아래 "탭 축"에서 PB 탭이 둘이 되며 규칙은 그대로 두고 자동으로 다시 나왔다.
- **전체 고지를 맨 아래로, 카드에서 한 줄 평문으로**(`.disclaimer`, ⚠ 아이콘 + 13px).
  색에 `--muted`를 쓰지 않았다 — 이 배경에서 대비 3.4:1이라 **읽히지 않는 고지는 고지가 아니다**
  (`--ink-2`는 7.6:1). `max-width`를 두지 않아 1024px까지 한 줄, 더 좁으면 `nowrap` 대신 접힌다.
  ❗ **아래 "브리핑 카드에 자체 고지가 없다"를 같이 읽을 것.**
- **`AI가 오늘 한 일`은 접이식 한 줄**(native `<details>`, 기본 닫힘). 늘 보고 있을 내용이
  아니다. 직접 토글을 만들면 키보드·스크린리더 동작을 다시 구현해야 하는데 네이티브는 공짜다.
- **탭 축 = "읽기 / 하기"** (PB 화면). 카드가 늘어나 한 화면이 길어지자 나눈 것이다.
  묶음의 이름이 안 나오면 묶음이 틀린 것이라는 게 판단 기준이었다 — "브리핑+처리 대기+노트 생성"은
  종목도 작업도 아니어서(처리 대기에 고객 문의가 섞여 있다) 이름이 안 나왔다.
  - **`상담 준비`** — AI가 오늘 한 일 → 타일 → 브리핑 → 고객 포트폴리오+상담 준비 메모
  - **`노트·승인`** — 종목 노트 생성 → 처리 대기
  - 브리핑은 **읽는 것**이라 상담 준비에 남는다(만드는 것도 처리하는 것도 아니다).
  - 큐가 첫 화면에서 빠지므로 **`처리 대기` 타일이 그 탭으로 가는 문**이다(`go: 'note'`).
    `div`+onClick이 아니라 **`button`으로 렌더**한다 — 아니면 키보드로 못 누른다.
  - ⚠️ **준법 화면은 이 축이 없다**(만들지 않으므로). 탭은 `심의 / 감시`(2026-07-27에
    `고객 관리`→`심의`, `AI 평가`→`감시`로 개명 — 준법감시인의 두 일)이고
    **큐도 원래 자리(심의 탭)에 남는다** — 준법에겐 노트 탭이 없어서 큐를 옮기면 **심의할 노트를
    화면에서 찾을 방법이 아예 없어진다.** 그래서 `queueCard`를 한 번만 정의하고 역할에 따라
    자리를 바꾼다(`{!cfg.research && queueCard}` / 노트 탭 안).
- **종목 노트 생성(F3)은 전용 탭에 있고, 뷰는 `hidden`으로만 감춘다 — 언마운트 금지.**
  실행이 1~2분 걸리는데 언마운트되면 `ResearchCard`의 cleanup이 SSE를 닫아 **크레딧만 쓰고
  노트가 안 나온다.** 그래서 생성을 걸어두고 다른 탭으로 건너가도 계속 돈다.
  다른 탭에서 실행 여부를 알 수 있게 `onRunningChange` → 탭 라벨에 `●`(`.cat-run`).
  `setRunning`과 짝으로 부르는 `setRun` 하나로 묶어 표시와 실제 상태가 갈라지지 않게 했다.
- **카드 부제를 걷어냈다** — 브리핑 카드는 제목이 `상담 전 브리핑` → **`브리핑`**이 되고
  부제("…전일 공시 · 밤사이 뉴스 · 지연시세")가 빠졌다. 상담 준비 메모의 안내문도 태그만 남았다.
  ✅ 덕분에 §0의 열린 항목이던 **"부제는 지연시세라는데 시세는 T-2"** 불일치가 같이 사라졌다.
  ⚠️ 기능 이름은 여전히 **F2 상담 전 브리핑**이다(코드 주석·`AI가 오늘 한 일` 문구·`CLAUDE.md`).
  카드 제목만 짧아진 것이니 문서에서 기능을 가리킬 때는 원래 이름을 쓸 것.
- **고객 표에 순번 열(`#`)과 상세 패널 순번.** 고객 id가 아니라 **표에 보이는 자리**이고,
  둘 다 같은 목록(`visibleCustomers`)에서 세므로 검색으로 걸러도 짝이 어긋나지 않는다.
- 타일: `담당 고객`(설명줄 = 위험 플래그 N명) / `처리 대기`(설명줄 = 종목 노트 N · 고객 문의 N).
  **설명줄은 바로 위 숫자를 쪼갠 것이어야 한다**(6 + 4 = 10).
- 처리 대기 행에서 **담당자 표기(미배정/관리자/박PB) 제거** — 1인용에서 구분하는 게 없다.
  누가 무엇을 했는지는 노트 모달의 확인·심의·발행 줄과 감사로그에 그대로 남는다.

### 명칭·표기

- **"팩트 노트" → "종목 노트"**로 통일(필터 탭·행 칩·타일·AI가 오늘 한 일·상담 준비 메모·
  생성 완료 토스트·F3 카드 제목). 한 화면에서 같은 것을 두 이름으로 부르면 다른 물건처럼 읽힌다.
  ⚠️ **아직 세 번째 이름이 있다** — 백엔드 큐 제목(`main.py`)과 노트 모달 제목(`ReviewModal`)이
  `실적·공시 노트`다. 그래서 한 줄이 `[종목 노트] 기아(000270) 실적·공시 노트 초안`으로 나온다.
  ⚠️ `CLAUDE.md`의 기능 표는 아직 `F3 종목 팩트 노트`다(문서이자 런타임 프롬프트).
- **날짜 표기를 `api.ts::fmtDate` 한 곳으로.** 공시는 `20260722`, 뉴스는 RFC 2822라 원본
  형식이 다른데 화면마다 알아서 자르고 있었고, **노트 인용 배지와 F1 채팅은 `.slice(0,10)`에
  요일이 잘려 `뉴스 Wed, 22 Ju`가 그대로 사용자에게 나갔다.** 세 형식(8자리·ISO·RFC 2822)을
  모두 받고 못 읽는 값은 지어내지 않고 원문을 돌려준다.
  **하루 경계는 KST 고정** — UTC로 찍으면 00~09시 뉴스가 전날로 밀리는데 브리핑이 내세우는 게
  하필 "밤사이 뉴스"라 정확히 그 구간이 어긋난다.
  프론트의 `BIZ_TZ`/`bizDay` 정의는 **`api.ts` 한 벌**로 모았다(§2의 "세 곳"을 늘리지 않게).

**라이브 확인 완료(2026-07-23)**: `/api/customers` 50명 전원 `PB` · 남의 고객 상세 404 ·
유니버스 = 브리프 #8과 일치 · PB 초안 `사실 확인 시작` 활성 · PB 심의중 발행 비활성(🔒) ·
준법 발행 활성 + 미인용 확인 UI · 준법 화면 `.fab` 0개 · 타일 6+4=10이 큐 실제 구성과 일치 ·
표 5행↔상세 5, 12행↔상세 12 · 검색 시 동반 재번호 · 화면에 사람 이름 0건.
자체 점검 10종 · `tsc`/`eslint` · `fmtDate` 경계 9종(`TZ=UTC`) 통과.

**❗ 새로 생긴 열린 항목 — 브리핑 카드에 자체 고지가 없다.** 산출물 셋 중 종목 노트는
워터마크(`ReviewModal`), F1은 답변마다 지연시세 고지(`F1Chat`)를 달고 다니는데 **브리핑 카드만
없다.** 백엔드가 만든 `ℹ 내부 참고용 — 투자권유·광고가 아닙니다.`가 `briefs.content_md`에
들어 있고 게이트도 통과하는데, **프론트 `Brief` 타입에 그 필드가 없어 화면이 안 그린다.**
전체 고지를 맨 아래로 내린 뒤로 **화면에서 가장 큰 카드가 첫 화면에서 고지 없이 보인다.**
고칠 거리는 작다 — 타입에 `content_md`(또는 notice 필드) 하나 더하고 카드 헤더에 한 줄.
문구를 새로 만들 필요 없다(`compliance.py::NOTICES["F2"]`를 그대로 쓴다).

---

## 0. 현재 상태

**계획서 🟢 필수(W1~W6) + 🟡 선택(F1)이 전부 라이브 검증으로 닫혔다. 남은 건 W7(발표)뿐 — 크레딧 무관.**

| 주 | 상태 | 내용 |
| :-: | --- | --- |
| W1 | ✅ 라이브 | 환경·플러그인(earnings-reviewer·equity-research)·자체 MCP 3개(DART/뉴스/KRX)·`CLAUDE.md`·SDK hello world·공통 셸 |
| W2 | ✅ 라이브 | DART MCP·a1·a2·O 위임·SSE 카드·docker-compose |
| W3 | ✅ 라이브 | a4·팬아웃(a2‖a4)·진행 타임라인·점진 렌더 |
| W4 | ✅ 라이브 | a5·토큰 스트리밍·citations·게이트·검토→심의→발행(사람)·워터마크 (§1) |
| W5 | ✅ 라이브 | F2 상담 전 브리핑(새 에이전트 0개)·H1 홈 |
| W6 | ✅ 라이브 | 3시나리오 안정화·간이 eval(`docs/EVAL.md`)·정량 가치 리포트(`docs/VALUE.md`) |
| F1 | ✅ 라이브 | 🟡선택. 대화형 Q&A: 규칙 라우팅·입력가드·인용·지연시세 + Redis 멀티턴 (§4) |
| W7 | ❌ 미착수 | 덱·데모 대본·백업 녹화·예상 Q&A |

**다음 할 일 = W7 발표 준비**
- 임원 덱: PB의 하루(상담 전 준비) → 라이브 데모 → 기술 깊이(팬아웃) → 규제 안전(AI는 대신 말하지 않음·사람 발행)
  → 로드맵(F4·F5) → 정량 가치(`docs/VALUE.md`: **PB 1인 ≈ 0.1 FTE**, 리서치 확장은 2차로 ~2/~5 FTE).
- 데모 대본 + **백업 녹화본**(라이브 실패 대비) + 예상 Q&A(환각/규제/비용/사내데이터).
- (선택) F1 확장 — 의도 이어받기·대명사 해소·비교(F4 영역). 없어도 무방.

**화면 감사에서 남은 것**(크기순 — ✅는 이후 닫힌 것):
- **고객 문의 모달이 에이전트와 안 이어져 있다.** `ReviewModal.prepFacts()`는 계좌 시드값 + 고정
  고지문구 3줄이 전부다. 질문("주식 비중을 줄일까요")이 이미 특정된 **유일한** 화면인데 그 고객
  보유 종목의 공시·뉴스가 한 줄도 없고 F1로 가는 버튼도 없다(상담 준비 메모엔 "이 종목 묻기"가 있다).
  **남은 것 중 제품 가치가 가장 크다** — 상담 준비 메모의 줄을 재사용하면 된다.
- **브리핑 카드에 자체 고지가 없다** — §0-1 말미 참조. 전체 고지를 맨 아래로 내린 뒤 생겼다.
- **"AI 진단" 시드 문구** — §0-0의 열린 결정. 실집계 숫자들 사이의 유일한 목업 문장이라 데모 리스크.
- **노트를 부르는 이름이 아직 둘이다** — 화면은 "종목 노트", 백엔드 큐 제목·노트 모달은
  "실적·공시 노트"(§0-1). `CLAUDE.md` 기능 표는 "F3 종목 팩트 노트".
- ✅ ~~브리핑 부제와 시세 신선도 차이~~ — **부제가 통째로 삭제되며 해소**(§0-1). 카드 안의
  `2026-07-21 지연시세` 배지는 원래 정직했고 그대로다.
- **진입 단위가 종목인데 PB의 단위는 "오늘 만날 고객"이다** — 가장 좋은 화면(상담 준비 메모)에
  가려면 페이지 최하단 표에서 고객을 직접 찾아야 한다.
- 작은 것: `/api/notes` 전건 상세 조회(노트 수만큼 요청, 지금 7건이라 무해하나 선형 증가) ·
  모달이 Esc로 안 닫히고 F1 채팅 모달엔 닫기 버튼이 없다(배경 클릭만) ·
  레포에 **prettier 설정이 없어** 에디터가 파일을 통째로 재포맷하고 diff가 부풀어 오른다
  (실제로 `page.tsx`·`dashboard.css`에서 반복됐다. 설정 파일 하나면 사라진다).
- ✅ ~~역할이 클라이언트 필터라 `/api/customers`가 50명 전체를 브라우저로 보낸다~~ —
  **§0-1에서 서버 스코핑으로 닫혔다.** W7 Q&A에서 "정보장벽"을 물으면 이제 이걸 말하면 된다.

**데모 데이터 현황**(2026-07-23 저녁 실집계 — API/DB에서 직접 확인)
- **노트 7건** · 발행 1 · **발행 통과율 14.3%(1/7)** · 부착률 **83.9%**(47/56 — 해석 30문장은
  분모에서 제외, §1-1) · 큐 10건(종목 노트 6 + 고객 문의 4) · 고객 50명.
  **워크플로 시연용으로 단계를 벌려 뒀다**(§1-2 작업의 결과 — 되돌리려면 상태만 draft로):

  | 노트 | 상태 | 데모에서 쓰는 방식 |
  |---|---|---|
  | #10 LG화학 | **발행완료** | 확인 4건 후 발행까지 간 완결 사례 — 발행 통과율을 만드는 유일한 건 |
  | #7 NAVER | **심의중**(확인 0) | **라이브로 확인→발행을 보여줄 건.** 미인용 6문장 = 해석 5 + UNSOURCED 1이라 판단이 필요한 화면이 나온다 |
  | #13 기아 | **심의중** | 사용자가 **새 흐름(PB가 사실 확인→심의 요청)으로 직접 올린 건.** 감사로그에 `review_started`/`deliberation_started` actor=PB로 남아 있다(2026-07-27 정규화 전엔 박PB) |
  | #11 POSCO | 초안 | **하드 블록 시연** — 목표주가·지연시세는 확인으로도 안 풀린다(§1-2) |
  | #8 기아 · #9 카카오 · #12 SK하이닉스 | 초안 | |
- **브리프 #8**(`brief_date` 2026-07-23 = SK하이닉스·셀트리온·KB금융)이 최신이고, **§0-1의 고객
  통합 이후 유니버스와 정확히 일치한다** — 재실행 불필요. 단 **고객 시드를 다시 바꾸면 유니버스도
  바뀌므로** 그때는 다시 돌려야 화면과 맞는다(`POST /api/briefs/run`, 약 40~50초·크레딧).
- ⚠️ **노트 중 브리프 종목과 겹치는 건 SK하이닉스(#12) 하나뿐**이다. 그 종목 보유 고객(21명)을
  열면 상담 준비 메모에 **브리핑·종목 노트가 같이** 붙은 줄을 보여줄 수 있고, NAVER 보유
  고객(예: 강준서)은 노트만 붙는다 — **둘 다 데모 대본에 넣을 것**. 나머지 종목은
  "오늘 브리핑·노트에 이 종목은 없습니다"로 뜬다(정직한 동작이지만 데모에선 빈손으로 보인다).
- `pb_customers(50)`·`pb_sessions(30)`·감사로그는 **보존**(§2의 시드 유실 주의).
  `pb_customers.pb`는 전원 `'PB'`로 통합됐다 — 원래 3인 배정은 `scripts/restore_pb_assignment.sql`.

**착수 전 필수**: `git pull` 후 **`docker compose build backend && up -d --force-recreate backend`** —
`redis` 의존성이 추가돼(§4) 안 하면 F1이 `ModuleNotFoundError`로 죽는다.

---

## 1. a5는 위임이 아니라 backend 2차 `query()` — `Agent` 도구 비동기 함정

*(코드 여러 곳이 "HANDOFF §1"로 이 절을 가리킨다. 상세 배경은 `CLAUDE.md` "검증됨" 절.)*

`Agent` 도구는 **비동기**다 — 결과가 `"Async agent launched successfully"`로 즉시 돌아오고 서브에이전트는
백그라운드에서 돈다. **O가 턴을 끝내면 `query()` 스트림이 닫히고 도구를 안 부르는 a5가 잘린다.**
(a1·a2·a4는 도구 결과가 스트림에 빨리 들어와 살아남는다.)

**채택한 구조**: O의 1차 쿼리가 끝나면 backend가 a5를 **메인 에이전트로 하는 2차 `query()`**로 직접
돌린다(`main.py::event_gen`). a5가 메인이라 `text_delta`가 `parent_tool_use_id=None`으로 흘러 토큰
스트리밍(`note_token`)이 산다. 지침은 `.claude/agents/a5.md` **본문만** 읽어 `system_prompt`로 쓰고
(`_agent_prompt`), 입력은 O의 산문이 아니라 **도구 결과를 그대로 직렬화**(`_a5_input`, LLM 미개입).
- `background: false`(프론트매터)도 위임을 동기화하지만 **StreamEvent가 안 나와 토큰 스트리밍이 죽는다** → 안 씀.
- ❌ **반복 금지**: 시스템 프롬프트에 "a5 결과를 기다려/인용해"를 넣었더니 O가 **a2 수치로 문장을 지어내
  a5가 쓴 것처럼 인용**했다(가드레일 3 위반). 지금은 O 프롬프트에서 노트 단계를 아예 뺐다.

---

## 1-1. 출처 부착률·게이트 정의 — 지표 함정 (설계 의도)

*(코드가 "HANDOFF §1-1"로 이 절을 가리킨다.)*

**핵심 교훈: 지표를 믿기 전에 지표부터 검증하라.** 낮은 수치를 보면 산출물을 탓하기 전에 측정기를 의심할 것.
한 종목만 보고 "규칙 준수"라 결론 내리지 말 것. 이번 세션에 **측정 정의 버그를 4건** 잡았다(전부 a5 오류 아님):
파서가 문장 중간 각주를 버림(노트6 10개 중 5개 유실 → 발행 하드 블록) · eval 하네스 3건(수치 표기 다양성
조/억↔백만원, 재무 카드 여러 개 중 첫 것으로 대조, 존댓말 해석 어미 미인식).

**확정된 정의**(`citations.citation_stats()` **단일 출처** — 게이트·대시보드가 같이 쓴다):
- 문장마다 `kind`: `heading`·`boilerplate`(고지·구분선)·`claim`(사실 주장)·`interpretation`(해석·전망).
- **출처 부착률 분모 = `claim`만.** 해석 문장은 규칙상 각주를 안 붙이므로(a5.md) 분모에서 뺀다. 안 빼면
  a5의 해석 문장 수 변덕이 품질로 오독된다(실측 10/14↔7/14로 흔들렸다).
- **분류는 보수적으로** — 애매하면 `claim`. 헐거우면 미인용 문장이 분모에서 빠져 부착률이 후해지는데,
  컴플라이언스 지표는 그 방향으로 틀리면 안 된다. 출처 붙은 문장은 어미가 해석 같아도 `claim`.
- **게이트(`unsourced_count`)는 F3에서 해석 문장도 센다** — 발행 전 사람이 판단하도록 올리는 게 설계다.
  지표와 게이트 정의가 다른 건 의도된 것. (F1은 발행 단계가 없어 `claim`만 센다 — §4.)
- 분모에서 뺀 해석 수는 `citation_interpretation`으로 노출(감추지 않기). 한 문장 다중 인용은 `sources` 배열로 전부.

⚠️ **분류기를 바꾸면** 기존 노트의 `sentences_json`은 저장 시점에 굳어 있으므로 `scripts/reparse_notes.py`로
재파싱해 대시보드와 맞춘다(재실행 없이, 저장된 출처에서 맵 복원).

---

## 1-2. 미인용 문장 확인(ack) — 게이트를 사람이 여는 유일한 경로 (2026-07-23)

*(§1-1을 먼저 읽을 것. 여기는 "그래서 사람이 어떻게 통과시키나"다.)*

**발견된 문제: 발행이 구조적으로 불가능했다.** §1-1대로 F3 게이트는 해석 문장까지 미인용으로 센다
(사람이 판단하도록 올리는 게 설계). 그런데 **올려만 놓고 사람이 풀 수단을 안 만들었다** — `publish`는
violations가 비어야만 통과하고 노트 편집 UI도 없다. 결과: **노트 6건이 6건 다 영구 `draft`**,
발행 통과율 0/6. "AI가 막힌다"는 보여줄 수 있는데 **"사람이 확인하고 통과시킨다"는 한 번도 못 보여줬다** —
제품 서사의 절반이 빠져 있었다.

막고 있던 문장을 열어 보니 각주를 붙일 수 **없는** 문장들이었다(전부 a5 잘못이 아니다):
해석·전망 · `"내부 참고용 초안이다"` 같은 고지 · `"위 수치는 2024 연결(CFS) 기준"` 같은 데이터 설명.
뒤의 둘은 분류기가 §1-1 원칙("애매하면 `claim`")대로 보수적으로 판정한 결과다 — **지표를 후하게
만들지 않으려던 보수성이 발행을 잠그는 쪽으로 작동했다.**

**채택한 구조**: 준법이 미인용 문장마다 **사유를 골라 확인**하면 그 문장만 미인용 집계에서 빠진다.
사유는 `해석·전망` / `고지·면책` / `데이터 설명` 고정값(`main.ACK_REASONS`) — 감사 대상이라 자유 입력이
아니다. 확인·해제 전부 감사로그(`ack_added`/`ack_removed`)에 남는다.

- **면제되는 건 미인용 규칙 하나뿐이다.** 투자권유·광고성 표현, MNPI, 지연시세 고지 누락은 확인으로
  **못 푼다** — 문장을 고쳐야 하는 위반이다(`test_ack_cannot_waive_forbidden_phrase`로 고정).
  POSCO 노트의 하드 블록 시연은 그대로 산다.
- **심의 단계에서만** 확인할 수 있다. 초안에서 미리 풀어두면 검토가 형식이 된다.
- **재파싱하면 확인이 무효가 된다** — 저장할 때 원문 앞 60자를 같이 남기고(`notes.acks_json`)
  발행 시 대조한다(`main._valid_acks`). 안 맞으면 **막히는 쪽**으로 판정한다. 컴플라이언스 판정은
  어긋나면 통과가 아니라 차단으로 틀려야 한다.
- 해제는 검사 없이 언제나 가능하다. 푸는 걸 막을 이유가 없고, 조건이 바뀐 뒤 남은 확인을 되돌릴
  길이 없으면 안 된다.
- 화면은 문장 목록 **위에** "출처 없는 문장 N개가 발행을 막고 있습니다"를 먼저 말한다 —
  발행 버튼을 눌러 409를 받고 나서야 아는 건 검토 화면이 할 일이 아니다.

**라이브 확인(2026-07-23, LG화학 #10)**: 초안 → 검토 → 심의 → 발행 시도 **⛔`publish_blocked`** →
`ack_added`×4(준법·해석·전망 — 2026-07-27 정규화 전엔 정준법) → **✅`published`**. 감사로그에 **막힌 사실과 푼 사실이 둘 다** 남는다.
발행 통과율 0/6 → **1/6**.

⚠️ 확인 대상은 "해석으로 분류된 문장"이 아니라 **미인용 문장 전부**다. 분류기가 `claim`으로 본
고지·메타 문장도 사람이 사유를 붙여 푼다 — 분류기를 느슨하게 고치는 대신 판단을 사람에게 남긴 것이다
(§1-1의 보수적 분류는 지표용으로 그대로 유지).

---

## 2. 반드시 알아둘 함정·운영 수칙

- 🚩 **`git commit`·`git push`는 실행 전에 반드시 사용자에게 묻는다 — 로컬 커밋도 예외 없다.**
  작업이 끝나면 변경 요약(`git status`·diff)만 보여주고 멈춘다. 커밋 메시지 초안까지는 준비해도 되지만
  **실행은 사용자가 "커밋해"라고 말한 뒤**다. 사후 보고("커밋했습니다")는 안 된다 — 사용자가 커밋 이력을
  직접 통제한다. (2026-07-22: 이 규칙을 어기고 `docs/GUIDE.md`를 자동 커밋했고, 사용자가 `git reset`으로
  직접 되돌린 뒤에도 또 자동 커밋해 지적받았다. 두 커밋 다 취소됨.)
- **자주 커밋할 것** — 이전 세션의 대시보드 구현이 **미커밋 상태로 통째 유실**된 적이 있다(pyc가 소스를
  덮어씀, git에도 없었음). 대시보드/문서 작업은 특히.
  ⚠️ 위 규칙과 충돌하는 게 아니다 — **"자주 커밋하자고 사용자에게 자주 제안하라"**는 뜻이다.
  작업 단위가 끝날 때마다 커밋할지 물어라. 묻지 않고 쌓아두다 유실되는 것도, 묻지 않고 커밋하는 것도 안 된다.
- **`pb_customers`·`pb_sessions`는 시드 소스가 유실돼 `.pyc`뿐이다 — 절대 지우지 말 것.** 재현 필요 시 디컴파일.
  `TRUNCATE notes, audit_log, briefs`는 가능하나 차트·알림이 같이 비니 데모 노트를 새로 만드는 편이 낫다.
  ⚠️ 이 테이블을 **UPDATE로 덮어쓸 때도 복원 경로를 먼저 만들어라** — 시드를 다시 만들 방법이 없으니
  덮어쓴 값은 그것으로 끝이다. 실제 사례: §0-1의 PB 통합 전에 `scripts/restore_pb_assignment.sql`을 남겼다.
- **`scripts/seed_brief.py`는 F2 파이프라인이 아니다** — 크레딧 없이 화면 검증용으로 에이전트를 건너뛰고
  MCP를 직접 부르는 시드다. 데이터는 진짜지만 "누가 수집했나"가 달라 **데모에서 F2 결과로 소개하면 안 된다**(멀티에이전트 서사 빠짐).
- **MCP 서버(`backend/mcp_servers/*.py`)에서 `backend` 패키지를 import하려면 sys.path 부트스트랩이 필요하다.**
  `.mcp.json`이 **스크립트로 직접 실행**하므로 `sys.path[0]`이 파일 디렉터리다 — cwd가 레포 루트여도
  `from backend import x`가 `ModuleNotFoundError`로 죽고, **서버가 안 뜨면 그 서버의 도구 전체가 조용히
  사라진다**(2026-07-22 실제 발생: krx가 죽어 브리프 3종목의 **시세가 전부 빔**, 게이트는 통과라 티가 안 남).
  MCP 서버를 손대면 `backend/.venv/bin/python backend/mcp_servers/<x>_server.py </dev/null`로 기동만이라도
  확인할 것(도구 목록·실호출은 `scripts/*_mcp_client_check.py`).
- **"오늘"의 정본은 `backend/bizdate.py`다 — KST 고정**(2026-07-23 `e5b1130`). 컨테이너·Postgres가
  UTC로 도는데 `date.today()`·`ts::date`를 그대로 쓰면 하루가 **KST 09:00에 끊긴다**. 증상 3종이
  실제로 났다: ①"AI가 오늘 한 일"이 아침 9시 전엔 통째로 비고 전날 오후 작업이 오늘로 잡힌다
  ②아침에 만든 브리프의 `brief_date`가 어제로 찍혀 화면이 "오늘 브리핑"으로 못 알아본다
  ③DART/KRX 조회 창의 종료일이 하루 밀려 **그날 아침 접수된 공시가 빠진다**.
  새로 날짜를 만들면 `biz_today()`, SQL은 `biz_date_sql()`을 쓴다(세션 타임존에 기대지 않고 식마다
  `AT TIME ZONE` 명시 — 커넥션 설정이 바뀌어도 집계 기준이 조용히 따라 움직이지 않게).
  ⚠️ `bizdate`는 **stdlib만 쓴다** — krx MCP 서버가 위 sys.path 부트스트랩으로 이 모듈을 끌어가므로
  무거운 의존성을 넣으면 바로 위 함정대로 **서버가 안 뜨고 도구가 통째로 사라진다.**
  ⚠️ 같은 상수가 **세 곳**에 있다: `bizdate.py`(정본) · `dart_server.py`(부트스트랩이 없어 자체 복사) ·
  프론트 `page.tsx`의 `BIZ_TZ`. **어긋나면 같은 사건이 서버 집계와 화면 차트에서 다른 날짜 칸에 꽂힌다.**
- **"처리할 일의 목록"과 "읽을 것의 목록"은 다르다** — 큐(`/api/dashboard/queue`)는 발행분을 빼고,
  노트 색인(`/api/notes`)은 포함한다. 화면에서 노트를 **읽으려면** 후자를 써라. 전자를 쓰면 사람이
  승인을 끝낸 노트가 그 순간 사라진다(2026-07-23 실제로 그랬다, §0-0).
- **도구 결과가 토큰 한도를 넘으면 `is_error=False`인 채 에러 문자열이 온다** → `main.py::_tool_failed()`로
  걸러 `completed`로 오집계되는 걸 막는다. 자체 MCP는 반환량 상한을 둘 것(`dart_search`는 하루 382건 나온 적 있음 → `limit=30`+최신순).
- **원본과 대조할 때는 파이프라인과 같은 조건으로 조회할 것** — F2 검증 때 `days=2` 결과를 7일 창과 비교해
  "4건 유실"이라 오진했다(실제 유실 없음).
- **SSE 이벤트명을 `error`로 쓰지 말 것** — EventSource의 연결 오류 핸들러와 겹친다. 실행 오류는 `run_error`로 보낸다.
  예외가 나도 스트림이 조용히 끊기지 않게 `run_error`+`done`을 반드시 보낸다(화면이 종료 상태에 도달).
- **재생 SSE로 화면을 검증할 땐 코드의 실제 분기를 보고 시나리오를 짤 것** — "파싱 실패=노트 없음"으로 짰다가
  허구를 검증할 뻔했다(실제론 뉴스만 있어도 a5가 돈다). `scenario_check.mjs` 참고.
- **DART 전자공시는 PDF가 아니라 HTML 뷰어** — "페이지 번호" 단위가 없다. `viewer_url`+`rcept_dt`(접수시점)가
  이 소스에서 가능한 최대 해상도(계획서의 "문서·페이지·접수시점"은 PDF 상정 표현).
- **실화면은 목업으로 폴백하지 않는다** — 백엔드가 없으면 연결 실패를 그대로 말한다(조용히 가짜를 보이면 더 위험).
  목업이 필요하면 시안 파일(`docs/design/pb-admin-dashboard.html`)을 연다.
- **`docs/ARCHITECTURE.md` 로드맵이 F1을 "확장 예정"으로 적어둔 채다** — F1은 구현됐다(§4). W7 문서 최종화 때 정정.

---

## 3. 대시보드 설계 (사용자와 합의 완료 — 재논의 불필요)

> ⚠️ **이 절은 §0-1 이전 설계다.** 역할·화면 구성은 §0-1이 최신이고, 아래는 그에 맞춰 고쳤다.

- **프레이밍**: **PB 1인용 상담 준비 화면**(고객용 화면 아님, §0-0·§0-1).
  단일 화면 `/dashboard`(`/`는 리다이렉트).
- **탭**(역할마다 다르다, §0-1): PB = `상담 준비` / `노트·승인` · 준법 = `심의` / `감시`.
  탭 줄은 그 역할의 탭이 2개 이상일 때만 나온다.
- **화면 토글**(목 로그인, **기본=PB**, **2종**): `내 화면`(PB) / `준법 화면 (데모)`.
  관리자 역할은 삭제됐다. PB=AI탭 숨김·전체 큐·고객 50명·**종목 노트 생성 가능**(발행은 불가) /
  준법=AI평가 기본·심의 건만·**포트폴리오 비노출(🔒)**·브리핑과 F1 버튼 없음.
- **승인 배분 = 셀프 클레임(풀)**: 미배정 건을 열어 검토 시작하면 그 사람이 담당자. 푸시 배정 없음.
- **검토 모달**: 노트=문장별 출처 배지→**사실 확인 시작(PB)**→**준법 심의 요청(PB)**→
  **발행(준법만, 게이트 재검사·미인용 시 하드 블록)** /
  고객 문의=고객 컨텍스트+**회신 전 확인할 사실**→**확인 완료(담당 PB만, 회신은 사람이 직접 작성)**.
  권한 없으면 버튼 비활성+🔒. ⚠️ AI가 고객 회신을 대신 쓰는 화면으로 되돌리지 말 것(§0-0).
  💡 **만드는 사람과 통과시키는 사람이 갈리는 지점은 발행 하나다** — 그게 이 제품의 핵심이라
  거기만 준법에게 남겼다(§0-1).
- **AI 신뢰도 카드**: 출처 부착률(목표 90% 미터)·발행 통과율·게이트 차단(7일) — KPI 타일이 아닌 미터/비율.
- **목업 고객 50명**: 세대혼합 이름 비복원 추출·잔고 로그균등·나이=이름 세대+재산 보정·**위험 플래그 규칙 3종**
  (①안정형인데 주식>40% ②단일종목≥65% ③수익률≤−5%)+사유 저장. 수치는 전부 실집계, 비교 데이터 없는 델타는 감춘다.
- **시안 파일**은 이제 디자인 시안 전용(아티팩트 `03fe22af-...`). 실화면과 갈라지는 게 정상.

**백엔드 라우트 9종**(포팅 후에도 그대로): `/api/dashboard/{summary|queue|agents|audit}` ·
`/api/customers[/{id}]` · `/api/sessions` · `/api/sessions/{id}/{approve|reject}`.
2026-07-23에 둘 추가: `GET /api/notes`(발행분 포함 노트 색인, §0-0) ·
`POST /api/notes/{id}/ack`(미인용 문장 확인, §1-2).
⚠️ **고객·상담 계열은 `main.PB_NAME`으로 서버에서 좁혀진다**(§0-1) — `/api/customers`,
`/api/customers/{id}`(남이면 404), `/api/sessions`, `/api/sessions/{id}/{approve|reject}`,
`summary.customers_total`, 큐의 고객 문의. **노트 계열은 안 좁힌다**(전부 이 PB의 것).

---

## 4. F1 대화형 종목 Q&A (규칙 라우팅 + Redis 멀티턴)

입구: 대시보드 **우하단 고정 버튼(`💬 종목 즉답`) 클릭 → 채팅 모달**(§0-1에서 기능 레일이
사라지며 옮겼다). 상담 준비 메모의 "이 종목 묻기"도 같은 모달을 종목 프리필로 연다.
파이프라인(`GET /api/chat/stream?q=...&session=...`): 입력 가드 → 규칙 라우팅 → 라우팅된 에이전트 도구 조회
→ 2차 query(f1 답변자, 구조화 데이터만) 스트리밍 → citations(시세 `[^krx]`) → F1 고지 강제 → 게이트 → 세션 갱신.
**라우팅·데이터취합·게이트는 코드가, LLM은 답변 문장만**(F3와 같은 각주 무결성).

- **입력 가드가 F1의 핵심**(`compliance.input_guard`) — 자유 텍스트라 F3·F2에 없던 공격면. MNPI·프롬프트
  인젝션·PII를 **에이전트 前에** 잡아 차단하면 도구가 아예 안 돈다(크레딧 0). 가드레일 2·5의 실전.
- **규칙 라우팅**(`f1.route`, 순수) — 6자리 코드/별칭 + 의도 키워드 → 에이전트(quote→krx·financials→a2·
  news→a4·disclosure→a1). 엔티티 없으면 되묻기. LLM에 안 맡긴 이유: 데이터 소스 선택은 컴플라이언스
  경계라 감사 가능한 규칙이어야 하고, "왜 이 에이전트로 갔나"를 배지로 보여준다.
- **게이트 F1 분기**: 고지문구(`CHAT_NOTICE`)에 "지연시세"를 넣어 QUOTE 게이트를 자기충족. F1은 발행 단계가
  없어 해석 문장은 위반으로 안 세고 **근거 없는 사실 주장만** 잡는다(`check_note`).
- **멀티턴(Redis)** — 현재 질문에 종목이 없으면(`주가는?`·`관련 뉴스는?`) 세션의 **직전 종목을 이어받는다**
  (`f1.route(q, prev_entity=...)`). 의도는 매 턴 새로 판단(종목만 잇는다). `session_store.py`는 **라우팅 맥락만**
  저장(최근 턴 상한 8·`last_entity`·TTL 1h) — 답변 본문·도구 결과는 안 담는다(턴 넘나들며 날조 방지, 가드레일 3).
  세션은 `/api/chat/stream`이 발급→`session` SSE→프론트가 다음 턴에 `&session=`. UI는 이어받은 턴에 **↩ 이어받음** 배지.
- **의존성**: `redis==8.0.1` 추가 → **backend 이미지 재빌드 필요**(§0). 통합 점검(Redis 필요, 크레딧 0):
  `REDIS_URL=redis://localhost:6379/0 backend/.venv/bin/python -m backend.scripts.session_store_check`.
- 파일: `backend/f1.py`(라우팅·답변조립·프롬프트)·`session_store.py`·`test_f1.py`·프론트 `F1Chat.tsx`.
  `citations.parse_sentences`에 `quote_source`(선택 4번째 인자) — 기존 3-arg 호출 무영향.
- **라이브 검증**: 삼성전자 실적(a2·DART 인용) → `주가는?`(종목 생략, krx로 이어받음·지연시세). 입력가드·되묻기 UI 확인.

---

## 5. 아키텍처 한눈에

```
[F3] 종목코드 → O(Opus) → a1 법인확인 → a2 재무 ‖ a4 뉴스 팬아웃        ← 위임은 여기까지
  → backend 도구결과 직렬화(_a5_input) → 2차 query()로 A5 직접 실행(§1) → citations(kind 부여)
  → 워터마크(F3) 강제 → 게이트 → notes 저장 → SSE(진행·토큰·노트·done{outcome})
     재무·뉴스 둘 다 없으면 a5 안 돌리고 사유만 보낸다
[F2] POST /api/briefs/run → pb_watchlist(PB_NAME 담당 고객 보유 상위 N) → O → a1 공시 ‖ a4 뉴스 + krx(O 직접) — 새 에이전트 0개
  → 지수는 market.py가 직접 조회(판단이 없어 위임 불필요, 실패 시 사유만 market_json에)
  → brief.assemble(지수·중요도 순위·출처 부착, LLM 미개입) → "내부 참고용" 강제 → 게이트 → briefs 저장
     공시 중요도(0주요 1정기 2기타 3지분): 지분공시는 빼지 않고 뒤로만 민다(조용한 날 안 비게)
[F1] GET /api/chat/stream → input_guard(MNPI·인젝션·PII, 에이전트 前) → f1.route(세션 이어받기)
  → 라우팅된 에이전트/krx 조회 → 2차 query(f1 답변자) 스트리밍 → citations([^krx]) → F1 고지 → 게이트 → session_store
     ← 에이전트(LLM)는 위 세 생산 구간에서만 실행
[대시보드] Next.js `/dashboard`(PB 1인용, 화면 토글 2종) · F1 입구 = 우하단 고정 버튼
  PB · 상담 준비 탭: AI가 오늘 한 일(접이식·감사로그 실집계) → 타일(담당 고객 · 처리 대기→)
    → 브리핑(=F2, 오늘 시장 + 고객 보유 종목) → 고객 50 + 상담 준비 메모 → 전체 고지(맨 아래 한 줄)
  PB · 노트·승인 탭: 종목 노트 생성(F3 SSE 직접 구독, hidden으로만 감춤) → 처리 대기 큐
  준법 · 심의 탭: AI가 오늘 한 일 → 처리 대기 큐(심의 건만, "N건"은 헤더에·타입 필터 없음)
    — 타일 전부·고객 포트폴리오 카드는 뺐다(2026-07-27): 게이트 차단은 감시 탭 중복,
      심의 대기 수는 처리 대기 카드가 이미 보여줌, 포트폴리오는 빈 잠금 카드였다
  준법 · 감시 탭: AI 신뢰도(미터·게이트 차단 포함) · 에이전트 호출 · 상담·노트 추이 · 컴플라이언스 알림 · 감사로그
[사람] 노트: 사실 확인(PB)→준법 심의 요청(PB)→[미인용 문장 확인·사유 기록(준법)]→발행(준법, 게이트 재검사)
       고객 문의: 확인 완료(담당 PB, 회신은 사람이 직접 작성)
       — AI 개입 없음, 전 과정 audit_log. 확인은 미인용 규칙만 푼다(§1-2)
       — 만드는 쪽과 통과시키는 쪽이 갈리는 지점은 **발행 하나**다(§0-1)
```

**파일 대응**: a5 작문 지침=`a5.md`(backend가 system_prompt로 읽음) / 출처매칭·문장범주·부착률 정의=`citations.py`
(단일 출처) / F1 라우팅·답변조립=`f1.py`·대화상태=`session_store.py` / 브리프 조립=`brief.py` /
고지·게이트·입력가드=`compliance.py` / 상태·로그=`db.py` / **날짜 경계(KST "오늘")=`bizdate.py`** /
배선·API=`main.py` / MCP=`mcp_servers/{dart,news,krx}_server.py` /
화면=`frontend/src/app/dashboard/{page,ResearchCard,ReviewModal,F1Chat,charts}.tsx`.

---

## 6. 환경/실행 메모

- 앱: `docker compose up`(backend 8000 / frontend 3000 / postgres 5432 / redis 6379). **requirements 변경 시
  `docker compose build backend && up -d --force-recreate backend`**.
- 대시보드: http://localhost:3000/dashboard.
- **자체 점검(크레딧·네트워크 불필요, 10종)**: `for t in compliance brief krx streaming tool_result a5_input brief_attribution citations run_outcome f1; do backend/.venv/bin/python -m backend.test_$t; done`
- **프론트 검증**: `cd frontend && npx tsc --noEmit && npx eslint src --max-warnings=0`. ⚠️ Next.js 16: 코드 전 `frontend/node_modules/next/dist/docs/` 확인(frontend/AGENTS.md).
- **화면 재생 검증**(크레딧 0, `frontend/`에서 실행 — playwright 모듈): `node scenario_check.mjs`(W6 빈/에러 5종).
- **Redis 세션 통합 점검**: `REDIS_URL=redis://localhost:6379/0 backend/.venv/bin/python -m backend.scripts.session_store_check`.
- **라이브 실행(비용)**: F3 `curl -sN "http://localhost:8000/api/research/stream?stock_code=005930" > /tmp/f3.sse`(~100초) /
  F2 `curl -s -X POST .../api/briefs/run -d '{}'`(~40초, 종목 생략 시 고객 보유 상위) / F1 `curl -sN ".../api/chat/stream?q=삼성전자 최근 실적"`(→`session` id를 다음 턴 `&session=`).
  → `grep "^event:" *.sse | sort | uniq -c`로 이벤트별 건수부터 본다.
- 위임 동기/비동기 판정(저비용): `docker compose exec -T backend python -m backend.scripts.background_flag_check`.
- 테스트 데이터 정리: `docker exec ai-pb-agent-postgres-1 psql -U app -d app -c "TRUNCATE notes, audit_log, briefs RESTART IDENTITY;"`(`pb_*`는 지우지 말 것 — §2).
- 고객 PB 배정 되돌리기(3인 체제로): `docker exec -i ai-pb-agent-postgres-1 psql -U app -d app < backend/scripts/restore_pb_assignment.sql`
  → 되돌리면 브리핑 유니버스도 바뀌므로 브리프를 다시 돌려야 화면과 맞는다(§0-1).
- API 키 4개(`.env`): `ANTHROPIC_API_KEY`·`DART_API_KEY`·`NAVER_CLIENT_ID/SECRET`·`KRX_API_KEY`(data.go.kr, 무료).
- git: main → **personal**(hmgthb/ai-pb-agent) 업스트림. origin(kwangtekNa/intern)에는 푸시 안 함.
  **커밋·푸시 둘 다 사용자 확인 후에만 실행한다(§2 첫 항목).**
- SDK API명은 `claude-agent-sdk==0.2.110` 실물 확인 — `CLAUDE.md` "검증됨" 절.

---

## 7. 아직 검증 못 한 것 / 리스크

- ✅ ~~발행까지 가는 전체 흐름~~ — 2026-07-23에 닫혔다. API로 초안→검토→심의→차단→확인→발행을 끝까지
  돌렸고(§1-2, LG화학 #10), React 모달에서는 준법 화면의 **확인 UI·잔여 건수 갱신까지** 실제 클릭으로 확인했다.
  ❓ 남은 건 **모달의 '발행' 버튼 클릭 자체**뿐이다(발행은 curl로 했다). 같은 엔드포인트라 리스크는 낮지만
  W7 리허설에서 한 번은 눌러 볼 것 — 지금 #7 NAVER·#13 기아가 심의중이라 그대로 쓸 수 있다.
  ✅ **초안→사실 확인→심의 요청은 §0-1 이후 사용자가 실제 UI에서 돌렸다**(노트 #13, 감사로그에
  `review_started`/`deliberation_started` 기록). 새 권한 배치(PB가 앞 두 단계)가 화면에서 동작한다.
- ❓ **파서 수정 후 A5 각주 규칙 라이브 재확인** — §1-1의 파서 버그를 고친 뒤 새로 관찰한 건 데모 노트
  5건 + 노트 #12뿐(`docs/EVAL.md`, 부착률로는 확인). #12는 2026-07-23 라이브에서 워터마크·각주
  **형식**까지 봤지만, 문장별 각주가 실제로 맞는 출처를 가리키는지 정성 관찰하면 완결.
- ❓ **3시나리오 예외 상태는 재생 SSE 기준** — 실제로 공시 없는/파싱 실패 종목을 라이브로 돌려본 건 아니다(조립 경로는 동일).
- ❓ **정량 가치 밴드는 추정 모델** — 가정(X=60분·물량) 의존적. Q&A에서 근거를 물으면 `docs/VALUE.md` 민감도표로 답하고 단일 숫자로 단정하지 말 것.
- ❗ **"고객에게 말 걸지 않기"는 프롬프트 지시일 뿐 게이트 규칙이 아니다**(2026-07-23 `ffabe22`의 남은 한계).
  `compliance.py`는 "고객님께…" 같은 호칭·회신문 형식을 **위반으로 세지 않는다**. 라이브 4종 산출물
  검사에서 실제 발생은 0건이었지만, 보장이 게이트가 아니라 프롬프트에 걸려 있다는 뜻이다.
  규칙화하면 F1·F2·F3에 동시에 적용된다 — W7 Q&A에서 "규제 안전" 질문이 나오면 이 경계를 정직하게 말할 것.
- ❗ **상담 전 브리핑 카드에 자체 고지가 없다**(§0-1). 전체 고지를 맨 아래로 내린 뒤 생긴 것이라
  **첫 화면에서 가장 큰 카드가 고지 없이 보인다.** 백엔드는 이미 만들고 게이트도 통과하는데
  프론트 타입에 필드가 없어 안 그릴 뿐이다 — 고칠 거리는 작다.
- ❗ **역할 강제는 여전히 화면에만 있다.** 백엔드는 `actor`를 감사로그용 문자열로 받을 뿐
  "발행은 준법만"을 강제하지 않는다(`/api/notes/{id}/publish`). **고객 데이터 스코핑은 §0-1에서
  서버로 내려갔지만 권한은 아직 아니다** — W7 Q&A에서 구분해 말할 것.
