# 인수인계 — AI PB 어시스턴트 (`ai-pb-agent`)

> 최종 갱신 2026-07-27. **다음 세션은 이 문서부터 읽으세요.**
> 읽는 순서: §0(지금 상태) → §0-1(되돌리면 안 되는 제품 결정) → §2(함정) → 나머지는 필요할 때.
> ⚠️ **절 번호를 바꾸지 말 것** — 코드 11곳이 `HANDOFF §0-1`·`§1`·`§1-1`·`§2`로 이 문서를 가리킨다.
> 관련 문서: 주차 계획 `주차별_실행계획.md`(원본 유지, 진행 기록은 여기에만) · 런타임 지침 `CLAUDE.md` ·
> 측정 `docs/EVAL.md` · 가치 `docs/VALUE.md`. **이 문서는 git 추적 대상이다 — 자주 커밋할 것.**

---

## 0. 현재 상태

**계획서 🟢 필수(W1~W6) + 🟡 선택(F1)이 전부 라이브 검증으로 닫혔다. 남은 건 W7(발표)뿐 — 크레딧 무관.**

| 주 | 상태 | 내용 |
| :-: | --- | --- |
| W1 | ✅ | 환경·플러그인·자체 MCP 3개(DART/뉴스/KRX)·`CLAUDE.md`·SDK hello world·공통 셸 |
| W2 | ✅ | DART MCP·a1·a2·O 위임·SSE 카드·docker-compose |
| W3 | ✅ | a4·팬아웃(a2‖a4)·진행 타임라인·점진 렌더 |
| W4 | ✅ | a5·토큰 스트리밍·citations·게이트·검토→심의→발행(사람)·워터마크 (§1) |
| W5 | ✅ | F2 상담 전 브리핑(새 에이전트 0개)·H1 홈 |
| W6 | ✅ | 3시나리오 안정화·간이 eval·정량 가치 리포트 |
| F1 | ✅ | 🟡선택. 대화형 Q&A: 규칙 라우팅·입력가드·인용·지연시세 + Redis 멀티턴 (§4) |
| W7 | ❌ | 덱·데모 대본·백업 녹화·예상 Q&A |

**다음 할 일 = W7 발표 준비**
- 임원 덱: PB의 하루 → 라이브 데모 → 기술 깊이(팬아웃) → 규제 안전(AI는 대신 말하지 않음·사람 발행)
  → 로드맵(F4·F5) → 정량 가치(`docs/VALUE.md`: **PB 1인 ≈ 0.1 FTE**, 리서치 확장은 2차로 ~2/~5 FTE).
- 데모 대본 + **백업 녹화본**(라이브 실패 대비) + 예상 Q&A(환각/규제/비용/사내데이터).

**데모 데이터**(2026-07-27 기준)
- **노트 7건** · 발행 1 · 발행 통과율 14.3% · 부착률 **83.9%**(47/56, 해석 30문장 제외 — §1-1) ·
  큐 10건(종목 노트 6 + 고객 문의 4) · 고객 50명. **워크플로 시연용으로 단계를 벌려 뒀다**:

  | 노트 | 상태 | 데모에서 쓰는 방식 |
  |---|---|---|
  | #10 LG화학 | **발행완료** | 확인 4건 후 발행까지 간 완결 사례 — 통과율을 만드는 유일한 건 |
  | #7 NAVER | **심의중**(확인 0) | **라이브로 확인→발행을 보여줄 건.** 미인용 6문장 = 해석 5 + UNSOURCED 1 |
  | #13 기아 | **심의중** | 사용자가 새 흐름(PB가 사실 확인→심의 요청)으로 직접 올린 건 |
  | #11 POSCO | 초안 | **하드 블록 시연** — 목표주가·지연시세는 확인으로도 안 풀린다(§1-2) |
  | #8 기아 · #9 카카오 · #12 SK하이닉스 | 초안 | |

- **브리프 #9**(`brief_date` 2026-07-27 = SK하이닉스 21명·셀트리온 19명·KB금융 18명)가 최신.
  **지수가 실린 첫 브리프**다(코스피 6,690.62 ▼5.72% · 코스닥 748.22 ▼5.32%, basDt 20260724).
  게이트 위반 0, 19문장 전건 출처. **고객 시드를 바꾸면 유니버스도 바뀌므로** 그때는
  다시 돌려야 화면과 맞는다(`POST /api/briefs/run`, 40~50초·크레딧).
- ⚠️ **월요일 브리프는 공시가 거의 빈다** — 파이프라인이 `dart_search days=2` 고정
  (`BRIEF_SYSTEM_PROMPT`)인데 월요일엔 그 창이 토·일·월이다. #9 실측: 3종목 중 공시가 붙은 건
  셀트리온 1건뿐(`days=4`면 셀트리온 6·KB금융 8). 뉴스·시세는 정상이라 게이트는 통과하지만
  **월요일 데모라면 공시 줄이 거의 없다.** 창을 거래일 기준으로 잡으면 되는데 `bizdate`에
  영업일 계산이 없어 추가가 필요하다. 아직 안 고쳤다.
- ⚠️ **노트 중 브리프 종목과 겹치는 건 SK하이닉스(#12) 하나뿐**. 그 종목 보유 고객(21명)은
  `PrepMemo`에 브리핑·노트가 같이 붙고, NAVER 보유 고객(예: 강준서)은 노트만 붙는다 —
  **둘 다 데모 대본에 넣을 것.** 나머지는 "이 종목은 없습니다"로 뜬다(정직하지만 빈손으로 보인다).
- `pb_customers(50)`·`pb_sessions(30)`·감사로그는 **보존**(§2 시드 유실 주의).

**착수 전 필수**: `git pull` 후 `docker compose build backend && up -d --force-recreate backend`
— `redis` 의존성 때문에 안 하면 F1이 `ModuleNotFoundError`로 죽는다.

---

## 0-1. 제품 확정 사항 — 되돌리지 말 것

### 사용자 = **PB 한 명**

계획서는 주 사용자를 리서치센터 애널리스트로 썼지만, 제품은 **PB가 고객 상담 전에 참고하는
사실 정보 도구**로 확정됐다(2026-07-22). 그리고 이 대시보드는 여러 PB가 공유하는 감독 콘솔이
아니라 **한 사람의 개인 화면**이다(2026-07-23).

**AI는 PB를 대신하지 않는다** — 사실을 모아줄 뿐이고 고객에게 나가는 말은 PB가 직접 쓴다.
상담 모달은 "AI 답변 초안 승인·전송"이 아니라 **"고객 문의 대응 준비"**다(회신 전 확인할 사실 +
"확인 완료(회신은 PB가 직접)"). ⚠️ **AI가 고객 회신을 대신 쓰는 화면으로 되돌리지 말 것.**

### 데이터 스코핑은 화면이 아니라 **서버**

- **`main.PB_NAME`**(compose 환경변수, 기본 `"PB"`)으로 `/api/customers`·`/api/sessions`·
  `summary.customers_total`·`pb_watchlist`를 전부 좁힌다.
- **남의 고객 상세는 404**(403이 아니다 — 존재를 알려주면 목록을 거른 의미가 없다).
  세션 승인·반려도 같이 막았다: 읽기만 좁히면 id를 아는 것만으로 남의 건을 처리할 수 있다.
- 값이 하나뿐이어도 **서버 스코핑은 유지한다** — 필터가 서버에 있어야 정보장벽이다.
- 프론트 `MY_PB`와 `PB_NAME`은 **같은 값이어야 한다** — 어긋나면 화면은 "내 고객"이라 적는데
  서버는 다른 사람의 고객을 보낸다.
- 고객 50명은 전원 `'PB'`로 통합돼 있다. 원래 3인 배정으로 되돌리려면
  `backend/scripts/restore_pb_assignment.sql`(되돌리면 브리핑 유니버스도 바뀐다).

### 역할 2종 — 만드는 쪽과 통과시키는 쪽이 갈리는 지점은 **발행 하나**

`초안 → [사실 확인 시작] PB → [준법 심의 요청] PB → [발행] 준법`

- 관리자 역할은 삭제됐다(공유 콘솔 전제에서만 의미가 있었다).
- 토글은 `내 화면`(PB) / `준법 화면 (데모)` — **역할 전환이 아니라 화면 전환**이다. 준법은 이
  대시보드의 사용자가 아니라 승인 단계를 맡는 다른 사람이고, 데모에서 그 단계를 보여줘야 해서
  미리보기로 남겼다. 그래서 준법 화면엔 **브리핑도 F1 버튼도 없다.**
- PB 화면에서 심의중 노트는 버튼이 비활성이고 🔒 사유를 말한다.
- **승인 배분 = 셀프 클레임**: 미배정 건을 열어 검토 시작하면 그 사람이 담당자. 푸시 배정 없음.

### 이름은 사람 이름이 아니라 **역할명**

화면·DB 모두 `PB`/`준법`으로 통일돼 있다(옛 `박PB`·`정준법`·`관리자`·`김애널` → 정규화).
화면 정규화는 `types.ts::actorLabel()` 한 곳. 되돌리려면 `backend/scripts/restore_audit_actors.sql`.

⚠️ **이건 프로토타입 fixture 정리이지 런타임 동작 변경이 아니다** — 앱은 감사를 소급 수정하지
않는다(발행 후 append-only 그대로). W7 Q&A에서 "감사 무결성"을 물으면 이 구분을 말할 것.

### 화면 구조

- **탭 축 = "읽기 / 하기"**(PB): `상담 준비` / `작성·검토`. 준법은 `심의` / `감시`.
  탭 줄은 그 역할의 탭이 **2개 이상일 때만** 낸다 — 선택지가 하나인 탭은 제목이지 장치가 아니다.
  판단 기준은 **"묶음의 이름이 안 나오면 묶음이 틀린 것"**이다.
- **F1 입구는 둘** — 우하단 고정 버튼(`.fab`, 아무 종목)과 고객 카드 인라인 채팅(보유 종목).
  FAB은 스크롤과 무관하게 고정이고 모달이 열리면 숨는다. F1만 상담 **중**에 쓰기 때문이다.
- **종목 노트 생성(F3) 뷰는 `hidden`으로만 감춘다 — 언마운트 금지.** 실행이 1~2분 걸리는데
  언마운트되면 `ResearchCard` cleanup이 SSE를 닫아 **크레딧만 쓰고 노트가 안 나온다.**
  다른 탭에서 실행 여부를 알 수 있게 `onRunningChange` → 탭 라벨에 `●`.
- **`AI가 오늘 한 일`은 접이식 한 줄**(native `<details>`). 값은 전부 감사로그 오늘치
  (`db.today_activity`) — 프론트에서 audit 목록을 세면 최근 N건 제한에 걸려 조용히 적게 세인다.
  **0인 항목은 적지 않는다** — 0건을 나열하면 아무것도 안 한 날도 일한 것처럼 보인다.
- **전체 고지는 맨 아래 한 줄**(`.disclaimer`, ⚠ + 13px):
  > 본 화면의 AI 산출물은 공개 공시·뉴스·지연시세에 근거한 내부 참고용 미검증 초안이며,
  > 투자권유·광고가 아닙니다. 대고객 문안 작성과 발행은 사람의 검토·심의·승인을 거칩니다.

  색에 `--muted`를 쓰지 않았다 — 이 배경에서 대비 3.4:1이라 **읽히지 않는 고지는 고지가 아니다**
  (`--ink-2`는 7.6:1). ⚠️ 이건 **프론트 전용**이다. `compliance.py::NOTICES`(기능별, 백엔드가
  산출물 본문에 강제 삽입)와 별개라 이 문구를 고쳐도 게이트에 영향이 없다. 반대로 `NOTICES`를
  고치면 `CLAUDE.md` 표도 같이 고쳐야 한다.
- 타일 설명줄은 **바로 위 숫자를 쪼갠 것이어야 한다**(처리 대기 10 = 종목 노트 6 + 고객 문의 4).
- 고객 표의 순번 열(`#`)은 고객 id가 아니라 **표에 보이는 자리**다. 상세 패널 순번과 같은
  목록(`visibleCustomers`)에서 세므로 검색으로 걸러도 짝이 어긋나지 않는다.

### 고객 포트폴리오 카드 = 찾기 · 읽기 · 묻기 3열 (2026-07-27)

`260px | minmax(0,1fr) | minmax(0,1fr)` — **리스트 / 상세 / 채팅**.

- **리스트는 660 → 260px.** 뺀 열(계좌·나이·위험성향·잔고)은 **전부 오른쪽 상세에 이미 있다.**
  남긴 건 이름과 수익률·▲플래그(훑을 때의 분류 신호)뿐. 이 표는 고객을 **찾는** 곳이다.
- **채팅은 "고객에 대해 묻는 챗봇"이 아니라 보유 종목으로 좁힌 F1이다.** 백엔드가 부를 수 있는
  건 공시·뉴스·시세뿐이라 고객 자체에 답할 데이터 경로가 없고, 고객 식별정보는 산출물에 들어가면
  안 되며(가드레일 1), 회신문 대필도 금지다(가드레일 4). 그래서 제목(`보유 종목 질문`)·칩·
  placeholder의 **주어를 전부 종목으로** 뒀다. ❗ 한계는 §7("입력 가드가 고객 이름을 못 잡는다").
- **칩은 입력창을 채울 뿐 보내지 않는다** — 실행은 크레딧을 쓰고 답변은 상담에 쓰인다.
  `F1Chat`에 `prefill:{q,n}`(같은 종목 재클릭도 먹히게 n)과 `compact`를 더했다. 프리필은
  effect가 아니라 **렌더 중 조정**이다 — effect면 빈 입력창이 한 번 그려졌다 채워진다.
- **고객을 바꾸면 대화를 새로 시작한다**(`key={selected.id}`). 편의가 아니라 정확성이다:
  세션을 이어가면 다음 질문이 멀티턴 `last_entity`로 **앞 고객의 종목을 이어받아**, 화면이
  가리키는 고객이 갖고 있지도 않은 종목을 답한다.
- **`PrepMemo`(상세 아래 사실 목록)에서 뺀 것 셋** — 셋 다 "같은 목록인데 줄마다 달라 보이는"
  문제였다:
  - **"이 종목 묻기" 버튼** — 칩이 보유 종목 전부를 이미 덮어(둘 다 `customer.holdings`) 같은
    동작이 두 곳에 있었다. `.btn.mini` CSS도 같이 삭제.
  - **시세 줄**(가격·등락률·지연시세 날짜) — 브리프에 든 종목만 시세가 잡혀서 어떤 줄엔 붙고
    어떤 줄엔 안 붙었다. 그 차이가 "이 종목이 더 중요하다"로 읽히는데 실제 뜻은 **"오늘 브리핑
    3종목에 들었다"**일 뿐이다(선정 기준은 담당 고객 전체의 보유 수라 이 고객과 무관).
    시세를 볼 곳은 브리핑 카드 한 곳으로 모았다.
  - **라벨**(옛 `상담 준비`) — 붙일 만한 이름을 화면이 전부 이미 쓰고 있었다: `상담 준비`는
    **PB 탭 이름**, `보유 종목`은 바로 위 표의 `aria-label`이자 채팅 머리말. **다시 붙이지 말 것.**
- **`.prep`의 `border-top` 제거** — 위 표의 마지막 행이 이미 `border-bottom`을 그어 12px 사이에
  선이 두 개 겹쳐 보였다. 간격(22px)만으로 두 블록이 갈린다.
- **`.bcode`(작은 회색 부가정보)는 전역 규칙이다** — 원래 `.bcard .bcode`로만 있어서 **14곳 중
  3곳에서만 먹었다.** 나머지는 클래스만 붙고 부모 값을 물려받아, `PrepMemo`의 종목코드가
  종목명과 똑같은 `12.5px w600 검정`으로 나왔다. 값은 브리핑 카드가 쓰던 것 그대로다.
- **높이**: 세 칸이 같은 행 높이로 끝난다(상세가 고객마다 507~849px로 달라도 따라간다).
  ⚠️ **`flex` 축약형 금지** — `flex: 1`은 basis가 `0%`인데 높이 미정 컨테이너에서 `content`로
  해석돼 표/대화 전체 높이가 행 크기에 들어간다(표가 스크롤을 잃고 **반대로 상세가 늘어난다**,
  실측 1788px). `flex: 1 1 0px`으로 적어도 소용없다 — **Next 16의 CSS 최적화가 `0px`를 떨어뜨려
  `flex: 1 1`로 내보낸다**(빌드 산출물 확인). `flex-grow: 1` + `height: 0`을 쓴다.
  `.tbl-scroll`·`.cust-chat .chat-log` 둘 다 같은 이유로 같은 방식이다.
- **반응형**: ≤1080px에서 채팅이 아래 한 줄로 내려가고(리스트·상세는 나란히) 로그는
  `flex-grow:0`+`max-height:40vh`로 되돌아간다 — 제 줄로 내려오면 높이를 정해줄 옆 칸이 없다.
  ≤720px는 전부 세로로 쌓이고 `.tbl-scroll`도 옛 상한(396px)을 다시 쓴다.

### 명칭·표기

- **"팩트 노트" → "종목 노트"**로 통일. 한 화면에서 같은 것을 두 이름으로 부르면 다른 물건처럼
  읽힌다. ⚠️ **아직 세 번째 이름이 있다** — 백엔드 큐 제목(`main.py`)과 노트 모달(`ReviewModal`)이
  `실적·공시 노트`라 한 줄이 `[종목 노트] 기아(000270) 실적·공시 노트 초안`으로 나온다.
  `CLAUDE.md` 기능 표는 아직 `F3 종목 팩트 노트`.
- 브리핑 카드 제목은 `브리핑`이지만 **기능 이름은 여전히 `F2 상담 전 브리핑`**이다(코드 주석·
  `CLAUDE.md`). 문서에서 기능을 가리킬 때는 원래 이름을 쓸 것.
- **날짜 표기는 `api.ts::fmtDate` 한 곳으로.** 공시는 `20260722`, 뉴스는 RFC 2822라 형식이 다른데
  화면마다 알아서 자르다 **`뉴스 Wed, 22 Ju`가 그대로 사용자에게 나갔다.** 세 형식(8자리·ISO·
  RFC 2822)을 받고 못 읽는 값은 지어내지 않고 원문을 돌려준다. **하루 경계는 KST 고정** —
  UTC로 찍으면 00~09시 뉴스가 전날로 밀리는데 브리핑이 내세우는 게 하필 "밤사이 뉴스"다.

### 런타임 프롬프트도 같이 정렬한다

**`CLAUDE.md`는 문서가 아니라 실제 프롬프트다** — 백엔드의 모든 `query()`가 `cwd`=레포 루트로
돌고 `setting_sources`를 안 좁혀서 파일이 그대로 실린다. 화면·문서만 PB로 바꿨을 때 에이전트는
여전히 자기를 "금융 리서치 코파일럿"으로 알고 있었다. 코드 쪽 프롬프트도 같이 봐야 한다:
`f1.py::ANSWER_SYSTEM_PROMPT` · `main.py::BRIEF_SYSTEM_PROMPT` · `.claude/agents/a5.md`.

### 열린 결정 — 고객 상세의 "AI 진단"

`pb_customers.diagnosis`에 박힌 **시드 예시 문구**인데 라벨이 "AI 진단"이라 AI 산출물처럼 읽힌다
(50명에 5종류가 돌아간다). ①라벨을 "예시 문구·시드"로 / ②**보유 종목·성향·위험 플래그 실값에서
규칙으로 재작성**(권장, LLM 없이 근거 있는 한 줄) / ③삭제.
**지금 상태로 데모하면 실집계 숫자들 사이에 목업 문장이 섞여 있다.**

---

## 1. a5는 위임이 아니라 backend 2차 `query()` — `Agent` 도구 비동기 함정

*(코드 여러 곳이 "HANDOFF §1"로 이 절을 가리킨다. 상세 배경은 `CLAUDE.md` "검증됨" 절.)*

`Agent` 도구는 **비동기**다 — 결과가 `"Async agent launched successfully"`로 즉시 돌아오고
서브에이전트는 백그라운드에서 돈다. **O가 턴을 끝내면 `query()` 스트림이 닫히고 도구를 안 부르는
a5가 잘린다.**(a1·a2·a4는 도구 결과가 스트림에 빨리 들어와 살아남는다.)

**채택한 구조**: O의 1차 쿼리가 끝나면 backend가 a5를 **메인 에이전트로 하는 2차 `query()`**로
직접 돌린다(`main.py::event_gen`). a5가 메인이라 `text_delta`가 `parent_tool_use_id=None`으로 흘러
토큰 스트리밍이 산다. 지침은 `a5.md` **본문만** 읽어 `system_prompt`로 쓰고(`_agent_prompt`),
입력은 O의 산문이 아니라 **도구 결과를 그대로 직렬화**(`_a5_input`, LLM 미개입).

- `background: false`도 위임을 동기화하지만 **StreamEvent가 안 나와 토큰 스트리밍이 죽는다** → 안 씀.
- ❌ **반복 금지**: 프롬프트에 "a5 결과를 기다려/인용해"를 넣었더니 O가 **a2 수치로 문장을 지어내
  a5가 쓴 것처럼 인용**했다(가드레일 3 위반). 지금은 O 프롬프트에서 노트 단계를 아예 뺐다.

---

## 1-1. 출처 부착률·게이트 정의 — 지표 함정 (설계 의도)

*(코드가 "HANDOFF §1-1"로 이 절을 가리킨다.)*

**핵심 교훈: 지표를 믿기 전에 지표부터 검증하라.** 낮은 수치를 보면 산출물을 탓하기 전에 측정기를
의심할 것. 한 종목만 보고 "규칙 준수"라 결론 내리지 말 것. 측정 정의 버그를 **4건** 잡았고 전부
a5 오류가 아니었다: 파서가 문장 중간 각주를 버림(노트6 10개 중 5개 유실 → 발행 하드 블록) ·
eval 하네스 3건(조/억↔백만원 표기, 재무 카드 여러 개 중 첫 것으로 대조, 존댓말 해석 어미 미인식).

**확정된 정의**(`citations.citation_stats()` **단일 출처** — 게이트·대시보드가 같이 쓴다):
- 문장마다 `kind`: `heading`·`boilerplate`·`claim`(사실 주장)·`interpretation`(해석·전망).
- **부착률 분모 = `claim`만.** 해석 문장은 규칙상 각주를 안 붙이므로 뺀다. 안 빼면 a5의 해석
  문장 수 변덕이 품질로 오독된다(실측 10/14↔7/14로 흔들렸다).
- **분류는 보수적으로** — 애매하면 `claim`. 헐거우면 미인용 문장이 분모에서 빠져 부착률이
  후해지는데, **컴플라이언스 지표는 그 방향으로 틀리면 안 된다.**
- **게이트(`unsourced_count`)는 F3에서 해석 문장도 센다** — 발행 전 사람이 판단하도록 올리는 게
  설계다. 지표와 게이트 정의가 다른 건 의도된 것. (F1은 발행 단계가 없어 `claim`만 센다.)

⚠️ **분류기를 바꾸면** 기존 노트의 `sentences_json`은 저장 시점에 굳어 있으므로
`scripts/reparse_notes.py`로 재파싱해 대시보드와 맞춘다.

---

## 1-2. 미인용 문장 확인(ack) — 게이트를 사람이 여는 유일한 경로

*(§1-1을 먼저 읽을 것. 여기는 "그래서 사람이 어떻게 통과시키나"다.)*

**발견된 문제: 발행이 구조적으로 불가능했다.** 게이트는 해석 문장까지 미인용으로 세는데
**올려만 놓고 사람이 풀 수단을 안 만들었다** — `publish`는 violations가 비어야 통과하고 노트 편집
UI도 없다. 결과: 노트 6건이 6건 다 영구 `draft`. "AI가 막힌다"는 보여줄 수 있는데 **"사람이
확인하고 통과시킨다"는 한 번도 못 보여줬다** — 제품 서사의 절반이 빠져 있었다.

막고 있던 문장은 각주를 붙일 수 **없는** 것들이었다(a5 잘못 아님): 해석·전망 · 고지 문장 ·
`"위 수치는 2024 연결(CFS) 기준"` 같은 데이터 설명. 뒤의 둘은 §1-1의 보수적 분류 결과다 —
**지표를 후하게 만들지 않으려던 보수성이 발행을 잠그는 쪽으로 작동했다.**

**채택한 구조**: 준법이 미인용 문장마다 **사유를 골라 확인**하면 그 문장만 집계에서 빠진다.
사유는 `해석·전망`/`고지·면책`/`데이터 설명` 고정값(`main.ACK_REASONS`) — 감사 대상이라 자유
입력이 아니다. 확인·해제 전부 감사로그(`ack_added`/`ack_removed`)에 남는다.

- **면제되는 건 미인용 규칙 하나뿐.** 투자권유·광고성 표현, MNPI, 지연시세 고지 누락은 확인으로
  **못 푼다**(`test_ack_cannot_waive_forbidden_phrase`로 고정). POSCO 하드 블록 시연은 그대로 산다.
- **심의 단계에서만** 확인할 수 있다 — 초안에서 미리 풀어두면 검토가 형식이 된다.
- **재파싱하면 확인이 무효가 된다** — 저장할 때 원문 앞 60자를 같이 남기고(`notes.acks_json`)
  발행 시 대조한다(`main._valid_acks`). 안 맞으면 **막히는 쪽**으로 판정한다.
  컴플라이언스 판정은 어긋나면 통과가 아니라 차단으로 틀려야 한다.
- 해제는 언제나 가능하다 — 조건이 바뀐 뒤 남은 확인을 되돌릴 길이 없으면 안 된다.
- 화면은 문장 목록 **위에** "출처 없는 문장 N개가 발행을 막고 있습니다"를 먼저 말한다 —
  발행 버튼을 눌러 409를 받고 나서야 아는 건 검토 화면이 할 일이 아니다.
- ⚠️ 확인 대상은 "해석으로 분류된 문장"이 아니라 **미인용 문장 전부**다. 분류기를 느슨하게
  고치는 대신 판단을 사람에게 남긴 것이다.

**라이브 확인(LG화학 #10)**: 초안 → 검토 → 심의 → 발행 시도 **⛔`publish_blocked`** →
`ack_added`×4 → **✅`published`**. 감사로그에 **막힌 사실과 푼 사실이 둘 다** 남는다.

---

## 2. 반드시 알아둘 함정·운영 수칙

- 🚩 **`git commit`·`git push`는 실행 전에 반드시 사용자에게 묻는다 — 로컬 커밋도 예외 없다.**
  변경 요약만 보여주고 멈춘다. 커밋 메시지 초안까지는 준비해도 되지만 **실행은 사용자가
  "커밋해"라고 말한 뒤**다. 사후 보고("커밋했습니다")는 안 된다.
- **자주 커밋할 것** — 이전 세션의 대시보드 구현이 **미커밋 상태로 통째 유실**된 적이 있다.
  ⚠️ 위 규칙과 충돌이 아니다: **"자주 커밋하자고 자주 제안하라"**는 뜻이다. 묻지 않고 쌓아두다
  유실되는 것도, 묻지 않고 커밋하는 것도 안 된다.
- **`pb_customers`·`pb_sessions`는 시드 소스가 유실돼 `.pyc`뿐이다 — 절대 지우지 말 것.**
  `TRUNCATE notes, audit_log, briefs`는 가능하나 차트·알림이 같이 빈다.
  ⚠️ **UPDATE로 덮어쓸 때도 복원 경로를 먼저 만들어라** — 시드를 다시 만들 방법이 없다.
- **MCP 서버(`backend/mcp_servers/*.py`)에서 `backend` 패키지를 import하려면 sys.path 부트스트랩이
  필요하다.** `.mcp.json`이 **스크립트로 직접 실행**하므로 `sys.path[0]`이 파일 디렉터리다 —
  cwd가 레포 루트여도 `ModuleNotFoundError`로 죽고, **서버가 안 뜨면 그 서버의 도구 전체가
  조용히 사라진다**(실제 발생: krx가 죽어 브리프 3종목의 **시세가 전부 빔**, 게이트는 통과라
  티가 안 남). 손대면 `backend/.venv/bin/python backend/mcp_servers/<x>_server.py </dev/null`로
  기동만이라도 확인할 것.
- **"오늘"의 정본은 `backend/bizdate.py` — KST 고정.** 컨테이너·Postgres가 UTC라 `date.today()`·
  `ts::date`를 그대로 쓰면 하루가 **KST 09:00에 끊긴다.** 증상 3종이 실제로 났다: ①"AI가 오늘
  한 일"이 아침 9시 전엔 통째로 빔 ②아침에 만든 브리프의 `brief_date`가 어제로 찍힘 ③DART/KRX
  조회 창 종료일이 밀려 **그날 아침 접수된 공시가 빠짐**. 새로 날짜를 만들면 `biz_today()`,
  SQL은 `biz_date_sql()`.
  ⚠️ `bizdate`는 **stdlib만 쓴다** — krx MCP 서버가 sys.path 부트스트랩으로 끌어가므로 무거운
  의존성을 넣으면 위 함정대로 **서버가 안 뜨고 도구가 통째로 사라진다.**
  ⚠️ 같은 상수가 **세 곳**: `bizdate.py`(정본) · `dart_server.py`(자체 복사) · 프론트 `api.ts`.
  **어긋나면 같은 사건이 서버 집계와 화면 차트에서 다른 날짜 칸에 꽂힌다.**
- **"처리할 일의 목록"과 "읽을 것의 목록"은 다르다** — 큐(`/api/dashboard/queue`)는 발행분을 빼고
  노트 색인(`/api/notes`)은 포함한다. 화면에서 노트를 **읽으려면** 후자를 써라. 전자를 쓰면
  사람이 승인을 끝낸 노트, 즉 **PB가 상담에 실제로 써도 되는 유일한 등급**이 그 순간 사라진다.
- **도구 결과가 토큰 한도를 넘으면 `is_error=False`인 채 에러 문자열이 온다** →
  `main.py::_tool_failed()`로 걸러 `completed`로 오집계되는 걸 막는다. 자체 MCP는 반환량 상한을
  둘 것(`dart_search`는 하루 382건 나온 적 있음 → `limit=30`+최신순).
- **원본과 대조할 때는 파이프라인과 같은 조건으로 조회할 것** — F2 검증 때 `days=2` 결과를
  7일 창과 비교해 "4건 유실"이라 오진했다(실제 유실 없음).
- **SSE 이벤트명을 `error`로 쓰지 말 것** — EventSource의 연결 오류 핸들러와 겹친다. 실행 오류는
  `run_error`로 보낸다. 예외가 나도 `run_error`+`done`을 반드시 보낸다(화면이 종료 상태에 도달).
- **재생 SSE로 화면을 검증할 땐 코드의 실제 분기를 보고 시나리오를 짤 것** — "파싱 실패=노트 없음"
  으로 짰다가 허구를 검증할 뻔했다(실제론 뉴스만 있어도 a5가 돈다). `scenario_check.mjs` 참고.
- **`scripts/seed_brief.py`는 F2 파이프라인이 아니다** — 크레딧 없이 화면 검증용으로 에이전트를
  건너뛰고 MCP를 직접 부르는 시드다. **데모에서 F2 결과로 소개하면 안 된다**(멀티에이전트 서사 빠짐).
- **DART 전자공시는 PDF가 아니라 HTML 뷰어** — "페이지 번호" 단위가 없다. `viewer_url`+`rcept_dt`가
  이 소스에서 가능한 최대 해상도다.
- **실화면은 목업으로 폴백하지 않는다** — 백엔드가 없으면 연결 실패를 그대로 말한다.
  목업이 필요하면 시안 파일(`docs/design/pb-admin-dashboard.html`)을 연다.
- **레포에 prettier 설정이 없어** 에디터가 파일을 통째로 재포맷하고 diff가 부풀어 오른다
  (`page.tsx`·`dashboard.css`·`F1Chat.tsx`에서 반복됐다). 설정 파일 하나면 사라진다.
- **`docs/ARCHITECTURE.md` 로드맵이 F1을 "확장 예정"으로 적어둔 채다** — F1은 구현됐다(§4).

---

## 3. 백엔드 라우트 · 목업 데이터

**라우트 11종**: `/api/dashboard/{summary|queue|agents|audit}` · `/api/customers[/{id}]` ·
`/api/sessions` · `/api/sessions/{id}/{approve|reject}` · `GET /api/notes`(발행분 포함 노트 색인) ·
`POST /api/notes/{id}/ack`(미인용 문장 확인, §1-2).

⚠️ **고객·상담 계열은 `main.PB_NAME`으로 서버에서 좁혀진다** — `/api/customers`,
`/api/customers/{id}`(남이면 404), `/api/sessions`, `/api/sessions/{id}/{approve|reject}`,
`summary.customers_total`, 큐의 고객 문의. **노트 계열은 안 좁힌다**(전부 이 PB의 것).

**목업 고객 50명**: 세대혼합 이름 비복원 추출 · 잔고 로그균등 · 나이=이름 세대+재산 보정 ·
**위험 플래그 규칙 3종**(①안정형인데 주식>40% ②단일종목≥65% ③수익률≤−5%)+사유 저장.
수치는 전부 실집계이고, 비교 데이터가 없는 델타는 감춘다.

**AI 신뢰도 카드**: 출처 부착률(목표 90% 미터)·발행 통과율·게이트 차단(7일) — KPI 타일이 아닌 미터/비율.

---

## 4. F1 대화형 종목 Q&A (규칙 라우팅 + Redis 멀티턴)

입구는 **둘**이다. ①우하단 고정 버튼(`💬 종목 즉답`) → 채팅 모달 — 아무 종목이나 물을 때.
②고객 포트폴리오 카드의 인라인 채팅 — 그 고객 보유 종목으로 좁혀 물을 때. 둘 다 같은
`/api/chat/stream`과 같은 `F1Chat` 컴포넌트다(인라인은 `compact`+`prefill`).

파이프라인: 입력 가드 → 규칙 라우팅 → 라우팅된 에이전트 도구 조회 → 2차 query(f1 답변자,
구조화 데이터만) 스트리밍 → citations(시세 `[^krx]`) → F1 고지 강제 → 게이트 → 세션 갱신.
**라우팅·데이터취합·게이트는 코드가, LLM은 답변 문장만**(F3와 같은 각주 무결성).

- **입력 가드가 F1의 핵심**(`compliance.input_guard`) — 자유 텍스트라 F3·F2에 없던 공격면.
  MNPI·인젝션·PII를 **에이전트 前에** 잡아 차단하면 도구가 아예 안 돈다(크레딧 0).
- **규칙 라우팅**(`f1.route`, 순수) — 6자리 코드/별칭 + 의도 키워드 → 에이전트(quote→krx·
  financials→a2·news→a4·disclosure→a1). 엔티티 없으면 되묻기. LLM에 안 맡긴 이유: **데이터 소스
  선택은 컴플라이언스 경계라 감사 가능한 규칙이어야 하고**, "왜 이 에이전트로 갔나"를 배지로 보여준다.
- **게이트 F1 분기**: `CHAT_NOTICE`에 "지연시세"를 넣어 QUOTE 게이트를 자기충족. F1은 발행 단계가
  없어 해석 문장은 위반으로 안 세고 **근거 없는 사실 주장만** 잡는다.
- **멀티턴(Redis)** — 현재 질문에 종목이 없으면 세션의 **직전 종목을 이어받는다**. 의도는 매 턴
  새로 판단(종목만 잇는다). `session_store.py`는 **라우팅 맥락만** 저장(턴 상한 8·`last_entity`·
  TTL 1h) — 답변 본문·도구 결과는 안 담는다(턴 넘나들며 날조 방지). UI는 **↩ 이어받음** 배지.
- 파일: `backend/f1.py`·`session_store.py`·`test_f1.py`·프론트 `F1Chat.tsx`.

---

## 5. 아키텍처 한눈에

```
[F3] 종목코드 → O(Opus) → a1 법인확인 → a2 재무 ‖ a4 뉴스 팬아웃        ← 위임은 여기까지
  → backend 도구결과 직렬화(_a5_input) → 2차 query()로 A5 직접 실행(§1) → citations(kind 부여)
  → 워터마크(F3) 강제 → 게이트 → notes 저장 → SSE(진행·토큰·노트·done{outcome})
     재무·뉴스 둘 다 없으면 a5 안 돌리고 사유만 보낸다
[F2] POST /api/briefs/run → pb_watchlist(PB_NAME 담당 고객 보유 상위 N) → O → a1 공시 ‖ a4 뉴스
  + krx(O 직접) — 새 에이전트 0개
  → 지수는 market.py가 직접 조회(판단이 없어 위임 불필요, 실패 시 사유만 market_json에)
  → brief.assemble(지수·중요도 순위·출처 부착, LLM 미개입) → "내부 참고용" 강제 → 게이트 → 저장
     공시 중요도(0주요 1정기 2기타 3지분): 지분공시는 빼지 않고 뒤로만 민다(조용한 날 안 비게)
[F1] GET /api/chat/stream → input_guard(에이전트 前) → f1.route(세션 이어받기)
  → 라우팅된 에이전트/krx 조회 → 2차 query 스트리밍 → citations → F1 고지 → 게이트 → session_store
     ← 에이전트(LLM)는 위 세 생산 구간에서만 실행
[대시보드] Next.js `/dashboard`(PB 1인용, 화면 토글 2종)
  PB · 상담 준비: AI가 오늘 한 일(접이식) → 타일 → 브리핑(F2) → 고객 포트폴리오(3열) → 전체 고지
  PB · 작성·검토: 종목 노트 생성(F3 SSE, hidden으로만 감춤) → 처리 대기 큐
  준법 · 심의: AI가 오늘 한 일 → 처리 대기 큐(심의 건만)
     — 큐는 역할에 따라 자리가 다르다. `queueCard`를 한 번만 정의하고 옮긴다: 준법에겐 노트 탭이
       없어서 큐를 옮기면 심의할 노트를 화면에서 찾을 방법이 없어진다
  준법 · 감시: AI 신뢰도 · 컴플라이언스 알림(접이식 요약) · 감사로그(색·날짜·접이식·페이징)
     — 알림은 감사로그의 부분집합이다(publish_blocked + 거부된 permission_check). 목적이 다르다:
       알림=골라낸 신호(triage), 감사로그=완전한 원장(evidence). 둘 다 유지
[사람] 노트: 사실 확인(PB)→심의 요청(PB)→[미인용 확인·사유 기록(준법)]→발행(준법, 게이트 재검사)
       고객 문의: 확인 완료(담당 PB, 회신은 사람이 직접 작성)
       — AI 개입 없음, 전 과정 audit_log. 확인은 미인용 규칙만 푼다(§1-2)
```

**파일 대응**: a5 작문 지침=`a5.md`(backend가 system_prompt로 읽음) / 출처매칭·문장범주·부착률
정의=`citations.py`(단일 출처) / F1 라우팅·답변조립=`f1.py`·대화상태=`session_store.py` /
브리프 조립=`brief.py` / 지수=`market.py` / 고지·게이트·입력가드=`compliance.py` / 상태·로그=`db.py` /
**날짜 경계(KST "오늘")=`bizdate.py`** / 배선·API=`main.py` / MCP=`mcp_servers/{dart,news,krx}_server.py` /
화면=`frontend/src/app/dashboard/{page,ResearchCard,ReviewModal,F1Chat,charts,api,types}.tsx`.

---

## 6. 환경/실행 메모

- 앱: `docker compose up`(backend 8000 / frontend 3000 / postgres 5432 / redis 6379).
  **requirements 변경 시 `docker compose build backend && up -d --force-recreate backend`**.
- 대시보드: http://localhost:3000/dashboard
- **자체 점검(크레딧·네트워크 불필요, 10종)**:
  `for t in compliance brief krx streaming tool_result a5_input brief_attribution citations run_outcome f1; do backend/.venv/bin/python -m backend.test_$t; done`
- **프론트 검증**: `cd frontend && npx tsc --noEmit && npx eslint src --max-warnings=0`.
  ⚠️ Next.js 16: 코드 전 `frontend/node_modules/next/dist/docs/` 확인(`frontend/AGENTS.md`).
- **화면 재생 검증**(크레딧 0, `frontend/`에서): `node scenario_check.mjs`(W6 빈/에러 5종).
- **Redis 세션 점검**: `REDIS_URL=redis://localhost:6379/0 backend/.venv/bin/python -m backend.scripts.session_store_check`
- **라이브 실행(비용)**: F3 `curl -sN "http://localhost:8000/api/research/stream?stock_code=005930" > /tmp/f3.sse`(~100초) /
  F2 `curl -s -X POST .../api/briefs/run -d '{}'`(~40초) / F1 `curl -sN ".../api/chat/stream?q=삼성전자 최근 실적"`.
  → `grep "^event:" *.sse | sort | uniq -c`로 이벤트별 건수부터 본다.
- 위임 동기/비동기 판정(저비용): `docker compose exec -T backend python -m backend.scripts.background_flag_check`
- 테스트 데이터 정리: `docker exec ai-pb-agent-postgres-1 psql -U app -d app -c "TRUNCATE notes, audit_log, briefs RESTART IDENTITY;"`
  (`pb_*`는 지우지 말 것 — §2)
- 고객 PB 배정 되돌리기: `docker exec -i ai-pb-agent-postgres-1 psql -U app -d app < backend/scripts/restore_pb_assignment.sql`
- API 키 4개(`.env`): `ANTHROPIC_API_KEY`·`DART_API_KEY`·`NAVER_CLIENT_ID/SECRET`·`KRX_API_KEY`(data.go.kr, 무료).
  ⚠️ KRX는 **서비스별로 활용신청이 따로**다 — 종목시세와 지수시세가 다른 신청이다(둘 다 승인됨).
- git: main → **personal**(hmgthb/ai-pb-agent) 업스트림. origin(kwangtekNa/intern)에는 푸시 안 함.
  **커밋·푸시 둘 다 사용자 확인 후에만 실행한다(§2 첫 항목).**
- SDK API명은 `claude-agent-sdk==0.2.110` 실물 확인 — `CLAUDE.md` "검증됨" 절.

---

## 7. 열린 항목 · 리스크

**제품 가치 순**
- ❗ **고객 문의 모달이 에이전트와 안 이어져 있다.** `ReviewModal.prepFacts()`는 계좌 시드값 +
  고정 고지문구 3줄이 전부다. 질문("주식 비중을 줄일까요")이 이미 특정된 **유일한** 화면인데 그
  고객 보유 종목의 공시·뉴스가 한 줄도 없고 F1로 가는 길도 없다. **남은 것 중 가치가 가장 크다** —
  `PrepMemo`의 줄과 보유 종목 칩+인라인 채팅(`.cust-chat`)을 그대로 재사용하면 된다.
- ❗ **상담 전 브리핑 카드에 자체 고지가 없다.** 종목 노트는 워터마크, F1은 답변마다 고지를 달고
  다니는데 **브리핑 카드만 없다.** 백엔드가 만든 `ℹ 내부 참고용 — 투자권유·광고가 아닙니다.`가
  `briefs.content_md`에 들어 있고 게이트도 통과하는데 **프론트 `Brief` 타입에 필드가 없어 안
  그린다.** 전체 고지를 맨 아래로 내린 뒤로 **화면에서 가장 큰 카드가 첫 화면에서 고지 없이
  보인다.** 타입에 필드 하나 + 카드 헤더 한 줄이면 끝난다(`NOTICES["F2"]`를 그대로 쓴다).
- **"AI 진단" 시드 문구**(§0-1 열린 결정) — 실집계 숫자들 사이의 유일한 목업 문장.
- **월요일 공시 공백**(§0) — `days=2` 창에 주말이 들어간다.
- **진입 단위가 종목인데 PB의 단위는 "오늘 만날 고객"이다** — 가장 좋은 화면(고객 포트폴리오)에
  가려면 페이지 최하단 표에서 고객을 직접 찾아야 한다.

**규제·경계에 대해 정직하게 말해야 할 것 (W7 Q&A)**
- ❗ **"고객에게 말 걸지 않기"는 프롬프트 지시일 뿐 게이트 규칙이 아니다.** `compliance.py`는
  "고객님께…" 같은 호칭·회신문 형식을 **위반으로 세지 않는다.** 라이브 4종 산출물 검사에서 실제
  발생은 0건이었지만, 보장이 게이트가 아니라 프롬프트에 걸려 있다는 뜻이다.
- ❗ **입력 가드가 고객 이름을 못 잡는다.** `compliance.PII_PATTERNS`는 주민·계좌번호의 **숫자
  형식**만 본다 — 한글 이름은 안 걸린다. PB가 "임민지 고객이…"라고 치면 그대로 LLM에 가고
  감사로그에 남는다(가드레일 1 위반). 지금 막고 있는 건 **인라인 채팅의 UI 프레이밍뿐**이다.
  규칙화 경로는 있다: 담당 고객명이 서버에 있으므로 `input_guard`에 사전 대조를 넣으면 된다.
- ❗ **역할 강제는 여전히 화면에만 있다.** 백엔드는 `actor`를 감사로그용 문자열로 받을 뿐
  "발행은 준법만"을 강제하지 않는다. **고객 데이터 스코핑은 서버로 내려갔지만 권한은 아직 아니다.**
- ❗ **감사로그 화면은 최근 200건까지만 본다.** `/api/dashboard/audit?limit=200`을 받아
  클라이언트에서 20건씩 페이징하므로 그보다 오래된 건 **화면에서** 못 넘긴다. **원본 DB는
  append-only로 전건 보존** — 사라지는 게 아니라 뷰가 캡을 둔 것이다.
- ❓ **정량 가치 밴드는 추정 모델** — 가정(X=60분·물량) 의존적. 근거를 물으면 `docs/VALUE.md`
  민감도표로 답하고 **단일 숫자로 단정하지 말 것.**

**남은 검증**
- ❓ **모달의 '발행' 버튼 클릭 자체**는 아직 안 눌러 봤다(발행은 curl로 했다). 같은 엔드포인트라
  리스크는 낮지만 W7 리허설에서 한 번은 눌러 볼 것 — #7 NAVER·#13 기아가 심의중이라 그대로 쓸 수 있다.
- ❓ **파서 수정 후 a5 각주가 실제로 맞는 출처를 가리키는지** 정성 관찰(부착률로는 확인됨).
- ❓ **3시나리오 예외 상태는 재생 SSE 기준** — 공시 없는/파싱 실패 종목을 라이브로 돌려본 건 아니다.
- 🔸 **FAB이 인라인 채팅의 `보내기` 오른쪽 끝을 덮는다**(가운데는 눌린다). 중간 스크롤에서만
  겹치는 FAB 본래 동작이라 손대지 않았다.
- 🔸 작은 것: `/api/notes` 전건 상세 조회(노트 수만큼 요청, 선형 증가) · 모달이 Esc로 안 닫히고
  F1 채팅 모달엔 닫기 버튼이 없다(배경 클릭만).
