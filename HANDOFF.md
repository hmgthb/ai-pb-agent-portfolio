# 인수인계 — AI PB 어시스턴트 (`ai-pb-agent`)

> 최종 갱신 2026-07-28. **다음 세션은 이 문서부터 읽으세요.**
> 읽는 순서: §0(지금 상태) → §0-1(되돌리면 안 되는 제품 결정) → §2(함정) → 나머지는 필요할 때.
> ⚠️ **절 번호를 바꾸지 말 것** — 코드가 `§0-1`·`§1`·`§1-1`·`§2`로 이 문서를 가리킨다.
> 관련 문서: 주차 계획 `주차별_실행계획.md`(원본 유지) · 런타임 지침 `CLAUDE.md` ·
> 측정 `docs/EVAL.md` · 가치 `docs/VALUE.md`. **git 추적 대상 — 자주 커밋할 것.**

---

## 0. 현재 상태

**계획서 🟢 필수(W1~W6) + 🟡 선택(F1)이 전부 라이브 검증으로 닫혔다. 남은 건 W7(발표)뿐.**

✅ W1 환경·MCP 3개 · W2 a1·a2·O 위임·SSE · W3 a4·팬아웃·진행 타임라인 · W4 a5·토큰
스트리밍·citations·게이트·발행(§1) · W5 F2 브리핑 · W6 3시나리오·eval·가치 리포트 ·
🟡F1 대화형 Q&A(§4)  |  ❌ **W7 덱·데모 대본·백업 녹화·예상 Q&A**

**다음 할 일 = W7 발표 준비**
- 임원 덱: PB의 하루 → 라이브 데모 → 기술 깊이(팬아웃) → 규제 안전(AI는 대신 말하지 않음·사람 발행)
  → 로드맵(F4·F5) → 정량 가치(`docs/VALUE.md`: **PB 1인 ≈ 0.1 FTE**, 리서치 확장은 2차로 ~2/~5 FTE).
- 데모 대본 + **백업 녹화본**(라이브 실패 대비) + 예상 Q&A(환각/규제/비용/사내데이터).

**데모 데이터** — 노트 7건 · 발행 1 · 통과율 14.3% · 부착률 **83.9%**(47/56, 해석 제외 §1-1) ·
큐 10건(노트 6 + 문의 4) · 고객 50명. **워크플로 시연용으로 단계를 벌려 뒀다**:

| 노트 | 상태 | 데모에서 쓰는 방식 |
|---|---|---|
| #10 LG화학 | **발행완료** | 확인 4건 후 발행까지 간 완결 사례 — 통과율을 만드는 유일한 건 |
| #7 NAVER | **심의중**(확인 0) | **라이브로 확인→발행을 보여줄 건.** 미인용 6문장 = 해석 5 + UNSOURCED 1 |
| #13 기아 | **심의중** | 사용자가 새 흐름(PB 확인→심의 요청)으로 직접 올린 건 |
| #11 POSCO | 초안 | **하드 블록 시연** — 목표주가·지연시세는 확인으로도 안 풀린다(§1-2) |
| #8 기아 · #9 카카오 · #12 SK하이닉스 | 초안 | |

- **브리프 #9**(`brief_date` 2026-07-27, SK하이닉스 21명·셀트리온 19명·KB금융 18명)가 최신.
  **지수가 실린 첫 브리프**(코스피 6,690.62 ▼5.72% · 코스닥 748.22 ▼5.32%, basDt 20260724).
  게이트 위반 0, 19문장 전건 출처. **고객 시드를 바꾸면 유니버스도 바뀌므로** 다시 돌려야
  화면과 맞는다(`POST /api/briefs/run`, 40~50초·크레딧).
- ⚠️ **월요일 브리프는 공시가 거의 빈다** — 파이프라인이 `dart_search days=2` 고정
  (`BRIEF_SYSTEM_PROMPT`)인데 월요일엔 그 창이 토·일·월이다. #9 실측: 3종목 중 공시가 붙은 건
  셀트리온 1건뿐(`days=4`면 셀트리온 6·KB금융 8). 창을 거래일 기준으로 잡으면 되는데
  `bizdate`에 영업일 계산이 없어 추가가 필요하다. **아직 안 고쳤다.**
- ⚠️ **노트 중 브리프 종목과 겹치는 건 SK하이닉스(#12) 하나뿐.** 그 종목 보유 고객(21명)은
  `PrepMemo`에 브리핑·노트가 같이 붙고, NAVER 보유 고객(예: 강준서)은 노트만 붙는다 —
  **둘 다 데모 대본에 넣을 것.** 나머지는 "이 종목은 없습니다"로 뜬다.
- `pb_customers(50)`·`pb_sessions(30)`·감사로그는 **보존**(§2 시드 유실 주의).

**착수 전 필수**: `git pull` 후 `docker compose build backend && up -d --force-recreate backend`
— `redis` 의존성 때문에 안 하면 F1이 `ModuleNotFoundError`로 죽는다.

---

## 0-1. 제품 확정 사항 — 되돌리지 말 것

### 사용자 = **PB 한 명**

주 사용자는 애널리스트가 아니라 **상담 전 사실을 확인하는 PB**이고(2026-07-22), 이 대시보드는
공유 감독 콘솔이 아니라 **한 사람의 개인 화면**이다.
**AI는 PB를 대신하지 않는다** — 고객에게 나가는 말은 PB가 직접 쓴다. 상담 모달은 "AI 답변 승인·
전송"이 아니라 **"고객 문의 대응 준비"**다. ⚠️ **회신을 대신 쓰는 화면으로 되돌리지 말 것.**

### 데이터 스코핑은 화면이 아니라 **서버**

- **`main.PB_NAME`**(compose 환경변수, 기본 `"PB"`)으로 `/api/customers`·`/api/sessions`·
  `summary.customers_total`·`pb_watchlist`를 좁힌다. 프론트 `MY_PB`와 **같은 값이어야 한다.**
- **남의 고객 상세는 404**(403이 아니다 — 존재를 알려주면 목록을 거른 의미가 없다). 세션
  승인·반려도 막았다: 읽기만 좁히면 id를 아는 것만으로 남의 건을 처리할 수 있다.
- 값이 하나뿐이어도 **서버 스코핑은 유지한다** — 필터가 서버에 있어야 정보장벽이다.
- 고객 50명은 전원 `'PB'`. 3인 배정 복원은 `backend/scripts/restore_pb_assignment.sql`
  (되돌리면 브리핑 유니버스도 바뀐다).

### 역할 2종 — 갈리는 지점은 **발행 하나**

`초안 → [사실 확인] PB → [준법 심의 요청] PB → [발행] 준법`
**거절도 두 갈래 있다**(2026-07-28): `검토중 —[보류]PB→ 보류됨`(종결) · `심의중 —[반려]준법→ 검토중`(되돌림)

- **거절이 없으면 이 화면은 "사람이 확인한다"가 아니라 "사람이 눌러 준다"가 된다** — 게이트
  차단(`publish_blocked`)은 **기계의 거절**이라 사람의 판단을 대신하지 못한다. 두 거절은 뜻이
  다르다: 보류는 고쳐 쓸 게 아니라는 종결, 반려는 고쳐서 다시 올리라는 되돌림이다.
  사유는 고정값(`main.REJECT_REASONS`/`DISCARD_REASONS`) — ack와 같은 이유로 자유 입력이 아니다.
  ⚠️ **되돌리는 전이는 담당자 칸을 덮어쓰지 않는다**(`db.advance_status(record_actor=False)`) —
  반려로 검토중에 돌아왔다고 `reviewer`가 준법 이름이 되면 확인한 PB가 사라진다. 되돌린 사람은
  감사로그가 남긴다. ⚠️ 종결 상태는 `db.NOTE_TERMINAL`에 모아 두고 **큐·대기 집계에서 뺀다**
  (발행분과 같은 취급) — 단 `publish_rate` 분모에서는 안 뺀다(발행까지 못 간 건 통과율이 말할 사실).
- 관리자 역할은 삭제됐다(공유 콘솔 전제에서만 의미가 있었다).
- 토글은 **역할 전환이 아니라 화면 전환**이다(`내 화면` / `준법 화면 (데모)`). 준법은 이 대시보드의
  사용자가 아니라 승인 단계를 맡는 다른 사람이라, 준법 화면엔 **브리핑도 F1도 없다.**
- PB 화면에서 심의중 노트는 버튼이 비활성이고 🔒 사유를 말한다. 배분은 **셀프 클레임**.
- 이름은 사람 이름이 아니라 **역할명**(`PB`/`준법`). 화면 정규화는 `types.ts::actorLabel()` 한 곳,
  복원은 `backend/scripts/restore_audit_actors.sql`.
  ⚠️ 이건 fixture 정리이지 **런타임 동작 변경이 아니다** — 앱은 감사를 소급 수정하지 않는다
  (append-only). W7 Q&A에서 "감사 무결성"을 물으면 이 구분을 말할 것.

### 화면 구조

- **탭 축 = "읽기 / 하기"**(PB `상담 준비`/`작성·검토`, 준법 `심의`/`감시`). 탭 줄은 그 역할의 탭이
  **2개 이상일 때만** 낸다 — 선택지가 하나인 탭은 제목이지 장치가 아니다.
- **F1 입구는 둘**(우하단 고정 버튼=아무 종목, 고객 카드 인라인=보유 종목). FAB은 모달이 열리면
  숨는다 — F1만 상담 **중**에 쓰기 때문이다.
- **F3 생성 뷰는 `hidden`으로만 감춘다 — 언마운트 금지.** 실행이 1~2분인데 언마운트되면 cleanup이
  SSE를 닫아 **크레딧만 쓰고 노트가 안 나온다.** 실행 표시는 `onRunningChange` → 탭 라벨 `●`.
- **`AI가 오늘 한 일`은 접이식 한 줄**, 값은 감사로그 오늘치(`db.today_activity`) — 프론트에서 세면
  최근 N건 제한에 걸려 적게 세인다. **0인 항목은 적지 않는다.** 큐와 같이 **한 번 정의하고 자리만
  옮긴다**(`aiworkCard`): **감시 탭이 있으면 거기, 없으면 첫 탭 맨 위.** 준법에게 이 줄은 "오늘 손댈
  것"이 아니라 지켜본 결과라 심의(처리) 탭이 아니다.
- **전체 고지는 맨 아래 한 줄**(`.disclaimer`, 13px, `--ink-2`). `--muted`(3.4:1)를 안 쓴 이유는
  **읽히지 않는 고지는 고지가 아니어서**다. ⚠️ **프론트 전용** — `compliance.py::NOTICES`(백엔드가
  본문에 강제 삽입)와 별개다. 반대로 `NOTICES`를 고치면 `CLAUDE.md` 표도 같이 고친다.
- 타일 설명줄은 **바로 위 숫자를 쪼갠 것**(처리 대기 10 = 노트 6 + 문의 4). 고객 표의 `#`는 id가
  아니라 **표에 보이는 자리**(`visibleCustomers`).
- **행 hover·포인터 커서는 `.tbl-scroll`(고객 목록)에만.** 전역 `tbody tr`이면 클릭 핸들러가 없는
  보유 종목·재무 수치 표까지 눌리는 것처럼 보인다.

### 출처 표시 = 노트는 번호 각주 · 대화는 인라인 배지 (2026-07-28)

규칙은 `sources.tsx` **한 곳**이다(예전엔 `ReviewModal`·`F1Chat`에 각각 있어 이미 갈라져
있었다 — 모달에만 '접수일 미상' 폴백이 있었다). 공시·뉴스 출처는 **원문 링크**다
(`viewer_url`/`url`, 새 탭 + `rel="noopener"`). 시세·보유는 사람이 볼 페이지가 없어 링크가
아니다 — "눌리게 생겼는데 아무 일도 안 일어나는" 걸 피한다.

- **노트 모달 = 번호 각주 + 하단 출처 목록.** 문장 옆 날짜 배지가 `.stext`를 `55%`로 눌러
  배지 2개짜리 문장이 4줄로 흘렀다(배지 없는 문장은 2줄). 배지 면적 자체는 6.8%뿐이었고
  **진짜로 먹던 건 줄 수**였다. 각주로 바꾼 뒤 본문 988→882px, 기사 제목·원문 링크가
  새로 드러났다(전에는 어디에도 없었다). 목록이 261px 붙어 모달 총 높이는 +16%다.
- ⚠️ **표시는 전부 문장 흐름(`.stext`) 안에 둔다** — 해석·UNSOURCED·확인함. `.sbadges`를
  형제 칸으로 두면 `.stext`가 폭을 다 쓰는 순간 통째로 아랫줄로 밀려 해석 문장마다 빈 줄이
  생긴다(실측 988→1336px). 밖에 남는 건 확인 셀렉트(조작)뿐이다.
- ⚠️ `.fnrefs`에 **`line-height: 0` 금지** — `<sup>` 높이가 0이 돼 안의 버튼을 못 누른다.
- 번호는 **화면에 보이는 순서**로 매긴다(모달이 `reviewTier`로 재정렬하므로 원문 순서로
  매기면 목록이 1,5,2…로 튄다). 같은 출처는 번호를 공유한다 — 중복이 번호 단계에서 사라진다.
- **F1 대화는 인라인 배지**(답변이 2~4문장이라 하단 목록이 답변보다 길어진다). 올해면 연도를
  떼고, **같은 출처의 반복만** `×2`로 묶는다. ⚠️ 날짜가 같아도 **다른 기사끼리는 합치지
  말 것** — 한쪽 링크가 사라져 가드레일 3 위반이다(`sourceKey`가 URL·접수번호까지 본다).
- ⚠️ 각주 목록의 공시는 문서명이 아니라 **접수번호**로 나간다 — 저장된 출처에 `report_nm`이
  없다(`dart_search`는 주는데 `dart_sources`에 안 담는다). 넣으려면 백엔드 + 기존 노트
  재파싱(§1-1)이 같이 필요하다.

### 모달 = 고정 머리말 + 스크롤 본문 (2026-07-28)

`.modal`은 세로 flex 기둥. `.m-head`(제목·상태·×)와 `.m-meta`는 **고정**, `.m-body`가 **스크롤**한다.

- ⚠️ `.m-body`에 **`min-height: 0` 필수**(없으면 상자가 `max-height`를 넘겨 자라고 스크롤이 안 생긴다),
  **`flex-grow`는 금지**(높이가 `auto`라 짧은 노트에서 본문이 0으로 접힌다 — 고객 카드 3열과 반대다).
- sticky로도 해 봤지만 상자 안쪽 `padding` 구간을 문장이 스쳐 지나갔다. **되돌리지 말 것.**
- `.m-body`가 없는 모달(F1 채팅)은 상자째 스크롤하는 폴백으로 돈다.

### 색·서체 체계 (2026-07-28 개편) — 다크 기본 + 강조색 하나

원본은 `docs/design/DESIGN-binance.md`이고 **근거·대비비는 `dashboard.css` 머리주석에 있다 —
여기는 규칙만.** ⚠️ 시안 `docs/design/pb-admin-dashboard.html`은 개편 전에서 멈춰 있다.
값을 고칠 때 시안을 따라가지 말 것.

- **강조색은 옐로 `#fcd535` 하나** — 1차 CTA·선택 상태 전용. ⚠️ **두 번째 브랜드 색 금지**(큐 행
  분류 배지에 옐로를 안 쓰는 이유도 이것 — 모든 행이 선택된 것처럼 읽힌다). 보조 색조는
  `#3b82f6`·`#2dbdb6` 둘뿐이다.
- **다크가 기본**(`:root`가 곧 다크). 깊이는 그림자가 아니라 **면의 단차**(`--page`→`--surface`→
  `--elevated`). 라이트는 `[data-theme='light']` **토글로만** 들어간다 — `prefers-color-scheme`은
  안 쓴다.
- ⚠️ 테마 부트스트랩은 `layout.tsx`의 **body 첫 자식 동기 스크립트**다. React 상태로 옮기면 라이트
  사용자에게 **검은 화면이 번쩍인다.** `<html>`의 `suppressHydrationWarning`도 그 짝이라 **아래로
  내리지 말 것**(자식 트리의 진짜 오류까지 가려진다).
- **등락은 국내 관례**(상승=적 `--mkt-up` / 하락=청 `--mkt-down`)이고 **상태색과 분리한다** —
  적색이 "나쁨"이 아니라 "상승"이라 `--good`/`--critical`을 뒤집으면 게이트 차단과 발행 성공이
  같이 뒤집힌다.
- ⚠️ **`--mkt-up`과 `--critical`은 같은 값**(`#ff5c72`)이고 의도다. 가르는 건 색이 아니라 **형태**
  (상승은 맨 숫자, 경고는 테두리 알약·`⚑`) — 기호를 `▲`로 되돌리지 말 것.
- **서체 = Inter(글) + IBM Plex Sans(숫자)**. ⚠️ 둘 다 **한글 글리프가 없어** 한글은 폴백으로
  떨어진다 — 바뀌는 건 숫자·영문뿐이고 의도한 것이다. `--font-num`은 시세·수익률처럼 **읽고
  비교하는 수**에만.
- **도넛 = 청록 4단계**(`charts.tsx` 단일 출처). ⚠️ 다크·라이트에서 **농도 순서가 뒤집힌다.**
- ⚠️ **새 색을 만들지 말 것** — 무채색이나 형태로 푼다. `.hint`는 기본 규칙이 없어 본문색 그대로다
  (F3 메시지는 `#gen-msg`+kind). `--card`는 정의 없이 불려 투명이었다 → `--elevated` 별칭.

### 고객 포트폴리오 카드 = 찾기 · 읽기 · 묻기 3열

`260px | minmax(0,1fr) | minmax(0,1fr)` — 리스트(찾기) / 상세(읽기) / 채팅(묻기).

- **채팅 = `포트폴리오 질문`**(2026-07-28 확장, 이전 이름 `보유 종목 질문`). 답하는 게 둘이다:
  **종목**(공시·뉴스·시세 — 에이전트 조회) + **구성**(집중도·배분·성향 대비 — **코드가 계산**).
  칩도 두 종류다: 보유 종목 칩 + 분석 칩 3종(집중도·자산배분·성향 대비, `page.PORTFOLIO_CHIPS`).
  - ⚠️ **주어는 종목에서 포트폴리오까지만 넓혔지 사람으로 옮기지 않았다.** "이 고객에게 묻기"로
    읽히는 순간 답할 수 없는 질문(고객 자체)과 해서는 안 되는 질문(회신문 대필, 가드레일 4)을
    부른다. **되돌리지 말 것** — 입력 가드가 한글 이름을 못 잡아 방어선이 아직 UI다(§7).
  - **가드레일 1의 명시적 예외**: 구성 답변의 근거는 공개데이터가 아니라 내부 계좌 보유데이터다.
    조건은 `CLAUDE.md` 가드레일 1의 인용 블록에 있다 — 수치는 `f1.portfolio_facts`(순수 함수)가
    계산하고 LLM은 문장만 쓴다 · 이름·계좌·나이는 프롬프트에도 답변에도 없다 · 위험 판정은
    저장된 `flag_reasons` 인용뿐(새로 안 내린다) · **조정 지시 금지**(쏠림까지만 말한다).
  - 출처는 `[^hold]` → 화면 배지 **`보유(내부)`**(청록 점선 — 공시·뉴스와 형태로 갈린다).
    `as_of`는 **None이다** — `pb_customers`에 스냅샷 시각 컬럼이 없어 날짜를 지어내지 않는다.
  - **포트폴리오 라우트는 에이전트를 안 돌린다**(도구 호출 0 = 크레딧 0). 그래서 진행 타임라인이
    비는데, 라우팅 배지가 `보유·배분(계산)`으로 그 사실을 대신 말한다.
  - 라우팅은 `f1.route(..., has_portfolio=True)`일 때만 켜진다 — 전역 F1(FAB)엔 고객이 없다.
    **분석 칩의 질문문은 `f1._PORTFOLIO_KEYWORDS`를 포함해야 한다**(라벨은 자유, q는 계약).
  - 스코핑은 서버다: `/api/chat/stream?customer_id=`가 담당이 아니면 **404**(라이브 확인).
- **칩은 입력창을 채울 뿐 보내지 않는다**(실행은 크레딧). 프리필은 effect가 아니라 **렌더 중 조정**.
- **고객을 바꾸면 대화를 새로 시작한다**(`key={selected.id}`) — 이어가면 멀티턴 `last_entity`가
  **앞 고객의 종목을 이어받는다.**
- **`PrepMemo`에 다시 넣지 말 것 셋**: "이 종목 묻기" 버튼(칩과 중복) · 시세 줄(브리프에 든 종목만
  붙어 "더 중요하다"로 오독 — 시세는 브리핑 카드 한 곳) · `상담 준비` 라벨(PB 탭 이름과 충돌).
- **`.bcode`는 전역 규칙이다** — `.bcard .bcode`였을 땐 14곳 중 3곳에서만 먹었다.
- ⚠️ **`flex` 축약형 금지** — `flex: 1`은 basis `0%`인데 높이 미정 컨테이너에서 `content`로 해석돼
  표가 스크롤을 잃고 **반대로 상세가 늘어난다**(실측 1788px). `flex: 1 1 0px`도 소용없다 —
  **Next 16 CSS 최적화가 `0px`를 떨어뜨린다.** `flex-grow: 1` + `height: 0`을 쓴다
  (`.tbl-scroll`·`.cust-chat .chat-log`).
- **반응형**: ≤1080px에서 채팅이 아래로 내려가고 로그는 `flex-grow:0`+`max-height:40vh`로 돌아간다
  (제 줄로 내려오면 높이를 정해줄 옆 칸이 없다). ≤720px는 세로로 쌓인다.

### 명칭·표기

- **"팩트 노트" → "종목 노트"**로 통일. ⚠️ **아직 세 번째 이름이 있다** — 백엔드 큐 제목과 노트
  모달이 `실적·공시 노트`라 `[종목 노트] 기아(000270) 실적·공시 노트 초안`으로 나온다.
  `CLAUDE.md` 기능 표는 아직 `F3 종목 팩트 노트`.
- 브리핑 카드 제목은 `브리핑`이지만 **기능 이름은 `F2 상담 전 브리핑`**이다.
- **날짜는 `api.ts::fmtDate` 한 곳으로.** 공시 `20260722`·뉴스 RFC 2822로 형식이 달라 화면마다
  자르다 **`뉴스 Wed, 22 Ju`가 사용자에게 나갔다.** 못 읽는 값은 원문을 돌려준다. **하루 경계는
  KST 고정** — UTC면 00~09시 뉴스가 전날로 밀리는데 브리핑이 내세우는 게 하필 "밤사이 뉴스"다.

### 런타임 프롬프트도 같이 정렬한다

**`CLAUDE.md`는 문서가 아니라 실제 프롬프트다** — 모든 `query()`가 `cwd`=레포 루트로 돌고
`setting_sources`를 안 좁혀 파일이 그대로 실린다. 화면·문서만 PB로 바꿨을 때 에이전트는 여전히
자기를 "금융 리서치 코파일럿"으로 알고 있었다. 코드 쪽도 같이 볼 것:
`f1.py::ANSWER_SYSTEM_PROMPT` · `main.py::BRIEF_SYSTEM_PROMPT` · `.claude/agents/a5.md`.

### 고객 상세의 한 줄 = "구성 요약" (2026-07-28 해결, 옛 "AI 진단")

**`pb_customers.diagnosis`는 더 이상 읽지 않는다**(컬럼은 §2대로 보존). `f1.portfolio_summary`가
계좌 실값에서 계산한다 — 수치는 `portfolio_facts`가 내고, 위험 판정은 저장된 `flag_reasons`를
**그대로 인용**한다(새로 안 내린다). 예: `국내주식 74% · 최대 단일 종목 삼성전자 50.9%(주식 내) ·
위험 플래그 없음`.

- 걷어낸 이유 둘: 고객 50명에 문장이 **6종뿐인 목업**이었고(카드의 나머지는 전부 실집계),
  문구가 `리밸런싱 검토 여지`·`방어적 재배분 논의 필요`처럼 **조정 지시**였다 —
  `CLAUDE.md` 가드레일 1의 F1 예외가 금지한 것이다.
- ⚠️ **서술만 한다.** 조정 지시로 읽히는 말은 `test_portfolio_summary_is_descriptive_only`가 막는다.
- ⚠️ 라벨이 `AI 진단`이 아니라 **`구성 요약`**이다 — AI가 쓴 문장이 아니라 코드가 계산한 값이다.
  같은 이유로 색도 `--chip-note`(AI 산출물)가 아니라 `--s5`(계좌·내부 데이터 계열)를 쓴다.
- 플래그가 없으면 `위험 플래그 없음`을 **적는다** — 줄이 비면 "규칙을 안 돌렸다"와 구분되지 않는다.

---

## 1. a5는 위임이 아니라 backend 2차 `query()` — `Agent` 도구 비동기 함정

*(코드 여러 곳이 "HANDOFF §1"로 이 절을 가리킨다. 상세 배경은 `CLAUDE.md` "검증됨" 절.)*

`Agent` 도구는 **비동기**다 — 결과가 `"Async agent launched successfully"`로 즉시 돌아오고
서브에이전트는 백그라운드에서 돈다. **O가 턴을 끝내면 스트림이 닫히고 도구를 안 부르는 a5가
잘린다.**(a1·a2·a4는 도구 결과가 스트림에 빨리 들어와 살아남는다.)

**채택한 구조**: O의 1차 쿼리가 끝나면 backend가 a5를 **메인 에이전트로 하는 2차 `query()`**로
직접 돌린다(`main.py::event_gen`). a5가 메인이라 `text_delta`가 `parent_tool_use_id=None`으로 흘러
토큰 스트리밍이 산다. 지침은 `a5.md` **본문만** 읽어 `system_prompt`로 쓰고(`_agent_prompt`),
입력은 O의 산문이 아니라 **도구 결과를 그대로 직렬화**(`_a5_input`, LLM 미개입).

- `background: false`도 위임을 동기화하지만 **StreamEvent가 안 나와 토큰 스트리밍이 죽는다** → 안 씀.
- ❌ **반복 금지**: 프롬프트에 "a5 결과를 기다려/인용해"를 넣었더니 O가 **a2 수치로 문장을 지어내
  a5가 쓴 것처럼 인용**했다(가드레일 3 위반). 지금은 O 프롬프트에서 노트 단계를 아예 뺐다.

---

## 1-1. 출처 부착률·게이트 정의 — 지표 함정 (설계 의도)

*(코드가 "HANDOFF §1-1"로 이 절을 가리킨다.)*

**핵심 교훈: 지표를 믿기 전에 지표부터 검증하라.** 낮은 수치를 보면 산출물을 탓하기 전에 측정기를
의심할 것 — 측정 정의 버그 **4건**을 잡았고 전부 a5 오류가 아니었다(파서가 문장 중간 각주를 버림,
eval 하네스 3건: 단위 표기·재무 카드 대조·존댓말 어미 미인식).

**확정된 정의**(`citations.citation_stats()` **단일 출처** — 게이트·대시보드가 같이 쓴다):
- 문장마다 `kind`: `heading`·`boilerplate`·`claim`(사실 주장)·`interpretation`(해석·전망).
- **부착률 분모 = `claim`만.** 해석 문장은 규칙상 각주를 안 붙이므로 뺀다. 안 빼면 a5의 해석
  문장 수 변덕이 품질로 오독된다(실측 10/14↔7/14로 흔들렸다).
- **분류는 보수적으로** — 애매하면 `claim`. 헐거우면 미인용 문장이 분모에서 빠져 부착률이
  후해지는데, **컴플라이언스 지표는 그 방향으로 틀리면 안 된다.**
- **게이트(`unsourced_count`)는 F3에서 해석 문장도 센다** — 발행 전 사람이 판단하도록 올리는 게
  설계다. 지표와 게이트 정의가 다른 건 의도된 것. (F1은 발행 단계가 없어 `claim`만 센다.)

⚠️ **분류기를 바꾸면** 기존 노트의 `sentences_json`은 저장 시점에 굳어 있으므로
`scripts/reparse_notes.py`로 재파싱해 대시보드와 맞춘다.

---

## 1-2. 미인용 문장 확인(ack) — 게이트를 사람이 여는 유일한 경로

*(§1-1을 먼저 읽을 것. 여기는 "그래서 사람이 어떻게 통과시키나"다.)*

**발견된 문제: 발행이 구조적으로 불가능했다.** 게이트는 해석 문장까지 미인용으로 세는데 **올려만
놓고 사람이 풀 수단을 안 만들었다** — 노트 6건이 전부 영구 `draft`였다. "AI가 막힌다"는 보여줄 수
있는데 **"사람이 확인하고 통과시킨다"는 못 보여줬다**(서사의 절반). 막던 문장은 각주를 붙일 수
**없는** 것들이었다(해석·전망 · 고지 · `"위 수치는 2024 연결(CFS) 기준"` 같은 데이터 설명) —
§1-1의 보수적 분류가 **발행을 잠그는 쪽으로 작동**한 것이지 a5 잘못이 아니다.

**채택한 구조**: 준법이 미인용 문장마다 **사유를 골라 확인**하면 그 문장만 집계에서 빠진다.
사유는 고정값(`main.ACK_REASONS`: 해석·전망 / 고지·면책 / 데이터 설명) — 감사 대상이라 자유
입력이 아니다. 확인·해제 전부 감사로그(`ack_added`/`ack_removed`)에 남는다.

- **면제되는 건 미인용 규칙 하나뿐.** 투자권유·광고성 표현, MNPI, 지연시세 고지 누락은 **못 푼다**
  (`test_ack_cannot_waive_forbidden_phrase`로 고정). POSCO 하드 블록 시연은 그대로 산다.
- **심의 단계에서만** 확인할 수 있다 — 초안에서 미리 풀면 검토가 형식이 된다. 해제는 언제나 가능.
- **재파싱하면 확인이 무효가 된다** — 저장 시 원문 앞 60자를 같이 남기고(`notes.acks_json`) 발행 시
  대조한다(`main._valid_acks`). 안 맞으면 **막히는 쪽**으로 판정한다.
- 화면은 문장 목록 **위에** "출처 없는 문장 N개가 발행을 막고 있습니다"를 먼저 말한다.
- ⚠️ 확인 대상은 "해석으로 분류된 문장"이 아니라 **미인용 문장 전부**다.

**라이브 확인(LG화학 #10)**: 초안 → 검토 → 심의 → 발행 시도 **⛔`publish_blocked`** →
`ack_added`×4 → **✅`published`**. 감사로그에 **막힌 사실과 푼 사실이 둘 다** 남는다.

---

## 2. 반드시 알아둘 함정·운영 수칙

- 🚩 **`git commit`·`git push`는 실행 전에 반드시 묻는다 — 로컬 커밋도 예외 없다.** 메시지 초안은
  준비해도 되지만 실행은 "커밋해" 뒤. 메시지는 **메모 말투**(`주제 — 변경 나열`), `~했다` 아님.
- **자주 커밋할 것** — 이전 세션의 대시보드 구현이 **미커밋 상태로 통째 유실**된 적이 있다.
  위 규칙과 충돌이 아니다: **"자주 커밋하자고 자주 제안하라"**는 뜻이다.
- **`pb_customers`·`pb_sessions`는 시드 소스가 유실돼 `.pyc`뿐 — 절대 지우지 말 것.**
  `TRUNCATE notes, audit_log, briefs`는 가능하나 차트·알림이 같이 빈다.
  ⚠️ **UPDATE로 덮어쓸 때도 복원 경로를 먼저 만들어라.**
- **MCP 서버에서 `backend` 패키지를 import하려면 sys.path 부트스트랩이 필요하다.** `.mcp.json`이
  스크립트로 직접 실행해 `sys.path[0]`이 파일 디렉터리다 — cwd가 레포 루트여도 `ModuleNotFoundError`로
  죽고, **서버가 안 뜨면 그 서버의 도구가 통째로 조용히 사라진다**(실제로 krx가 죽어 브리프 3종목
  **시세가 전부 빔**, 게이트는 통과라 티가 안 났다). 손대면
  `backend/.venv/bin/python backend/mcp_servers/<x>_server.py </dev/null`로 기동만이라도 확인.
- **"오늘"의 정본은 `backend/bizdate.py` — KST 고정.** 컨테이너·Postgres가 UTC라 `date.today()`·
  `ts::date`를 그대로 쓰면 하루가 **KST 09:00에 끊긴다**(실제 증상: "오늘 한 일"이 아침에 빔 ·
  `brief_date`가 어제로 · 그날 아침 공시가 빠짐). 새 날짜는 `biz_today()`, SQL은 `biz_date_sql()`.
  ⚠️ `bizdate`는 **stdlib만 쓴다**(krx MCP가 부트스트랩으로 끌어간다 — 무거운 의존성 = 서버 사망).
  ⚠️ 같은 상수가 **세 곳**(`bizdate.py` 정본 · `dart_server.py` 복사 · 프론트 `api.ts`) —
  어긋나면 같은 사건이 서버 집계와 화면 차트에서 다른 날짜 칸에 꽂힌다.
- **"처리할 일의 목록"과 "읽을 것의 목록"은 다르다** — 큐는 발행분을 빼고 `/api/notes`는 포함한다.
  노트를 **읽으려면** 후자를 써라. 전자를 쓰면 **PB가 상담에 실제로 써도 되는 유일한 등급**(발행분)이
  그 순간 사라진다.
- **도구 결과가 토큰 한도를 넘으면 `is_error=False`인 채 에러 문자열이 온다** → `_tool_failed()`로
  걸러 `completed` 오집계를 막는다. 자체 MCP는 반환량 상한을 둘 것(`dart_search` → `limit=30`+최신순).
- **원본과 대조할 때는 파이프라인과 같은 조건으로 조회할 것** — `days=2` 결과를 7일 창과 비교해
  "4건 유실"이라 오진한 적이 있다(실제 유실 없음).
- **SSE 이벤트명을 `error`로 쓰지 말 것**(EventSource 연결 오류 핸들러와 겹친다) → `run_error`.
  예외가 나도 `run_error`+`done`을 반드시 보낸다(화면이 종료 상태에 도달).
- **재생 SSE로 화면을 검증할 땐 코드의 실제 분기를 보고 시나리오를 짤 것** — "파싱 실패=노트 없음"
  으로 짰다가 허구를 검증할 뻔했다(실제론 뉴스만 있어도 a5가 돈다). `scenario_check.mjs` 참고.
- **`scripts/seed_brief.py`는 F2 파이프라인이 아니다**(에이전트를 건너뛰는 화면 검증용 시드) —
  **데모에서 F2 결과로 소개하면 안 된다.**
- **DART 전자공시는 PDF가 아니라 HTML 뷰어** — "페이지 번호"가 없다. `viewer_url`+`rcept_dt`가 최대 해상도.
- **실화면은 목업으로 폴백하지 않는다** — 백엔드가 없으면 연결 실패를 그대로 말한다.
- **레포에 prettier 설정이 없어** 에디터가 파일을 재포맷하고 diff가 부푼다.
- **`docs/ARCHITECTURE.md`가 F1을 "확장 예정"으로 적어둔 채다** — F1은 구현됐다(§4).

---

## 3. 백엔드 라우트 · 목업 데이터

**라우트**: `/api/dashboard/{summary|queue|agents|audit}` · `/api/customers[/{id}]` ·
`/api/sessions` · `/api/sessions/{id}/{approve|reject}` · `GET /api/notes`(발행분 포함 색인) ·
`POST /api/notes/{id}/ack`(미인용 확인, §1-2) · `POST /api/notes/{id}/{review|deliberate|publish}` ·
`POST /api/notes/{id}/{reject|discard}`(거절 2종, §0-1 — 사유 고정값) · SSE `research/stream`·`chat/stream` ·
`POST /api/briefs/run`·`GET /api/briefs/latest`.

- `/api/dashboard/audit`는 `limit`(기본 30, 최대 200) 또는 **`note_id`**를 받는다. 노트를 지정하면
  **limit을 안 쓰고 전건**을 준다 — 감사로그의 87%가 도구 호출(`tool_use_*`)이라 최근 N건 창이
  그것으로 다 차서, 옛 노트의 확인·심의 이력이 창 밖으로 밀린다(노트 13건 중 9건이 그랬다).
  노트별 조회는 도구 호출 행이 안 딸려온다(그 행들은 `note_id`가 NULL).
- ⚠️ **고객·상담 계열은 `main.PB_NAME`으로 서버에서 좁혀진다**(남의 고객 상세는 404).
  **노트 계열은 안 좁힌다**(전부 이 PB의 것).

**목업 고객 50명**: **위험 플래그 규칙 3종**(①안정형인데 주식>40% ②단일종목≥65% ③수익률≤−5%)
+사유 저장. 수치는 전부 실집계, 비교 데이터가 없는 델타는 감춘다.
**AI 신뢰도 카드**: 출처 부착률(목표 90% 미터)·발행 통과율·게이트 차단(7일).

---

## 4. F1 대화형 종목 Q&A (규칙 라우팅 + Redis 멀티턴)

입구 둘(우하단 고정 버튼 / 고객 카드 인라인 채팅) 다 같은 `/api/chat/stream`·`F1Chat`
(인라인은 `compact`+`prefill`).

파이프라인: 입력 가드 → 규칙 라우팅 → 라우팅된 에이전트 조회 → 2차 query(구조화 데이터만)
스트리밍 → citations(시세 `[^krx]`) → F1 고지 강제 → 게이트 → 세션 갱신.
**라우팅·데이터취합·게이트는 코드가, LLM은 답변 문장만**(F3와 같은 각주 무결성).

- **입력 가드가 F1의 핵심**(`compliance.input_guard`) — 자유 텍스트라 F3·F2에 없던 공격면.
  MNPI·인젝션·PII를 **에이전트 前에** 잡으면 도구가 아예 안 돈다(크레딧 0).
- **규칙 라우팅**(`f1.route`, 순수) — 6자리 코드/별칭 + 의도 키워드 → 에이전트(quote→krx·
  financials→a2·news→a4·disclosure→a1). 엔티티 없으면 되묻기. LLM에 안 맡긴 이유: **데이터 소스
  선택은 컴플라이언스 경계라 감사 가능한 규칙이어야 하고**, "왜 이 에이전트로 갔나"를 배지로 보여준다.
- **포트폴리오 라우트**(2026-07-28, `has_portfolio=True`일 때만) — 집중·배분·성향 키워드가 있으면
  **종목 추출보다 먼저** 판정한다. 종목이 없어도 되묻지 않고(대상이 고객 계좌라 이미 정해져 있다),
  종목이 같이 오면 들고 가 그 종목 비중을 콕 집어 답한다("035720 비중"). 근거는 코드가 계산한
  내부 보유데이터라 **에이전트를 안 돌린다**(크레딧 0). 조건·한계는 §0-1 고객 포트폴리오 카드.
- **게이트 F1 분기**: `CHAT_NOTICE`에 "지연시세"를 넣어 QUOTE 게이트를 자기충족. 발행 단계가
  없어 해석 문장은 위반으로 안 세고 **근거 없는 사실 주장만** 잡는다.
- **멀티턴(Redis)** — 현재 질문에 종목이 없으면 세션의 **직전 종목을 이어받는다**(의도는 매 턴
  새로 판단). `session_store.py`는 **라우팅 맥락만** 저장(턴 상한 8·`last_entity`·TTL 1h) —
  답변 본문·도구 결과는 안 담는다(턴 넘나들며 날조 방지). UI는 **↩ 이어받음** 배지.
- ⚠️ **추측은 하나까지다**(2026-07-28). 종목을 이어받았는데 의도 키워드도 없으면 **되묻는다** —
  예전엔 기본값(재무)까지 태워서 `"오늘 점심 뭐 먹지?"`가 직전 종목의 a2 조회를 돌렸다
  (실측: 에이전트가 실제로 돌아 크레딧을 썼다). 종목을 **질문에 직접 적었으면** 기본값을
  그대로 쓴다 — 종목을 적었다는 것 자체가 의도 신호다. `"관련 뉴스는?"`류 정상 이어받기는 산다.
- **되묻기는 두 종류이고 문구가 다르다**(`f1.clarify_text`, 백엔드가 만든다):
  `entity`=종목을 모른다(이 패널이 답할 수 있는 것을 나열) / `intent`=종목은 아는데 무엇을
  물었는지 모른다(`"카카오에 대해 무엇을 확인할까요?"`). ⚠️ **마크다운 금지** — 화면
  (`.chat-clarify`)이 문자열을 그대로 렌더해 별표가 글자로 보인다.
- ⚠️ **`_PORTFOLIO_KEYWORDS`에 없는 말은 답할 데이터가 있어도 못 묻는다.** `수익률`·`잔고`가
  그래서 빠져 있었다(portfolio_facts는 담고 있는데 되묻기로 떨어졌다). 반대로 넓히면 종목
  질문을 뺏는다 — 맨 `수익`은 넣지 않았다(`"카카오 수익성"`은 a2다). **종목별 수익률은
  데이터에 없다** — 계좌 전체 값을 종목에 옮겨 적지 말라고 프롬프트가 막는다.
- 파일: `backend/f1.py`·`session_store.py`·`test_f1.py`·프론트 `F1Chat.tsx`.

---

## 5. 아키텍처 한눈에

```
[F3] 종목코드 → O(Opus) → a1 법인확인 → a2 재무 ‖ a4 뉴스 팬아웃          ← 위임은 여기까지
  → 도구결과 직렬화(_a5_input) → 2차 query()로 a5 직접 실행(§1) → citations(kind 부여)
  → 워터마크 강제 → 게이트 → notes 저장 → SSE(진행·토큰·노트·done{outcome})
     재무·뉴스 둘 다 없으면 a5 안 돌리고 사유만 보낸다
[F2] POST /api/briefs/run → pb_watchlist(담당 고객 보유 상위 N) → O → a1 공시 ‖ a4 뉴스 + krx
  → 지수는 market.py가 직접(판단이 없어 위임 불필요) → brief.assemble(LLM 미개입)
  → "내부 참고용" 강제 → 게이트 → 저장.  새 에이전트 0개.
     공시 중요도(0주요 1정기 2기타 3지분): 지분공시는 빼지 않고 뒤로만 민다
[F1] GET /api/chat/stream[&customer_id] → input_guard(에이전트 前) → f1.route(세션 이어받기)
  → ①종목 라우트: 에이전트 조회  ②포트폴리오 라우트: f1.portfolio_facts(순수·에이전트 0·크레딧 0)
  → 2차 query 스트리밍 → citations([^hold] 포함) → F1 고지 → 게이트 → session_store
     customer_id는 서버가 PB_NAME으로 검사한다 — 담당 아니면 404
[대시보드] Next.js `/dashboard` (PB 1인용, 화면 토글 2종)
  PB·상담 준비: 오늘 한 일 → 타일 → 브리핑(F2) → 고객 포트폴리오(3열) → 전체 고지
  PB·작성·검토: 종목 노트 생성(F3 SSE) → 처리 대기 큐
  준법·심의: 처리 대기 큐(심의 건만) — 오늘 한 일은 여기가 아니라 감시 탭에
  준법·감시: 오늘 한 일 · AI 신뢰도 · 컴플라이언스 알림 · 감사로그(노트별 필터·페이징)
     — 큐는 역할에 따라 자리가 다르다(`queueCard`를 한 번 정의하고 옮긴다): 준법에겐 노트 탭이
       없어서 큐를 옮기면 심의할 노트를 찾을 방법이 없어진다
     — 알림은 감사로그의 부분집합(publish_blocked + 거부된 permission_check)이지만 목적이 다르다:
       알림=골라낸 신호(triage), 감사로그=완전한 원장(evidence). 둘 다 유지
[사람] 노트: 사실 확인(PB)→심의 요청(PB)→[미인용 확인·사유(준법)]→발행(준법, 게이트 재검사)
       거절 2종: 검토중→보류됨(PB, 종결) · 심의중→검토중(준법 반려, 되돌림) — 사유 고정값
       고객 문의: 처리 완료(담당 PB, 회신은 사람이 직접) — AI 개입 없음, 전 과정 audit_log
         조작은 이것 하나다. 모달을 닫으면 `확인 대기`로 남으므로 "보류"는 기본 상태다
         (`/api/sessions/{id}/reject`는 남아 있으나 화면이 부르지 않는다)
```

**파일 대응**: a5 작문 지침=`a5.md`(backend가 system_prompt로 읽음) / 출처매칭·문장범주·부착률
정의=`citations.py`(단일 출처) / F1 라우팅·답변조립=`f1.py`·대화상태=`session_store.py` /
브리프 조립=`brief.py` / 지수=`market.py` / 고지·게이트·입력가드=`compliance.py` / 상태·로그=`db.py` /
**날짜 경계(KST "오늘")=`bizdate.py`** / 배선·API=`main.py` / MCP=`mcp_servers/{dart,news,krx}_server.py` /
화면=`frontend/src/app/dashboard/{page,ResearchCard,ReviewModal,F1Chat,charts,api,types}.tsx` /
**출처 표시(각주·배지) 단일 출처=`sources.tsx`** / 색·서체 토큰=`dashboard.css` 머리주석 +
`docs/design/DESIGN-binance.md` / 테마 부트스트랩·서체 로딩=`app/layout.tsx`.

---

## 6. 환경/실행 메모

- 앱: `docker compose up`(backend 8000 / frontend 3000 / postgres 5432 / redis 6379).
  **requirements 변경 시 `docker compose build backend && up -d --force-recreate backend`**.
- 대시보드: http://localhost:3000/dashboard
- **자체 점검(크레딧·네트워크 불필요, 10종)**:
  `for t in compliance brief krx streaming tool_result a5_input brief_attribution citations run_outcome f1; do backend/.venv/bin/python -m backend.test_$t; done`
- **프론트 검증**: `cd frontend && npx tsc --noEmit && npx eslint src --max-warnings=0`.
  ⚠️ Next.js 16: 코드 전 `frontend/node_modules/next/dist/docs/` 확인(`frontend/AGENTS.md`).
- **화면 재생 검증**(크레딧 0, `frontend/`에서): `node scenario_check.mjs`(W6 빈/에러 5종).
- **Redis 세션 점검**: `REDIS_URL=redis://localhost:6379/0 backend/.venv/bin/python -m backend.scripts.session_store_check`
- **라이브 실행(비용)**: F3 `curl -sN "http://localhost:8000/api/research/stream?stock_code=005930" > /tmp/f3.sse`(~100초) /
  F2 `curl -s -X POST .../api/briefs/run -d '{}'`(~40초) / F1 `curl -sN ".../api/chat/stream?q=삼성전자 최근 실적"`.
  → `grep "^event:" *.sse | sort | uniq -c`로 이벤트별 건수부터 본다.
- 위임 동기/비동기 판정(저비용): `docker compose exec -T backend python -m backend.scripts.background_flag_check`
- 테스트 데이터 정리: `docker exec ai-pb-agent-postgres-1 psql -U app -d app -c "TRUNCATE notes, audit_log, briefs RESTART IDENTITY;"`
  (`pb_*`는 지우지 말 것 — §2)
- 고객 PB 배정 되돌리기: `docker exec -i ai-pb-agent-postgres-1 psql -U app -d app < backend/scripts/restore_pb_assignment.sql`
- API 키 4개(`.env`): `ANTHROPIC_API_KEY`·`DART_API_KEY`·`NAVER_CLIENT_ID/SECRET`·`KRX_API_KEY`(data.go.kr, 무료).
  ⚠️ KRX는 **서비스별로 활용신청이 따로**다 — 종목시세와 지수시세가 다른 신청이다(둘 다 승인됨).
- git: main → **personal**(hmgthb/ai-pb-agent) 업스트림. origin(kwangtekNa/intern)에는 푸시 안 함.
  **커밋·푸시 둘 다 사용자 확인 후에만(§2 첫 항목).**
- SDK API명은 `claude-agent-sdk==0.2.110` 실물 확인 — `CLAUDE.md` "검증됨" 절.

---

## 7. 열린 항목 · 리스크

**제품 가치 순**
- ❗ **고객 문의 모달이 에이전트와 안 이어져 있다.** `prepFacts()`는 계좌 시드값 + 고정 고지 3줄이
  전부다. 질문이 이미 특정된 **유일한** 화면인데 그 고객 보유 종목의 공시·뉴스가 한 줄도 없고 F1로
  가는 길도 없다. **남은 것 중 가치가 가장 크다** — `PrepMemo`의 줄과 보유 종목 칩+인라인 채팅을
  그대로 재사용하면 된다.
- ❗ **브리핑 카드에 자체 고지가 없다.** 노트는 워터마크, F1은 답변마다 고지를 다는데 브리핑만
  없다. 백엔드가 만든 고지가 `briefs.content_md`에 있는데 **프론트 `Brief` 타입에 필드가 없어 안
  그린다.** 전체 고지를 맨 아래로 내린 뒤로 **가장 큰 카드가 첫 화면에서 고지 없이 보인다.**
  타입 필드 하나 + 헤더 한 줄이면 끝난다.
- **월요일 공시 공백**(§0). ("AI 진단" 시드 문구는 2026-07-28 해결 — §0-1 구성 요약)
- **진입 단위가 종목인데 PB의 단위는 "오늘 만날 고객"이다** — 가장 좋은 화면(고객 포트폴리오)에
  가려면 페이지 최하단 표에서 고객을 직접 찾아야 한다.

**규제·경계에 대해 정직하게 말해야 할 것 (W7 Q&A)**
- ❗ **"고객에게 말 걸지 않기"는 프롬프트 지시일 뿐 게이트 규칙이 아니다.** `compliance.py`는
  호칭·회신문 형식을 위반으로 세지 않는다. 라이브 4종 검사에서 발생은 0건이었지만, 보장이
  게이트가 아니라 프롬프트에 걸려 있다는 뜻이다.
- ❗ **입력 가드가 고객 이름을 못 잡는다.** `PII_PATTERNS`는 주민·계좌번호의 **숫자 형식**만 본다 —
  "임민지 고객이…"는 그대로 LLM에 가고 감사로그에 남는다(가드레일 1 위반). 지금 막는 건 **인라인
  채팅의 UI 프레이밍뿐.** 규칙화 경로는 있다(담당 고객명이 서버에 있으니 `input_guard`에 사전 대조).
- ❗ **역할 강제는 여전히 화면에만 있다.** 백엔드는 `actor`를 감사로그용 문자열로 받을 뿐 "발행은
  준법만"을 강제하지 않는다. **데이터 스코핑은 서버로 내려갔지만 권한은 아직 아니다.**
- **감사로그 200건 캡은 노트에 한해 풀렸다**(§3 노트별 조회). `note_id`가 없는 사건(브리프·F1·도구
  호출)은 여전히 최근 200건까지만 보인다. **원본 DB는 append-only 전건 보존.** ⚠️ 감사로그 카드는
  **준법·감시 탭 전용**이라 PB 화면엔 없다(모달에서도 뺐다 — 노트 모달에 남은 이력은 머리말 한 줄).
- ❓ **정량 가치 밴드는 추정 모델**(X=60분·물량 가정 의존) — `docs/VALUE.md` 민감도표로 답하고
  **단일 숫자로 단정하지 말 것.**

**남은 검증**
- ❓ 모달의 **'발행' 버튼 클릭 자체**는 아직 안 눌러 봤다(발행은 curl로). W7 리허설에서 한 번은
  눌러 볼 것 — #7·#13이 심의중이라 그대로 쓸 수 있다.
- ❓ 파서 수정 후 **a5 각주가 맞는 출처를 가리키는지** 정성 관찰(부착률로는 확인됨).
- ❓ **3시나리오 예외 상태는 재생 SSE 기준** — 라이브로 돌려본 건 아니다.
- ❓ **2026-07-28 색·레이아웃은 육안 확인이 사용자 화면 기준**(대비는 전 토큰 계산값, 타입·린트 통과).
  기본이 다크가 됐으니 확인 기준도 다크다 — **라이트는 토글로만 들어가는 경로라 회귀 확인이 남았다.**
  라이트에서만 갈라지는 토큰이 있다(`--accent-text`·`--on-critical`·`--alloc-*` 순서).
- 🔸 FAB이 인라인 채팅 `보내기`의 오른쪽 끝을 덮는다(FAB 본래 동작이라 둠) · `/api/notes` 전건 상세
  조회(노트 수만큼 요청) · 모달이 Esc로 안 닫힘 · F1 채팅 모달에 닫기 버튼 없음 ·
  **FAB에 접근성 이름이 없다**(라벨이 제거돼 `aria-hidden` 이모지만 남았다 → `aria-label` 필요).
