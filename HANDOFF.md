# 인수인계 — AI PB 어시스턴트 (`ai-pb-agent`)

> **결정과 함정만 담는다.** "무엇을 언제 했다"는 커밋 로그에, 세부 근거는 **코드 주석**에 있다
> (이 저장소는 주석이 두껍다 — 여기서 그걸 되풀이하지 않는다).
> 읽는 순서: §0(상태) → §0-1(되돌리면 안 되는 결정) → §2(함정) → 나머지는 필요할 때.
> ⚠️ **절 번호를 바꾸지 말 것** — 코드가 `§0-1`·`§1`·`§1-1`·`§1-2`·`§1-3`·`§2`·`§7`로 가리킨다.
> 관련: 런타임 프롬프트 `CLAUDE.md` · 측정 `docs/EVAL.md` · 가치 `docs/VALUE.md` ·
> 배포 `docs/DEMO-DEPLOY.md`.

---

## 0. 현재 상태

계획서 필수(W1~W6) + 선택(F1)이 라이브 검증으로 닫혔다. **다음 할 일은 §7에서 고른다.**

데이터 현황은 문서에 적지 않는다(금방 낡는다) — 한 줄로 센다:

```
docker compose exec -T postgres psql -U app -d app -c "
select (select count(*) from notes) notes, (select count(*) from briefs) briefs,
       (select count(*) from prep_notes) preps, (select count(*) from pb_sessions) sessions,
       (select count(*) from pb_customers) customers, (select count(*) from audit_log) audit"
```

| 비면 | 다시 채우는 법 | 비용 |
|---|---|---|
| 고객 문의(큐) | `scripts/seed_pb_sessions.py`(§6) | 0 |
| 노트 | F3 라이브 실행(§6) | ~100초·크레딧 |
| 브리프 | 브리핑 카드 `생성` 또는 `POST /api/briefs/run` | ~11초·크레딧 소액(LLM 1회) |

- **상태가 한 곳에 몰리면 화면이 절반만 돈다** — 검토·심의·발행이 각각 1건은 있어야 큐와 통과율이
  뜻을 갖는다. 하드 블록을 보려면 목표주가가 실린 노트가 하나 필요하다.
- ⚠️ ~~노트와 브리프의 종목이 겹쳐야~~ — **더는 해당 없다**(2026-08-07). 브리핑이 거시 전용이
  되면서 브리프에 종목이 없다. 상담 준비 줄(`PrepMemo`)에 남는 건 **발행분 노트 하나**다.
- 빈 상태도 정상이다: `citation_rate`·`publish_rate`는 0이 아니라 **`null`**(분모 0을 0%로 쓰면
  거짓말이다), `/api/briefs/latest` 404는 "아직 없다"는 상태다.
- **착수 전**: `git pull` 후 `docker compose build backend && up -d --force-recreate backend`.

---

## 0-1. 되돌리지 말 것

### 사용자·경계

- 사용자는 **상담 전 사실을 확인하는 PB 한 명**이고 화면은 개인용이다(감독 콘솔이 아니다).
  **AI는 PB를 대신하지 않는다** — 고객에게 나가는 말은 PB가 쓴다. 문의 모달은 "답변 승인·전송"이
  아니라 **대응 준비**다. ⚠️ **회신 대필 화면으로 되돌리지 말 것**(가드레일 4).
- **스코핑은 화면이 아니라 서버**(`main.PB_NAME`, 프론트 `MY_PB`와 같은 값). 남의 고객·상담은
  **404**(403이 아니다 — 존재를 알리면 거른 의미가 없다). 읽기만 좁히면 id를 아는 것으로 남의 건을
  처리할 수 있어 승인·반려도 막았다. 값이 하나뿐이어도 **서버 스코핑은 유지한다.**
- **CLAUDE.md는 문서가 아니라 실제 프롬프트다**(모든 `query()`가 레포 루트에서 돈다). 화면 문구를
  바꿀 때 같이 볼 곳: `f1.ANSWER_SYSTEM_PROMPT` · `main.BRIEF_SYSTEM_PROMPT` · `.claude/agents/a5.md`.

### 사람이 판단하는 자리

- 흐름은 `검토 —[사실 확인]PB→ 심의 —[발행]준법`. **거절 2종**: `보류`(PB·종결) · `반려`(준법 →
  검토로 되돌림). 사유는 고정값(`main.REJECT_REASONS`/`DISCARD_REASONS`).
  - **거절이 없으면 "사람이 확인한다"가 아니라 "사람이 눌러 준다"가 된다** — 게이트 차단은 기계의
    거절이라 사람의 판단을 대신하지 못한다.
  - ⚠️ 되돌리는 전이는 **담당자 칸을 덮지 않는다**(`advance_status(record_actor=False)`).
  - ⚠️ 종결(`db.NOTE_TERMINAL`)은 큐·대기 집계에서 빼되 **`publish_rate` 분모에서는 안 뺀다.**
- **못 하는 조작은 버튼을 아예 안 그린다**(회색 버튼 금지 — 남는 건 사유 한 줄 `.m-deny`).
  배분은 셀프 클레임, 이름은 사람이 아니라 **역할명**(`PB`/`준법`, `types.actorLabel`).
- **되돌릴 수 없는 조작은 두 번 누른다**(무장 → 실행): 노트 보류·반려 · 브리핑 삭제 · 메모 삭제.
  ⚠️ 되돌릴 수 있는 것은 즉시 실행이 맞다(문장 확인 · 브리핑 재생성).
- ⚠️ **앱은 감사를 소급 수정하지 않는다**(append-only). 지운 사실도 **이벤트를 더해서** 남긴다.

### 화면 구조

- 탭 축은 **"읽기 / 하기"**(PB `상담 준비`/`작성·검토`, 준법 `심의`/`감시`). 탭이 2개 이상일 때만
  탭 줄을 낸다. 토글은 역할 전환이 아니라 **화면 전환**이고 준법 화면엔 브리핑도 F1도 없다.
- 🚩 **긴 실행이 도는 뷰·모달은 `hidden`으로만 감춘다 — 언마운트 금지.** 언마운트되면 cleanup이
  SSE를 닫아 **크레딧만 쓰고 결과가 사라진다**(F3 생성 뷰 · 전역 F1 모달). 실행 표시는
  `onRunningChange` → 탭 라벨 `●` / FAB `●`. 회귀는 `f1_modal_check.mjs`가 잡는다.
  - 감출 수 없는 자리는 **부모가 대화를 보관한다**(`ChatKeep`): 문의 모달 `session-{문의id}` ·
    고객 카드 `customer-{고객id}`. ⚠️ **키를 뭉뚱그리지 말 것** — 세션 id가 같이 보관되므로
    키가 겹치는 순간 앞 대화의 종목이 다음 답에 섞인다.
  - ⚠️ **마운트 시점에만 읽는 prop 금지** — 두 번째 열기부터 조용히 무시된다(입력창을 채우는 길은
    `prefill`의 n 카운터 하나).
  - 오버레이가 동시에 둘일 수 있어 `#overlay`가 아니라 **`.overlay` 클래스**다.
- **모달 = 고정 머리말 + 스크롤 본문.** ⚠️ `.m-body`에 `min-height: 0` 필수 · `flex-grow` 금지 ·
  sticky로 되돌리지 말 것. 머리말 조작이 둘 이상이면 `.m-acts`로 묶는다(`margin-left: auto`가
  둘로 갈려 벌어진다). Esc·배경 클릭·`×`는 모두 `setModal(null)` 한 곳.
- **강조색은 옐로 하나**(1차 CTA·선택 상태). ⚠️ **새 색 금지** — 무채색이나 형태로 푼다.
  등락은 국내 관례(상승 적/하락 청)이고 상태색과 분리한다(`--mkt-up`과 `--critical`이 같은 값인
  건 의도 — 가르는 건 형태다). **`⚑`는 어디서나 맨 글리프**이고 설명은 `aria-label`·`title`뿐.
- **서체 = Inter(글) + IBM Plex Sans(숫자)**. ⚠️ 둘 다 한글 글리프가 없어 한글은 폴백으로
  떨어진다(의도). `--font-num`은 **읽고 비교하는 수**에만 쓴다.
- **처음 열면 다크**(`layout.tsx`가 `<html data-theme="dark">`를 서버에서 박는다). ⚠️ 기본값이
  **세 곳**이다(그 속성 · `page.tsx::serverTheme` · `readTheme`) — 같이 고친다. 부트스트랩은
  body 첫 자식 동기 스크립트여야 한다(React 상태로 옮기면 흰 화면이 번쩍인다).
- ⚠️ **`flex` 축약형 금지**(Next 16 CSS 최적화가 `0px`를 떨어뜨린다) — `flex-grow: 1` + `height: 0`.
- 근거·대비비는 `dashboard.css` 머리주석이 정본이다. ⚠️ 시안 `docs/design/*.html`은 개편 전에서
  멈춰 있다 — 값을 고칠 때 따라가지 말 것.

### 브리핑(F2)

- **띠에 서는 다섯 지표**(2026-08-09 교체): 나스닥 · S&P500 · 원/달러 · 국고채10년 ·
  미국채30년. 예전에는 코스피 · 코스닥 · 원/달러 · 국고채3년이었다. 국내 금리도 3년 → 10년인데,
  미국채30년과 같은 축에서 읽으려면 장기물이어야 하기 때문이다.
  ⚠️ **KRX 지수 조회(`market.fetch_index`)는 지우지 않았다** — 띠에서만 빠졌고 에이전트 도구
  `krx_index`가 그대로 쓴다. 옛 브리프에는 코스피·코스닥이 저장된 채로 남아 있다.
- **거시 띠의 공급자가 둘이다**(2026-08-09 교체): 해외 지수·미국채 = FRED(`fred.py`, **키 불필요**) ·
  환율·국내 금리 = 한국은행 ECOS(`ecos.py`). 두 모듈은 서로를 모르고 반환 규약
  `(지표 목록, 미연결 사유)`만 같다 — **합치는 곳은 `main.build_brief` 하나**이고 거기
  `band_order`가 곧 화면 순서다(공급자별로 이어 붙이지 않는다).
  ⚠️ ECOS에는 해외 지수·미국채가 **없다**(2026-08-09 통계표 전수 확인 — 국제 통계는 전부 월/분기).
  ⚠️ FRED는 시리즈를 여러 개 한 요청에 담으면 CSV가 아니라 **ZIP이 200으로 온다** — 하나씩 부른다.
- **지표는 `move` + `move_unit`으로 읽는다.** 주가지수·환율은 `%`, 금리는 `bp`다 — 금리를 %로
  적으면 3.742→3.669가 `-1.95%`가 되는데 채권에서 그렇게 말하지 않는다(`-7.3bp`다).
  ⚠️ 옛 브리프에는 `move`가 없다(`change_pct`뿐) → `brief.move_of`가 폴백을 한 곳에서 맡는다.
- **`지연시세`(주가지수 종가)와 `공표치`(한국은행 · 미 재무부)를 가려 적는다**(`basis`). 환율에
  `지연시세`라고 쓰면 틀린 말이고, 출처 `type`도 `krx`가 아니라 `ecos`다 — 게이트가 `krx`를
  **수치 표기와 무관하게** 시세 인용으로 보기 때문에(`compliance.quotes_market_data`) 아무 데나
  달면 안 된다. 반대로 나스닥·S&P500에서 `지연시세`를 떼면 그 문장의 지연시세 고지가 빠진다.
- **수준(`close`)은 출처가 준 문자열 그대로 싣는다.** `f"{x:g}"`로 찍었더니 나스닥 `26690.620`이
  유효숫자 6자리로 깎여 **`26690.6`이 화면에 나갔다**(2026-08-09 실측). 숫자로 만들었다 되돌리지
  말 것 — 계산이 필요한 쪽만 float으로 바꾼다(`fred._observations` ↔ `fred._values`).
- **띠의 날짜는 `대표 기준일 + 벗어난 것만 제 날짜`다.** 환율은 매매기준율이라 당일 아침에
  나오고 지수·국고채는 전 거래일 종가라, `전부 같을 때만 한 번 적기` 규칙이면 다섯 지표가
  전부 제 날짜를 달고 선다(실측 2026-08-07: 원/달러만 08-07).

- **문장을 전부 코드가 만든다**(`brief.assemble`, LLM 미개입). 없는 사실이 안 섞이는 대신 말할 수
  있는 것도 규칙에 적힌 것뿐이다 — 그게 계약이다. ⚠️ **화면에서 문장을 조립하지 말 것**(저장된
  브리프와 화면이 다른 말을 하게 된다).
- **거시 전용이다**(2026-08-07). 담는 것은 오늘 지수와 **어제 대비 달라진 것** 둘이다:
  ② 평소 대비(`market.rank_recent_move` — 지수에도 태웠다) · ③ 방향 전환(`brief.compare_macro`).
  ⚠️ **기준일(`as_of`)이 같으면 견주지 않는다** — 주말·휴장에는 어제 브리프도 오늘 브리프도 같은
  종가를 싣는다. 그때 "안 바뀌었다"고 적으면 견주지 않은 것을 견줬다고 말하는 셈이라, 유의사항이
  **왜 비교하지 않았는지**를 대신 말한다.
  ⚠️ 종목·고객 경로(a1‖a4 · `pb_watchlist` · 공시/뉴스 고르기)는 **지우지 않고 떼어만 뒀다** —
  `brief.py`·`main.py`의 「배선 해제」 주석이 무엇이 죽었는지 이름으로 적어 두었다.
- **예외는 밤사이 헤드라인 한 줄뿐**(`brief.HEADLINE_SYSTEM_PROMPT` · 2026-08-07). 규칙은 지표가
  **얼마나** 움직였는지까지만 말할 수 있고, **왜**는 제목에만 있다. 감싸는 게 다섯이다: 입력은
  **제목뿐**(기사 본문 아님) · 전언 형식 강제 · 순수 함수 재검사(`brief.parse_headline` —
  길이·문장 수·금지 표현·**입력에 없는 숫자**) · 화면 `AI 요약` 배지 + **근거 각주 링크** ·
  통과 못 하면 **불릿을 안 낸다**(대신할 규칙 문장이 없다 — 지어내느니 비운다).
  ⚠️ **근거 링크를 빼지 말 것** — 종목 카드가 없어져서 이 문장의 출처를 확인할 자리가 화면에서
  각주뿐이다(가드레일 3). ⚠️ 신선도 창 24시간을 넓히지 말 것: `국제금융시장` 검색은 실제로
  일주일 전 기사를 상위에 올렸고, 이 자리가 답하는 건 "밤사이"다.
  ⚠️ 검색어를 **지표 이름으로 늘리지 말 것** — `국고채 금리`를 넣었더니 밤사이 시장이 아니라
  **입찰 담합 과징금** 기사가 상위를 채웠다(실측).
- ~~예외는 종목 한 줄뿐~~ — 배선 해제됐다. 아래는 그 시절 기록이다: 입력은 공시명·뉴스
  제목뿐 · 전언 형식 강제 · 순수 함수 재검사(`parse_summaries` — 형식·길이·금지 표현·**입력에 없는
  숫자**) · 화면 `AI 요약` 배지 · 실패하면 **규칙 문장으로 떨어진다**(조용히 비지 않는다).
  ⚠️ 이 불릿은 본문이 아니라 `lead_json`이라 **게이트를 안 탄다** — 검증이 `parse_summaries`에만
  있으므로 그 함수를 느슨하게 만들면 검사하는 곳이 사라진다.
- **없으면 그 불릿을 안 낸다**(0건 나열은 아무 일 없는 날도 일한 것처럼 보인다). 예외는 `delta`
  하나. **불릿당 한 문장**(테스트로 고정). 종목 줄만은 실린 종목 **전부** 낸다.
- **어제 대비 새것**은 `db.brief_before(오늘)` 기준이다 — ⚠️ `latest_brief()`로 잡으면 같은 날 두
  번째 실행부터 전부 "어제도 있던 것"이 되어 강조가 꺼진다. 어제가 없으면 **필드 자체를 안 붙인다**
  ("전부 새것"과 "견줄 게 없었다"는 다르다). 뉴스는 URL이 아니라 **제목 키**로 맞춘다.
- **평소 대비 꼬리표**는 이미 받아 온 수십 일치로 만든다(**추가 조회 0회** — `krx_server`의
  `LOOKBACK_DAYS`가 28인 이유는 직전 영업일이 아니라 **비교 창**이다). 같은 방향끼리만 견주고,
  창이 짧거나 상위 3위 밖이면 **아무 말도 안 한다.**
- **임원·주요주주 보고는 카드 안에서 접힌 한 줄**(대형주는 매일 수십 건이다). ⚠️ 접히는 건 임원
  개인의 소액 매매뿐 — 5%룰과 `풍문또는보도에대한해명`은 `_MATERIAL`이다. ⚠️ 등급을 배지로 내지
  말 것(줄이려고 접은 글자가 도로 는다), 프론트에서 보고서명으로 다시 판정하지 말 것
  (`brief.IMPORTANCE`가 정본), 게이트가 보는 기록(`content_md`·`sentences`)에서는 **빼지 않는다.**

### 고객 포트폴리오 카드 · F1

3열 `260px | 1fr | 1fr`(찾기 · 읽기 · 묻기). 채팅 제목은 **`포트폴리오 질문`**.

- ⚠️ **주어는 포트폴리오까지만 넓혔지 사람으로 옮기지 않았다.** "이 고객에게 묻기"로 읽히면 답할
  수 없는 질문과 해서는 안 되는 질문(회신 대필)을 부른다 — 입력 가드가 한글 이름을 못 잡아
  방어선이 아직 UI다(§7). 문의 모달의 인라인 F1도 **글자까지 같게** 둔다.
- **가드레일 1의 명시적 예외**: 수치는 `f1.portfolio_facts`(순수)가 계산하고 **LLM은 문장만** ·
  이름·계좌·나이는 프롬프트에도 답변에도 없다 · 위험 판정은 저장된 `flag_reasons` 인용뿐 ·
  출처 배지는 `보유`(`as_of`는 **None** — 스냅샷 시각 컬럼이 없어 날짜를 안 지어낸다).
- **조정 선택지까지 쓴다**(관찰에서 멈추면 도구를 덜 쓰는 것이다). ⚠️ **셋은 금지**: 특정 안 추천·
  순위 · 목표 비중/조정 금액 같은 **새 수치** · 미보유 종목 매수 권유(언급하면 그 문장에서 미보유임을
  밝힌다). 규칙 전문은 `f1.ANSWER_SYSTEM_PROMPT`.
  - **후보도 코드가 뽑는다**(`f1.rebalance_options`, 순수) — LLM에게 생각하게 하면 데이터에 없는
    근거가 섞이고 거기에도 각주를 붙이려 든다. ⚠️ 후보 문턱값을 **"위험 플래그"라 부르지 말 것**
    (플래그는 저장된 판정, 문턱은 무엇을 이야깃거리로 올릴지 고르는 훨씬 낮은 별개 기준이다).
  - ⚠️ 업종은 **`f1.SECTORS`(이 저장소가 정한 표)**라 각주를 안 붙인다(화면에서 `해석`).
  - ⚠️ 후보·등락은 **비식별화된 payload만** 보고 계산한다 — `egress_guard`는 후보 블록을 안 본다.
- **라우트 둘, 가르는 건 비용**: 조회형 `portfolio`는 계산된 내부 데이터만 써서 **에이전트를 안
  돌린다**(크레딧 0), 제안형 `portfolio_advice`는 시세 배치 + 뉴스 위임이라 수십 초다.
  ⚠️ **제안형을 먼저 검사한다**(조회형 키워드가 같이 들어 있다). 칩의 질문문은 계약이고
  (`_PORTFOLIO_KEYWORDS`/`_ADVICE_KEYWORDS` 포함), 뉴스는 3종목까지(자른 사실은 SSE에 남긴다).
  ⚠️ 시세·뉴스가 실패해도 후보는 낸다("제안할 게 없다"와 "조회가 실패했다"는 다르다).
- **대화는 고객별로 남는다**(`chatKeep`, `customer-{id}`). 고객을 바꾸면 컴포넌트는 갈리지만 대화는
  보관되고, 세션 id도 고객별이라 앞 고객 종목을 이어받지 않는다. 비우는 길은 제목 줄 `↻ 새 대화`
  (말풍선·세션은 버리고 **쓰던 질문은 남긴다**). 새로고침하면 사라진다.
- **보유 표의 종목 이름 = 그 종목의 최종본 PDF 버튼.** 발행분이 없으면 **버튼을 안 그린다**
  (점선 밑줄이 누를 수 있다는 표시 — 색으로 가르지 말 것).
- ⚠️ **화면에서 비중을 다시 나누지 말 것**(분모·반올림은 `portfolio_facts`가 단일 출처).

### 비식별화 경계

현실 배치의 **망분리 GPU** 자리를 이 레포에서는 규칙(순수 코드)이 맡는다.

- ⚠️ 정직하게: **LLM에게 비식별화를 시키는 것 자체는 보안이 아니다**(그 모델이 원본을 이미 봤다).
  얻는 건 기밀성이 아니라 **구조** — 경계가 코드에 있고 지나는 것이 검사받는다.
- 경계는 `main.chat_stream` 한 곳: `portfolio_facts`(원본) → `redact_portfolio` → `egress_guard` →
  프롬프트. ⚠️ **`portfolio_facts`를 여기 맞춰 고치지 말 것**(화면 보유 표가 쓰는 단일 출처다).
- **삭제가 아니라 일반화**: 이름→가명 · 나이→나이대 · 잔고→구간 · 종목 금액→비중 · 계좌번호는 제외.
  ⚠️ **구간 폭을 좁히지 말 것** — 비중이 같이 나가 `구간 × 비중`으로 금액이 역산된다(폭이 방어선).
  ⚠️ **가명은 익명이 아니다**(원본 DB와 대조하면 특정된다) — 그래도 두는 건 대화에 **주어**가
  필요해서다.
- **`egress_guard`가 실제 방어선**(허용 키 화이트리스트 · 고객명 대조 · 계좌 형식 · 큰 정수).
  ⚠️ 자리가 둘인 건 의도다: ① 라우팅 **전** 질문 문장만(없으면 이름 하나에 크레딧을 쓰고 막힌다)
  ② 프롬프트 완성 후 전체. ⚠️ 큰 정수 규칙을 프롬프트 전체에 걸지 말 것(KRX·DART 공개 수치가 막힌다).
- 화면은 경계 **안**이라 실금액을 그대로 본다. ⚠️ **프론트에서 변환하지 말 것**(보장이 안 생기고
  규칙이 두 곳으로 갈린다). 라벨은 **`AI가 보는 정보`** 하나 — `비식별화`로 되돌리지 말 것
  (법 용어는 가명처리이고, 읽는 사람은 PB다). 코드 어휘는 `redact` 그대로 둔다.

### 상담 준비 메모

**AI가 낸 것 중 PB가 담은 것** → 카드 안 메모 → PDF. AI는 재료를 내고 **고르는 노동이 PB의 것**이다.

- 항목 셋: `sentence`(AI 문장·출처째) · `option`(코드가 뽑은 선택지) · `memo`(PB가 쓴 줄).
  **고친 줄은 `memo`가 된다** — AI 문장을 고쳐 놓고 `sentence`로 남기면 각주가 AI가 쓰지 않은
  문장을 뒷받침하는 것처럼 인쇄된다. 문서는 `AI 분석`·`PB 메모`로 갈리고 제목이 서는 근거는
  위치가 아니라 **저자가 바뀌었다는 사실**이다(가드레일 4).
- **담는 동안은 저장하지 않는다**(`sessionStorage`도 안 쓴다). **만든 것은 남는다**(`prep_notes`) —
  PDF 바이트가 아니라 **재료 + 그때의 고객 사실**을 담아, 다시 열 때 같은 함수로 그리고 **게이트를
  다시 받는다**(오늘 막혀야 할 것이 오늘 열려선 안 된다). 시각도 `created_at`을 되먹인다.
  ⚠️ 계좌번호는 담지 않는다. ⚠️ 저장 실패가 인쇄를 막지 않는다(`prep_id: null`).
- 목록 카드는 고객 카드 **바로 아래**, `발행된 노트`와 같은 형식이되 **고객으로 접는다**(기본 접힘 —
  새 메모가 펼쳐진 채 끼어들면 카드가 뛴다). ⚠️ 노트와 달리 **상태가 없다**(승인 흐름 밖의 PB
  본인용 문서다) — 상태 배지를 달지 말 것. 준법 화면에는 안 낸다.
- 삭제는 줄마다, **두 번 누른다**, 지우는 범위는 **그 한 줄뿐**이다. ⚠️ 감사로그는 남는다.
- ⚠️ **PB가 쓴 줄도 게이트를 받는다**(사람이 썼다고 규정을 비켜 가지 않는다) — 다만
  `interpretation`이라 미인용 집계에는 안 들어간다.
- ⚠️ **고객 정보는 클라이언트가 보내지 않는다** — 서버가 DB에서 읽는다(담당 아니면 404).
- 문서 머리에 **위험 플래그**(`⚑` + 저장된 사유 인용, 없으면 줄 자체 없음)와 **미처리 문의**
  (원문 + 오른쪽 끝 접수시각)가 실린다. ⚠️ 문의 원문은 신뢰하지 않는 데이터라
  `compliance.input_guard`를 거치고, 걸린 건만 `[확인 필요]`로 실린다(**메모 전체를 막지 않는다** —
  PB가 손쓸 수 없는 사유로 상담 자료가 사라지면 안 된다).
- ⚠️ **파일명·문서에 이름 규칙**: 본문엔 이름을 싣고 **파일명에는 안 쓴다**(`상담메모_고객19_0806.pdf`).
- 검사: `frontend/prep_list_check.mjs`(실백엔드 · 자기가 만든 것만 치운다).

### 문서(PDF) 공통

- **조립은 코드가 한다(LLM 미개입)** — 다시 쓰게 하면 각주가 문장과 어긋나고 통과한 게이트를 다시
  받아야 한다. 발행분 노트는 `GET /api/notes/{id}/pdf`이고 **발행분만** 열린다(다른 상태는 409).
- ⚠️ **`제거` 판정이 산출물에서 실제로 빠지는 자리가 PDF다.** 저장(`content_md`)은 AI 원문 그대로
  두고, 뺀 문장은 2면 이력표에 판정·담당과 함께 남는다.
- ⚠️ **한글 폰트가 없으면 통째로 빈 칸이 되고 예외는 안 난다** → 라우트가 **503으로 거부**한다.
  폰트에 없는 글자는 `_renderable`이 **지운다**(`⚠`·`⚑`가 그랬다). 기호가 꼭 필요하면 **그린다**
  (`notepdf._flag_mark`) — 기호 하나 때문에 폰트를 더 얹지 않는다.
- ⚠️ **fetch가 아니라 링크로 연다**(파일명 헤더를 CORS로 다시 열 필요가 없고 예비요청을 안 탄다).
  `inline=1`이면 뜨고 없으면 내려받는다. 뷰어 모달은 **백엔드 PDF를 그대로 싣는다**(화면용 HTML을
  따로 만들면 각주 번호부터 갈린다).
- ⚠️ **모달은 종목이 아니라 노트 id로 연다**(종목으로 열면 큐에서 고른 건과 열리는 건이 갈린다).

### 명칭·표기

- 화면에 **`a1`·`a2`·`a4`·`a5`를 적지 않는다**(읽는 사람은 PB다). 단계는 `법인 확인`·`재무 핵심수치`·
  `뉴스`·`노트 초안 작성`, F1 배지는 `공시`·`재무`·`뉴스`·`보유·배분`. **코드 키는 그대로.**
  ⚠️ **라우트를 늘리면 배지 표(`AGENT_LABEL`)도 같이 늘릴 것** — 없으면 빈 상자로 뜬다.
- 날짜 함수는 `api.ts` 두 개이고 **섞으면 안 된다**: `fmtDate`(시각 없는 날짜 — 형식이 달라 화면마다
  자르다 `뉴스 Wed, 22 Ju`가 나갔다) · `fmtDateTime`(시각 있는 사건 — 여기에 `fmtDate`를 쓰면 KST
  00~09시 사건이 하루 전으로 찍힌다). 하루 경계는 **KST 고정**(`bizDay`).
- **화면 문구에 `—`를 접속사로 쓰지 않는다**(사람이 안 쓰는 말투라 "AI가 썼다"가 먼저 읽힌다).
  예외 둘: 값 없음 표시 `'—'` · 규제 문구(`types.WATERMARK`는 `compliance.py`와 1:1).
- 이름이 아직 셋이다: 화면 `종목 노트` / 백엔드 큐·모달 `실적·공시 노트` / `CLAUDE.md` 표
  `F3 종목 팩트 노트`.

---

## 1. a5는 위임이 아니라 backend 2차 `query()`

`Agent` 도구는 **비동기**다 — 결과가 `"Async agent launched successfully"`로 즉시 돌아오고,
**O가 턴을 끝내면 스트림이 닫혀 도구를 안 부르는 a5가 잘린다**(a1·a2·a4는 도구 결과가 스트림에
빨리 들어와 살아남는다).

**채택**: O의 1차 쿼리가 끝나면 backend가 a5를 **메인 에이전트로 하는 2차 `query()`**로 돌린다
(`main.event_gen`). a5가 메인이라 토큰 스트리밍이 산다. 지침은 `a5.md` **본문만** 읽어
`system_prompt`로 쓰고, 입력은 **도구 결과를 그대로 직렬화**(`_a5_input`, LLM 미개입).

- `background: false`도 동기화는 되지만 **StreamEvent가 안 나와 토큰 스트리밍이 죽는다** → 안 씀.
- ❌ **반복 금지**: 프롬프트에 "a5 결과를 기다려/인용해"를 넣었더니 O가 **a2 수치로 문장을 지어내
  a5가 쓴 것처럼 인용**했다. 지금은 O 프롬프트에서 노트 단계를 아예 뺐다.

---

## 1-1. 출처 부착률·게이트 정의

**지표를 믿기 전에 지표부터 검증하라** — 낮은 수치의 원인 4건이 전부 측정 정의 버그였다.

정의는 `citations.citation_stats()` **단일 출처**(게이트·대시보드가 같이 쓴다):

- 문장마다 `kind`: `heading`·`boilerplate`·`claim`·`interpretation`.
- **부착률 분모 = `claim`만**(해석 문장은 규칙상 각주를 안 붙인다).
- **분류는 보수적으로 — 애매하면 `claim`.** 헐거우면 부착률이 후해지는데 **컴플라이언스 지표는
  그 방향으로 틀리면 안 된다.**
- **게이트(`unsourced_count`)는 F3에서 해석 문장도 센다**(발행 전 사람이 판단하도록). 지표와
  게이트 정의가 다른 건 의도다. F1은 발행 단계가 없어 `claim`만 센다.
- ⚠️ 분류기를 바꾸면 기존 노트는 저장 시점에 굳어 있다 → `scripts/reparse_notes.py`(멱등·크레딧 0).
- ⚠️ **`_BOILERPLATE_RE`는 어형을 좇지 말 것**(두 번 빗나갔다) — **뜻의 서명** 세 갈래로 잡는다.
  근본 해법은 a5가 자기 고지를 붙이지 않게 하는 것인데(`WATERMARK`는 백엔드가 이미 강제 삽입한다),
  `a5.md`에 그걸 **금지**로 적어야 한다(미착수).

**지연시세 규칙은 문장 단위다.** 문서 단위 공존으로 판정하던 때는 시세를 한 줄도 인용하지 않은
노트가 **자기 실적 수치 때문에** 막혔다. 지금은 `compliance.quotes_market_data(sentence)` 하나가
판정한다(출처가 `[^krx]`이거나, 시세 낱말과 수치가 같은 문장에 있을 때).

- ⚠️ **뺀 문장은 인덱스로 받는다**(본문 잔존으로 추론하면 규칙이 조용히 꺼진다).
- ⚠️ **문장 목록이 비면 문서 단위로 되돌아간다**(규칙이 사라지느니 과검출한다).

---

## 1-2. 미인용 문장 확인(ack)

**해결한 문제: 발행이 구조적으로 불가능했다** — 게이트는 해석 문장까지 미인용으로 세는데 막던 문장은
각주를 붙일 수 **없는** 것들이었다(§1-1의 보수적 분류가 발행을 잠근 것이지 a5 잘못이 아니다).

**채택**: 미인용 문장마다 **사유를 골라 확인**하면 그 문장만 집계에서 빠진다. 사유는 고정값
(`main.ACK_REASONS` — 감사 대상이라 자유 입력이 아니다). 확인·해제 모두 감사로그에 남는다.

- **면제되는 건 미인용 규칙 하나뿐.** 투자권유·광고성 표현, MNPI, 지연시세 누락은 **못 푼다.**
- **심의 단계에서만** 확인할 수 있고(초안에서 미리 풀면 검토가 형식이 된다) 해제는 언제나 가능.
- ⚠️ 대상은 "해석으로 분류된 문장"이 아니라 **미인용 문장 전부**다.
- **재파싱하면 확인이 무효가 된다**(인덱스로 저장한다) → 원문 앞 60자를 같이 남겨 대조하고,
  어긋나면 **막히는 쪽**으로 판정한다. 판정은 `compliance.live_acks` 한 곳이다 — 한때 대조가 발행
  경로에만 있어 **"이제 발행할 수 있습니다"라고 말한 뒤 409로 막혔다.**
  ⚠️ **프론트에서 60자 대조를 다시 구현하지 말 것**(판정이 두 곳으로 갈린다).

---

## 1-3. 금지 표현 예외(waiver)

**해결한 문제: 규칙이 제시와 인용을 구분하지 못한다** — 부분문자열 검사라 제3자 목표주가를 **각주와
함께 인용한** 문장도 막혔고, 준법에게 남는 수단이 문장 삭제뿐이었다.

**채택**: 준법이 **사유를 직접 적어** 그 문장의 금지 표현 위반만 통과시킨다
(`POST /api/notes/{id}/waive` · `notes.waivers_json` · 감사 `waiver_added`/`removed`).

- ⚠️ 여는 건 **금지 표현 하나뿐**이고 **문장 단위**다(본문 전체로 번지면 규칙 해제가 된다).
  **MNPI는 어떤 예외로도 안 열린다**(판단이 아니라 정보장벽이다).
- ⚠️ **사유가 자유 입력인 유일한 곳**이다(통과 근거가 건마다 달라서) — 대신 **사유별 집계를
  포기했다.** 빈 사유는 400, 상한 `WAIVER_MAX_LEN`.
- 막는 문장을 찾아 주는 것도 백엔드다(`compliance.forbidden_carriers`).
  ⚠️ **금지 표현 목록을 프론트로 복사하지 말 것.**
- 게이트를 부르는 자리는 `main._live_violations` 하나로 모았다(발행·PDF·확인 응답이 각자 조립하면
  화면과 발행이 갈린다).

---

## 2. 반드시 알아둘 함정·운영 수칙

- 🚩 **`git commit`·`git push`는 실행 전에 반드시 묻는다 — 로컬 커밋도 예외 없다.** 메시지는
  **메모 말투**(`주제 — 변경 나열`), `~했다` 아님. 그럼에도 **자주 커밋할 것**(미커밋 상태로 통째
  유실된 적이 있다) — 충돌이 아니라 **"자주 제안하라"**는 뜻이다.
- 🚩 **크레딧이 없으면 코드 버그처럼 보인다** — `AssistantMessage(error='billing_error')`를 SDK가
  `Exception: Claude Code returned an error result: success`로 올린다. 진단 한 줄:
  ```
  docker compose exec -T backend python -c "
  import anyio; from claude_agent_sdk import query, ClaudeAgentOptions
  async def m():
      async for msg in query(prompt='1+1은?', options=ClaudeAgentOptions(max_turns=1)): print(msg)
  anyio.run(m)"
  ```
  크레딧 없이 되는 것: 자체 점검 전부 · 프론트 타입/린트 · **재생 SSE 화면 검증**(§6).
- 🚩 **새 HTTP 메서드로 라우트를 추가하면 `CORSMiddleware allow_methods`도 같이 늘려라.** 안 늘리면
  예비요청이 400으로 끊기고 화면에는 **`Load failed`**만 뜬다(서버 로그에 라우트가 아예 안 찍혀
  라우트 버그처럼 보이지도 않는다). 진단: `curl -i -X OPTIONS <url> -H "Origin: …" -H
  "Access-Control-Request-Method: DELETE"`.
- 🚩 **데모 배포에서 긴 요청은 30초에 끊긴다** — Next rewrites 프록시 기본값이다. **백엔드는 200으로
  완주해 저장했는데** 화면은 실패를 띄운다(크레딧이 나간 뒤라 더 나쁘다). 해결은
  `next.config.ts`의 `experimental.proxyTimeout`(현재 300초) — ⚠️ **공식 문서에 없는 키**라 Next를
  올릴 때 살아 있는지 확인할 것. 터널(Cloudflare)은 최소 120초까지 통과하므로 범인이 아니다.
  **로컬 개발은 rewrites를 안 타서 이 함정이 안 보인다.**
- ⚠️ **playwright `route.fulfill()`로 가로채면 CORS·프리플라이트를 못 본다**(위 버그를 화면 검사
  8종이 전부 통과한 채로 놓쳤다) — **새 메서드·새 헤더를 쓰는 라우트는 한 번은 진짜로 보내라.**
- **도구 결과가 토큰 한도를 넘으면 `is_error=False`인 채 에러 문자열이 온다** → `_tool_failed()`로
  걸러 `completed` 오집계를 막는다. 자체 MCP는 반환량 상한을 둘 것.
- **외부 API가 실패를 HTTP 200으로 준다** — 한국은행 ECOS는 오류도 200이고 본문이
  `{"RESULT": {"CODE": "ERROR-…"}}`다(`ecos.py`가 걸러낸다). 상태코드만 보면 실패가 성공처럼
  보이는 건 도구 결과 한도 초과(§1)와 같은 모양이다.
- ⚠️ **건수 제한에 걸려 잘리면 창의 「앞부분」이 온다** — ECOS는 오래된 쪽부터 준다. 그대로
  쓰면 몇 주 전 값이 오늘 값이 되고 **오류는 안 난다.** `list_total_count`와 대조해 꼬리를
  다시 받는다(`ecos.fetch_series`). 공개 `sample` 키(10건)로 이 분기를 실제로 밟아 봤다.
- **MCP 서버가 안 뜨면 그 도구가 통째로 조용히 사라진다** — 실제로 krx가 죽어 브리프 3종목 **시세가
  전부 빈** 채 게이트를 통과했다. 손대면
  `backend/.venv/bin/python backend/mcp_servers/<x>_server.py </dev/null`로 확인.
- **"오늘"의 정본은 `bizdate.py`(KST 고정)** — 컨테이너·Postgres가 UTC라 `date.today()`·`ts::date`를
  그대로 쓰면 하루가 **KST 09:00에 끊긴다**. ⚠️ `bizdate`는 **stdlib만** 쓴다(krx MCP가 부트스트랩으로
  끌어간다). ⚠️ 같은 상수가 세 곳이다(`bizdate.py` 정본 · `dart_server.py` · 프론트 `api.ts`).
- **"처리할 일의 목록"과 "읽을 것의 목록"은 다르다** — 큐는 발행분을 빼고 `/api/notes`는 포함한다.
  읽으려면 후자를 써라(전자를 쓰면 **상담에 써도 되는 유일한 등급**이 사라진다).
- **`pb_customers`·`pb_sessions`는 앱에 생성 경로가 없다**(문의는 외부에서 유입되는 것이라 설계상
  맞다). ⚠️ **시드 소스가 유실돼 `scripts/restore_pb_customers.sql`이 유일한 원본이다**(3인 배정은
  `restore_pb_assignment.sql`). ⚠️ **고객을 지우고 새로 만들지 말 것**(id가 바뀌면 감사로그 참조와
  옛 덤프가 어긋난다) — **UPDATE로 열만 바꾸고**, 덮어쓰기 전에 복원 경로부터 만들어라.
- ⚠️ **되돌릴 수 없는 조작을 검증할 때는 버릴 데이터를 만들어 그것에 대고 눌러라**(브리프는 가짜
  날짜의 더미 행으로 확인했다). 손대기 전 `pg_dump --table=… --data-only`면 되돌릴 수 있다.
- **SSE 이벤트명을 `error`로 쓰지 말 것**(EventSource 오류 핸들러와 겹친다) → `run_error`.
  예외가 나도 `run_error` + `done`을 반드시 보낸다.
- **원본과 대조할 때는 파이프라인과 같은 조건으로 조회할 것**(`days=2` 결과를 7일 창과 비교해
  "4건 유실"로 오진한 적이 있다).
- ⚠️ **`map` 콜백의 반환식 앞에 `{/* */}`를 넣지 말 것** — `=> ( … )`는 표현식 하나만 담아 JSX가
  통째로 깨진다.
- ⚠️ **React 이벤트 객체를 setState 갱신 함수 안에서 읽지 말 것**(`e.currentTarget`은 그때 비어 있다)
  — 값을 먼저 꺼내 둔다. `<details onToggle>`에서 화면이 통째로 죽었다.
- ~~`scripts/seed_brief.py`는 F2 파이프라인이 아니다~~ — **이제 같은 함수를 부른다**
  (`main.build_brief` · 2026-08-07). 브리핑에서 LLM이 빠지면서 베낄 이유가 없어졌다.
  남은 차이는 웹 서버를 안 거친다는 것뿐이다.
- **DART 전자공시는 PDF가 아니라 HTML 뷰어** — "페이지 번호"가 없다(`viewer_url`+`rcept_dt`가 최대
  해상도). **실화면은 목업으로 폴백하지 않는다**(백엔드가 없으면 연결 실패를 그대로 말한다).
- 레포에 prettier 설정이 없어 에디터가 재포맷하면 diff가 부푼다. `docs/ARCHITECTURE.md`는 F1을
  "확장 예정"으로 적어둔 채다(구현됐다 — §4).

---

## 3. 백엔드 라우트 · 목업 데이터

`/api/dashboard/{summary|queue|agents|audit}` · `/api/customers[/{id}]` ·
`GET /api/customers/{id}/egress-preview`(LLM 미개입) ·
`POST /api/customers/{id}/prep-note/pdf`(만든 것을 `prep_notes`에 남긴다) ·
`GET /api/prep-notes`(목록·본문 없음) · `GET /api/prep-notes/{id}/pdf`(재발행·게이트 재통과) ·
`DELETE /api/prep-notes/{id}`(한 건) ·
`/api/sessions[/{id}/{approve|reject}]` · `GET /api/notes`(발행분 포함 색인) ·
`POST /api/notes/{id}/{ack|waive|review|deliberate|publish|reject|discard}` ·
`GET /api/notes/{id}/pdf`(발행분 최종본) · SSE `research/stream`·`chat/stream` ·
`POST /api/briefs/run` · `GET /api/briefs/latest` · `DELETE /api/briefs/{id}`(그 **날짜 전부**).

- 모든 쓰기는 감사로그를 남긴다(`prep_note_exported`·`prep_note_deleted`·`brief_deleted` …).
- `/api/dashboard/audit`는 `limit`(기본 30·최대 200) 또는 **`note_id`**(전건)를 받는다 — 감사로그의
  87%가 도구 호출이라 최근 N건 창이 그것으로 다 찬다.
- ⚠️ **고객·상담 계열만 `PB_NAME`으로 좁힌다. 노트 계열은 안 좁힌다.**
- 🔸 `/api/sessions`는 프론트가 부르지 않는다(쓰는 자리와 같이 들여올 것).

**목업 고객 50명** — 위험 플래그 3종(안정형인데 주식>40% · 단일종목≥65% · 수익률≤−5%)과 사유를
저장한다. 보유 유니버스 12종목(삼성전자 005930 · SK하이닉스 000660 · 한화에어로스페이스 012450 ·
삼성바이오로직스 207940 · LG에너지솔루션 373220 · KB금융 105560 · 두산에너빌리티 034020 ·
HD현대중공업 329180 · 현대차 005380 · 기아 000270 · NAVER 035420 · HD현대일렉트릭 267260)은
전부 **DART 법인 + KRX 시세**를 확인했다.

- ⚠️ **브리핑은 더는 이 유니버스를 쓰지 않는다**(2026-08-07 · 거시 전용). 아래는 `pb_watchlist`가
  살아 있을 때의 규칙이고, 종목 수집이 다른 카드로 살아나면 그대로 쓴다.
- **보유 고객이 가장 많은 3종목이 곧 브리핑 유니버스**(`pb_watchlist`, `BRIEF_MAX_STOCKS=3` — 지금은
  앞의 셋이고 4위와 벌어져 동수 tie-break에 안 걸린다). ⚠️ `main.FALLBACK_WATCHLIST`도 같은
  3종목으로 맞춰 뒀다(다르면 "빈 DB"인지 "다른 고객군"인지 구분이 안 된다).
- 재배정은 `scripts/reseed_holdings.py`(난수 없음 · **UPDATE SQL만 출력** · `holdings`·`flag_reasons`만).
- **고객마다 `scenario`(상황)가 붙어 있다**(2026-08-07 · `scripts/seed_scenarios.py`). 계좌 숫자가
  못 보여 주는 것 — 계좌 밖 자산·제약·정리 순서·목표, 그리고 **등록 성향과 다를 수 있는 실질
  성향**(`effective_risk`)이다. 등록이 공격투자형이어도 반년 뒤 보증금을 치러야 하면 지금 실질은
  안정형이고, 그 간극이 상담에서 먼저 확인할 것이다.
  - **이 칸만은 시드가 살아 있다**(위 ⚠️의 예외). 난수가 없어 다시 돌리면 같은 결과가 나오므로
    덤프가 아니라 **스크립트가 원본**이다. 되돌리기: `UPDATE pb_customers SET scenario = '{}';`
  - ⚠️ **금액은 구간뿐이다**(`redact.BALANCE_BANDS`와 같은 어휘). 산문이 아니라 **구조**인 것도
    같은 이유다 — 자유 텍스트는 규칙이 비식별화할 수 없다(§7).
  - ⚠️ **F1 프롬프트로 자동으로 안 나간다.** 경계가 화이트리스트라(`redact.SANITIZED_KEYS`)
    `egress_guard`가 막는다(실측 확인). 답변 근거로 쓰려면 그 목록에 넣는 것이 **명시적 결정**
    이어야 하고, 그때 `constraints`·`plan` 문장이 큰 정수·이름을 안 물고 있는지 같이 봐야 한다.
  - ⚠️ **AI가 판단한 것이 아니다** — 실제 배치에서는 PB가 상담 기록에서 적는 칸이다.
    화면에 `AI 요약` 배지를 붙이지 말 것(출처를 거짓으로 말하게 된다).
- ⚠️ **유니버스를 바꾸면 `f1.ALIASES`도 같이 넓힌다** — 없으면 칩은 멀쩡한데 **손으로 친 종목명만**
  되묻기로 빠진다. 짧은 별칭은 넣지 않는다(부분매칭 사고).
- ⚠️ `079550`의 DART 법인명은 **LIG디펜스앤에어로스페이스**(옛 LIG넥스원).

---

## 4. F1 대화형 Q&A (규칙 라우팅 + Redis 멀티턴)

입구 둘 다 같은 `/api/chat/stream`·`F1Chat`. **라우팅·데이터취합·게이트는 코드가, LLM은 문장만.**

- **`Next Best Action`(옛 `포트폴리오 질문`)이 답하는 것은 둘이다**(2026-08-07): ① 고객 상황
  요약(`scenario`) ② 상담 이력 기반 성향 점검(`history`). 이유는 PB가 담당 고객이 많아 **각각의
  경위를 기억할 수 없다**는 것이다.
  - **자산배분·조정 선택지 라우트는 지우지 않았다** — 손으로 치면 답한다. 없앤 것은 칩과 안내
    문구다. ⚠️ 이게 깨지면 "유도하지 않는다"가 "못 쓴다"로 바뀐 것이다(다른 결정 · `test_f1`).
  - ⚠️ **새 키워드는 가장 먼저 검사된다** — 넓은 말을 넣으면 옛 라우트를 조용히 뺏는다. 실제로
    `정리해줘`(→리밸런싱)·`목표`(→목표주가)·`이 고객`(→수익률)을 넣었다가 테스트가 잡아 뺐다.
  - ⚠️ **`Next Best Action`이라는 이름이 곧 권한이 아니다.** 다음 행동을 정해 주지 않는다 —
    답변 규칙이 막고 있고, 풀면 회신 대필로 미끄러진다(가드레일 4).
  - ⚠️ **`AI가 보는 정보` 패널이 상황·이력도 그려야 한다** — 실제로 나가는데 화면이 안 보여
    주면 그 패널이 나가는 양을 축소해 말하는 셈이다.
  - ⚠️ 비식별화 호출부는 **`main._redacted_portfolio` 하나**다. 예전엔 `/chat/stream`과
    `/egress-preview`가 각자 인자를 넘겼고, 상황·이력이 붙던 날 **미리보기에만 빠져** 실제로
    갈라졌다(2026-08-07 실측). 인자를 늘릴 일이 생기면 거기 한 곳만 고친다.
- **입력 가드가 F1의 핵심**(`compliance.input_guard`) — 자유 텍스트라 F3·F2에 없던 공격면이다.
  MNPI·인젝션·PII를 **에이전트 전에** 잡으면 도구가 아예 안 돈다(크레딧 0).
- **규칙 라우팅**(`f1.route`, 순수) — LLM에 안 맡긴 이유: **데이터 소스 선택은 컴플라이언스 경계라
  감사 가능한 규칙이어야 한다.** 포트폴리오 라우트 2종은 §0-1 참조.
- **멀티턴(Redis)** — 종목이 없으면 직전 종목을 이어받는다(의도는 매 턴 새로 판단).
  `session_store`는 **라우팅 맥락만** 저장(턴 8·TTL 1h) — 답변 본문·도구 결과는 안 담는다.
  만료·미지의 세션은 **빈 맥락**으로 떨어진다(에러가 아니다).
- ⚠️ **추측은 하나까지다.** 종목을 이어받았는데 의도 키워드도 없으면 **되묻는다** — 예전엔 기본값까지
  태워서 `"오늘 점심 뭐 먹지?"`가 직전 종목의 a2 조회를 실제로 돌렸다.
  ⚠️ 되묻기 문구에 **마크다운 금지**(그대로 렌더된다).
- ⚠️ **답변을 `sessionStorage`에 담지 말 것**(고객 이야기가 브라우저 저장소에 쌓인다).

---

## 5. 아키텍처 한눈에

```
[F3] 종목코드 → O → a1 ‖ (a2 재무 · a4 뉴스) → 도구결과 직렬화 → 2차 query로 a5(§1)
     → citations → 워터마크 강제 → 게이트 → notes 저장 → SSE
     재무·뉴스 둘 다 없으면 a5 안 돌리고 사유만 보낸다
[F2] briefs/run → market.py(지수 직접 조회) → compare_macro(직전 브리프의 market_json)
     → macro_digest(불릿) ‖ assemble(본문) → 고지 강제 → 게이트 → 저장
     ‖ news_search(직접 호출) → pick_headlines → LLM 1회 → parse_headline
     **에이전트는 안 탄다**(거시 전환). LLM은 헤드라인 한 줄뿐. a1‖a4 경로는 떼어만 뒀다
[F1] chat/stream → portfolio_facts(순수) → redact(경계) → input_guard·egress_guard(질문문)
     → f1.route → ①종목: 에이전트 ②조회형: 계산된 값(크레딧 0) ③제안형: 후보+시세+뉴스(≤3)
     → egress_guard(프롬프트 전체) → 2차 query → citations → 고지 → 게이트 → session_store
[사람] 사실 확인(PB) → 심의 요청(PB) → [미인용 확인·예외(준법)] → 발행(준법, 게이트 재검사)
```

**파일 대응**: 출처매칭·문장범주·부착률 `citations.py` / 고지·게이트·입력가드·반출가드·확인 유효성
`compliance.py` / 비식별화 경계 `redact.py` / F1 라우팅·포트폴리오 사실·선택지 후보 `f1.py` ·
`session_store.py` / 브리프 `brief.py` / 지수·등락 배치 `market.py` / PDF `notepdf.py` /
상태·로그 `db.py` / 날짜 경계 `bizdate.py` / 배선·API `main.py` /
MCP `mcp_servers/{dart,news,krx}_server.py` / a5 작문 지침 `.claude/agents/a5.md` /
화면 `dashboard/{page,ResearchCard,ReviewModal,F1Chat,PrepMemo,charts,api,types}.tsx` ·
출처 표시 `sources.tsx` · 비식별화 표시 `redaction.tsx` · 색 `dashboard.css` 머리주석 ·
테마 `app/layout.tsx` / 시드 `scripts/{reseed_holdings,seed_pb_sessions}.py`(SQL만 출력한다 —
되돌릴 수 없는 조작은 사람이 실행한다).

---

## 6. 환경/실행

- `docker compose up`(backend 8000 / frontend 3000 / postgres 5432 / redis 6379).
  **requirements를 바꾸면** `docker compose build backend && up -d --force-recreate backend`.
  대시보드는 http://localhost:3000/dashboard.
- **자체 점검(크레딧·네트워크 불필요)**: `docker compose exec -T backend python -m pytest backend -q`
- **프론트**: `cd frontend && npx tsc --noEmit && npx eslint src --max-warnings=0`
  ⚠️ **CSS는 이 둘이 검사하지 않는다.** Next 16: 코드 전 `frontend/node_modules/next/dist/docs/` 확인.
- **화면 검증**(크레딧 0, `frontend/`에서): `scenario_check.mjs`(F3 재생 SSE) ·
  `f1_modal_check.mjs`(전역 F1 모달) · `cust_chat_keep_check.mjs`(고객별 대화 보관) ·
  `prep_list_check.mjs`(상담 메모 목록·PDF·삭제 · 실백엔드).
- **라이브 실행(비용)**: F3 `curl -sN ".../api/research/stream?stock_code=005930" > /tmp/f3.sse`(~100초) ·
  F2 `curl -s -X POST .../api/briefs/run -d '{}'`(~11초) · F1 `curl -sN ".../api/chat/stream?q=…"`.
  → `grep "^event:" *.sse | sort | uniq -c`로 이벤트별 건수부터 본다.
- **고객 문의 시드**(⚠️ 기본 출력이 `TRUNCATE pb_sessions`로 시작한다):
  `docker compose exec -T backend python /repo/backend/scripts/seed_pb_sessions.py | docker exec -i ai-pb-agent-postgres-1 psql -U app -d app`
  — `--pending N` · `--done N` · `--append`.
- **노트 재파싱**(분류기 변경 후): `docker compose exec -T backend python -m backend.scripts.reparse_notes`
- **`pb_customers` 복원**: `TRUNCATE pb_customers;` 후 `restore_pb_customers.sql`(INSERT만 들어 있어
  비우지 않으면 PK 충돌 · ⚠️ `RESTART IDENTITY`는 붙이지 말 것).
  ⚠️ `restore_pb_sessions.sql`·`shift_pb_sessions_time.sql`은 **옛 경로다**(절대시각·id 박힘).
- **고객 상황(시나리오) 재생성**: `docker compose exec -T backend python /repo/backend/scripts/seed_scenarios.py`
  → UPDATE SQL을 `psql`로 넣는다(난수 없음 · `scenario` 칸만 건드린다).
- 정리: `TRUNCATE notes, audit_log, briefs RESTART IDENTITY;`(`pb_*`는 지우지 말 것 — §2).
- API 키 4개(`.env`): `ANTHROPIC_API_KEY`·`DART_API_KEY`·`NAVER_CLIENT_ID/SECRET`·`KRX_API_KEY`.
  ⚠️ KRX는 **서비스별로 활용신청이 따로**다(종목시세·지수시세).
- **데모 배포**는 `docs/DEMO-DEPLOY.md`가 정본이다. 핵심만: 터널은 **3000에만** 뚫는다(8000을
  노출하면 비밀번호가 우회된다) · **개발 모드로 되돌리기 전에 터널부터 닫는다**(dev에는 비밀번호가
  없다) · Next 16에서 `middleware.ts`가 **`proxy.ts`로 이름이 바뀌었다**(옛 이름은 조용히 무시된다
  = 무인증 공개) · `DEMO_PASSWORD`가 없으면 프론트 컨테이너가 아예 안 뜬다(의도).
- **발표 슬라이드**: 정본은 `docs/presentation/slides-final.md`(Marp, `./build.sh`).
  🚩 **지금 `slides-final.html`에는 `.md`에 없는 손편집이 있다** — 빌드하면 사라진다.
  빌드가 필요하면 **HTML 내용을 `.md`로 먼저 옮겨라.** 문구 규칙은 `.md` 머리주석에 있다.
- git: main → **personal**(hmgthb/ai-pb-agent). origin(kwangtekNa/intern)에는 푸시하지 않는다.
  **커밋·푸시 둘 다 사용자 확인 후에만**(§2).

---

## 7. 열린 항목 · 리스크

**제품**
- **진입 단위가 종목인데 PB의 단위는 "오늘 만날 고객"이다** — 가장 좋은 화면(고객 포트폴리오)에
  가려면 페이지 아래에서 고객을 직접 찾아야 한다.
- ✅ ~~월요일 브리프는 공시가 거의 빈다~~ — **사라졌다**(2026-08-07). 브리핑이 `dart_search`를
  안 부른다. ⚠️ 종목 수집을 되살리면 **같이 돌아온다**(`days=2` 고정 · `bizdate`에 영업일 계산 없음).
- ✅ ~~`ECOS_API_KEY`가 아직 없다~~ — **켜졌다**(2026-08-09 실측: 원/달러 1,418.8 · 국고채10년
  4.208%가 띠에 뜬다). FRED 쪽은 키 자체가 필요 없다. ⚠️ 에이전트 도구 `krx_index`가 쓰는 KRX
  지수는 여전히 **`금융위원회_지수시세정보` 활용신청이 따로** 필요하다(`.env.example`).
- 🔸 **`NOTABLE_RANK=3`은 변동성이 큰 구간에서 아무것도 말하지 않는다** — 실측(2026-08-07):
  코스피 ▼4.58%인데 21거래일 창에서 더 큰 하락이 셋 이상이라 `rank=null`이었다. 규칙대로면
  "평소 수준"이 맞지만, PB가 그 날 가장 먼저 볼 숫자가 아무 표시 없이 서는 건 맞는지 미확인이다.
- 🔸 **준법이 발행분을 나중에 다시 뽑는 길이 없다** — 발행분을 여는 경로는 PB 쪽과 발행 직후
  모달뿐이다. 감시 탭의 노트별 감사 조회에 문서 링크 한 줄이면 된다.

**규제·경계에 대해 정직하게 말할 것**
- ❗ **"고객에게 말 걸지 않기"는 프롬프트 지시일 뿐 게이트 규칙이 아니다** — `compliance.py`는
  호칭·회신문 형식을 위반으로 세지 않는다(라이브 검사에서 발생은 0건).
- ❗ **자유 텍스트 비식별화기가 없다** — `redact.py`에 `mode:'llm'` 자리만 뚫려 있다. 고객 문의
  원문·상담 메모처럼 정규식으로 안 되는 텍스트가 그 일감이고, 실제 배치의 망분리 GPU 몫이다.
- ❗ **역할 강제는 화면에만 있다** — 백엔드는 `actor`를 감사로그용 문자열로 받을 뿐 "발행은 준법만"을
  강제하지 않는다. **스코핑은 서버로 내려갔지만 권한은 아직 아니다.**
- **고객 이름 대조에 남은 구멍은 2글자 이름뿐**(일반 낱말과 겹쳐 오탐이 크다 · 목업 50명은 전원
  3글자). `PII_PATTERNS`는 숫자 형식만 본다.
- 🔸 **안내 줄은 지연시세 누락을 모른다** — 그 경우 "이제 발행할 수 있습니다"라고 적고 발행에서
  409를 받는다. 화면이 위반을 스스로 세는 대신 응답의 `violations`를 그리면 사라지는 종류다.
- ❓ **정량 가치 밴드는 추정 모델**(`docs/VALUE.md` 민감도표) — **단일 숫자로 단정하지 말 것.**
  근거 하나: 삼성증권 SNI PB 100명 설문(2024-03)에서 만나기 **전** 업무가 53%. ⚠️ 업무시간
  *비중*이라 "상담 1건 20분"을 받쳐 주지 않고, SNI는 주 4건이라 가치 산식의 세그먼트와 다르다.
  국내 실측은 없다(파일럿에서 잰다).

**남은 검증**
- ❓ 3시나리오 예외 상태는 **재생 SSE 기준**이다(라이브로 돌려본 건 아니다).
- ❓ **구간·비율만 받은 모델이 금액을 추정해 적지 않는지**를 못 봤다(프롬프트로 금지만 해 뒀다).
- 🔸 **LLM이 쓰는 문장의 말투는 아직 안 건드렸다** — 화면 정적 문구에서만 `—` 접속사를 걷어냈다.
  노트·F1 답변은 `a5.md`·`f1.ANSWER_SYSTEM_PROMPT`가 지배하고 둘 다 문체 규칙이 없다.
  고치면 **라이브 실행으로만** 결과가 보인다(§1-1의 boilerplate 재발 방지와도 겹친다).
