# 인수인계 — 리서치 코파일럿 / AI PB 대시보드

> 최종 갱신: 2026-07-22 (W6 완료 — 3시나리오 안정화 · 간이 eval 5종목 · 데모 노트 5건) · 다음 세션은 이 문서부터 읽으세요.
> 주차 계획은 `주차별_실행계획.md`(**원본 유지 — 진행 기록을 적지 않는다**), 런타임 지침은 `CLAUDE.md`,
> 대시보드 설계 이력·상세는 `~/.claude/plans/inherited-beaming-canyon.md`.

---

## 0. 지금 어디까지 왔나

**W1~W6이 전부 라이브 검증으로 닫혔다. 남은 건 W7(발표: 덱·대본·녹화·Q&A) — 전부 크레딧 무관.**

이번 세션(2026-07-21~22)에 일어난 일:

1. **a5 블로킹 해소** — 위임이 비동기라 잘리던 걸 backend 2차 `query()`로 바꿨다(§1).
   노트가 처음으로 생성됐고 **최대 리스크였던 A5 각주 규칙 준수를 처음 관찰해 통과 확인**(§1-1).
2. **F2 라이브 검증** — 비동기 위임에도 정상 동작(§3-5). 검증 중 오진을 하나 했는데 그 경위도 적어뒀다.
3. **대시보드 React 포팅**(§3-6) — 시안 HTML 서빙을 걷어내고 화면을 하나로 합쳤다.
   **노트 생성이 시뮬레이션에서 실제 SSE로 바뀌었고**, 이때 토큰 스트리밍 화면 렌더도 처음 확인됐다.
4. **README·기술 문서 작성**(§3-7) — 계획서 §7 제출물 중 코드 레포·기술 문서 항목이 채워졌다.

5. **출처 부착률 정의 정리 + 파서 버그 수정**(§1-3) — 분모를 사실 주장 문장으로 좁혔고,
   그 과정에서 **각주를 조용히 버리던 파서 버그**를 찾아 고쳤다(노트 하나에서 10개 중 5개
   유실, 발행이 하드 블록되고 있었다). W6 정량 측정의 전제가 갖춰졌다.

6. **W6 3시나리오 안정화**(§3-8) — 빈/에러 상태 구현 + 재생 SSE로 5종 검증(크레딧 0원).

7. **간이 eval 실행 + 데모 노트 5건 생성**(§3-9) — 시안 데모 5종목 라이브. **핵심수치
   정확도 100%·출처 부착률 87.5%**. 측정 중 내 하네스의 측정 버그 3건을 찾아 고쳤고
   (전부 a5 오류가 아니었다), 그중 분류기 개선은 프로덕션에 반영(§1-3 후속). 정량 리포트는
   `docs/EVAL.md`.

**W6가 사실상 닫혔다. 남은 건 W7(발표: 덱·대본·녹화·Q&A) — 전부 크레딧 무관.**

---

## 1. ✅ 해소됨 — a5 실행 (2026-07-21)

**원인**(라이브 실측): `Agent` 도구가 **비동기**다. 결과가 `"Async agent launched successfully"`로
즉시 돌아오고 서브에이전트는 백그라운드에서 돈다. **O가 턴을 끝내면 `query()` 스트림이 닫히고
아직 안 끝난 a5가 잘린다.** a1·a2·a4는 도구를 호출해 결과가 스트림에 빨리 들어오므로 살아남지만,
도구 없이 텍스트만 만드는 a5는 항상 잘렸다.

**검증한 두 갈래** (`backend/scripts/background_flag_check.py` — hello-subagent·haiku로 싸게 판정):

| | a5 노트 도착 | 토큰 스트리밍 | `O→a5` 위임 | 각주 무결성 |
|---|---|---|---|---|
| 비동기(기존) | ❌ 잘림 | ❌ | ✅ | — |
| `background: false` | ✅ | ❌ **죽음** | ✅ | O가 다듬을 위험 잔존 |
| **채택: 백엔드 2차 `query()`** | ✅ | ✅ | ❌ | ✅ 구조화 데이터 직전달 |

- **`background: false`는 프론트매터에서 실제로 동작한다** (CLI 스키마에 있음: *"If true, the
  agent runs in the background by default"*). 위임이 동기가 되고 산출물이 Agent 도구의
  ToolResultBlock으로 온다. **단 대가가 있다 — 동기 위임에서는 서브에이전트의 StreamEvent가
  전혀 노출되지 않아**(실측 `parent_tool_use_id` 분포 `{None: 26}`) W4의 토큰 스트리밍이 죽는다.
- 그래서 **2안을 채택**했다: O의 1차 쿼리가 끝나면 backend가 a5를 **메인 에이전트로 하는 2차
  `query()`**를 돌린다(`main.py::event_gen`). a5가 메인이라 텍스트가 `parent_tool_use_id=None`으로
  흘러 `note_token`이 그대로 살아난다.
  - a5 지침은 `.claude/agents/a5.md`에 그대로 두고 **본문만 읽어 `system_prompt`로** 쓴다
    (`_agent_prompt("a5")` — 프론트매터는 벗겨낸다).
  - 입력은 O의 산문이 아니라 **도구 결과를 그대로 직렬화**한다(`_a5_input()`, LLM 미개입 순수
    함수). O가 옮겨 적는 경로가 사라져 CLAUDE.md가 경고하던 "각주 깨짐" 위험이 제거됐다.
  - a5는 `allowed_tools=[]`·`max_turns=1`로 넘겨받은 데이터 밖으로 못 나간다.
  - 잃은 것: 토폴로지에서 `O→a5` 위임 한 단계. 팬아웃(`O→a1→a2‖a4`)은 그대로고, 진행
    타임라인에는 backend가 `a5/note_draft` progress를 직접 쏴서 화면상 차이가 없다.

**❌ 시도했다가 되돌린 것 — 반복하지 말 것**: 시스템 프롬프트에 "a5의 결과를 반드시 기다려라 /
a5가 반환한 첫 문장을 인용해라"를 넣었더니, O는 기다리지 않고 **a2가 가져온 수치로 문장을
지어내 a5가 쓴 것처럼 인용했다**(가드레일 3 위반). 지시를 강화할수록 날조 압력만 커진다.
지금은 O의 프롬프트에서 노트 단계 자체를 뺐다 — 애초에 O가 관여하지 않으니 날조할 대상이 없다.

---

## 1-1. A5 각주 규칙 — 첫 관찰, 통과 (2026-07-21)

프로젝트 최대 리스크였고 그동안 한 번도 못 봤던 항목. 삼성전자(005930) 라이브 2회 기준:

- ✅ **DART 근거 문장은 `rcpNo` 숫자 태그**, 뉴스 근거 문장은 **링크 전체 태그**로 정확히 부착
- ✅ **태그 날조 없음** — 실제로 넘긴 URL/접수번호만 썼다
- ✅ **추론·해석 문장(4건)에는 각주를 붙이지 않았다** — 규칙대로다. 게이트가 이를 "미인용
  4개"로 잡아 위반 표시했는데, **이건 오작동이 아니라 설계대로**다(사람이 검토에서 판단)
- ✅ 소제목 3건은 `is_heading: true`로 분리돼 위반 집계에서 빠진다 (미인용 7 → 위반 4)
- ✅ 지연시세 게이트 **과검출 없었다** — 뉴스에 "주가"가 있었는데도 발동 안 함(W5에서 좁힌 게 유효)

**⚠️ W6 측정 정의에 반영할 것 — 출처 부착률의 분모.** 2회 실행에서 본문 문장 대비 부착률이
10/14 → 7/14로 흔들렸다. 각주 규칙 위반이 아니라 **a5가 쓰는 해석·전망 문장 수가 회차마다
다르기 때문**이다(해석 문장에 각주를 안 다는 건 규칙대로다). 따라서 "출처 부착률"을 그냥
`부착/전체`로 재면 모델의 문체 변덕을 품질로 오독한다 — **사실 주장 문장만 분모에 넣거나,
해석 문장을 별도 범주로 분리**해야 지표가 의미를 갖는다. 대시보드의 78.6%도 같은 정의 문제를
안고 있다.

**→ 해소됨 (2026-07-21, §1-3). 단 위 진단은 절반만 맞았다** — 분모 정의 문제 위에
**파서가 각주를 통째로 버리던 버그**가 겹쳐 있었다.

**같은 실행에서 잡은 버그**: `corp_name`이 법인명이 아니라 종목코드(`"005930"`)로 저장됐다.
`_extract_corp_name(a1_text)`이 a1의 산문을 파싱하는데, **a1도 비동기라 최종 텍스트가 Agent
도구 결과로만 오고 backend는 그걸 버린다** — 라이브에서 계속 조용히 폴백하고 있었다.
→ `_resolve_corp_name()`으로 **dart_parse가 준 구조화 `corp_name`을 1순위**로 쓴다.
(⚠️ 노트 id 4는 이 버그로 `corp_name="005930"`인 채 DB에 남아 있다.)

---

## 1-2. 크레딧 상태 (해소됨)

- 2026-07-21 결제수단 등록 완료. 최소 호출로 확인: **HTTP 200 정상**.
- 이제 크레딧은 블로킹이 아니다. 실행 비용은 종목 1건 기준 소액이지만, **디버깅 목적의
  반복 실행은 비용이 쌓이므로** 한 번 돌릴 때 최대한 많이 관찰할 것(SSE를 파일로 저장해
  이벤트별로 분석하는 방식이 효율적이었다 — `curl -sN ... > f3.sse`).

---

## 1-3. ✅ 출처 부착률 — 파서 버그 + 분모 재정의 (2026-07-21)

§1-1이 "분모 정의 문제"로 남겨둔 항목을 처리하다가 **그 아래 깔린 파서 버그**를 먼저 찾았다.
순서가 중요하다 — 분자가 틀린 채로 분모만 고치면 지표는 여전히 거짓이다.

**① 파서가 각주를 조용히 버렸다 (진짜 버그, 발행까지 막던 것)**

옛 `_UNIT_RE`는 **종결부호 바로 뒤**에 붙은 각주만 인정했다. 그런데 a5는 한국어 연결어미
뒤에도 자연스럽게 각주를 단다 — `...보도가 있어[^URL], 플랫폼 리스크도...`. 이런 각주는
매칭도 안 되고 텍스트에서 지워지지도 않아 **화면에 `[^https://...]`가 그대로 노출된 채
"UNSOURCED"로 표시**됐다.

| 노트 | 본문 각주 | 옛 파서 매칭 | 새 파서 매칭 |
|---|---|---|---|
| 4 삼성전자 | 7 | 7 | 7 |
| 5 삼성전자 | 7 | 7 | 7 |
| 6 NAVER | 10 | **5** | 10 |

노트 6은 게이트가 `출처 없는 문장 11개`로 발행을 **하드 블록**하고 있었다 → 이제 5개
(해석 3 + 진짜 미인용 2). **§1-1의 "각주 규칙 준수 통과" 판정은 운이 좋았던 것**이다 —
관찰한 두 노트(4·5)가 마침 각주를 전부 문장 끝에 달았을 뿐이고, a5는 두 방식을 다 쓴다.
⚠️ **모델 산출물을 파싱할 때는 한 종목만 보고 "규칙 준수"라고 결론 내리지 말 것.**

**② 우리가 붙인 고지문구가 스스로 발행을 막고 있었다**

`---` 구분선과 `※`로 시작하는 면책 문구가 "출처 없는 문장"으로 세어졌다. 이제
`kind="boilerplate"`로 분리해 게이트·지표 양쪽에서 뺀다.

**③ 분모 재정의 (원래 과제)**

`citations.parse_sentences`가 문장마다 `kind`를 매긴다:
`heading` · `boilerplate` · `claim`(사실 주장) · `interpretation`(해석·전망).

- **출처 부착률 분모 = `claim`만.** 정의는 `citations.citation_stats()` 하나뿐이고
  게이트·대시보드가 같이 쓴다(main.py `_citation_stats`가 이걸 부른다).
- **게이트(`unsourced_count`)는 해석 문장을 계속 센다** — 사람이 검토에서 판단하도록
  올리는 게 설계대로다(§1-1). 지표와 게이트의 정의가 다른 건 의도된 것.
- **분류는 보수적으로**: 애매하면 `claim`으로 남긴다. 헐거우면 미인용 문장이 분모에서
  빠져나가 부착률이 실제보다 좋아 보이는데, 컴플라이언스 지표는 그 방향으로 틀리면 안 된다.
  출처가 붙은 문장은 어미가 해석처럼 보여도 무조건 `claim`이다.
- 분모에서 뺀 해석 문장 수를 `citation_interpretation`으로 같이 노출한다(감추지 않기).
- 한 문장이 두 건을 인용하면 `sources` 배열로 **전부** 남긴다(가드레일 3). `source`는
  첫 건으로 유지 — 옛 노트·기존 화면 호환.

**재측정** (같은 노트, 새 정의): 4=100%(7/7) · 5=77.8%(7/9) · 6=80%(8/10) · 합계 84.6%.
남은 회차 간 편차는 **문체 변덕이 아니라 진짜 품질 차이**다 — 노트 5에는 근거 없이 사실을
주장하는 문장이 2개 있다("…움직임이 두드러진다", "…경계도 나온다"). 지표가 이걸 잡는 건 정상.

**⚠️ DB의 옛 노트 6건은 수치가 그대로다** — `sentences_json`이 저장 시점에 굳었기 때문
(대시보드가 아직 54.5%를 보여주는 이유). 옛 행은 `kind`가 없어 전부 `claim`으로 폴백한다
(하위호환 유지). **데모 전 정리(§3)에서 노트를 새로 만들면 자연히 해소된다.**

자체 점검: `backend/test_citations.py` 12종(파서·범주·하위호환). 전체 자체 점검 통과
(총 개수는 §5에 한 곳으로 적는다 — 절마다 적으면 늘 때마다 어긋난다).

---

## 2. 대시보드 시안 — 확정된 설계 (2026-07-13, 재논의 불필요)

> 이 절은 **설계 합의 기록**이다(재논의 불필요). 구현 현황은 §3-6을 봐라 —
> 시안은 React로 포팅됐고, 시안 파일은 이제 **디자인 시안 전용**으로 남는다.

- **시안 원본**: `docs/design/pb-admin-dashboard.html` — 단일 자립형 HTML. 브라우저로 열면 동작(서버 불필요), 데이터는 전부 시뮬레이션/목업.
- **아티팩트**: https://claude.ai/code/artifact/03fe22af-3c96-4839-84ae-1a2c58996da1 — 파일 수정 후 이 URL 지정해 재게시하면 같은 링크 유지.
- **제품 프레이밍**: "대고객 AI PB 프로토타입"의 **관리자/PB용 감독 콘솔** (고객용 화면 아님).

### 확정된 설계 (사용자와 합의 완료 — 재논의 불필요)

1. **카테고리 탭 2개**: 고객 관리(처리할 일·고객 현황) / AI 평가(신뢰도·컴플라이언스·감사) — 운영 기능과 AI 감독 기능 분리.
2. **역할 토글 실동작**(목 로그인): 관리자=전체 / PB(박PB 시점)=AI탭 숨김·내 담당 큐·내 담당 고객 17명 / 준법=AI평가 기본·심의 건만·**고객 개인 포트폴리오 비노출**(🔒).
3. **승인 배분 = 셀프 클레임(풀)**: 미배정 건을 실무자가 열어 검토 시작하면 그 사람이 담당자로 기록. 관리자 배정(푸시) 기능은 만들지 않음. 상담 답변은 고객별 담당 PB 자동 라우팅.
4. **검토 워크플로 모달**: 노트=문장별 출처 배지([공시]/[뉴스]/[UNSOURCED])→검토 시작(관리자)→심의 요청(관리자)→**발행(준법만, 게이트 재검사·미인용 시 하드 블록+감사기록)** / 상담=고객 컨텍스트(계좌·성향·플래그 사유)+답변 초안→**승인·전송/반려(담당 PB만)**. 권한 없는 역할은 버튼 비활성+🔒 사유.
5. **리서치 노트 생성 카드**(관리자만 노출): 종목코드 입력→a1→a2‖a4→a5 진행 타임라인→완료 시 초안이 큐 맨 위 등장. 시안은 setTimeout 시뮬레이션(데모 5종목: NAVER·기아·카카오·LG화학·POSCO홀딩스). 기존 page.tsx의 입력 화면을 이 카드로 흡수하기로 함. **(✅ §3-6에서 완료 — 이제 시뮬레이션이 아니라 실제 SSE)**
6. **AI 신뢰도 카드**: 출처 부착률(목표 90% 미터)·발행 통과율(오늘 발행/초안 비율)·게이트 차단(7일 카운트+미니 추이) — KPI 타일이 아닌 미터/비율 형태(사용자 요구).
7. **목업 데이터 규칙**: 고객 50명 — 세대혼합 이름 56풀 비복원 추출(동명이인 불가), 잔고 300만~30억 **로그균등**, **나이=이름 세대 구간(GIVEN 14개 단위) 기본 + 재산 보정**(잔고 자릿수 지터, PRNG 소비 없음), **위험 플래그는 규칙 3종으로 도출+사유 저장**(①안정형·안정추구형인데 주식>40% ②단일종목 집중≥65% ③수익률≤−5%), AI 진단 문구는 첫 사유와 일치. AUM은 KPI가 아니라 포트폴리오 카드 요약 스트립.

---

## 3. 다음 할 일 (우선순위)

**W1~W5는 닫혔다. 아래는 전부 W6·W7이고, 크레딧이 필요한 건 ②의 eval 실행뿐이다.**

1. ~~**출처 부착률 분모 재정의**~~ → **✅ 완료(§1-3)**. 파서 버그도 같이 잡았다.
   남은 후속: **DB의 옛 노트 6건은 저장 시점 수치가 굳어 있어** 대시보드가 아직 옛 값을
   보여준다 — 아래 "데모 전 정리"에서 노트를 새로 만들면 해소된다.
2. **W6 안정화·측정**
   - ~~3시나리오 예외 처리~~ → **✅ 완료(§3-8)**. 재생 SSE로 5종 검증, 크레딧 0원.
   - 간이 eval(출처 부착률·핵심수치 정확도) + 가치 밴드 + 대표 지표 — **실행 필요(크레딧)**.
     ← W6에 남은 유일한 항목.
3. **W7 발표 준비**: 덱·데모 대본·백업 녹화·예상 Q&A. 전부 크레딧 무관.
4. (선택) **F1 대화형 Q&A** — 계획서상 "여유 되면". 핵심이 흔들리면 미련 없이 로드맵으로.

**데모 전 정리 필요**: 큐에 테스트 잔여물이 쌓여 있다(삼성전자 노트 3건 중복,
`corp_name="005930"`인 노트 1건 — 버그 시절 레코드). `TRUNCATE notes, audit_log`로 지우면
컴플라이언스 알림·에이전트 호출 차트도 같이 비므로, **데모용 노트를 새로 몇 건 만드는 편**이
화면이 살아 있다. `pb_customers`·`pb_sessions`는 절대 지우지 말 것(§3-2).

---

## 3-1. 주차별 진행 상태 (계획서는 원본 유지 — 진행 기록은 여기에만)

> `주차별_실행계획.md`는 **최초 커밋(`f23130a`) 원본 그대로 유지**한다. 체크박스·완료 표시를
> 그 문서에 적지 않는다. 진행 상태는 아래 표가 유일한 출처다.

| 주  | 상태 | 비고 |
| :-: | --- | --- |
| W1 | ✅ 완료(라이브 검증) | 환경·플러그인(earnings-reviewer·equity-research)·DART/뉴스 MCP 2개·`CLAUDE.md`·SDK hello world |
| W2 | ✅ 완료(라이브 검증) | DART MCP 자체구현·a1·a2·O 위임·SSE 카드·docker-compose |
| W3 | ✅ 완료(라이브 검증) | a4·팬아웃(a2‖a4)·진행 타임라인·점진 렌더 |
| W4 | ✅ 완료(라이브 검증) | a5 실행·각주 규칙 준수 확인(단 §1-3의 파서 수정 후 재확인은 안 함). 아래 상세 |
| W5 | ✅ 완료(라이브 검증) | F2 파이프라인·화면·H1 요건 완료(§3-3). a1·a4 위임 실행 확인(§3-5) |
| W6 | ✅ 완료(라이브) | 3시나리오 안정화(§3-8)·부착률 정의(§1-3)·간이 eval+데모노트 5건(§3-9, `docs/EVAL.md`) |
| W7 | ❌ 미착수 | 덱·대본·백업 녹화·Q&A — 크레딧 무관 |

**W4 상세 (2026-07-21 기준)**

- ✅ a5 지침 정의(`a5.md`) + **backend 2차 `query()` 실행**(위임 아님 — §1)
- ✅ **토큰 스트리밍** — 2차 `query()`에 `include_partial_messages=True`, a5가 메인 에이전트라 `text_delta`가 `parent_tool_use_id=None`으로 흘러 그대로 `note_token` SSE가 된다. 프론트는 `note` 도착 전까지 "작성 중" 카드에 점진 표시
- ✅ Citations — 문장 단위 출처 부착 + `[UNSOURCED]` 시각 경고
  - **"페이지" 단위는 해당 없음으로 종결**: DART 전자공시는 PDF가 아니라 HTML 뷰어 문서라 페이지 번호라는 단위 자체가 없다. 문서 링크(`viewer_url`) + 접수시점(`rcept_dt`)이 이 데이터 소스에서 가능한 최대 해상도. (계획서 원문의 "문서·페이지·접수시점"은 PDF 기반 리서치 문서를 상정한 표현)
- ✅ 컴플라이언스 게이트 — 훅(PreToolUse/PostToolUse) + `can_use_tool`, 규칙은 `compliance.py`에 직접 작성. 계획서가 요구한 4종(필수문구·인용누락·금지표현·지연시세) **전부 구현** + MNPI 추가
  - **지연시세 체크(2026-07-21 추가)**: 기능별 플래그 없이 "본문에 시세 단어(`QUOTE_TERMS`)가 있는데 '지연시세'가 없으면 위반"하는 조건부 규칙. F3(공시·뉴스만)은 대개 안 걸리고 **F2 모닝 브리프에서 자동으로 켜진다**
  - ~~⚠️ 알려진 과검출~~ → W5에서 "시세 단어 + 실제 수치" 동시 조건으로 좁혔고(§3-3), **2026-07-21 라이브에서 뉴스에 "주가"가 있었는데도 발동하지 않는 것을 확인**했다
- ✅ 검토→심의→발행(사람만) 3단 + 감사로그(append-only)
- ✅ "AI 초안·미검증" 워터마크 — 백엔드 강제 삽입 + 프론트 배너
- ✅ **라이브 e2e 검증 완료(2026-07-21)** — a5 실행 구조 교체 후 노트 생성·각주 규칙 준수 확인(§1, §1-1)
  - a5는 위임이 아니라 backend 2차 `query()`로 돈다. `a5.md`는 그대로 지침 원본이다.
  - 토큰 스트리밍은 SSE(`note_token` 44건)에 이어 **화면 점진 렌더까지 확인**(§3-6)

**W4를 닫는 조건**: (1) A5 각주 규칙 실준수 ✅ / (2) 토큰 스트리밍 화면 실동작 ✅ — **둘 다 충족.**

## 3-2. ⚠️ 대시보드 구현 유실 사고 + 백엔드 라우트 (2026-07-20~21)

**⚠️ 먼저 알아둘 것: 이전 세션의 대시보드 구현이 유실됐었다.** 백엔드 로그(2026-07-20)에
`/api/dashboard/*`·`/api/customers`·`/api/sessions/13/approve`(200→409)가 남아 있는데,
07-21 08:35경 `main.py`·`db.py`가 커밋 상태로 되돌려지고 `frontend/src/app/dashboard/`가
비워졌다. 커밋된 적이 없어 git(stash·reflog·dangling)에도 없다.
**교훈: 대시보드 작업은 자주 커밋할 것.**

살아남은 것 / 복구한 것:
- ✅ **postgres 볼륨의 `pb_customers`(50)·`pb_sessions`(30) 데이터와 스키마** — 이게 데이터 계약을 그대로 보존해줬다
- ✅ `backend/__pycache__/seed_pb.cpython-*.pyc`(7/20) — 소스는 없지만 재컴파일된 적이 없어 남음. 시드 재현이 필요하면 디컴파일할 것 (스크래치패드에 백업)
- ❌ `main.py` 라우트·`db.py` DDL·프론트 페이지 — pyc가 덮어써져 소실. **아래로 재작성함**

**현재 구성**
- 백엔드: `db.py`에 `pb_customers`·`pb_sessions` 멱등 DDL + 조회 8종, `main.py`에 라우트 9개
  (`/api/dashboard/summary|queue|agents|audit`, `/api/customers[/{id}]`, `/api/sessions`,
  `/api/sessions/{id}/approve|reject`).
- 프론트: 처음에는 시안 HTML을 백엔드가 그대로 서빙했으나 **지금은 React로 포팅됐다(§3-6)**.
- **수치는 전부 실집계.** 비교할 과거 데이터가 없는 "vs 전주" 델타는 지어내지 않고 감춘다.

**검증(playwright, 실백엔드)**: 출처 부착률 78.6%(11/14 실집계) · 큐 7건(노트 3+상담 4) ·
고객 50명 · **준법 역할로 현대차 발행 시도 → 백엔드 게이트가 미인용 3문장으로 차단(409)** ·
관리자 검토 시작 → DB에 검토자 기록. 테스트로 바꾼 상태는 전부 원복했다
(감사로그 흔적은 append-only라 남겨둠).

**이 절의 백엔드 라우트 9종은 포팅 후에도 그대로다** — 바뀐 건 소비하는 화면뿐이다.

---

## 3-3. W5 — F2 모닝 브리프 + H1 (2026-07-21)

**새 에이전트 0개 — 멘토 체크 항목, 증명됨.** W4 커밋(`bbc7145`) 이후 `.claude/agents/` 변경
0건. O가 기존 a1(공시)·a4(뉴스)에 병렬 위임하고, 시세만 `mcp__krx__krx_quote`를 직접 부른다.

**핵심 설계**: 에이전트에게 산문을 쓰게 하지 않는다. 도구 결과(ToolResultBlock)에서 구조화
데이터를 뽑아 backend가 카드로 조립하므로(`brief.assemble`) 모든 줄이 구조적으로 출처를
갖는다 — F3 노트처럼 각주가 깨질 여지가 없다. `research_stream`에서 이미 검증된 패턴이다.

- `backend/brief.py` — 조립(LLM 미개입 순수 함수) + 공시 중요도 순위
- `backend/mcp_servers/krx_server.py` — 공공데이터포털 금융위 주식시세정보(일별 종가=지연시세)
- `POST /api/briefs/run` (배치 트리거) · `GET /api/briefs/latest` · `briefs` 테이블
- 화면: 대시보드에 **오늘의 브리프 카드 + 5기능 레일**(H1 요건 흡수)

**공시 중요도 순위** (0=주요사항 1=정기 2=기타 3=지분): 대형주는 30일 공시가 177건이고 거의
전부 임원 지분 보고라 그대로 실으면 중요 공시가 묻힌다. **지분공시는 빼지 않고 뒤로만 민다**
— 조용한 날 브리프가 비지 않도록. DART 응답에 공시유형 필드가 없어 보고서명으로 분류한다.

**게이트 기능별 분기**: 필수 고지문구가 F3 워터마크로 하드코딩돼 있어 F2를 검사하면 엉뚱한
문구를 요구했다. `compliance.NOTICES`로 분기(F2=내부 참고용 / F3=워터마크), 모르는 기능
코드는 `ValueError`로 터뜨린다. **지연시세 체크도 좁혔다** — 시세 단어만으로 발동해 F2 빈
결과 안내문까지 위반 처리하는 오탐이 실제로 났다. 이제 시세 단어 + 실제 수치가 함께 있을
때만 발동하고, 가격 정규식은 "7만원"·"1.7조원" 같은 단위 표기도 잡는다.

**⚠️ `backend/scripts/seed_brief.py`는 F2 파이프라인이 아니다.** 크레딧 없이 화면을 실데이터로
검증하려고 에이전트를 건너뛰고 MCP를 직접 호출하는 시드다. 데이터는 전부 진짜고 조립·게이트·
저장 경로도 같지만 **"누가 수집했는가"가 다르다 — 데모에서 이걸 F2 결과로 소개하면 안 된다**
(멀티에이전트 서사가 빠진다). 크레딧이 생기면 필요 없어진다.

**실데이터가 잡아낸 버그** (목업으로는 전부 놓쳤을 것들 — 새 외부 API는 반드시 실호출할 것):
- `dart_search` 응답에 `stock_code`가 없다(`corp_code`만) → 종목 귀속이 `KeyError`로 죽던 버그
- 같은 보고서명이 제출인만 달라 중복 표시 → `flr_nm` 병기로 해결
- 공시 링크가 문장 출처에만 붙고 카드용 `items`에는 빠져 있어 링크가 `#`로 죽던 버그
- 지연시세 게이트 오탐 / 가격 정규식이 `"7만원"`을 못 잡던 문제

**남은 것**: a1·a4 위임 실행(크레딧). 실제 cron 스케줄은 걸지 않았다 — 배치 트리거가 있으면
cron 한 줄이면 되고, 데모에서는 원할 때 돌리는 수동 트리거가 오히려 낫다.

---

## 3-4. 첫 라이브 실행에서 드러난 것 (2026-07-21)

크레딧이 풀리고 처음으로 에이전트를 실제로 돌린 세션. **코드를 읽어서는 알 수 없던 문제들이
한 번에 드러났다** — 라이브 검증을 미루지 말아야 하는 이유의 실증이다.

**① `dart_search`가 토큰 한도를 넘겨 a1이 데이터를 못 받았다 → 고침**
- 삼성전자 공시가 하루 **382건**(임원·주요주주 소유상황보고 대량 제출일). 조회 버그가 아니라
  실제 데이터. `days=2`만 해도 58,943자, `days=90`이면 70,461자.
- 도구 결과가 한도를 넘어 a1이 받은 건 데이터가 아니라 에러 문자열이었다. a1은 `jq`로 파일을
  읽어 우회하려 했고(권한 콜백이 막는 동작) 실패했다.
- **고침**: `dart_search(limit=30)` 기본값 + DART API에 `sort=date&sort_mth=desc` 명시(잘라낸
  30건이 곧 "가장 최근"이 되도록) + 필요한 만큼 모이면 남은 페이지를 받지 않음.
  → 70,461자 **→ 4,654자**. a2의 정기공시 경로(`pblntf_ty="A"`)는 608자로 정상 동작 확인.

**② 실패가 `completed`로 찍혀 원인이 가려졌다 → 고침**
- 토큰 한도 초과 결과가 `is_error=False`로 와서 성공으로 집계됐다.
- **고침**: `main.py::_tool_failed()` — `is_error` 또는 `"exceeds maximum allowed tokens"` 패턴을
  실패로 본다. `backend/test_tool_result.py`로 고정(정상 데이터에 "error"라는 단어가 있어도
  오판하지 않는지 포함).

**③ a5가 잘린다 → 미해결.** §1 참고.

**정상 확인된 것**: 팬아웃 토폴로지(a1 법인확인 → a2‖a4 병렬 → a5 위임)가 진행 이벤트로 그대로
보인다. 서브에이전트 메시지의 `parent_tool_use_id` 귀속도 정상. 재무 카드(매출액·영업이익)와
뉴스 카드 10건이 실제로 렌더된다. 법인 확인(005930 → 삼성전자)도 DART 조회로 정확히 된다.

**진단 방법 메모**: 서브에이전트 산출물이 스트림 어디로 오는지 확인할 때는 a1만 위임하는
최소 프롬프트로 원시 메시지(type·`parent_tool_use_id`·Agent 도구 결과 내용)를 덤프하면
싸게 알 수 있다(a1=Haiku). 이 방법으로 "Async agent launched successfully"를 발견했다.

---

## 3-5. F2 모닝 브리프 — 라이브 검증 (2026-07-21)

`POST /api/briefs/run` 기본 워치리스트(005930·000660·005380) 실행: **40초, 게이트 위반 0건,
3종목 전부 시세·뉴스 수집.** F2는 서브에이전트의 **텍스트가 아니라 도구 결과만** 쓰기 때문에
(§3-3의 설계) **비동기 위임 문제를 겪지 않는다** — a1·a4가 도구를 호출하는 순간 결과가
스트림에 들어오고, 그 뒤 서브에이전트가 잘려도 backend는 이미 필요한 걸 다 받았다.
감사로그로 확인: dart_search(a1) 3회 · news_search(a4) 3회 · krx_quote(O 직접) 3회 전부 실행.

**⚠️ 오진 기록 — 같은 실수 반복 금지.** SK하이닉스 공시 0건을 보고 "귀속 실패로 4건 유실"이라
결론 내렸으나 **틀렸다.** 파이프라인은 `days=2`로 조회하는데 나는 7일 창(4건)과 비교했다.
`days=2`로 다시 재니 000660=0건·005930=30건(상한 5로 잘림)·005380=1건으로 **브리프 결과와
정확히 일치**했다. 유실은 없었다. → **원본과 대조할 때는 파이프라인과 같은 조건으로 조회할 것.**

그 과정에서 귀속 로직은 그대로 두지 않고 손봤다(버그 수정이 아니라 방어):
- 공시는 이제 **`dart_search`의 도구 입력에 있는 `stock_code`로 직접 귀속**한다 — 위임문을
  읽어 추측하던 걸 없앴다.
- 뉴스는 검색어를 **krx_quote가 준 법인명과 대조**해 귀속하고, 못 맞추면 위임문 폴백,
  그것도 모호하면 **버린다**(엉뚱한 종목에 남의 뉴스를 싣지 않는다 — 가드레일 3).
- 위임문 기반 추출은 종목코드가 **정확히 하나**일 때만 인정한다(예전엔 "처음 나온 것").

---

## 3-6. 대시보드 React 포팅 (2026-07-21)

시안 HTML을 백엔드가 서빙하던 배선을 걷어내고 `frontend/src/app/dashboard/`로 옮겼다.
`page.tsx`(F3 단독 화면)와 `/dashboard`(시안 HTML)로 갈라져 있던 화면이 하나로 합쳐졌고,
`/`는 대시보드로 리다이렉트한다. **W1의 "공통 셸" 미완성이 이걸로 닫혔다.**

**가장 중요한 변화: 노트 생성이 진짜가 됐다.** 시안의 `setTimeout` 시뮬레이션 대신
`GET /api/research/stream`을 직접 구독한다 — progress→단계 타임라인, card/source→부분결과
점진 렌더(W3), note_token→본문 점진 표시(W4), note→큐 갱신. 발표의 핵심 장면 3이
플랫폼 화면에서 가짜였던 문제가 해소됐다.

시안과 의도적으로 달라진 점:
- **백엔드가 없으면 목업으로 폴백하지 않고 연결 실패를 그대로 말한다.** 폴백은 자립형
  시안 파일의 성질이지 실화면의 성질이 아니다 — 실화면이 조용히 가짜를 보여주면 더 위험하다.
- 추이 차트·컴플라이언스 알림·타일 수치를 하드코딩에서 **감사로그·세션 집계**로 바꿨다.
  (시안은 컴플라이언스 알림에 "DB 실데이터" 배지를 달고도 하드코딩이었다 — 라벨 불일치)
- CSS는 시안 `<style>`을 **그대로** 옮겼고(번역 오류 방지), 토큰 스트리밍·부분결과 카드
  스타일만 "시안에 없는 추가분"으로 표시해 뒤에 붙였다.

**시안 파일은 디자인 시안으로 남는다**(아티팩트 게시용·오프라인 열람용). 이제 실화면과
갈라지지만 포팅 후에는 그게 정상이다. `next.config.ts` rewrite와 `main.py`의 `/dashboard`
FileResponse는 삭제했다.

**남은 목업**: 상담 답변 초안(F1 미구현이라 목업 — 화면에 "목업 · F1 미구현" 배지),
일별 API 비용 차트는 제거하고 노트 생성 추이(실집계)로 교체.

---

## 3-7. 문서 (2026-07-21)

계획서 §7 "완료의 정의" 중 코드 레포·기술 문서 항목을 채웠다. 역할을 나눠 뒀다:

- **`README.md`** — *쓰는 법*. 빠른 시작(`docker compose up`), API 키 4개 발급처,
  MCP 설정법(`.mcp.json` stdio·`DART_MCP_PYTHON` 스위치), 실행법(대시보드·F3·F2·자체 점검),
  구조도, 가드레일 요약, 알려진 한계.
- **`docs/ARCHITECTURE.md`** — *왜 이렇게 만들었나*. 플랫폼 구성도(생산 구간/가공 구간 경계),
  에이전트 토폴로지+모델 3-tier 근거, **컴포넌트→Agent SDK 매핑표**, 의사결정 11건,
  한계·리스크 7건, 확장 로드맵(F1·F4·F5·사내데이터·온프렘).

**아직 없는 제출물**: 정량 가치 리포트(W6 측정 결과가 나와야 씀), 발표 덱·데모 대본·
예상 Q&A(W7).

---

## 3-8. W6 — 3시나리오 예외 상태 (2026-07-21)

계획서의 "공시 없음·파싱 실패·뉴스 없음" 안정화. 화면에 빈/에러 상태가 **거의 없던** 걸
채웠다. 원칙 하나: **"조회했는데 0건"과 "도구가 실패"를 절대 뭉뚱그리지 않는다** —
공시가 원래 없는 종목은 정상 동작인데, 뭉뚱그리면 그것까지 장애처럼 보인다.

**백엔드**

- `_run_outcome()` — 수집 결과를 구조화 + 사람이 읽을 사유 목록으로 정리하는 **순수 함수**
  (LLM 미개입, `_a5_input`·`brief.assemble`과 같은 원칙). `done` 이벤트에 `outcome`으로 실린다.
  예전 `done`은 `unsourced_agents`뿐이라 화면이 "실패 지점을 확인하세요"밖에 못 말했다.
- **예외가 나도 스트림이 조용히 끊기지 않는다** — `event_gen()`이 `_run()`을 감싸
  `run_error` + `done`을 반드시 보낸다. 화면이 항상 종료 상태에 도달한다.
  - ⚠️ **이벤트명이 `error`면 안 된다** — EventSource의 연결 오류 핸들러와 이름이 겹쳐
    "백엔드가 보낸 실행 오류"와 "연결 끊김"을 프론트가 구분할 수 없다. 그래서 `run_error`.
  - 예외 메시지는 그대로 흘리지 않고 **타입만** 노출한다(내부 경로·키가 섞일 수 있다).
- **재무·뉴스가 둘 다 없으면 a5를 아예 돌리지 않는다** — 근거 없이 쓰게 하면 지어내는
  수밖에 없다(가드레일 3). 노트가 없는 유일한 정상 경로이고, 사유로 그렇게 말한다.
- **부분 데이터일 때 a5에게 "없는 건 없다"고 명시한다**(`_a5_input`). 침묵하면 a5는
  "실적 요약 → 뉴스 해석 → 주의 지점" 구성(a5.md)을 채우려고 없는 내용을 지어낼 압력을 받는다.

**화면** (`ResearchCard.tsx`)

- 확보 못 한 항목도 **점선 빈 카드로 남긴다** — 카드가 사라지면 "아직 안 왔다"와 "없다"를
  구분할 수 없다. 실패가 아니라 결과이므로 경고색은 쓰지 않는다.
- 빈 상태는 **실행이 끝난 뒤에만** 말한다(진행 중 "뉴스 0건"은 아직 안 온 걸 없다고 단정하는 것).
- `done` 시점에 `run`으로 남은 단계 칩을 정리한다. **단 `run_error`로 중단된 실행은
  `fail`로 표시한다** — ✓로 바꾸면 성공했다고 거짓말하게 된다(스크린샷으로 잡은 문제).
- `onerror`는 정상 종료 직후에도 한 번 뜨므로 `closedCleanly`로 걸러 오탐을 막는다.

**검증** — 크레딧 0원. `frontend/scenario_check.mjs`가 `/api/research/stream`만 가로채
시나리오별 SSE를 재생하고(나머지 API는 실백엔드) 화면이 그 사유를 실제로 말하는지 본다.
5종(`no-disclosure`·`parse-failed`·`no-news`·`nothing`·`run-error`) 통과, 콘솔 에러 0.
스크린샷은 `/tmp/w6-*.png`.

⚠️ **시나리오는 백엔드 실제 동작과 맞춰야 한다.** 처음엔 "파싱 실패면 노트도 없음"으로
짰는데, 실제로는 뉴스가 있으면 a5가 돌아 노트가 나온다(`if financials or news_items`).
**허구를 검증할 뻔했다** — 재생 스크립트는 코드 경로를 보고 짤 것.

`backend/test_run_outcome.py` 13종: 0건/실패 구분, 사유 문구, `failed_tools` 직렬화,
a5 입력의 결측 안내, **예외 → `run_error`+`done` 도달**(query를 터뜨려 SSE 바이트로 확인),
잘못된 종목코드 400.

**남은 것**: ~~간이 eval~~ → **✅ 완료(§3-9)**.

---

## 3-9. W6 — 간이 eval + 데모 노트 5건 (2026-07-22)

시안 데모 5종목(NAVER·기아·카카오·LG화학·POSCO홀딩스)을 F3 파이프라인에 **라이브로**
태워(크레딧 사용) 데모 노트를 만들고, 그 산출물을 정량 측정했다. 정식 리포트는
**`docs/EVAL.md`**. 핵심만:

| 지표 | 값 |
|---|---|
| 핵심수치 정확도 | **100%** (9/9) — 옮긴 모든 재무 수치가 DART 원문과 일치 |
| 출처 부착률 | **87.5%** (35/40) — 사실 주장 문장 기준, 대시보드와 동일 정의 |
| 노트 생성 | **5/5**, `run_error` 0 |
| 게이트 실제 발동 | POSCO에서 '목표주가'(투자권유) + 지연시세 누락 포착 → 하드 블록 |
| 평균 실행 | ~84초/종목 |

**측정 중 내 하네스의 측정 버그 3건을 찾아 고쳤다 — 전부 a5 오류가 아니었다.** 처음엔
부착률 65.8%·수치정확 71.4%로 나왔는데, 각주 파서 때(§1-3)와 **같은 부류의 측정 정의 함정**이었다:

1. **핵심수치 체커가 조/억 표기만 인식** → 기아 0% 오판. a5는 종목마다 단위를 바꾼다
   (대부분 조/억, 기아는 "107,448,752백만원"). 백만원·원 후보 추가로 해결.
2. **해석 분류기가 판단유보 어미를 못 잡음** → 부착률 65.8% 과소평가. "…근거가 없다/부족하다",
   "…확인되지 않는다", "…바람직하다", "…볼 여지가 있다"가 사실 주장으로 집계됐다. 또 a5가
   자기 고지문구를 `*본 노트는…*`(마크다운 이탤릭)로 써서 boilerplate를 빠져나가 게이트에
   "출처 없는 문장"으로 올라갔다(진짜 버그). → 낮은 오탐 위험 어미만 정규식 추가 + 고지문구
   서명으로 감지(`citations.py`, `test_citations.py`에 고정). **이건 프로덕션 변경**이다.
3. **여러 재무 카드 중 첫 카드로 대조** → LG화학 0% 오판. a2가 연도를 바꿔 dart_parse를
   여러 번 부르면(LG화학: 2025 잠정 → 2024 확정) 백엔드는 **마지막** 카드를 a5에게 넘긴다.
   체커도 마지막 카드로 대조하게 고쳤다. a5는 2024 수치를 정확히 썼고 매출액이 입력에 없다는
   것까지 옳게 밝혔다.

**교훈(재확인): 지표를 믿으려면 지표부터 검증한다.** 낮은 수치를 보면 산출물을 탓하기 전에
측정기를 의심할 것. 한 종목만 보고 결론 내리지 말 것.

**도구/스크립트**
- `backend/scripts/eval_report.py` — DB 노트(부착률·게이트) + SSE(핵심수치) 측정. 대시보드와
  같은 DB에서 읽어 정의가 갈라지지 않는다. 재실행 무료.
- `backend/scripts/reparse_notes.py` — 분류기를 바꿨을 때 기존 노트의 sentences_json을
  **재실행 없이** 새 기준으로 맞춘다(저장된 출처에서 맵 복원 → content_md 재파싱). 멱등.

**데모 상태**: 노트 5건 전부 `draft`. 큐에 노트 5 + 상담 4 = 9건. 대시보드 부착률 카드가
87.5%(35/40)·해석 23 노출. POSCO 노트는 게이트 3건 달린 채라 **발행 하드 블록 데모에 그대로
쓸 수 있다**. `pb_customers`·`pb_sessions`·감사로그(221→더 늘어남)는 보존.
⚠️ 5건 다 draft이므로 검토→심의→발행 워크플로 데모를 하려면 몇 건을 진행시켜 둘 것(선택).

---

## 4. 검증된 것 vs 미검증

- ✅ (W4, 목업 API 테스트) 발행 3단 흐름 200·게이트 409 차단·잘못된 전이 409·감사로그 순서, citations 파싱, DDL 자동생성, 프론트 tsc/eslint.
- ✅ (2026-07-21, playwright) **시안 HTML** 기준 역할 전환·큐 필터·승인 워크플로 전이·게이트 차단·상담 승인·라이트/다크 렌더. (포팅 전 검증이라 지금 실화면은 아래 항목을 보라)
- ✅ (2026-07-21) 토큰 스트리밍 델타 추출 `backend/test_streaming.py`, `main.py` 임포트, 프론트 tsc/eslint.
- ✅ (2026-07-21) 게이트 규칙 5종 `backend/test_compliance.py` — 워터마크 강제·멱등, 금지표현, MNPI, 미인용 차단(제목 줄은 예외), 지연시세 조건부 발동·미발동.
- ✅ (2026-07-21) **KRX 실호출** 5종목(KOSDAQ 포함) 종목명·시장구분 일치, 응답 필드명 확인, MCP stdio 노출·오류 처리.
- ✅ (2026-07-21) **브리프 실데이터** — DART·KRX·네이버 실호출로 조립→게이트 통과→저장→조회→화면 렌더, 링크 20개 전수 유효, 콘솔 에러 0.
- ✅ (2026-07-21) 자체 점검 **9종** 전부 통과 — 크레딧·네트워크 불필요
  (`compliance`·`brief`·`krx`·`streaming`·`tool_result`·`a5_input`·`brief_attribution`·`citations`·`run_outcome`).
- ✅ (2026-07-21, playwright) **W6 3시나리오 빈/에러 화면** `frontend/scenario_check.mjs` 5종 —
  공시 없음·파싱 실패·뉴스 없음·아무것도 없음·실행 예외. 크레딧 0원(SSE 재생). 콘솔 에러 0.
- ✅ (2026-07-21) **각주 파서 + 문장 범주** `backend/test_citations.py` — 문장 중간 각주,
  한 문장 다중 출처, 알려지지 않은 태그 불인정, `2.96%`·URL에서 문장 안 쪼개짐,
  고지문구·해석 문장의 분모 제외, 해석 문장은 게이트에 그대로 노출, `kind` 없는 옛 행 하위호환.
  (2026-07-22 추가) 판단유보 어미 해석 분류, bare "없다"는 사실 주장 유지, `*`·`※` 고지문구 감지.
- ✅ (2026-07-22 **라이브**) **간이 eval 5종목**(§3-9, `docs/EVAL.md`) — 핵심수치 정확도 100%(9/9),
  출처 부착률 87.5%(35/40), 노트 5/5 생성, POSCO에서 게이트가 '목표주가' 실제 포착. 측정 버그 3건
  발견·수정(전부 a5 오류 아님). 대시보드 부착률 카드 87.5% 렌더·검토 모달 배지(출처/해석/UNSOURCED) 확인.
- ✅ (2026-07-21 **라이브**) 팬아웃 토폴로지·법인 확인(005930→삼성전자)·재무 카드·뉴스 카드 10건·`parent_tool_use_id` 귀속·훅 감사로그.
- ✅ (2026-07-21) `dart_search` 상한·최신순 정렬, `_tool_failed` 소프트 실패 감지(`test_tool_result`).
- ✅ (2026-07-21 **라이브**) **a5 실행** — backend 2차 `query()`로 교체 후 `note_token` 44건·`note` 1건. §1.
- 🟡 (2026-07-21 **라이브**) **A5 각주 규칙 실준수** — 삼성전자 2회에서 통과(§1-1). **단 그건
  파서가 문장 끝 각주만 인정하던 상태의 관찰이라 반쪽이었다**: NAVER 노트에서 a5가 문장 중간에
  단 각주 10개 중 5개가 버려지고 있었다(§1-3). 파서를 고친 뒤 **다시 라이브로 확인한 적은 없다.**
- ✅ (2026-07-21 **라이브**) 지연시세 게이트 과검출 없음(뉴스에 "주가" 포함된 실데이터에서).
- ✅ (2026-07-21) a5 입력 조립·법인명 해석 `backend/test_a5_input.py`, `background: false` 동작
  판정 `backend/scripts/background_flag_check.py`(hello-subagent·haiku, 저비용).
- ✅ (2026-07-21 **라이브**) **F2 배치 실행** — `POST /api/briefs/run` 40초, 3종목 시세·공시·뉴스
  수집, 게이트 위반 0건. 비동기 위임에도 정상 동작한다(§3-5).
- ✅ (2026-07-21) F2 귀속 로직 `backend/test_brief_attribution.py`.
- ✅ (2026-07-21 **라이브·playwright**) **대시보드 노트 생성 전 과정** — 단계 진행 8초 →
  부분결과 카드 22초 → 토큰 스트리밍 58초 → 완료 86초(본문 1,873자) → 큐 9→10.
  **토큰 스트리밍 화면 점진 렌더를 처음 확인**했다. 콘솔 에러 0.
- ✅ (2026-07-21, playwright) **포팅된 React 대시보드** — 역할 3종 전환(준법=AI탭 기본·포트폴리오
  🔒·노트생성 숨김 / PB=AI탭 숨김·담당 스코프), 기능 레일 5, 큐 9건, 고객 50명, 브리프 3종목,
  검토 모달 출처 배지, 콘솔 에러 0. tsc·eslint 통과.

**아직 검증 못 한 것**
- ~~❓ 3시나리오 예외 상태~~ → **✅ 구현·검증 완료(§3-8)**. 단 **재생 SSE 기준**이고,
  실제 그런 종목으로 라이브 실행해본 건 아니다(크레딧). 조립 경로는 같다.
- ❓ **발행까지 가는 전체 흐름을 포팅된 화면에서** — 검토→심의→발행 전이는 시안 HTML에서
  검증했고 백엔드는 그대로지만, React 모달로는 아직 끝까지 돌려보지 않았다.
- ~~❓ 간이 eval 실행~~ → **✅ 완료(§3-9)**. 핵심수치 100%·부착률 87.5%.
- ❓ **파서 수정 후 A5 각주 규칙 라이브 재확인** — 새 노트를 만들면 자연히 같이 관찰된다.

---

## 5. 환경/실행 메모

- 앱 실행: `docker compose up` (backend 8000 / frontend 3000 / postgres 5432 / redis 6379). requirements 변경 시 `docker compose build backend` 후 `up -d --force-recreate backend`.
- **대시보드: http://localhost:3000/dashboard** (`/`는 여기로 리다이렉트). React 페이지이며
  백엔드 `/dashboard` 서빙과 Next rewrite는 포팅하며 삭제했다(§3-6).
- **F3를 화면에서 실행**: 대시보드 "리서치 노트 생성" 카드에 종목코드 입력(관리자 역할).
  실제 에이전트가 돈다 — 비용 발생, 1~2분.
- 자체 점검(크레딧·네트워크 불필요, 9종): `for t in compliance brief krx streaming tool_result a5_input brief_attribution citations run_outcome; do backend/.venv/bin/python -m backend.test_$t; done`
- **라이브 F3 실행**(비용 발생): `curl -sN --max-time 900 "http://localhost:8000/api/research/stream?stock_code=005930" > /tmp/f3.sse`
  → `grep "^event:" /tmp/f3.sse | sort | uniq -c`로 이벤트 종류별 건수부터 본다.
  `note_token`·`note`가 0이면 a5 단계를 보라(§1). 한 번 실행에 약 100초.
- 위임이 동기냐 비동기냐를 싸게 판정: `docker compose exec -T backend python -m backend.scripts.background_flag_check`
  (hello-subagent·haiku 1회. Agent 도구 결과가 "Async agent launched successfully"면 비동기다.)
- 브리프 시드(에이전트 없이 실데이터, §3-3 경고 먼저 읽을 것):
  `DATABASE_URL=postgresql://app:app@localhost:5432/app backend/.venv/bin/python -m backend.scripts.seed_brief`
- MCP 서버 단독 확인: `backend/.venv/bin/python backend/scripts/{dart,news_mcp_client,krx_mcp_client}_check.py`
- 테스트 데이터 정리: `docker exec research-copilot-postgres-1 psql -U app -d app -c "TRUNCATE notes, audit_log, briefs RESTART IDENTITY;"`
  (`pb_customers`·`pb_sessions`는 지우지 말 것 — 시드 소스가 유실돼 `.pyc`뿐이다. §3-2 참고)
- 시안 보기: `docs/design/pb-admin-dashboard.html` 더블클릭 — 목업 데이터로 동작하는 **디자인
  시안**이다(포팅 후 실화면과 갈라진다 — 정상). 시안 수정 절차: `dataviz`·`artifact-design`
  스킬 로드 → 파일 수정 → playwright로 스크린샷 검증 → 아티팩트 URL 지정 재게시.
- **프론트 검증**: `cd frontend && npx tsc --noEmit && npx eslint src --max-warnings=0`.
- **W6 빈/에러 화면 검증**(크레딧 0원): `cd frontend && node scenario_check.mjs`
  — `/api/research/stream`만 가로채 시나리오 5종 SSE를 재생한다. 백엔드 8000·프론트 3000 필요.
  스크린샷은 `/tmp/w6-*.png`. **시나리오를 고칠 때는 main.py의 실제 분기를 보고 맞출 것**(§3-8).
  브라우저 검증 스크립트는 `frontend/`에서 실행해야 playwright 모듈을 찾는다.
  ⚠️ Next.js 16: 코드 작성 전 `frontend/node_modules/next/dist/docs/` 확인(frontend/AGENTS.md).
- API 키 4개(`.env`): `ANTHROPIC_API_KEY`(크레딧 소진) · `DART_API_KEY` · `NAVER_CLIENT_ID/SECRET` · `KRX_API_KEY`(data.go.kr, 무료). 뒤 3개는 정상 동작.
- git: main → **personal**(hmgthb/research-copilot)이 업스트림. origin(kwangtekNa/intern)에는 푸시하지 않았음.
  **작업은 자주 커밋할 것** — 대시보드 구현이 미커밋 상태에서 통째로 유실된 적이 있다(§3-2).
- SDK API명은 `claude-agent-sdk==0.2.110` 실물 확인 — CLAUDE.md "검증됨" 절 참고.

---

## 6. 아키텍처 한눈에

```
[F3 입구] 종목코드 입력 → O(Opus) → a1 법인확인 → a2 재무 ‖ a4 뉴스 팬아웃   ← 위임은 여기까지
  → backend가 도구 결과를 구조화 그대로 직렬화(_a5_input) → **2차 query()로 A5 직접 실행**
    (위임 아님 — Agent 도구가 비동기라 a5가 잘렸다, §1. a5 지침은 a5.md 본문)
  → citations 매칭(문장 범주 kind 부여) → 워터마크(F3) 강제 → 게이트 → notes 저장
  → SSE(진행·토큰·노트·done{outcome}) — 재무·뉴스 둘 다 없으면 a5를 아예 안 돌리고 사유만 보낸다(§3-8)
[F2 입구] POST /api/briefs/run (배치, cron이 때리면 됨)
  → O → a1 공시 ‖ a4 뉴스 + krx 시세(O가 직접) — **새 에이전트 0개**
  → backend: 도구 결과에서 구조화 데이터 추출 → brief.assemble(중요도 순위·출처 부착)
    → "내부 참고용"(F2) 강제 → 게이트 → briefs 저장
     ← 에이전트(LLM)는 위 두 생산 구간에서만 실행
[대시보드 = 유일한 화면] Next.js `/dashboard` (`/`는 리다이렉트)
  기능 레일(F1~F5 · 동작/로드맵) · 오늘의 브리프 카드
  고객 관리 탭: 액션 타일 2 · **노트 생성 카드(관리자) ← F3 SSE를 직접 구독** ·
                검토·승인 대기 큐 · 고객 포트폴리오(50)
  AI 평가 탭: AI 신뢰도(미터·비율) · 에이전트 호출/노트 생성 차트 · 컴플라이언스 알림 · 감사로그
[사람] 노트: 검토 시작(관리자)→심의 요청→발행(준법, 게이트 재검사) / 상담: 승인·전송(담당 PB)
  — 승인 경로에 AI 개입 없음, 전 과정 audit_log(append-only)
```

파일 대응: 작문 지침=`a5.md`(backend가 system_prompt로 읽음) /
출처매칭·문장범주·부착률 정의=`citations.py`(게이트·대시보드가 같이 쓰는 단일 출처) /
화면=`frontend/src/app/dashboard/{page,ResearchCard,ReviewModal,charts}.tsx` / 브리프 조립·공시 순위=`brief.py` /
기능별 고지·게이트 규칙=`compliance.py` / 상태·로그=`db.py` / 배선·API=`main.py` /
MCP 서버=`mcp_servers/{dart,news,krx}_server.py` /
디자인 시안=`docs/design/pb-admin-dashboard.html`(목업 전용) /
문서=`README.md`(쓰는 법)·`docs/ARCHITECTURE.md`(왜 이렇게 만들었나).
